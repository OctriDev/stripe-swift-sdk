// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriStripe
import XCTest

extension SdkMethodTests {
    func testPostCreditNotesIdPostCreditNotesIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.creditNotes.void.postCreditNotesId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture358Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postCreditNotesId decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostCreditNotesIdVoidPostCreditNotesIdVoidReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.creditNotes.void.postCreditNotesId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture359Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postCreditNotesIdVoid decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostCustomersPostCustomersReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1CustomersMethods.PostCustomersOptions()
        options.address = try decoded(sdkMethodFixture360Value1)
        options.balance = try decoded("1")
        options.businessName = try decoded("\"Example Name\"")
        options.cashBalance = try decoded("{\"settings\":{\"reconciliation_mode\":\"automatic\"}}")
        options.description = try decoded("\"example text\"")
        options.email = try decoded("\"user@example.com\"")
        options.expand = try decoded("[\"expand\"]")
        options.individualName = try decoded("\"Example Name\"")
        options.invoicePrefix = try decoded("\"invoice_prefix\"")
        options.invoiceSettings = try decoded(sdkMethodFixture360Value2)
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.name = try decoded("\"Example Name\"")
        options.nextInvoiceSequence = try decoded("1")
        options.paymentMethod = try decoded("\"payment_method\"")
        options.phone = try decoded("\"+15555550100\"")
        options.preferredLocales = try decoded("[\"preferred_locales\"]")
        options.shipping = try decoded(sdkMethodFixture360Value3)
        options.source = try decoded("\"source\"")
        options.tax = try decoded("{\"ip_address\":\"ip_address\",\"validate_location\":\"deferred\"}")
        options.taxExempt = try decoded("\"\"")
        options.taxIdData = try decoded("[{\"type\":\"ad_nrt\",\"value\":\"value\"}]")
        options.testClock = try decoded("\"test_clock\"")
        let actual = try await client.v1.customers.post(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture360Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postCustomers decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostCustomersCustomerPostCustomersCustomerReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.balanceTransactions.postCustomersCustomer(
            customer: decoded("\"customer\""),
            amount: decoded("\"sample\""),
            currency: decoded("\"sample\""),
            description: decoded("\"example text\""),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture361Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postCustomersCustomer decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostCustomersCustomerBalanceTransactionsPostCustomersCustomerX0dcb7d2471ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.balanceTransactions.postCustomersCustomer(
            customer: decoded("\"customer\""),
            amount: decoded("100"),
            currency: decoded("\"currency\""),
            description: decoded("\"example text\""),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture362Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postCustomersCustomerBalanceTransactions decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostCustomersCustomerBalanceTransactionsTransactionPostCustomX7f203fc070ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.balanceTransactions.postCustomersCustomerTransaction(
            customer: decoded("\"customer\""),
            transaction: decoded("\"transaction\""),
            description: decoded("\"example text\""),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture363Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postCustomersCustomerBalanceTransactionsTransaction decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostCustomersCustomerBankAccountsPostCustomersCustomerBankAccountsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.bankAccounts.postCustomersCustomer(
            customer: decoded("\"customer\""),
            alipayAccount: decoded("\"alipay_account\""),
            bankAccount: decoded(sdkMethodFixture364Value1),
            card: decoded(sdkMethodFixture364Value2),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}"),
            source: decoded("\"source\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture364Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postCustomersCustomerBankAccounts decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostCustomersCustomerBankAccountsIdPostCustomersCustomerBankAccountsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.bankAccounts.verify.postCustomersCustomerBankAccountsId(
            customer: decoded("\"customer\""),
            id: decoded("\"id-1\""),
            amounts: decoded("\"sample\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture365Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postCustomersCustomerBankAccountsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostCustomersCustomerBankAccountsIdVerifyPostCustomersCustomeX569f607122ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.bankAccounts.verify.postCustomersCustomerBankAccountsId(
            customer: decoded("\"customer\""),
            id: decoded("\"id-1\""),
            amounts: decoded("[100]"),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture366Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postCustomersCustomerBankAccountsIdVerify decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostCustomersCustomerCardsPostCustomersCustomerCardsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.cards.postCustomersCustomer(
            customer: decoded("\"customer\""),
            alipayAccount: decoded("\"alipay_account\""),
            bankAccount: decoded(sdkMethodFixture367Value1),
            card: decoded(sdkMethodFixture367Value2),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}"),
            source: decoded("\"source\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture367Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postCustomersCustomerCards decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostCustomersCustomerCardsIdPostCustomersCustomerCardsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1CustomersCardsMethods.PostCustomersCustomerCardsIdOptions(customer: decoded("\"customer\""), id: decoded("\"id-1\""))
        options.accountHolderName = try decoded("\"Example Name\"")
        options.accountHolderType = try decoded("\"company\"")
        options.addressCity = try decoded("\"address_city\"")
        options.addressCountry = try decoded("\"address_country\"")
        options.addressLine1 = try decoded("\"address_line1\"")
        options.addressLine2 = try decoded("\"address_line2\"")
        options.addressState = try decoded("\"address_state\"")
        options.addressZip = try decoded("\"address_zip\"")
        options.expMonth = try decoded("\"exp_month\"")
        options.expYear = try decoded("\"exp_year\"")
        options.expand = try decoded("[\"expand\"]")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.name = try decoded("\"Example Name\"")
        options.owner = try decoded(sdkMethodFixture368Value1)
        let actual = try await client.v1.customers.cards.postCustomersCustomerId(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture368Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postCustomersCustomerCardsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostCustomersCustomerCashBalancePostCustomersCustomerCashBalanceReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.cashBalance.postCustomersCustomer(
            customer: decoded("\"customer\""),
            expand: decoded("[\"expand\"]"),
            settings: decoded("{\"reconciliation_mode\":\"automatic\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture369Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postCustomersCustomerCashBalance decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostCustomersCustomerFundingInstructionsPostCustomersCustomerXa338aa7812ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.fundingInstructions.postCustomersCustomer(
            customer: decoded("\"customer\""),
            bankTransfer: decoded(sdkMethodFixture370Value1),
            currency: decoded("\"currency\""),
            fundingType: decoded("\"bank_transfer\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture370Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postCustomersCustomerFundingInstructions decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostCustomersCustomerSourcesPostCustomersCustomerSourcesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.sources.postCustomersCustomer(
            customer: decoded("\"customer\""),
            alipayAccount: decoded("\"alipay_account\""),
            bankAccount: decoded(sdkMethodFixture371Value1),
            card: decoded(sdkMethodFixture371Value2),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}"),
            source: decoded("\"source\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture371Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postCustomersCustomerSources decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostCustomersCustomerSourcesIdPostCustomersCustomerSourcesIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.sources.verify.postCustomersCustomerSourcesId(
            customer: decoded("\"customer\""),
            id: decoded("\"id-1\""),
            amounts: decoded("\"sample\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture372Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postCustomersCustomerSourcesId decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostCustomersCustomerSourcesIdVerifyPostCustomersCustomerSourcesIdVerifyReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.sources.verify.postCustomersCustomerSourcesId(
            customer: decoded("\"customer\""),
            id: decoded("\"id-1\""),
            amounts: decoded("[100]"),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture373Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postCustomersCustomerSourcesIdVerify decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostCustomersCustomerSubscriptionsPostCustomersCustomerSubscriptionsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1CustomersSubscriptionsMethods.PostCustomersCustomerSubscriptionsOptions(customer: decoded("\"customer\""))
        options.addInvoiceItems = try decoded(sdkMethodFixture374Value1)
        options.applicationFeePercent = try decoded("1")
        options.automaticTax = try decoded(sdkMethodFixture374Value2)
        options.backdateStartDate = try decoded("1")
        options.billingCycleAnchor = try decoded("1")
        options.billingThresholds = try decoded("{\"amount_gte\":100,\"reset_billing_cycle_anchor\":false}")
        options.cancelAt = try decoded("1")
        options.cancelAtPeriodEnd = try decoded("false")
        options.collectionMethod = try decoded("\"charge_automatically\"")
        options.currency = try decoded("\"currency\"")
        options.daysUntilDue = try decoded("1")
        options.defaultPaymentMethod = try decoded("\"default_payment_method\"")
        options.defaultSource = try decoded("\"default_source\"")
        options.defaultTaxRates = try decoded("[\"default_tax_rates\"]")
        options.discounts = try decoded(sdkMethodFixture374Value3)
        options.expand = try decoded("[\"expand\"]")
        options.invoiceSettings = try decoded(sdkMethodFixture374Value4)
        options.items = try decoded(sdkMethodFixture374Value5)
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.offSession = try decoded("false")
        options.paymentBehavior = try decoded("\"allow_incomplete\"")
        options.paymentSettings = try decoded(sdkMethodFixture374Value6)
        options.pendingInvoiceItemInterval = try decoded("{\"interval\":\"day\",\"interval_count\":1}")
        options.prorationBehavior = try decoded("\"always_invoice\"")
        options.transferData = try decoded("{\"amount_percent\":9.99,\"destination\":\"destination\"}")
        options.trialEnd = try decoded("\"now\"")
        options.trialFromPlan = try decoded("false")
        options.trialPeriodDays = try decoded("1")
        options.trialSettings = try decoded("{\"end_behavior\":{\"missing_payment_method\":\"cancel\"}}")
        let actual = try await client.v1.customers.subscriptions.postCustomersCustomer(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture374Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postCustomersCustomerSubscriptions decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostCustomersCustomerSubscriptionsSubscriptionExposedIdPostCuXe12fa2dc17ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1CustomersSubscriptionsMethods.PostCustomersCustomerSubscriptionsSubscriptionExposedIdOptions(customer: decoded("\"customer\""), subscriptionExposedId: decoded("\"subscription_exposed_-1\""))
        options.addInvoiceItems = try decoded(sdkMethodFixture375Value1)
        options.applicationFeePercent = try decoded("1")
        options.automaticTax = try decoded(sdkMethodFixture375Value2)
        options.billingCycleAnchor = try decoded("\"now\"")
        options.billingThresholds = try decoded("{\"amount_gte\":100,\"reset_billing_cycle_anchor\":false}")
        options.cancelAt = try decoded("1")
        options.cancelAtPeriodEnd = try decoded("false")
        options.cancellationDetails = try decoded(sdkMethodFixture375Value3)
        options.collectionMethod = try decoded("\"charge_automatically\"")
        options.daysUntilDue = try decoded("1")
        options.defaultPaymentMethod = try decoded("\"default_payment_method\"")
        options.defaultSource = try decoded("\"default_source\"")
        options.defaultTaxRates = try decoded("[\"default_tax_rates\"]")
        options.discounts = try decoded(sdkMethodFixture375Value4)
        options.expand = try decoded("[\"expand\"]")
        options.invoiceSettings = try decoded(sdkMethodFixture375Value5)
        options.items = try decoded(sdkMethodFixture375Value6)
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.offSession = try decoded("false")
        options.pauseCollection = try decoded("{\"behavior\":\"keep_as_draft\",\"resumes_at\":1}")
        options.paymentBehavior = try decoded("\"allow_incomplete\"")
        options.paymentSettings = try decoded(sdkMethodFixture375Value7)
        options.pendingInvoiceItemInterval = try decoded("{\"interval\":\"day\",\"interval_count\":1}")
        options.prorationBehavior = try decoded("\"always_invoice\"")
        options.prorationDate = try decoded("1")
        options.transferData = try decoded("{\"amount_percent\":9.99,\"destination\":\"destination\"}")
        options.trialEnd = try decoded("\"now\"")
        options.trialFromPlan = try decoded("false")
        options.trialSettings = try decoded("{\"end_behavior\":{\"missing_payment_method\":\"cancel\"}}")
        let actual = try await client.v1.customers.subscriptions.postCustomersCustomerSubscriptionExposedId(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture375Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postCustomersCustomerSubscriptionsSubscriptionExposedId decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostCustomersCustomerTaxIdsPostCustomersCustomerTaxIdsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.taxIds.postCustomersCustomer(
            customer: decoded("\"customer\""),
            type: decoded("\"ad_nrt\""),
            value: decoded("\"value\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture376Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postCustomersCustomerTaxIds decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostCustomerSessionsPostCustomerSessionsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customerSessions.post(
            components: decoded(sdkMethodFixture377Value1),
            customer: decoded("\"customer\""),
            customerAccount: decoded("\"customer_account\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture377Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postCustomerSessions decoded a response " +
                "that differs from the mock"
        )
    }
}
