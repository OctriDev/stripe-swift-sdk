// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriStripe
import XCTest

extension SdkMethodTests {
    func testPostAccountsAccountRejectPostAccountsAccountRejectReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.accounts.reject.postAccountsAccount(
            account: decoded("\"account\""),
            reason: decoded("\"reason\""),
            expand: decoded("[\"expand\"]"),
            payoutsAction: decoded("\"none\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture311Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postAccountsAccountReject decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostAccountsAccountUnrejectPostAccountsAccountUnrejectReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.accounts.unreject.postAccountsAccount(
            account: decoded("\"account\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture312Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postAccountsAccountUnreject decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostAccountSessionsPostAccountSessionsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.accountSessions.post(
            account: decoded("\"account\""),
            components: decoded(sdkMethodFixture313Value1),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture313Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postAccountSessions decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostApplePayDomainsPostApplePayDomainsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.applePay.domains.postApplePay(
            domainName: decoded("\"Example Name\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture314Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postApplePayDomains decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostApplicationFeesFeeRefundsIdPostApplicationFeesFeeRefundsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.applicationFees.refunds.postApplicationFeesFeeId(
            fee: decoded("\"fee\""),
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture315Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postApplicationFeesFeeRefundsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostApplicationFeesIdRefundPostApplicationFeesIdRefundReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.applicationFees.refund.postApplicationFeesId(
            id: decoded("\"id-1\""),
            amount: decoded("100"),
            directive: decoded("\"directive\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture316Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postApplicationFeesIdRefund decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostApplicationFeesIdRefundsPostApplicationFeesIdRefundsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.applicationFees.refunds.postApplicationFeesId(
            id: decoded("\"id-1\""),
            amount: decoded("100"),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture317Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postApplicationFeesIdRefunds decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostAppsSecretsPostAppsSecretsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.apps.secrets.delete.postAppsSecrets(
            name: decoded("\"Example Name\""),
            scope: decoded("{\"type\":\"account\",\"user\":\"user\"}"),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture318Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postAppsSecrets decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostAppsSecretsDeletePostAppsSecretsDeleteReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.apps.secrets.delete.postAppsSecrets(
            name: decoded("\"Example Name\""),
            scope: decoded("{\"type\":\"account\",\"user\":\"user\"}"),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture319Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postAppsSecretsDelete decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostBalanceSettingsPostBalanceSettingsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.balanceSettings.post(
            expand: decoded("[\"expand\"]"),
            payments: decoded(sdkMethodFixture320Value1)
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture320Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postBalanceSettings decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostBillingAlertsPostBillingAlertsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.billing.alerts.postBilling(
            alertType: decoded("\"usage_threshold\""),
            title: decoded("\"title\""),
            expand: decoded("[\"expand\"]"),
            usageThreshold: decoded(sdkMethodFixture321Value1)
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture321Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postBillingAlerts decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostBillingAlertsIdActivatePostBillingAlertsIdActivateReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.billing.alerts.activate.postBillingAlertsId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture322Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postBillingAlertsIdActivate decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostBillingAlertsIdArchivePostBillingAlertsIdArchiveReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.billing.alerts.archive.postBillingAlertsId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture323Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postBillingAlertsIdArchive decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostBillingAlertsIdDeactivatePostBillingAlertsIdDeactivateReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.billing.alerts.deactivate.postBillingAlertsId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture324Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postBillingAlertsIdDeactivate decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostBillingCreditGrantsPostBillingCreditGrantsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1BillingCreditGrantsMethods.PostBillingCreditGrantsOptions(
            amount: decoded(sdkMethodFixture325Value1),
            applicabilityConfig: decoded(sdkMethodFixture325Value2)
        )
        options.category = try decoded("\"paid\"")
        options.customer = try decoded("\"customer\"")
        options.customerAccount = try decoded("\"customer_account\"")
        options.effectiveAt = try decoded("1")
        options.expand = try decoded("[\"expand\"]")
        options.expiresAt = try decoded("1")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.name = try decoded("\"Example Name\"")
        options.priority = try decoded("1")
        let actual = try await client.v1.billing.creditGrants.postBilling(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture325Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postBillingCreditGrants decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostBillingCreditGrantsIdPostBillingCreditGrantsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.billing.creditGrants.expire.postBillingCreditGrantsId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture326Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postBillingCreditGrantsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostBillingCreditGrantsIdExpirePostBillingCreditGrantsIdExpireReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.billing.creditGrants.expire.postBillingCreditGrantsId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture327Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postBillingCreditGrantsIdExpire decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostBillingCreditGrantsIdVoidPostBillingCreditGrantsIdVoidReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.billing.creditGrants.void.postBillingCreditGrantsId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture328Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postBillingCreditGrantsIdVoid decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostBillingFeedbackOptionsPostBillingFeedbackOptionsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.billing.feedbackOptions.postBilling(
            description: decoded("\"example text\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture329Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postBillingFeedbackOptions decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostBillingFeedbackOptionsIdPostBillingFeedbackOptionsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.billing.feedbackOptions.deactivate.postBillingFeedbackOptionsId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture330Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postBillingFeedbackOptionsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostBillingFeedbackOptionsIdDeactivatePostBillingFeedbackOptiX13869239eaReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.billing.feedbackOptions.deactivate.postBillingFeedbackOptionsId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture331Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postBillingFeedbackOptionsIdDeactivate decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostBillingMeterEventAdjustmentsPostBillingMeterEventAdjustmentsReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.billing.meterEventAdjustments.postBilling(
            eventName: decoded("\"Example Name\""),
            type: decoded("\"cancel\""),
            cancel: decoded("{\"identifier\":\"identifier\"}"),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture332Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postBillingMeterEventAdjustments decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostBillingMeterEventsPostBillingMeterEventsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.billing.meterEvents.postBilling(
            eventName: decoded("\"Example Name\""),
            payload: decoded("{\"key\":\"key\"}"),
            expand: decoded("[\"expand\"]"),
            identifier: decoded("\"identifier\""),
            timestamp: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture333Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postBillingMeterEvents decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostBillingMetersPostBillingMetersReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.billing.meters.postBilling(
            defaultAggregation: decoded("{\"formula\":\"count\"}"),
            displayName: decoded("\"Example Name\""),
            eventName: decoded("\"Example Name\""),
            customerMapping: decoded("{\"event_payload_key\":\"event_payload_key\",\"type\":\"by_id\"}"),
            eventTimeWindow: decoded("\"day\""),
            expand: decoded("[\"expand\"]"),
            valueSettings: decoded("{\"event_payload_key\":\"event_payload_key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture334Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postBillingMeters decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostBillingMetersIdPostBillingMetersIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.billing.meters.deactivate.postBillingMetersId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture335Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postBillingMetersId decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostBillingMetersIdDeactivatePostBillingMetersIdDeactivateReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.billing.meters.deactivate.postBillingMetersId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture336Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postBillingMetersIdDeactivate decoded a response " +
                "that differs from the mock"
        )
    }
}
