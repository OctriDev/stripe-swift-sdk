// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriStripe
import XCTest

extension SdkMethodTests {
    func testPostInvoiceRenderingTemplatesTemplateUnarchivePostInvoiceRendX7024caa746ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.invoiceRenderingTemplates.unarchive.postInvoiceRenderingTemplatesTemplate(
            template: decoded("\"template\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture400Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postInvoiceRenderingTemplatesTemplateUnarchive decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostInvoicesPostInvoicesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1InvoicesCreatePreviewMethods.PostInvoicesCreatePreviewOptions()
        options.automaticTax = try decoded(sdkMethodFixture401Value1)
        options.currency = try decoded("\"currency\"")
        options.customer = try decoded("\"customer\"")
        options.customerAccount = try decoded("\"customer_account\"")
        options.discounts = try decoded(sdkMethodFixture401Value2)
        options.expand = try decoded("[\"expand\"]")
        options.issuer = try decoded("{\"account\":\"account\",\"type\":\"account\"}")
        options.onBehalfOf = try decoded("\"on_behalf_of\"")
        options.subscription = try decoded("\"subscription\"")
        let actual = try await client.v1.invoices.createPreview.postInvoices(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture401Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postInvoices decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostInvoicesCreatePreviewPostInvoicesCreatePreviewReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1InvoicesCreatePreviewMethods.PostInvoicesCreatePreviewOptions()
        options.automaticTax = try decoded(sdkMethodFixture402Value1)
        options.currency = try decoded("\"currency\"")
        options.customer = try decoded("\"customer\"")
        options.customerAccount = try decoded("\"customer_account\"")
        options.customerDetails = try decoded(sdkMethodFixture402Value2)
        options.discounts = try decoded(sdkMethodFixture402Value3)
        options.expand = try decoded("[\"expand\"]")
        options.invoiceItems = try decoded(sdkMethodFixture402Value4)
        options.issuer = try decoded("{\"account\":\"account\",\"type\":\"account\"}")
        options.onBehalfOf = try decoded("\"on_behalf_of\"")
        options.previewMode = try decoded("\"next\"")
        options.schedule = try decoded("\"schedule\"")
        options.scheduleDetails = try decoded(sdkMethodFixture402Value5)
        options.subscription = try decoded("\"subscription\"")
        options.subscriptionDetails = try decoded(sdkMethodFixture402Value6)
        let actual = try await client.v1.invoices.createPreview.postInvoices(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture402Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postInvoicesCreatePreview decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostInvoicesInvoicePostInvoicesInvoiceReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.invoices.addLines.postInvoicesInvoice(
            invoice: decoded("\"invoice\""),
            lines: decoded("\"sample\""),
            expand: decoded("[\"expand\"]"),
            invoiceMetadata: decoded("\"sample\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture403Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postInvoicesInvoice decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostInvoicesInvoiceAddLinesPostInvoicesInvoiceAddLinesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.invoices.addLines.postInvoicesInvoice(
            invoice: decoded("\"invoice\""),
            lines: decoded(sdkMethodFixture404Value1),
            expand: decoded("[\"expand\"]"),
            invoiceMetadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture404Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postInvoicesInvoiceAddLines decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostInvoicesInvoiceAttachPaymentPostInvoicesInvoiceAttachPaymentReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.invoices.attachPayment.postInvoicesInvoice(
            invoice: decoded("\"invoice\""),
            expand: decoded("[\"expand\"]"),
            paymentIntent: decoded("\"payment_intent\""),
            paymentRecord: decoded("\"payment_record\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture405Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postInvoicesInvoiceAttachPayment decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostInvoicesInvoiceFinalizePostInvoicesInvoiceFinalizeReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.invoices.finalize.postInvoicesInvoice(
            invoice: decoded("\"invoice\""),
            autoAdvance: decoded("false"),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture406Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postInvoicesInvoiceFinalize decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostInvoicesInvoiceLinesLineItemIdPostInvoicesInvoiceLinesLineItemIdReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1InvoicesLinesMethods.PostInvoicesInvoiceLinesLineItemIdOptions(
            invoice: decoded("\"invoice\""),
            lineItemId: decoded("\"line_item_-1\"")
        )
        options.amount = try decoded("100")
        options.description = try decoded("\"example text\"")
        options.discountable = try decoded("false")
        options.discounts = try decoded(sdkMethodFixture407Value1)
        options.expand = try decoded("[\"expand\"]")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.period = try decoded("{\"end\":1,\"start\":1}")
        options.priceData = try decoded(sdkMethodFixture407Value2)
        options.pricing = try decoded("{\"price\":\"price\"}")
        options.quantity = try decoded("1")
        options.quantityDecimal = try decoded("\"quantity_decimal\"")
        options.taxAmounts = try decoded(sdkMethodFixture407Value3)
        options.taxRates = try decoded("[\"tax_rates\"]")
        let actual = try await client.v1.invoices.lines.postInvoicesInvoiceLineItemId(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture407Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postInvoicesInvoiceLinesLineItemId decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostInvoicesInvoiceMarkUncollectiblePostInvoicesInvoiceMarkUncollectibleReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.invoices.markUncollectible.postInvoicesInvoice(
            invoice: decoded("\"invoice\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture408Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postInvoicesInvoiceMarkUncollectible decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostInvoicesInvoicePayPostInvoicesInvoicePayReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1InvoicesPayMethods.PostInvoicesInvoicePayOptions(invoice: decoded("\"invoice\""))
        options.expand = try decoded("[\"expand\"]")
        options.forgive = try decoded("false")
        options.mandate = try decoded("\"2024-01-01T00:00:00Z\"")
        options.offSession = try decoded("false")
        options.paidOutOfBand = try decoded("false")
        options.paymentMethod = try decoded("\"payment_method\"")
        options.source = try decoded("\"source\"")
        let actual = try await client.v1.invoices.pay.postInvoicesInvoice(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture409Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postInvoicesInvoicePay decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostInvoicesInvoiceRemoveLinesPostInvoicesInvoiceRemoveLinesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.invoices.removeLines.postInvoicesInvoice(
            invoice: decoded("\"invoice\""),
            lines: decoded("[{\"behavior\":\"delete\",\"id\":\"id-1\"}]"),
            expand: decoded("[\"expand\"]"),
            invoiceMetadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture410Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postInvoicesInvoiceRemoveLines decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostInvoicesInvoiceSendPostInvoicesInvoiceSendReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.invoices.send.postInvoicesInvoice(
            invoice: decoded("\"invoice\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture411Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postInvoicesInvoiceSend decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostInvoicesInvoiceUpdateLinesPostInvoicesInvoiceUpdateLinesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.invoices.updateLines.postInvoicesInvoice(
            invoice: decoded("\"invoice\""),
            lines: decoded(sdkMethodFixture412Value1),
            expand: decoded("[\"expand\"]"),
            invoiceMetadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture412Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postInvoicesInvoiceUpdateLines decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostInvoicesInvoiceVoidPostInvoicesInvoiceVoidReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.invoices.void.postInvoicesInvoice(
            invoice: decoded("\"invoice\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture413Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postInvoicesInvoiceVoid decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostIssuingAuthorizationsAuthorizationPostIssuingAuthorizatioX030e233496ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.issuing.authorizations.approve.postIssuingAuthorizationsAuthorization(
            authorization: decoded("\"authorization\""),
            amount: decoded("\"sample\""),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture414Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postIssuingAuthorizationsAuthorization decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostIssuingAuthorizationsAuthorizationApprovePostIssuingAuthoX31d4328b21ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.issuing.authorizations.approve.postIssuingAuthorizationsAuthorization(
            authorization: decoded("\"authorization\""),
            amount: decoded("100"),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture415Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postIssuingAuthorizationsAuthorizationApprove decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostIssuingAuthorizationsAuthorizationDeclinePostIssuingAuthoXf29cf66d3dReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.issuing.authorizations.decline.postIssuingAuthorizationsAuthorization(
            authorization: decoded("\"authorization\""),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture416Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postIssuingAuthorizationsAuthorizationDecline decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostIssuingCardholdersPostIssuingCardholdersReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1IssuingCardholdersMethods.PostIssuingCardholdersOptions(
            billing: decoded(sdkMethodFixture417Value1),
            name: decoded("\"Example Name\"")
        )
        options.company = try decoded("{\"tax_id\":\"tax_-1\"}")
        options.email = try decoded("\"user@example.com\"")
        options.expand = try decoded("[\"expand\"]")
        options.individual = try decoded(sdkMethodFixture417Value2)
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.phoneNumber = try decoded("\"+15555550100\"")
        options.preferredLocales = try decoded("[\"de\"]")
        options.spendingControls = try decoded(sdkMethodFixture417Value3)
        options.status = try decoded("\"active\"")
        options.type = try decoded("\"company\"")
        let actual = try await client.v1.issuing.cardholders.postIssuing(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture417Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postIssuingCardholders decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostIssuingCardholdersCardholderPostIssuingCardholdersCardholderReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1IssuingCardholdersMethods
            .PostIssuingCardholdersCardholderOptions(cardholder: decoded("\"cardholder\""))
        options.billing = try decoded(sdkMethodFixture418Value1)
        options.company = try decoded("{\"tax_id\":\"tax_-1\"}")
        options.email = try decoded("\"user@example.com\"")
        options.expand = try decoded("[\"expand\"]")
        options.individual = try decoded(sdkMethodFixture418Value2)
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.phoneNumber = try decoded("\"+15555550100\"")
        options.preferredLocales = try decoded("[\"de\"]")
        options.spendingControls = try decoded(sdkMethodFixture418Value3)
        options.status = try decoded("\"active\"")
        let actual = try await client.v1.issuing.cardholders.postIssuingCardholder(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture418Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postIssuingCardholdersCardholder decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostIssuingCardsPostIssuingCardsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1IssuingCardsMethods.PostIssuingCardsOptions(
            currency: decoded("\"currency\""),
            type: decoded("\"physical\"")
        )
        options.cardholder = try decoded("\"cardholder\"")
        options.expMonth = try decoded("1")
        options.expYear = try decoded("1")
        options.expand = try decoded("[\"expand\"]")
        options.financialAccount = try decoded("\"financial_account\"")
        options.lifecycleControls = try decoded("{\"cancel_after\":{\"payment_count\":1}}")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.personalizationDesign = try decoded("\"personalization_design\"")
        options.pin = try decoded("{\"encrypted_number\":\"encrypted_number\"}")
        options.replacementFor = try decoded("\"replacement_for\"")
        options.replacementReason = try decoded("\"damaged\"")
        options.secondLine = try decoded("\"second_line\"")
        options.shipping = try decoded(sdkMethodFixture419Value1)
        options.spendingControls = try decoded(sdkMethodFixture419Value2)
        options.status = try decoded("\"active\"")
        let actual = try await client.v1.issuing.cards.postIssuing(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture419Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postIssuingCards decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostIssuingCardsCardPostIssuingCardsCardReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1IssuingCardsMethods.PostIssuingCardsCardOptions(card: decoded("\"card\""))
        options.cancellationReason = try decoded("\"lost\"")
        options.expand = try decoded("[\"expand\"]")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.personalizationDesign = try decoded("\"personalization_design\"")
        options.pin = try decoded("{\"encrypted_number\":\"encrypted_number\"}")
        options.shipping = try decoded(sdkMethodFixture420Value1)
        options.spendingControls = try decoded(sdkMethodFixture420Value2)
        options.status = try decoded("\"active\"")
        let actual = try await client.v1.issuing.cards.postIssuingCard(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture420Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postIssuingCardsCard decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostIssuingDisputesPostIssuingDisputesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.issuing.disputes.postIssuing(
            amount: decoded("100"),
            evidence: decoded(sdkMethodFixture421Value1),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}"),
            transaction: decoded("\"transaction\""),
            treasury: decoded("{\"received_debit\":\"received_debit\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture421Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postIssuingDisputes decoded a response " +
                "that differs from the mock"
        )
    }
}
