// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriStripe
import XCTest

extension SdkMethodTests {
    func testGetTransfersGetTransfersReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.transfers.get(
            created: decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}"),
            destination: decoded("\"destination\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\""),
            transferGroup: decoded("\"transfer_group\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture271Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTransfers decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTransfersIdReversalsGetTransfersIdReversalsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.transfers.reversals.getTransfersId(
            id: decoded("\"id-1\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture272Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTransfersIdReversals decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTransfersTransferGetTransfersTransferReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.transfers.getTransfer(
            transfer: decoded("\"transfer\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture273Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTransfersTransfer decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTransfersTransferReversalsIdGetTransfersTransferReversalsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.transfers.reversals.getTransfersTransferId(
            id: decoded("\"id-1\""),
            transfer: decoded("\"transfer\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture274Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTransfersTransferReversalsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTreasuryCreditReversalsGetTreasuryCreditReversalsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.treasury.creditReversals.getTreasury(
            financialAccount: decoded("\"financial_account\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            receivedCredit: decoded("\"received_credit\""),
            startingAfter: decoded("\"starting_after\""),
            status: decoded("\"canceled\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture275Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTreasuryCreditReversals decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTreasuryCreditReversalsCreditReversalGetTreasuryCreditReveX7ee858c925ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.treasury.creditReversals.getTreasuryCreditReversal(
            creditReversal: decoded("\"credit_reversal\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture276Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTreasuryCreditReversalsCreditReversal decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTreasuryDebitReversalsGetTreasuryDebitReversalsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1TreasuryDebitReversalsMethods
            .GetTreasuryDebitReversalsOptions(financialAccount: decoded("\"financial_account\""))
        options.endingBefore = try decoded("\"ending_before\"")
        options.expand = try decoded("[\"expand\"]")
        options.limit = try decoded("1")
        options.receivedDebit = try decoded("\"received_debit\"")
        options.resolution = try decoded("\"lost\"")
        options.startingAfter = try decoded("\"starting_after\"")
        options.status = try decoded("\"canceled\"")
        let actual = try await client.v1.treasury.debitReversals.getTreasury(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture277Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTreasuryDebitReversals decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTreasuryDebitReversalsDebitReversalGetTreasuryDebitReversaX438df60b2eReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.treasury.debitReversals.getTreasuryDebitReversal(
            debitReversal: decoded("\"debit_reversal\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture278Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTreasuryDebitReversalsDebitReversal decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTreasuryFinancialAccountsGetTreasuryFinancialAccountsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.treasury.financialAccounts.getTreasury(
            created: decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}"),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\""),
            status: decoded("\"closed\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture279Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTreasuryFinancialAccounts decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTreasuryFinancialAccountsFinancialAccountGetTreasuryFinancXdb0a779ce8ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.treasury.financialAccounts.features
            .getTreasuryFinancialAccountsFinancialAccount(
                financialAccount: decoded("\"financial_account\""),
                expand: decoded("[\"expand\"]")
            )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture280Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTreasuryFinancialAccountsFinancialAccount decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTreasuryFinancialAccountsFinancialAccountFeaturesGetTreasuXdff3fcc558ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.treasury.financialAccounts.features
            .getTreasuryFinancialAccountsFinancialAccount(
                financialAccount: decoded("\"financial_account\""),
                expand: decoded("[\"expand\"]")
            )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture281Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTreasuryFinancialAccountsFinancialAccountFeatures decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTreasuryInboundTransfersGetTreasuryInboundTransfersReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.treasury.inboundTransfers.getTreasury(
            financialAccount: decoded("\"financial_account\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\""),
            status: decoded("\"canceled\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture282Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTreasuryInboundTransfers decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTreasuryInboundTransfersIdGetTreasuryInboundTransfersIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.treasury.inboundTransfers.getTreasuryId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture283Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTreasuryInboundTransfersId decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTreasuryOutboundPaymentsGetTreasuryOutboundPaymentsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1TreasuryOutboundPaymentsMethods
            .GetTreasuryOutboundPaymentsOptions(financialAccount: decoded("\"financial_account\""))
        options.created = try decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}")
        options.customer = try decoded("\"customer\"")
        options.endingBefore = try decoded("\"ending_before\"")
        options.expand = try decoded("[\"expand\"]")
        options.limit = try decoded("1")
        options.startingAfter = try decoded("\"starting_after\"")
        options.status = try decoded("\"canceled\"")
        let actual = try await client.v1.treasury.outboundPayments.getTreasury(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture284Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTreasuryOutboundPayments decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTreasuryOutboundPaymentsIdGetTreasuryOutboundPaymentsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.treasury.outboundPayments.getTreasuryId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture285Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTreasuryOutboundPaymentsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTreasuryOutboundTransfersGetTreasuryOutboundTransfersReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.treasury.outboundTransfers.getTreasury(
            financialAccount: decoded("\"financial_account\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\""),
            status: decoded("\"canceled\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture286Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTreasuryOutboundTransfers decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTreasuryOutboundTransfersOutboundTransferGetTreasuryOutbouXdd2d60ea37ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.treasury.outboundTransfers.getTreasuryOutboundTransfer(
            outboundTransfer: decoded("\"outbound_transfer\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture287Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTreasuryOutboundTransfersOutboundTransfer decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTreasuryReceivedCreditsGetTreasuryReceivedCreditsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.treasury.receivedCredits.getTreasury(
            financialAccount: decoded("\"financial_account\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            linkedFlows: decoded("{\"source_flow_type\":\"credit_reversal\"}"),
            startingAfter: decoded("\"starting_after\""),
            status: decoded("\"failed\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture288Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTreasuryReceivedCredits decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTreasuryReceivedCreditsIdGetTreasuryReceivedCreditsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.treasury.receivedCredits.getTreasuryId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture289Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTreasuryReceivedCreditsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTreasuryReceivedDebitsGetTreasuryReceivedDebitsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.treasury.receivedDebits.getTreasury(
            financialAccount: decoded("\"financial_account\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\""),
            status: decoded("\"failed\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture290Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTreasuryReceivedDebits decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTreasuryReceivedDebitsIdGetTreasuryReceivedDebitsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.treasury.receivedDebits.getTreasuryId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture291Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTreasuryReceivedDebitsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTreasuryTransactionEntriesGetTreasuryTransactionEntriesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1TreasuryTransactionEntriesMethods
            .GetTreasuryTransactionEntriesOptions(financialAccount: decoded("\"financial_account\""))
        options.created = try decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}")
        options.effectiveAt = try decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}")
        options.endingBefore = try decoded("\"ending_before\"")
        options.expand = try decoded("[\"expand\"]")
        options.limit = try decoded("1")
        options.orderBy = try decoded("\"created\"")
        options.startingAfter = try decoded("\"starting_after\"")
        options.transaction = try decoded("\"transaction\"")
        let actual = try await client.v1.treasury.transactionEntries.getTreasury(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture292Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTreasuryTransactionEntries decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTreasuryTransactionEntriesIdGetTreasuryTransactionEntriesIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.treasury.transactionEntries.getTreasuryId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture293Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTreasuryTransactionEntriesId decoded a response " +
                "that differs from the mock"
        )
    }
}
