// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriStripe
import XCTest

extension SdkMethodTests {
    func testGetCustomersCustomerBalanceTransactionsTransactionGetCustomerX456eabae7aReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.balanceTransactions.getCustomersCustomerTransaction(
            customer: decoded("\"customer\""),
            transaction: decoded("\"transaction\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture101Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCustomersCustomerBalanceTransactionsTransaction decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCustomersCustomerBankAccountsGetCustomersCustomerBankAccountsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.bankAccounts.getCustomersCustomer(
            customer: decoded("\"customer\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture102Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCustomersCustomerBankAccounts decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCustomersCustomerBankAccountsIdGetCustomersCustomerBankAccountsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.bankAccounts.getCustomersCustomerId(
            customer: decoded("\"customer\""),
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture103Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCustomersCustomerBankAccountsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCustomersCustomerCardsGetCustomersCustomerCardsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.cards.getCustomersCustomer(
            customer: decoded("\"customer\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture104Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCustomersCustomerCards decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCustomersCustomerCardsIdGetCustomersCustomerCardsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.cards.getCustomersCustomerId(
            customer: decoded("\"customer\""),
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture105Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCustomersCustomerCardsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCustomersCustomerCashBalanceGetCustomersCustomerCashBalanceReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.cashBalance.getCustomersCustomer(
            customer: decoded("\"customer\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture106Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCustomersCustomerCashBalance decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCustomersCustomerCashBalanceTransactionsGetCustomersCustomXfca4eab7a8ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.cashBalanceTransactions.getCustomersCustomer(
            customer: decoded("\"customer\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture107Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCustomersCustomerCashBalanceTransactions decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCustomersCustomerCashBalanceTransactionsTransactionGetCustXca5873d34fReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.cashBalanceTransactions.getCustomersCustomerTransaction(
            customer: decoded("\"customer\""),
            transaction: decoded("\"transaction\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture108Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCustomersCustomerCashBalanceTransactionsTransaction decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCustomersCustomerDiscountGetCustomersCustomerDiscountReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.discount.getCustomersCustomer(
            customer: decoded("\"customer\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture109Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCustomersCustomerDiscount decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCustomersCustomerPaymentMethodsGetCustomersCustomerPaymentMethodsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.paymentMethods.getCustomersCustomer(
            customer: decoded("\"customer\""),
            allowRedisplay: decoded("\"always\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\""),
            type: decoded("\"acss_debit\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture110Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCustomersCustomerPaymentMethods decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCustomersCustomerPaymentMethodsPaymentMethodGetCustomersCuXf116976ddbReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.paymentMethods.getCustomersCustomerPaymentMethod(
            customer: decoded("\"customer\""),
            paymentMethod: decoded("\"payment_method\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture111Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCustomersCustomerPaymentMethodsPaymentMethod decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCustomersCustomerSourcesGetCustomersCustomerSourcesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.sources.getCustomersCustomer(
            customer: decoded("\"customer\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            object: decoded("\"object\""),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture112Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCustomersCustomerSources decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCustomersCustomerSourcesIdGetCustomersCustomerSourcesIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.sources.getCustomersCustomerId(
            customer: decoded("\"customer\""),
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture113Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCustomersCustomerSourcesId decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCustomersCustomerSubscriptionsGetCustomersCustomerSubscriptionsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.subscriptions.getCustomersCustomer(
            customer: decoded("\"customer\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture114Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCustomersCustomerSubscriptions decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCustomersCustomerSubscriptionsSubscriptionExposedIdGetCustX2ebcfde64eReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.subscriptions.discount.getCustomersCustomerSubscriptionsSubscriptionExposedId(
            customer: decoded("\"customer\""),
            subscriptionExposedId: decoded("\"subscription_exposed_-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture115Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCustomersCustomerSubscriptionsSubscriptionExposedId decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCustomersCustomerSubscriptionsSubscriptionExposedIdDiscounX2ac467fbd4ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.subscriptions.discount.getCustomersCustomerSubscriptionsSubscriptionExposedId(
            customer: decoded("\"customer\""),
            subscriptionExposedId: decoded("\"subscription_exposed_-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture116Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCustomersCustomerSubscriptionsSubscriptionExposedIdDiscount decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCustomersCustomerTaxIdsGetCustomersCustomerTaxIdsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.taxIds.getCustomersCustomer(
            customer: decoded("\"customer\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture117Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCustomersCustomerTaxIds decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCustomersCustomerTaxIdsIdGetCustomersCustomerTaxIdsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.taxIds.getCustomersCustomerId(
            customer: decoded("\"customer\""),
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture118Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCustomersCustomerTaxIdsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCustomersSearchGetCustomersSearchReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.search.getCustomers(
            query: decoded("\"query\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            page: decoded("\"page\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture119Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCustomersSearch decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetDisputesGetDisputesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.disputes.get(
            charge: decoded("\"charge\""),
            created: decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}"),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            paymentIntent: decoded("\"payment_intent\""),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture120Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getDisputes decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetDisputesDisputeGetDisputesDisputeReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.disputes.getDispute(
            dispute: decoded("\"dispute\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture121Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getDisputesDispute decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetEntitlementsActiveEntitlementsGetEntitlementsActiveEntitlementsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.entitlements.activeEntitlements.getEntitlements(
            customer: decoded("\"customer\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture122Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getEntitlementsActiveEntitlements decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetEntitlementsActiveEntitlementsIdGetEntitlementsActiveEntitlementsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.entitlements.activeEntitlements.getEntitlementsId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture123Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getEntitlementsActiveEntitlementsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetEntitlementsFeaturesGetEntitlementsFeaturesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.entitlements.features.getEntitlements(
            archived: decoded("false"),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            lookupKey: decoded("\"lookup_key\""),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture124Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getEntitlementsFeatures decoded a response " +
                "that differs from the mock"
        )
    }
}
