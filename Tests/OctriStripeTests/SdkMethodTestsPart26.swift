// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriStripe
import XCTest

extension SdkMethodTests {
    func testPostTestHelpersTreasuryInboundTransfersIdFailPostTestHelpersTX927066009eReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.testHelpers.treasury.inboundTransfers.fail.postTestHelpersTreasuryInboundTransfersId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]"),
            failureDetails: decoded("{\"code\":\"account_closed\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture560Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTestHelpersTreasuryInboundTransfersIdFail decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTestHelpersTreasuryInboundTransfersIdReturnPostTestHelperX678cc472c3ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.testHelpers.treasury.inboundTransfers.`return`.postTestHelpersTreasuryInboundTransfersId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture561Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTestHelpersTreasuryInboundTransfersIdReturn decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTestHelpersTreasuryInboundTransfersIdSucceedPostTestHelpeX695b33db30ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.testHelpers.treasury.inboundTransfers.succeed.postTestHelpersTreasuryInboundTransfersId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture562Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTestHelpersTreasuryInboundTransfersIdSucceed decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTestHelpersTreasuryOutboundPaymentsIdPostTestHelpersTreasX407330b884ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.testHelpers.treasury.outboundPayments.fail.postTestHelpersTreasuryOutboundPaymentsId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture563Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTestHelpersTreasuryOutboundPaymentsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTestHelpersTreasuryOutboundPaymentsIdFailPostTestHelpersTX41af13fce2ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.testHelpers.treasury.outboundPayments.fail.postTestHelpersTreasuryOutboundPaymentsId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture564Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTestHelpersTreasuryOutboundPaymentsIdFail decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTestHelpersTreasuryOutboundPaymentsIdPostPostTestHelpersTX5a913bbdeaReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.testHelpers.treasury.outboundPayments.post.postTestHelpersTreasuryOutboundPaymentsId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture565Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTestHelpersTreasuryOutboundPaymentsIdPost decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTestHelpersTreasuryOutboundPaymentsIdReturnPostTestHelperX452fe6702aReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.testHelpers.treasury.outboundPayments.`return`.postTestHelpersTreasuryOutboundPaymentsId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]"),
            returnedDetails: decoded("{\"code\":\"account_closed\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture566Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTestHelpersTreasuryOutboundPaymentsIdReturn decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTestHelpersTreasuryOutboundTransfersOutboundTransferPostTX12ab3ddd94ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.testHelpers.treasury.outboundTransfers.fail.postTestHelpersTreasuryOutboundTransfersOutboundTransfer(
            outboundTransfer: decoded("\"outbound_transfer\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture567Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTestHelpersTreasuryOutboundTransfersOutboundTransfer decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTestHelpersTreasuryOutboundTransfersOutboundTransferFailPX1c213f5ad0ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.testHelpers.treasury.outboundTransfers.fail.postTestHelpersTreasuryOutboundTransfersOutboundTransfer(
            outboundTransfer: decoded("\"outbound_transfer\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture568Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTestHelpersTreasuryOutboundTransfersOutboundTransferFail decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTestHelpersTreasuryOutboundTransfersOutboundTransferPostPXe63be5a3c7ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.testHelpers.treasury.outboundTransfers.post.postTestHelpersTreasuryOutboundTransfersOutboundTransfer(
            outboundTransfer: decoded("\"outbound_transfer\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture569Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTestHelpersTreasuryOutboundTransfersOutboundTransferPost decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTestHelpersTreasuryOutboundTransfersOutboundTransferReturX9d13d1bdc4ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.testHelpers.treasury.outboundTransfers.`return`.postTestHelpersTreasuryOutboundTransfersOutboundTransfer(
            outboundTransfer: decoded("\"outbound_transfer\""),
            expand: decoded("[\"expand\"]"),
            returnedDetails: decoded("{\"code\":\"account_closed\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture570Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTestHelpersTreasuryOutboundTransfersOutboundTransferReturn decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTestHelpersTreasuryReceivedCreditsPostTestHelpersTreasuryXad36664063ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.testHelpers.treasury.receivedCredits.postTestHelpersTreasury(
            amount: decoded("100"),
            currency: decoded("\"currency\""),
            financialAccount: decoded("\"financial_account\""),
            network: decoded("\"ach\""),
            description: decoded("\"example text\""),
            expand: decoded("[\"expand\"]"),
            initiatingPaymentMethodDetails: decoded(sdkMethodFixture571Value1)
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture571Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTestHelpersTreasuryReceivedCredits decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTestHelpersTreasuryReceivedDebitsPostTestHelpersTreasuryRX09ba9eae5cReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.testHelpers.treasury.receivedDebits.postTestHelpersTreasury(
            amount: decoded("100"),
            currency: decoded("\"currency\""),
            financialAccount: decoded("\"financial_account\""),
            network: decoded("\"ach\""),
            description: decoded("\"example text\""),
            expand: decoded("[\"expand\"]"),
            initiatingPaymentMethodDetails: decoded(sdkMethodFixture572Value1)
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture572Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTestHelpersTreasuryReceivedDebits decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTokensPostTokensReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1TokensMethods.PostTokensOptions()
        options.account = try decoded(sdkMethodFixture573Value1)
        options.bankAccount = try decoded(sdkMethodFixture573Value2)
        options.card = try decoded(sdkMethodFixture573Value3)
        options.customer = try decoded("\"customer\"")
        options.cvcUpdate = try decoded("{\"cvc\":\"cvc\"}")
        options.expand = try decoded("[\"expand\"]")
        options.person = try decoded(sdkMethodFixture573Value4)
        options.pii = try decoded("{\"id_number\":\"id_number\"}")
        let actual = try await client.v1.tokens.post(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture573Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTokens decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTopupsPostTopupsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1TopupsMethods.PostTopupsOptions(amount: decoded("100"), currency: decoded("\"currency\""))
        options.description = try decoded("\"example text\"")
        options.expand = try decoded("[\"expand\"]")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.paymentMethod = try decoded("\"payment_method\"")
        options.paymentMethodOptions = try decoded("{\"us_bank_account\":{\"network\":\"ach\"}}")
        options.source = try decoded("\"source\"")
        options.statementDescriptor = try decoded("\"statement_descr\"")
        options.transferGroup = try decoded("\"transfer_group\"")
        let actual = try await client.v1.topups.post(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture574Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTopups decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTopupsTopupPostTopupsTopupReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.topups.cancel.postTopupsTopup(
            topup: decoded("\"topup\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture575Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTopupsTopup decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTopupsTopupCancelPostTopupsTopupCancelReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.topups.cancel.postTopupsTopup(
            topup: decoded("\"topup\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture576Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTopupsTopupCancel decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTransfersPostTransfersReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1TransfersMethods.PostTransfersOptions(currency: decoded("\"currency\""), destination: decoded("\"destination\""))
        options.amount = try decoded("100")
        options.description = try decoded("\"example text\"")
        options.expand = try decoded("[\"expand\"]")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.sourceTransaction = try decoded("\"source_transaction\"")
        options.sourceType = try decoded("\"bank_account\"")
        options.transferGroup = try decoded("\"transfer_group\"")
        let actual = try await client.v1.transfers.post(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture577Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTransfers decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTransfersIdReversalsPostTransfersIdReversalsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.transfers.reversals.postTransfersId(
            id: decoded("\"id-1\""),
            amount: decoded("100"),
            description: decoded("\"example text\""),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}"),
            refundApplicationFee: decoded("false")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture578Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTransfersIdReversals decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTransfersTransferPostTransfersTransferReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.transfers.postTransfer(
            transfer: decoded("\"transfer\""),
            description: decoded("\"example text\""),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture579Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTransfersTransfer decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTransfersTransferReversalsIdPostTransfersTransferReversalsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.transfers.reversals.postTransfersTransferId(
            id: decoded("\"id-1\""),
            transfer: decoded("\"transfer\""),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture580Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTransfersTransferReversalsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTreasuryCreditReversalsPostTreasuryCreditReversalsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.treasury.creditReversals.postTreasury(
            receivedCredit: decoded("\"received_credit\""),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture581Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTreasuryCreditReversals decoded a response " +
                "that differs from the mock"
        )
    }
}
