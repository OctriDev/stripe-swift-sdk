// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriStripe
import XCTest

extension SdkMethodTests {
    func testGetReportingReportRunsGetReportingReportRunsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.reporting.reportRuns.getReporting(
            created: decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}"),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture222Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getReportingReportRuns decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetReportingReportRunsReportRunGetReportingReportRunsReportRunReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.reporting.reportRuns.getReportingReportRun(
            reportRun: decoded("\"report_run\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture223Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getReportingReportRunsReportRun decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetReportingReportTypesGetReportingReportTypesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.reporting.reportTypes.getReporting(
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture224Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getReportingReportTypes decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetReportingReportTypesReportTypeGetReportingReportTypesReportTypeReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.reporting.reportTypes.getReportingReportType(
            reportType: decoded("\"report_type\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture225Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getReportingReportTypesReportType decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetReviewsGetReviewsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.reviews.get(
            created: decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}"),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture226Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getReviews decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetReviewsReviewGetReviewsReviewReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.reviews.getReview(
            review: decoded("\"review\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture227Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getReviewsReview decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetSetupAttemptsGetSetupAttemptsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.setupAttempts.get(
            setupIntent: decoded("\"setup_intent\""),
            created: decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}"),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture228Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getSetupAttempts decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetSetupIntentsGetSetupIntentsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1SetupIntentsMethods.GetSetupIntentsOptions()
        options.attachToSelf = try decoded("false")
        options.created = try decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}")
        options.customer = try decoded("\"customer\"")
        options.customerAccount = try decoded("\"customer_account\"")
        options.endingBefore = try decoded("\"ending_before\"")
        options.expand = try decoded("[\"expand\"]")
        options.limit = try decoded("1")
        options.paymentMethod = try decoded("\"payment_method\"")
        options.startingAfter = try decoded("\"starting_after\"")
        let actual = try await client.v1.setupIntents.get(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture229Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getSetupIntents decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetSetupIntentsIntentGetSetupIntentsIntentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.setupIntents.getIntent(
            intent: decoded("\"intent\""),
            clientSecret: decoded("\"client_secret\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture230Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getSetupIntentsIntent decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetShippingRatesGetShippingRatesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.shippingRates.get(
            active: decoded("false"),
            created: decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}"),
            currency: decoded("\"currency\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture231Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getShippingRates decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetShippingRatesShippingRateTokenGetShippingRatesShippingRateTokenReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.shippingRates.getShippingRateToken(
            shippingRateToken: decoded("\"shipping_rate_token\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture232Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getShippingRatesShippingRateToken decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetSigmaScheduledQueryRunsGetSigmaScheduledQueryRunsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.sigma.scheduledQueryRuns.getSigma(
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture233Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getSigmaScheduledQueryRuns decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetSigmaScheduledQueryRunsScheduledQueryRunGetSigmaScheduledQX9d7340124bReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.sigma.scheduledQueryRuns.getSigmaScheduledQueryRun(
            scheduledQueryRun: decoded("\"scheduled_query_run\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture234Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getSigmaScheduledQueryRunsScheduledQueryRun decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetSourcesSourceGetSourcesSourceReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.sources.sourceTransactions.getSourcesSource(
            source: decoded("\"source\""),
            endingBefore: decoded("\"sample\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("\"sample\""),
            startingAfter: decoded("\"sample\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture235Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getSourcesSource decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetSourcesSourceMandateNotificationsMandateNotificationGetSouX71f1de37b6ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.sources.mandateNotifications.getSourcesSourceMandateNotification(
            mandateNotification: decoded("\"2024-01-01T00:00:00Z\""),
            source: decoded("\"source\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture236Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getSourcesSourceMandateNotificationsMandateNotification decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetSourcesSourceSourceTransactionsGetSourcesSourceSourceTransactionsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.sources.sourceTransactions.getSourcesSource(
            source: decoded("\"source\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture237Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getSourcesSourceSourceTransactions decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetSourcesSourceSourceTransactionsSourceTransactionGetSourcesX8ecb004d85ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.sources.sourceTransactions.getSourcesSourceSourceTransaction(
            source: decoded("\"source\""),
            sourceTransaction: decoded("\"source_transaction\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture238Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getSourcesSourceSourceTransactionsSourceTransaction decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetSubscriptionItemsGetSubscriptionItemsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.subscriptionItems.get(
            subscription: decoded("\"subscription\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture239Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getSubscriptionItems decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetSubscriptionItemsItemGetSubscriptionItemsItemReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.subscriptionItems.getItem(
            item: decoded("\"item\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture240Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getSubscriptionItemsItem decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetSubscriptionsGetSubscriptionsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.subscriptions.search.getSubscriptions(
            query: decoded("\"sample\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            page: decoded("\"sample\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture241Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getSubscriptions decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetSubscriptionSchedulesGetSubscriptionSchedulesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1SubscriptionSchedulesMethods.GetSubscriptionSchedulesOptions()
        options.canceledAt = try decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}")
        options.completedAt = try decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}")
        options.created = try decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}")
        options.customer = try decoded("\"customer\"")
        options.customerAccount = try decoded("\"customer_account\"")
        options.endingBefore = try decoded("\"ending_before\"")
        options.expand = try decoded("[\"expand\"]")
        options.limit = try decoded("1")
        options.releasedAt = try decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}")
        options.scheduled = try decoded("false")
        options.startingAfter = try decoded("\"starting_after\"")
        let actual = try await client.v1.subscriptionSchedules.get(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture242Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getSubscriptionSchedules decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetSubscriptionSchedulesScheduleGetSubscriptionSchedulesScheduleReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.subscriptionSchedules.getSchedule(
            schedule: decoded("\"schedule\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture243Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getSubscriptionSchedulesSchedule decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetSubscriptionsSearchGetSubscriptionsSearchReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.subscriptions.search.getSubscriptions(
            query: decoded("\"query\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            page: decoded("\"page\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture244Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getSubscriptionsSearch decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetSubscriptionsSubscriptionExposedIdGetSubscriptionsSubscripX955a039508ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.subscriptions.getSubscriptionExposedId(
            subscriptionExposedId: decoded("\"subscription_exposed_-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture245Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getSubscriptionsSubscriptionExposedId decoded a response " +
                "that differs from the mock"
        )
    }
}
