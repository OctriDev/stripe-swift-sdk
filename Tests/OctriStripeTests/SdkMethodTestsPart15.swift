// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriStripe
import XCTest

extension SdkMethodTests {
    func testPostBillingMetersIdReactivatePostBillingMetersIdReactivateReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.billing.meters.reactivate.postBillingMetersId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture337Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postBillingMetersIdReactivate decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostBillingPortalConfigurationsPostBillingPortalConfigurationsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.billingPortal.configurations.postBillingPortal(
            features: decoded(sdkMethodFixture338Value1),
            businessProfile: decoded(sdkMethodFixture338Value2),
            defaultReturnUrl: decoded("\"https://example.com\""),
            expand: decoded("[\"expand\"]"),
            loginPage: decoded("{\"enabled\":false}"),
            metadata: decoded("{\"key\":\"key\"}"),
            name: decoded("\"Example Name\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture338Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postBillingPortalConfigurations decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostBillingPortalConfigurationsConfigurationPostBillingPortalXfc1e8ca977ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1BillingPortalConfigurationsMethods.PostBillingPortalConfigurationsConfigurationOptions(configuration: decoded("\"configuration\""))
        options.active = try decoded("false")
        options.businessProfile = try decoded(sdkMethodFixture339Value1)
        options.defaultReturnUrl = try decoded("\"https://example.com\"")
        options.expand = try decoded("[\"expand\"]")
        options.features = try decoded(sdkMethodFixture339Value2)
        options.loginPage = try decoded("{\"enabled\":false}")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.name = try decoded("\"Example Name\"")
        let actual = try await client.v1.billingPortal.configurations.postBillingPortalConfiguration(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture339Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postBillingPortalConfigurationsConfiguration decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostBillingPortalSessionsPostBillingPortalSessionsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1BillingPortalSessionsMethods.PostBillingPortalSessionsOptions()
        options.configuration = try decoded("\"configuration\"")
        options.customer = try decoded("\"customer\"")
        options.customerAccount = try decoded("\"customer_account\"")
        options.expand = try decoded("[\"expand\"]")
        options.flowData = try decoded(sdkMethodFixture340Value1)
        options.locale = try decoded("\"auto\"")
        options.onBehalfOf = try decoded("\"on_behalf_of\"")
        options.returnUrl = try decoded("\"https://example.com\"")
        let actual = try await client.v1.billingPortal.sessions.postBillingPortal(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture340Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postBillingPortalSessions decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostChargesPostChargesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1ChargesMethods.PostChargesOptions()
        options.amount = try decoded("100")
        options.applicationFee = try decoded("1")
        options.applicationFeeAmount = try decoded("100")
        options.capture = try decoded("false")
        options.card = try decoded(sdkMethodFixture341Value1)
        options.currency = try decoded("\"currency\"")
        options.customer = try decoded("\"customer\"")
        options.description = try decoded("\"example text\"")
        options.destination = try decoded("{\"account\":\"account\",\"amount\":100}")
        options.expand = try decoded("[\"expand\"]")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.onBehalfOf = try decoded("\"on_behalf_of\"")
        options.radarOptions = try decoded("{\"session\":\"session\"}")
        options.receiptEmail = try decoded("\"user@example.com\"")
        options.shipping = try decoded(sdkMethodFixture341Value2)
        options.source = try decoded("\"source\"")
        options.statementDescriptor = try decoded("\"statement_descriptor\"")
        options.statementDescriptorSuffix = try decoded("\"statement_descriptor_s\"")
        options.transferData = try decoded(sdkMethodFixture341Value3)
        options.transferGroup = try decoded("\"transfer_group\"")
        let actual = try await client.v1.charges.post(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture341Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postCharges decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostChargesChargePostChargesChargeReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1ChargesCaptureMethods.PostChargesChargeCaptureOptions(charge: decoded("\"charge\""))
        options.expand = try decoded("[\"expand\"]")
        options.receiptEmail = try decoded("\"user@example.com\"")
        options.transferGroup = try decoded("\"transfer_group\"")
        let actual = try await client.v1.charges.capture.postChargesCharge(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture342Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postChargesCharge decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostChargesChargeCapturePostChargesChargeCaptureReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1ChargesCaptureMethods.PostChargesChargeCaptureOptions(charge: decoded("\"charge\""))
        options.amount = try decoded("100")
        options.applicationFee = try decoded("1")
        options.applicationFeeAmount = try decoded("100")
        options.expand = try decoded("[\"expand\"]")
        options.receiptEmail = try decoded("\"user@example.com\"")
        options.statementDescriptor = try decoded("\"statement_descriptor\"")
        options.statementDescriptorSuffix = try decoded("\"statement_descriptor_s\"")
        options.transferData = try decoded("{\"amount\":100}")
        options.transferGroup = try decoded("\"transfer_group\"")
        let actual = try await client.v1.charges.capture.postChargesCharge(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture343Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postChargesChargeCapture decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostChargesChargeDisputePostChargesChargeDisputeReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.charges.dispute.close.postChargesChargeDispute(
            charge: decoded("\"charge\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture344Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postChargesChargeDispute decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostChargesChargeDisputeClosePostChargesChargeDisputeCloseReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.charges.dispute.close.postChargesChargeDispute(
            charge: decoded("\"charge\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture345Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postChargesChargeDisputeClose decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostChargesChargeRefundPostChargesChargeRefundReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.charges.refunds.postChargesChargeRefund(
            charge: decoded("\"charge\""),
            refund: decoded("\"sample\""),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture346Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postChargesChargeRefund decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostChargesChargeRefundsPostChargesChargeRefundsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1ChargesRefundsMethods.PostChargesChargeRefundsOptions(charge: decoded("\"charge\""))
        options.amount = try decoded("100")
        options.currency = try decoded("\"currency\"")
        options.customer = try decoded("\"customer\"")
        options.expand = try decoded("[\"expand\"]")
        options.instructionsEmail = try decoded("\"user@example.com\"")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.origin = try decoded("\"customer_balance\"")
        options.paymentIntent = try decoded("\"payment_intent\"")
        options.reason = try decoded("\"duplicate\"")
        options.refundApplicationFee = try decoded("false")
        options.reverseTransfer = try decoded("false")
        let actual = try await client.v1.charges.refunds.postChargesCharge(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture347Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postChargesChargeRefunds decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostChargesChargeRefundsRefundPostChargesChargeRefundsRefundReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.charges.refunds.postChargesChargeRefund(
            charge: decoded("\"charge\""),
            refund: decoded("\"refund\""),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture348Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postChargesChargeRefundsRefund decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostCheckoutSessionsPostCheckoutSessionsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1CheckoutSessionsMethods.PostCheckoutSessionsOptions()
        options.adaptivePricing = try decoded("{\"enabled\":false}")
        options.afterExpiration = try decoded("{\"recovery\":{\"allow_promotion_codes\":false,\"enabled\":false}}")
        options.allowPromotionCodes = try decoded("false")
        options.automaticTax = try decoded(sdkMethodFixture349Value1)
        options.billingAddressCollection = try decoded("\"auto\"")
        options.brandingSettings = try decoded(sdkMethodFixture349Value2)
        options.cancelUrl = try decoded("\"https://example.com\"")
        options.clientReferenceId = try decoded("\"client_reference_-1\"")
        options.consentCollection = try decoded(sdkMethodFixture349Value3)
        options.currency = try decoded("\"currency\"")
        options.customFields = try decoded(sdkMethodFixture349Value4)
        options.customText = try decoded(sdkMethodFixture349Value5)
        options.customer = try decoded("\"customer\"")
        options.customerAccount = try decoded("\"customer_account\"")
        options.customerCreation = try decoded("\"always\"")
        options.customerEmail = try decoded("\"user@example.com\"")
        options.customerUpdate = try decoded("{\"address\":\"auto\",\"name\":\"auto\",\"shipping\":\"auto\"}")
        options.discounts = try decoded("[{\"coupon\":\"coupon\",\"promotion_code\":\"promotion_code\"}]")
        options.excludedPaymentMethodTypes = try decoded("[\"acss_debit\"]")
        options.expand = try decoded("[\"expand\"]")
        options.expiresAt = try decoded("1")
        options.integrationIdentifier = try decoded("\"integration_identifier\"")
        options.invoiceCreation = try decoded(sdkMethodFixture349Value6)
        options.lineItems = try decoded(sdkMethodFixture349Value7)
        options.locale = try decoded("\"auto\"")
        options.managedPayments = try decoded("{\"enabled\":false}")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.mode = try decoded("\"payment\"")
        options.nameCollection = try decoded(sdkMethodFixture349Value8)
        options.optionalItems = try decoded(sdkMethodFixture349Value9)
        options.originContext = try decoded("\"mobile_app\"")
        options.paymentIntentData = try decoded(sdkMethodFixture349Value10)
        options.paymentMethodCollection = try decoded("\"always\"")
        options.paymentMethodConfiguration = try decoded("\"payment_method_configuration\"")
        options.paymentMethodData = try decoded("{\"allow_redisplay\":\"always\"}")
        options.paymentMethodOptions = try decoded(sdkMethodFixture349Value11)
        options.paymentMethodTypes = try decoded("[\"acss_debit\"]")
        options.permissions = try decoded("{\"update_shipping_details\":\"client_only\"}")
        options.phoneNumberCollection = try decoded("{\"enabled\":false}")
        options.redirectOnCompletion = try decoded("\"always\"")
        options.returnUrl = try decoded("\"https://example.com\"")
        options.savedPaymentMethodOptions = try decoded(sdkMethodFixture349Value12)
        options.setupIntentData = try decoded(sdkMethodFixture349Value13)
        options.shippingAddressCollection = try decoded("{\"allowed_countries\":[\"AC\"]}")
        options.shippingOptions = try decoded(sdkMethodFixture349Value14)
        options.submitType = try decoded("\"auto\"")
        options.subscriptionData = try decoded(sdkMethodFixture349Value15)
        options.successUrl = try decoded("\"https://example.com\"")
        options.taxIdCollection = try decoded("{\"enabled\":false,\"required\":\"if_supported\"}")
        options.uiMode = try decoded("\"elements\"")
        options.walletOptions = try decoded("{\"link\":{\"display\":\"auto\"}}")
        let actual = try await client.v1.checkout.sessions.postCheckout(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture349Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postCheckoutSessions decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostCheckoutSessionsSessionPostCheckoutSessionsSessionReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.checkout.sessions.expire.postCheckoutSessionsSession(
            session: decoded("\"session\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture350Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postCheckoutSessionsSession decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostCheckoutSessionsSessionExpirePostCheckoutSessionsSessionExpireReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.checkout.sessions.expire.postCheckoutSessionsSession(
            session: decoded("\"session\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture351Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postCheckoutSessionsSessionExpire decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostClimateOrdersPostClimateOrdersReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.climate.orders.postClimate(
            product: decoded("\"product\""),
            amount: decoded("100"),
            beneficiary: decoded("{\"public_name\":\"Example Name\"}"),
            currency: decoded("\"currency\""),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}"),
            metricTons: decoded("\"metric_tons\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture352Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postClimateOrders decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostClimateOrdersOrderPostClimateOrdersOrderReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.climate.orders.cancel.postClimateOrdersOrder(
            order: decoded("\"order\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture353Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postClimateOrdersOrder decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostClimateOrdersOrderCancelPostClimateOrdersOrderCancelReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.climate.orders.cancel.postClimateOrdersOrder(
            order: decoded("\"order\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture354Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postClimateOrdersOrderCancel decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostCouponsPostCouponsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1CouponsMethods.PostCouponsOptions()
        options.amountOff = try decoded("100")
        options.appliesTo = try decoded("{\"products\":[\"products\"]}")
        options.currency = try decoded("\"currency\"")
        options.currencyOptions = try decoded("{\"key\":{\"amount_off\":100}}")
        options.duration = try decoded("\"forever\"")
        options.durationInMonths = try decoded("1")
        options.expand = try decoded("[\"expand\"]")
        options.id = try decoded("\"id-1\"")
        options.maxRedemptions = try decoded("1")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.name = try decoded("\"Example Name\"")
        options.percentOff = try decoded("1")
        options.redeemBy = try decoded("1")
        let actual = try await client.v1.coupons.post(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture355Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postCoupons decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostCouponsCouponPostCouponsCouponReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.coupons.postCoupon(
            coupon: decoded("\"coupon\""),
            currencyOptions: decoded("{\"key\":{\"amount_off\":100}}"),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}"),
            name: decoded("\"Example Name\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture356Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postCouponsCoupon decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostCreditNotesPostCreditNotesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1CreditNotesMethods.PostCreditNotesOptions(invoice: decoded("\"invoice\""))
        options.amount = try decoded("100")
        options.creditAmount = try decoded("100")
        options.effectiveAt = try decoded("1")
        options.emailType = try decoded("\"credit_note\"")
        options.expand = try decoded("[\"expand\"]")
        options.lines = try decoded(sdkMethodFixture357Value1)
        options.memo = try decoded("\"memo\"")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.outOfBandAmount = try decoded("100")
        options.reason = try decoded("\"duplicate\"")
        options.refundAmount = try decoded("100")
        options.refunds = try decoded(sdkMethodFixture357Value2)
        options.shippingCost = try decoded("{\"shipping_rate\":\"shipping_rate\"}")
        let actual = try await client.v1.creditNotes.post(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture357Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postCreditNotes decoded a response " +
                "that differs from the mock"
        )
    }
}
