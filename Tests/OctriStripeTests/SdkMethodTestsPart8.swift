// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriStripe
import XCTest

extension SdkMethodTests {
    func testGetLinkAccountSessionsSessionGetLinkAccountSessionsSessionReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.linkAccountSessions.getSession(
            session: decoded("\"session\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture173Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getLinkAccountSessionsSession decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetLinkedAccountsGetLinkedAccountsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.linkedAccounts.get(
            accountHolder: decoded(sdkMethodFixture174Value1),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            session: decoded("\"session\""),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture174Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getLinkedAccounts decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetLinkedAccountsAccountGetLinkedAccountsAccountReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.linkedAccounts.owners.getLinkedAccountsAccount(
            account: decoded("\"account\""),
            ownership: decoded("\"sample\""),
            endingBefore: decoded("\"sample\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("\"sample\""),
            startingAfter: decoded("\"sample\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture175Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getLinkedAccountsAccount decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetLinkedAccountsAccountOwnersGetLinkedAccountsAccountOwnersReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.linkedAccounts.owners.getLinkedAccountsAccount(
            account: decoded("\"account\""),
            ownership: decoded("\"ownership\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture176Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getLinkedAccountsAccountOwners decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetMandatesMandateGetMandatesMandateReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.mandates.getMandate(
            mandate: decoded("\"2024-01-01T00:00:00Z\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture177Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getMandatesMandate decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetPaymentAttemptRecordsGetPaymentAttemptRecordsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.paymentAttemptRecords.get(
            paymentRecord: decoded("\"payment_record\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture178Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getPaymentAttemptRecords decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetPaymentAttemptRecordsIdGetPaymentAttemptRecordsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.paymentAttemptRecords.getId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture179Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getPaymentAttemptRecordsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetPaymentIntentsGetPaymentIntentsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.paymentIntents.search.getPaymentIntents(
            query: decoded("\"sample\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            page: decoded("\"sample\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture180Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getPaymentIntents decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetPaymentIntentsIntentGetPaymentIntentsIntentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.paymentIntents.amountDetailsLineItems.getPaymentIntentsIntent(
            intent: decoded("\"intent\""),
            endingBefore: decoded("\"sample\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("\"sample\""),
            startingAfter: decoded("\"sample\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture181Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getPaymentIntentsIntent decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetPaymentIntentsIntentAmountDetailsLineItemsGetPaymentIntentX17441cfc9cReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.paymentIntents.amountDetailsLineItems.getPaymentIntentsIntent(
            intent: decoded("\"intent\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture182Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getPaymentIntentsIntentAmountDetailsLineItems decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetPaymentIntentsSearchGetPaymentIntentsSearchReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.paymentIntents.search.getPaymentIntents(
            query: decoded("\"query\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            page: decoded("\"page\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture183Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getPaymentIntentsSearch decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetPaymentLinksGetPaymentLinksReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.paymentLinks.get(
            active: decoded("false"),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture184Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getPaymentLinks decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetPaymentLinksPaymentLinkGetPaymentLinksPaymentLinkReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.paymentLinks.lineItems.getPaymentLinksPaymentLink(
            paymentLink: decoded("\"https://example.com\""),
            endingBefore: decoded("\"sample\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("\"sample\""),
            startingAfter: decoded("\"sample\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture185Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getPaymentLinksPaymentLink decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetPaymentLinksPaymentLinkLineItemsGetPaymentLinksPaymentLinkLineItemsReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.paymentLinks.lineItems.getPaymentLinksPaymentLink(
            paymentLink: decoded("\"https://example.com\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture186Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getPaymentLinksPaymentLinkLineItems decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetPaymentMethodConfigurationsGetPaymentMethodConfigurationsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.paymentMethodConfigurations.get(
            active: decoded("false"),
            application: decoded("\"application\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture187Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getPaymentMethodConfigurations decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetPaymentMethodConfigurationsConfigurationGetPaymentMethodCoX9ea8107661ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.paymentMethodConfigurations.getConfiguration(
            configuration: decoded("\"configuration\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture188Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getPaymentMethodConfigurationsConfiguration decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetPaymentMethodDomainsGetPaymentMethodDomainsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.paymentMethodDomains.get(
            domainName: decoded("\"Example Name\""),
            enabled: decoded("false"),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture189Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getPaymentMethodDomains decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetPaymentMethodDomainsPaymentMethodDomainGetPaymentMethodDomXb99efbc81dReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.paymentMethodDomains.getPaymentMethodDomain(
            paymentMethodDomain: decoded("\"payment_method_domain\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture190Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getPaymentMethodDomainsPaymentMethodDomain decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetPaymentMethodsGetPaymentMethodsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1PaymentMethodsMethods.GetPaymentMethodsOptions()
        options.allowRedisplay = try decoded("\"always\"")
        options.customer = try decoded("\"customer\"")
        options.customerAccount = try decoded("\"customer_account\"")
        options.endingBefore = try decoded("\"ending_before\"")
        options.expand = try decoded("[\"expand\"]")
        options.limit = try decoded("1")
        options.startingAfter = try decoded("\"starting_after\"")
        options.type = try decoded("\"acss_debit\"")
        let actual = try await client.v1.paymentMethods.get(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture191Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getPaymentMethods decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetPaymentMethodsPaymentMethodGetPaymentMethodsPaymentMethodReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.paymentMethods.getPaymentMethod(
            paymentMethod: decoded("\"payment_method\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture192Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getPaymentMethodsPaymentMethod decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetPaymentRecordsGetPaymentRecordsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.paymentRecords.get(
            createdAfter: decoded("1"),
            createdBefore: decoded("1"),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture193Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getPaymentRecords decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetPaymentRecordsIdGetPaymentRecordsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.paymentRecords.getId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture194Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getPaymentRecordsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetPayoutsGetPayoutsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1PayoutsMethods.GetPayoutsOptions()
        options.arrivalDate = try decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}")
        options.created = try decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}")
        options.destination = try decoded("\"destination\"")
        options.endingBefore = try decoded("\"ending_before\"")
        options.expand = try decoded("[\"expand\"]")
        options.limit = try decoded("1")
        options.startingAfter = try decoded("\"starting_after\"")
        options.status = try decoded("\"active\"")
        let actual = try await client.v1.payouts.get(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture195Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getPayouts decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetPayoutsPayoutGetPayoutsPayoutReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.payouts.getPayout(
            payout: decoded("\"payout\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture196Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getPayoutsPayout decoded a response " +
                "that differs from the mock"
        )
    }
}
