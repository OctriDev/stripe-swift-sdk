// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriStripe
import XCTest

extension SdkMethodTests {
    func testPostTreasuryDebitReversalsPostTreasuryDebitReversalsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.treasury.debitReversals.postTreasury(
            receivedDebit: decoded("\"received_debit\""),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture582Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTreasuryDebitReversals decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTreasuryFinancialAccountsPostTreasuryFinancialAccountsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.treasury.financialAccounts.postTreasury(
            supportedCurrencies: decoded("[\"supported_currencies\"]"),
            expand: decoded("[\"expand\"]"),
            features: decoded(sdkMethodFixture583Value1),
            metadata: decoded("{\"key\":\"key\"}"),
            nickname: decoded("\"Example Name\""),
            platformRestrictions: decoded("{\"inbound_flows\":\"restricted\",\"outbound_flows\":\"restricted\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture583Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTreasuryFinancialAccounts decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTreasuryFinancialAccountsFinancialAccountPostTreasuryFinaX14a034780bReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.treasury.financialAccounts.close.postTreasuryFinancialAccountsFinancialAccount(
            financialAccount: decoded("\"financial_account\""),
            expand: decoded("[\"expand\"]"),
            forwardingSettings: decoded(sdkMethodFixture584Value1)
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture584Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTreasuryFinancialAccountsFinancialAccount decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTreasuryFinancialAccountsFinancialAccountClosePostTreasurX1adfef7ebdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.treasury.financialAccounts.close.postTreasuryFinancialAccountsFinancialAccount(
            financialAccount: decoded("\"financial_account\""),
            expand: decoded("[\"expand\"]"),
            forwardingSettings: decoded(sdkMethodFixture585Value1)
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture585Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTreasuryFinancialAccountsFinancialAccountClose decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTreasuryFinancialAccountsFinancialAccountFeaturesPostTreaXf3d00b1703ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1TreasuryFinancialAccountsFeaturesMethods.PostTreasuryFinancialAccountsFinancialAccountFeaturesOptions(financialAccount: decoded("\"financial_account\""))
        options.cardIssuing = try decoded("{\"requested\":false}")
        options.depositInsurance = try decoded("{\"requested\":false}")
        options.expand = try decoded("[\"expand\"]")
        options.financialAddresses = try decoded("{\"aba\":{\"requested\":false}}")
        options.inboundTransfers = try decoded("{\"ach\":{\"requested\":false}}")
        options.intraStripeFlows = try decoded("{\"requested\":false}")
        options.outboundPayments = try decoded(sdkMethodFixture586Value1)
        options.outboundTransfers = try decoded(sdkMethodFixture586Value2)
        let actual = try await client.v1.treasury.financialAccounts.features.postTreasuryFinancialAccountsFinancialAccount(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture586Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTreasuryFinancialAccountsFinancialAccountFeatures decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTreasuryInboundTransfersPostTreasuryInboundTransfersReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1TreasuryInboundTransfersMethods.PostTreasuryInboundTransfersOptions(amount: decoded("100"), currency: decoded("\"currency\""), financialAccount: decoded("\"financial_account\""), originPaymentMethod: decoded("\"origin_payment_method\""))
        options.description = try decoded("\"example text\"")
        options.expand = try decoded("[\"expand\"]")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.statementDescriptor = try decoded("\"statement_\"")
        let actual = try await client.v1.treasury.inboundTransfers.postTreasury(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture587Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTreasuryInboundTransfers decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTreasuryInboundTransfersInboundTransferCancelPostTreasuryX2aa99854ebReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.treasury.inboundTransfers.cancel.postTreasuryInboundTransfersInboundTransfer(
            inboundTransfer: decoded("\"inbound_transfer\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture588Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTreasuryInboundTransfersInboundTransferCancel decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTreasuryOutboundPaymentsPostTreasuryOutboundPaymentsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1TreasuryOutboundPaymentsMethods.PostTreasuryOutboundPaymentsOptions(amount: decoded("100"), currency: decoded("\"currency\""), financialAccount: decoded("\"financial_account\""))
        options.customer = try decoded("\"customer\"")
        options.description = try decoded("\"example text\"")
        options.destinationPaymentMethod = try decoded("\"destination_payment_method\"")
        options.destinationPaymentMethodData = try decoded(sdkMethodFixture589Value1)
        options.destinationPaymentMethodOptions = try decoded("{\"us_bank_account\":{\"network\":\"ach\"}}")
        options.endUserDetails = try decoded("{\"ip_address\":\"ip_address\",\"present\":false}")
        options.expand = try decoded("[\"expand\"]")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.statementDescriptor = try decoded("\"statement_descriptor\"")
        let actual = try await client.v1.treasury.outboundPayments.postTreasury(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture589Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTreasuryOutboundPayments decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTreasuryOutboundPaymentsIdCancelPostTreasuryOutboundPaymentsIdCancelReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.treasury.outboundPayments.cancel.postTreasuryOutboundPaymentsId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture590Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTreasuryOutboundPaymentsIdCancel decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTreasuryOutboundTransfersPostTreasuryOutboundTransfersReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1TreasuryOutboundTransfersMethods.PostTreasuryOutboundTransfersOptions(amount: decoded("100"), currency: decoded("\"currency\""), financialAccount: decoded("\"financial_account\""))
        options.description = try decoded("\"example text\"")
        options.destinationPaymentMethod = try decoded("\"destination_payment_method\"")
        options.destinationPaymentMethodData = try decoded(sdkMethodFixture591Value1)
        options.destinationPaymentMethodOptions = try decoded("{\"us_bank_account\":{\"network\":\"ach\"}}")
        options.expand = try decoded("[\"expand\"]")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.statementDescriptor = try decoded("\"statement_descriptor\"")
        let actual = try await client.v1.treasury.outboundTransfers.postTreasury(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture591Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTreasuryOutboundTransfers decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTreasuryOutboundTransfersOutboundTransferCancelPostTreasuX1d6da4e682ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.treasury.outboundTransfers.cancel.postTreasuryOutboundTransfersOutboundTransfer(
            outboundTransfer: decoded("\"outbound_transfer\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture592Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTreasuryOutboundTransfersOutboundTransferCancel decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostWebhookEndpointsPostWebhookEndpointsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.webhookEndpoints.post(
            enabledEvents: decoded("[\"*\"]"),
            url: decoded("\"https://example.com\""),
            apiVersion: decoded("\"2011-01-01\""),
            connect: decoded("false"),
            description: decoded("\"example text\""),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture593Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postWebhookEndpoints decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostWebhookEndpointsWebhookEndpointPostWebhookEndpointsWebhookEndpointReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.webhookEndpoints.postWebhookEndpoint(
            webhookEndpoint: decoded("\"webhook_endpoint\""),
            description: decoded("\"example text\""),
            disabled: decoded("false"),
            enabledEvents: decoded("[\"*\"]"),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}"),
            url: decoded("\"https://example.com\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture594Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postWebhookEndpointsWebhookEndpoint decoded a response " +
                "that differs from the mock"
        )
    }
}
