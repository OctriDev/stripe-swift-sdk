// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriStripe
import XCTest

extension SdkMethodTests {
    func testGetAppsSecretsFindGetAppsSecretsFindReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.apps.secrets.find.getAppsSecrets(
            name: decoded("\"Example Name\""),
            scope: decoded("{\"type\":\"account\",\"user\":\"user\"}"),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture52Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getAppsSecretsFind decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetBalanceGetBalanceReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1BalanceHistoryMethods.GetBalanceHistoryOptions()
        options.expand = try decoded("[\"expand\"]")
        let actual = try await client.v1.balance.history.getBalance(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture53Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getBalance decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetBalanceHistoryGetBalanceHistoryReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1BalanceHistoryMethods.GetBalanceHistoryOptions()
        options.created = try decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}")
        options.currency = try decoded("\"currency\"")
        options.endingBefore = try decoded("\"ending_before\"")
        options.expand = try decoded("[\"expand\"]")
        options.limit = try decoded("1")
        options.payout = try decoded("\"payout\"")
        options.source = try decoded("\"source\"")
        options.startingAfter = try decoded("\"starting_after\"")
        options.type = try decoded("\"type\"")
        let actual = try await client.v1.balance.history.getBalance(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture54Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getBalanceHistory decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetBalanceHistoryIdGetBalanceHistoryIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.balance.history.getBalanceId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture55Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getBalanceHistoryId decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetBalanceSettingsGetBalanceSettingsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.balanceSettings.get(
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture56Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getBalanceSettings decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetBalanceTransactionsGetBalanceTransactionsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1BalanceTransactionsMethods.GetBalanceTransactionsOptions()
        options.created = try decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}")
        options.currency = try decoded("\"currency\"")
        options.endingBefore = try decoded("\"ending_before\"")
        options.expand = try decoded("[\"expand\"]")
        options.limit = try decoded("1")
        options.payout = try decoded("\"payout\"")
        options.source = try decoded("\"source\"")
        options.startingAfter = try decoded("\"starting_after\"")
        options.type = try decoded("\"type\"")
        let actual = try await client.v1.balanceTransactions.get(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture57Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getBalanceTransactions decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetBalanceTransactionsIdGetBalanceTransactionsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.balanceTransactions.getId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture58Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getBalanceTransactionsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetBillingAlertsGetBillingAlertsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.billing.alerts.getBilling(
            alertType: decoded("\"usage_threshold\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            meter: decoded("\"meter\""),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture59Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getBillingAlerts decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetBillingAlertsIdGetBillingAlertsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.billing.alerts.getBillingId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture60Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getBillingAlertsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetBillingCreditBalanceSummaryGetBillingCreditBalanceSummaryReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.billing.creditBalanceSummary.getBilling(
            filter: decoded(sdkMethodFixture61Value1),
            customer: decoded("\"customer\""),
            customerAccount: decoded("\"customer_account\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture61Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getBillingCreditBalanceSummary decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetBillingCreditBalanceTransactionsGetBillingCreditBalanceTransactionsReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.billing.creditBalanceTransactions.getBilling(
            creditGrant: decoded("\"credit_grant\""),
            customer: decoded("\"customer\""),
            customerAccount: decoded("\"customer_account\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture62Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getBillingCreditBalanceTransactions decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetBillingCreditBalanceTransactionsIdGetBillingCreditBalanceTX84a4c602c5ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.billing.creditBalanceTransactions.getBillingId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture63Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getBillingCreditBalanceTransactionsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetBillingCreditGrantsGetBillingCreditGrantsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.billing.creditGrants.getBilling(
            customer: decoded("\"customer\""),
            customerAccount: decoded("\"customer_account\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture64Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getBillingCreditGrants decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetBillingCreditGrantsIdGetBillingCreditGrantsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.billing.creditGrants.getBillingId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture65Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getBillingCreditGrantsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetBillingFeedbackOptionsGetBillingFeedbackOptionsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.billing.feedbackOptions.getBilling(
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\""),
            status: decoded("\"active\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture66Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getBillingFeedbackOptions decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetBillingFeedbackOptionsIdGetBillingFeedbackOptionsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.billing.feedbackOptions.getBillingId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture67Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getBillingFeedbackOptionsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetBillingMetersGetBillingMetersReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.billing.meters.getBilling(
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\""),
            status: decoded("\"active\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture68Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getBillingMeters decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetBillingMetersIdGetBillingMetersIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1BillingMetersEventSummariesMethods.GetBillingMetersIdEventSummariesOptions(
            id: decoded("\"id-1\""),
            customer: decoded("\"sample\""),
            endTime: decoded("\"sample\""),
            startTime: decoded("\"sample\"")
        )
        options.expand = try decoded("[\"expand\"]")
        let actual = try await client.v1.billing.meters.eventSummaries.getBillingMetersId(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture69Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getBillingMetersId decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetBillingMetersIdEventSummariesGetBillingMetersIdEventSummariesReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1BillingMetersEventSummariesMethods.GetBillingMetersIdEventSummariesOptions(
            id: decoded("\"id-1\""),
            customer: decoded("\"customer\""),
            endTime: decoded("1"),
            startTime: decoded("1")
        )
        options.endingBefore = try decoded("\"ending_before\"")
        options.expand = try decoded("[\"expand\"]")
        options.limit = try decoded("1")
        options.startingAfter = try decoded("\"starting_after\"")
        options.valueGroupingWindow = try decoded("\"day\"")
        let actual = try await client.v1.billing.meters.eventSummaries.getBillingMetersId(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture70Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getBillingMetersIdEventSummaries decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetBillingPortalConfigurationsGetBillingPortalConfigurationsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.billingPortal.configurations.getBillingPortal(
            active: decoded("false"),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            isDefault: decoded("false"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture71Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getBillingPortalConfigurations decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetBillingPortalConfigurationsConfigurationGetBillingPortalCoX5d93ff357fReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.billingPortal.configurations.getBillingPortalConfiguration(
            configuration: decoded("\"configuration\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture72Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getBillingPortalConfigurationsConfiguration decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetChargesGetChargesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.charges.search.getCharges(
            query: decoded("\"sample\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            page: decoded("\"sample\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture73Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCharges decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetChargesChargeGetChargesChargeReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.charges.dispute.getChargesCharge(
            charge: decoded("\"charge\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture74Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getChargesCharge decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetChargesChargeDisputeGetChargesChargeDisputeReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.charges.dispute.getChargesCharge(
            charge: decoded("\"charge\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture75Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getChargesChargeDispute decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetChargesChargeRefundsGetChargesChargeRefundsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.charges.refunds.getChargesCharge(
            charge: decoded("\"charge\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture76Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getChargesChargeRefunds decoded a response " +
                "that differs from the mock"
        )
    }
}
