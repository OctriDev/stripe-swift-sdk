// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriStripe
import XCTest

extension SdkMethodTests {
    func testGetTaxAssociationsFindGetTaxAssociationsFindReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.tax.associations.find.getTaxAssociations(
            paymentIntent: decoded("\"payment_intent\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture246Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTaxAssociationsFind decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTaxCalculationsCalculationGetTaxCalculationsCalculationReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.tax.calculations.lineItems.getTaxCalculationsCalculation(
            calculation: decoded("\"calculation\""),
            endingBefore: decoded("\"sample\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("\"sample\""),
            startingAfter: decoded("\"sample\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture247Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTaxCalculationsCalculation decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTaxCalculationsCalculationLineItemsGetTaxCalculationsCalcuXbbeaa65aabReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.tax.calculations.lineItems.getTaxCalculationsCalculation(
            calculation: decoded("\"calculation\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture248Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTaxCalculationsCalculationLineItems decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTaxCodesGetTaxCodesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.taxCodes.get(
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture249Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTaxCodes decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTaxCodesIdGetTaxCodesIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.taxCodes.getId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture250Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTaxCodesId decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTaxIdsGetTaxIdsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.taxIds.get(
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            owner: decoded(sdkMethodFixture251Value1),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture251Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTaxIds decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTaxIdsIdGetTaxIdsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.taxIds.getId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture252Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTaxIdsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTaxRatesGetTaxRatesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.taxRates.get(
            active: decoded("false"),
            created: decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}"),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            inclusive: decoded("false"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture253Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTaxRates decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTaxRatesTaxRateGetTaxRatesTaxRateReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.taxRates.getTaxRate(
            taxRate: decoded("\"tax_rate\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture254Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTaxRatesTaxRate decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTaxRegistrationsGetTaxRegistrationsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.tax.registrations.getTax(
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\""),
            status: decoded("\"active\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture255Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTaxRegistrations decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTaxRegistrationsIdGetTaxRegistrationsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.tax.registrations.getTaxId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture256Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTaxRegistrationsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTaxSettingsGetTaxSettingsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.tax.settings.getTax(
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture257Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTaxSettings decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTaxTransactionsTransactionGetTaxTransactionsTransactionReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.tax.transactions.lineItems.getTaxTransactionsTransaction(
            transaction: decoded("\"transaction\""),
            endingBefore: decoded("\"sample\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("\"sample\""),
            startingAfter: decoded("\"sample\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture258Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTaxTransactionsTransaction decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTaxTransactionsTransactionLineItemsGetTaxTransactionsTransXde98af769eReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.tax.transactions.lineItems.getTaxTransactionsTransaction(
            transaction: decoded("\"transaction\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture259Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTaxTransactionsTransactionLineItems decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTerminalConfigurationsGetTerminalConfigurationsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.terminal.configurations.getTerminal(
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            isAccountDefault: decoded("false"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture260Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTerminalConfigurations decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTerminalConfigurationsConfigurationGetTerminalConfiguratioX96a16b044dReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.terminal.configurations.getTerminalConfiguration(
            configuration: decoded("\"configuration\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture261Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTerminalConfigurationsConfiguration decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTerminalLocationsGetTerminalLocationsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.terminal.locations.getTerminal(
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture262Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTerminalLocations decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTerminalLocationsLocationGetTerminalLocationsLocationReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.terminal.locations.getTerminalLocation(
            location: decoded("\"location\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture263Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTerminalLocationsLocation decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTerminalReadersGetTerminalReadersReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1TerminalReadersMethods.GetTerminalReadersOptions()
        options.deviceType = try decoded("\"bbpos_chipper2x\"")
        options.endingBefore = try decoded("\"ending_before\"")
        options.expand = try decoded("[\"expand\"]")
        options.limit = try decoded("1")
        options.location = try decoded("\"location\"")
        options.serialNumber = try decoded("\"serial_number\"")
        options.startingAfter = try decoded("\"starting_after\"")
        options.status = try decoded("\"offline\"")
        let actual = try await client.v1.terminal.readers.getTerminal(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture264Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTerminalReaders decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTerminalReadersReaderGetTerminalReadersReaderReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.terminal.readers.getTerminalReader(
            reader: decoded("\"reader\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture265Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTerminalReadersReader decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTestHelpersTestClocksGetTestHelpersTestClocksReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.testHelpers.testClocks.getTestHelpers(
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture266Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTestHelpersTestClocks decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTestHelpersTestClocksTestClockGetTestHelpersTestClocksTestClockReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.testHelpers.testClocks.getTestHelpersTestClock(
            testClock: decoded("\"test_clock\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture267Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTestHelpersTestClocksTestClock decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTokensTokenGetTokensTokenReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.tokens.getToken(
            token: decoded("\"token\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture268Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTokensToken decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTopupsGetTopupsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.topups.get(
            amount: decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}"),
            created: decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}"),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\""),
            status: decoded("\"canceled\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture269Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTopups decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTopupsTopupGetTopupsTopupReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.topups.getTopup(
            topup: decoded("\"topup\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture270Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTopupsTopup decoded a response " +
                "that differs from the mock"
        )
    }
}
