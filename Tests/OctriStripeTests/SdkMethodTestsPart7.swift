// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriStripe
import XCTest

extension SdkMethodTests {
    func testGetInvoicePaymentsInvoicePaymentGetInvoicePaymentsInvoicePaymentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.invoicePayments.getInvoicePayment(
            invoicePayment: decoded("\"invoice_payment\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture149Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getInvoicePaymentsInvoicePayment decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetInvoiceRenderingTemplatesGetInvoiceRenderingTemplatesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.invoiceRenderingTemplates.get(
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\""),
            status: decoded("\"active\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture150Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getInvoiceRenderingTemplates decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetInvoiceRenderingTemplatesTemplateGetInvoiceRenderingTemplatesTemplateReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.invoiceRenderingTemplates.getTemplate(
            template: decoded("\"template\""),
            expand: decoded("[\"expand\"]"),
            version: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture151Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getInvoiceRenderingTemplatesTemplate decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetInvoicesGetInvoicesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.invoices.search.getInvoices(
            query: decoded("\"sample\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            page: decoded("\"sample\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture152Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getInvoices decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetInvoicesInvoiceGetInvoicesInvoiceReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.invoices.lines.getInvoicesInvoice(
            invoice: decoded("\"invoice\""),
            endingBefore: decoded("\"sample\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("\"sample\""),
            startingAfter: decoded("\"sample\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture153Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getInvoicesInvoice decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetInvoicesInvoiceLinesGetInvoicesInvoiceLinesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.invoices.lines.getInvoicesInvoice(
            invoice: decoded("\"invoice\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture154Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getInvoicesInvoiceLines decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetInvoicesSearchGetInvoicesSearchReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.invoices.search.getInvoices(
            query: decoded("\"query\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            page: decoded("\"page\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture155Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getInvoicesSearch decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetIssuingAuthorizationsGetIssuingAuthorizationsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1IssuingAuthorizationsMethods.GetIssuingAuthorizationsOptions()
        options.card = try decoded("\"card\"")
        options.cardholder = try decoded("\"cardholder\"")
        options.created = try decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}")
        options.endingBefore = try decoded("\"ending_before\"")
        options.expand = try decoded("[\"expand\"]")
        options.limit = try decoded("1")
        options.startingAfter = try decoded("\"starting_after\"")
        options.status = try decoded("\"closed\"")
        let actual = try await client.v1.issuing.authorizations.getIssuing(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture156Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getIssuingAuthorizations decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetIssuingAuthorizationsAuthorizationGetIssuingAuthorizationsX39e316c200ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.issuing.authorizations.getIssuingAuthorization(
            authorization: decoded("\"authorization\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture157Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getIssuingAuthorizationsAuthorization decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetIssuingCardholdersGetIssuingCardholdersReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1IssuingCardholdersMethods.GetIssuingCardholdersOptions()
        options.created = try decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}")
        options.email = try decoded("\"user@example.com\"")
        options.endingBefore = try decoded("\"ending_before\"")
        options.expand = try decoded("[\"expand\"]")
        options.limit = try decoded("1")
        options.phoneNumber = try decoded("\"+15555550100\"")
        options.startingAfter = try decoded("\"starting_after\"")
        options.status = try decoded("\"active\"")
        options.type = try decoded("\"company\"")
        let actual = try await client.v1.issuing.cardholders.getIssuing(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture158Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getIssuingCardholders decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetIssuingCardholdersCardholderGetIssuingCardholdersCardholderReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.issuing.cardholders.getIssuingCardholder(
            cardholder: decoded("\"cardholder\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture159Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getIssuingCardholdersCardholder decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetIssuingCardsGetIssuingCardsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1IssuingCardsMethods.GetIssuingCardsOptions()
        options.cardholder = try decoded("\"cardholder\"")
        options.created = try decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}")
        options.endingBefore = try decoded("\"ending_before\"")
        options.expMonth = try decoded("1")
        options.expYear = try decoded("1")
        options.expand = try decoded("[\"expand\"]")
        options.last4 = try decoded("\"last4\"")
        options.limit = try decoded("1")
        options.personalizationDesign = try decoded("\"personalization_design\"")
        options.startingAfter = try decoded("\"starting_after\"")
        options.status = try decoded("\"active\"")
        options.type = try decoded("\"physical\"")
        let actual = try await client.v1.issuing.cards.getIssuing(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture160Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getIssuingCards decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetIssuingCardsCardGetIssuingCardsCardReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.issuing.cards.getIssuingCard(
            card: decoded("\"card\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture161Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getIssuingCardsCard decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetIssuingDisputesGetIssuingDisputesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.issuing.disputes.getIssuing(
            created: decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}"),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\""),
            status: decoded("\"expired\""),
            transaction: decoded("\"transaction\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture162Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getIssuingDisputes decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetIssuingDisputesDisputeGetIssuingDisputesDisputeReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.issuing.disputes.getIssuingDispute(
            dispute: decoded("\"dispute\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture163Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getIssuingDisputesDispute decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetIssuingPersonalizationDesignsGetIssuingPersonalizationDesignsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.issuing.personalizationDesigns.getIssuing(
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            lookupKeys: decoded("[\"lookup_keys\"]"),
            preferences: decoded("{\"is_default\":false,\"is_platform_default\":false}"),
            startingAfter: decoded("\"starting_after\""),
            status: decoded("\"active\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture164Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getIssuingPersonalizationDesigns decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetIssuingPersonalizationDesignsPersonalizationDesignGetIssuiX3c38670487ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.issuing.personalizationDesigns.getIssuingPersonalizationDesign(
            personalizationDesign: decoded("\"personalization_design\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture165Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getIssuingPersonalizationDesignsPersonalizationDesign decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetIssuingPhysicalBundlesGetIssuingPhysicalBundlesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.issuing.physicalBundles.getIssuing(
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\""),
            status: decoded("\"active\""),
            type: decoded("\"custom\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture166Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getIssuingPhysicalBundles decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetIssuingPhysicalBundlesPhysicalBundleGetIssuingPhysicalBundX94f5b976cfReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.issuing.physicalBundles.getIssuingPhysicalBundle(
            physicalBundle: decoded("\"physical_bundle\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture167Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getIssuingPhysicalBundlesPhysicalBundle decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetIssuingSettlementsSettlementGetIssuingSettlementsSettlementReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.issuing.settlements.getIssuingSettlement(
            settlement: decoded("\"settlement\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture168Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getIssuingSettlementsSettlement decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetIssuingTokensGetIssuingTokensReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.issuing.tokens.getIssuing(
            card: decoded("\"card\""),
            created: decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}"),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\""),
            status: decoded("\"active\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture169Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getIssuingTokens decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetIssuingTokensTokenGetIssuingTokensTokenReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.issuing.tokens.getIssuingToken(
            token: decoded("\"token\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture170Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getIssuingTokensToken decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetIssuingTransactionsGetIssuingTransactionsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1IssuingTransactionsMethods.GetIssuingTransactionsOptions()
        options.card = try decoded("\"card\"")
        options.cardholder = try decoded("\"cardholder\"")
        options.created = try decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}")
        options.endingBefore = try decoded("\"ending_before\"")
        options.expand = try decoded("[\"expand\"]")
        options.limit = try decoded("1")
        options.startingAfter = try decoded("\"starting_after\"")
        options.type = try decoded("\"capture\"")
        let actual = try await client.v1.issuing.transactions.getIssuing(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture171Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getIssuingTransactions decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetIssuingTransactionsTransactionGetIssuingTransactionsTransactionReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.issuing.transactions.getIssuingTransaction(
            transaction: decoded("\"transaction\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture172Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getIssuingTransactionsTransaction decoded a response " +
                "that differs from the mock"
        )
    }
}
