// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriStripe
import XCTest

extension SdkMethodTests {
    func testPostPaymentRecordsIdReportRefundPostPaymentRecordsIdReportRefundReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1PaymentRecordsReportRefundMethods.PostPaymentRecordsIdReportRefundOptions(id: decoded("\"id-1\""), outcome: decoded("\"refunded\""), processorDetails: decoded(sdkMethodFixture456Value1))
        options.amount = try decoded("{\"currency\":\"currency\",\"value\":1}")
        options.expand = try decoded("[\"expand\"]")
        options.initiatedAt = try decoded("1")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.refunded = try decoded("{\"refunded_at\":1}")
        let actual = try await client.v1.paymentRecords.reportRefund.postPaymentRecordsId(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture456Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postPaymentRecordsIdReportRefund decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostPaymentRecordsReportPaymentPostPaymentRecordsReportPaymentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1PaymentRecordsReportPaymentMethods.PostPaymentRecordsReportPaymentOptions(amountRequested: decoded("{\"currency\":\"currency\",\"value\":1}"), initiatedAt: decoded("1"), paymentMethodDetails: decoded(sdkMethodFixture457Value1))
        options.customerDetails = try decoded(sdkMethodFixture457Value2)
        options.customerPresence = try decoded("\"off_session\"")
        options.description = try decoded("\"example text\"")
        options.expand = try decoded("[\"expand\"]")
        options.failed = try decoded("{\"failed_at\":1}")
        options.guaranteed = try decoded("{\"guaranteed_at\":1}")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.outcome = try decoded("\"failed\"")
        options.processorDetails = try decoded(sdkMethodFixture457Value3)
        options.shippingDetails = try decoded(sdkMethodFixture457Value4)
        let actual = try await client.v1.paymentRecords.reportPayment.postPaymentRecords(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture457Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postPaymentRecordsReportPayment decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostPayoutsPostPayoutsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1PayoutsMethods.PostPayoutsOptions(amount: decoded("100"), currency: decoded("\"currency\""))
        options.description = try decoded("\"example text\"")
        options.destination = try decoded("\"destination\"")
        options.expand = try decoded("[\"expand\"]")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.method = try decoded("\"instant\"")
        options.payoutMethod = try decoded("\"payout_method\"")
        options.sourceType = try decoded("\"bank_account\"")
        options.statementDescriptor = try decoded("\"statement_descriptor\"")
        let actual = try await client.v1.payouts.post(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture458Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postPayouts decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostPayoutsPayoutPostPayoutsPayoutReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.payouts.cancel.postPayoutsPayout(
            payout: decoded("\"payout\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture459Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postPayoutsPayout decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostPayoutsPayoutCancelPostPayoutsPayoutCancelReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.payouts.cancel.postPayoutsPayout(
            payout: decoded("\"payout\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture460Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postPayoutsPayoutCancel decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostPayoutsPayoutReversePostPayoutsPayoutReverseReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.payouts.reverse.postPayoutsPayout(
            payout: decoded("\"payout\""),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture461Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postPayoutsPayoutReverse decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostPlansPostPlansReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1PlansMethods.PostPlansOptions(currency: decoded("\"currency\""), interval: decoded("\"day\""))
        options.active = try decoded("false")
        options.amount = try decoded("100")
        options.amountDecimal = try decoded("\"amount_decimal\"")
        options.billingScheme = try decoded("\"per_unit\"")
        options.expand = try decoded("[\"expand\"]")
        options.id = try decoded("\"id-1\"")
        options.intervalCount = try decoded("1")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.meter = try decoded("\"meter\"")
        options.nickname = try decoded("\"Example Name\"")
        options.product = try decoded(sdkMethodFixture462Value1)
        options.tiers = try decoded(sdkMethodFixture462Value2)
        options.tiersMode = try decoded("\"graduated\"")
        options.transformUsage = try decoded("{\"divide_by\":1,\"round\":\"down\"}")
        options.trialPeriodDays = try decoded("1")
        options.usageType = try decoded("\"licensed\"")
        let actual = try await client.v1.plans.post(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture462Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postPlans decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostPlansPlanPostPlansPlanReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.plans.postPlan(
            plan: decoded("\"plan\""),
            active: decoded("false"),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}"),
            nickname: decoded("\"Example Name\""),
            product: decoded("\"product\""),
            trialPeriodDays: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture463Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postPlansPlan decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostPricesPostPricesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1PricesMethods.PostPricesOptions(currency: decoded("\"currency\""))
        options.active = try decoded("false")
        options.billingScheme = try decoded("\"per_unit\"")
        options.currencyOptions = try decoded(sdkMethodFixture464Value1)
        options.customUnitAmount = try decoded("{\"enabled\":false,\"maximum\":1,\"minimum\":1,\"preset\":1}")
        options.expand = try decoded("[\"expand\"]")
        options.lookupKey = try decoded("\"lookup_key\"")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.nickname = try decoded("\"Example Name\"")
        options.product = try decoded("\"product\"")
        options.productData = try decoded(sdkMethodFixture464Value2)
        options.recurring = try decoded(sdkMethodFixture464Value3)
        options.taxBehavior = try decoded("\"exclusive\"")
        options.tiers = try decoded(sdkMethodFixture464Value4)
        options.tiersMode = try decoded("\"graduated\"")
        options.transferLookupKey = try decoded("false")
        options.transformQuantity = try decoded("{\"divide_by\":1,\"round\":\"down\"}")
        options.unitAmount = try decoded("100")
        options.unitAmountDecimal = try decoded("\"unit_amount_decimal\"")
        let actual = try await client.v1.prices.post(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture464Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postPrices decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostPricesPricePostPricesPriceReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1PricesMethods.PostPricesPriceOptions(price: decoded("\"price\""))
        options.active = try decoded("false")
        options.currencyOptions = try decoded(sdkMethodFixture465Value1)
        options.expand = try decoded("[\"expand\"]")
        options.lookupKey = try decoded("\"lookup_key\"")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.nickname = try decoded("\"Example Name\"")
        options.taxBehavior = try decoded("\"exclusive\"")
        options.transferLookupKey = try decoded("false")
        let actual = try await client.v1.prices.postPrice(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture465Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postPricesPrice decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostProductsPostProductsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1ProductsMethods.PostProductsOptions(name: decoded("\"Example Name\""))
        options.active = try decoded("false")
        options.defaultPriceData = try decoded(sdkMethodFixture466Value1)
        options.description = try decoded("\"example text\"")
        options.expand = try decoded("[\"expand\"]")
        options.id = try decoded("\"id-1\"")
        options.images = try decoded("[\"images\"]")
        options.marketingFeatures = try decoded("[{\"name\":\"Example Name\"}]")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.packageDimensions = try decoded("{\"height\":1,\"length\":1,\"weight\":1,\"width\":1}")
        options.shippable = try decoded("false")
        options.statementDescriptor = try decoded("\"statement_descriptor\"")
        options.taxCode = try decoded("\"tax_code\"")
        options.unitLabel = try decoded("\"unit_label\"")
        options.url = try decoded("\"https://example.com\"")
        let actual = try await client.v1.products.post(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture466Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postProducts decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostProductsIdPostProductsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1ProductsMethods.PostProductsIdOptions(id: decoded("\"id-1\""))
        options.active = try decoded("false")
        options.defaultPrice = try decoded("\"default_price\"")
        options.description = try decoded("\"example text\"")
        options.expand = try decoded("[\"expand\"]")
        options.images = try decoded("[\"images\"]")
        options.marketingFeatures = try decoded("[{\"name\":\"Example Name\"}]")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.name = try decoded("\"Example Name\"")
        options.packageDimensions = try decoded("{\"height\":1,\"length\":1,\"weight\":1,\"width\":1}")
        options.shippable = try decoded("false")
        options.statementDescriptor = try decoded("\"statement_descriptor\"")
        options.taxCode = try decoded("\"tax_code\"")
        options.unitLabel = try decoded("\"unit_label\"")
        options.url = try decoded("\"https://example.com\"")
        let actual = try await client.v1.products.postId(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture467Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postProductsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostProductsProductFeaturesPostProductsProductFeaturesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.products.features.postProductsProduct(
            product: decoded("\"product\""),
            entitlementFeature: decoded("\"entitlement_feature\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture468Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postProductsProductFeatures decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostPromotionCodesPostPromotionCodesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1PromotionCodesMethods.PostPromotionCodesOptions(promotion: decoded("{\"coupon\":\"coupon\",\"type\":\"coupon\"}"))
        options.active = try decoded("false")
        options.code = try decoded("\"code\"")
        options.customer = try decoded("\"customer\"")
        options.customerAccount = try decoded("\"customer_account\"")
        options.expand = try decoded("[\"expand\"]")
        options.expiresAt = try decoded("1")
        options.maxRedemptions = try decoded("1")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.restrictions = try decoded(sdkMethodFixture469Value1)
        let actual = try await client.v1.promotionCodes.post(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture469Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postPromotionCodes decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostPromotionCodesPromotionCodePostPromotionCodesPromotionCodeReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.promotionCodes.postPromotionCode(
            promotionCode: decoded("\"promotion_code\""),
            active: decoded("false"),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}"),
            restrictions: decoded("{\"currency_options\":{\"key\":{\"minimum_amount\":100}}}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture470Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postPromotionCodesPromotionCode decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostQuotesPostQuotesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1QuotesMethods.PostQuotesOptions()
        options.applicationFeeAmount = try decoded("100")
        options.applicationFeePercent = try decoded("1")
        options.automaticTax = try decoded(sdkMethodFixture471Value1)
        options.collectionMethod = try decoded("\"charge_automatically\"")
        options.customer = try decoded("\"customer\"")
        options.customerAccount = try decoded("\"customer_account\"")
        options.defaultTaxRates = try decoded("[\"default_tax_rates\"]")
        options.description = try decoded("\"example text\"")
        options.discounts = try decoded(sdkMethodFixture471Value2)
        options.expand = try decoded("[\"expand\"]")
        options.expiresAt = try decoded("1")
        options.footer = try decoded("\"footer\"")
        options.fromQuote = try decoded("{\"is_revision\":false,\"quote\":\"quote\"}")
        options.header = try decoded("\"header\"")
        options.invoiceSettings = try decoded(sdkMethodFixture471Value3)
        options.lineItems = try decoded(sdkMethodFixture471Value4)
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.onBehalfOf = try decoded("\"on_behalf_of\"")
        options.subscriptionData = try decoded(sdkMethodFixture471Value5)
        options.testClock = try decoded("\"test_clock\"")
        options.transferData = try decoded(sdkMethodFixture471Value6)
        let actual = try await client.v1.quotes.post(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture471Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postQuotes decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostQuotesQuotePostQuotesQuoteReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.quotes.accept.postQuotesQuote(
            quote: decoded("\"quote\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture472Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postQuotesQuote decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostQuotesQuoteAcceptPostQuotesQuoteAcceptReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.quotes.accept.postQuotesQuote(
            quote: decoded("\"quote\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture473Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postQuotesQuoteAccept decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostQuotesQuoteCancelPostQuotesQuoteCancelReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.quotes.cancel.postQuotesQuote(
            quote: decoded("\"quote\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture474Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postQuotesQuoteCancel decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostQuotesQuoteFinalizePostQuotesQuoteFinalizeReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.quotes.finalize.postQuotesQuote(
            quote: decoded("\"quote\""),
            expand: decoded("[\"expand\"]"),
            expiresAt: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture475Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postQuotesQuoteFinalize decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostRadarPaymentEvaluationsPostRadarPaymentEvaluationsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.radar.paymentEvaluations.postRadar(
            customerDetails: decoded(sdkMethodFixture476Value1),
            paymentDetails: decoded(sdkMethodFixture476Value2),
            clientDeviceMetadataDetails: decoded("{\"radar_session\":\"radar_session\"}"),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture476Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postRadarPaymentEvaluations decoded a response " +
                "that differs from the mock"
        )
    }
}
