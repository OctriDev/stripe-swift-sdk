// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriStripe
import XCTest

extension SdkMethodTests {
    func testGetChargesChargeRefundsRefundGetChargesChargeRefundsRefundReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.charges.refunds.getChargesChargeRefund(
            charge: decoded("\"charge\""),
            refund: decoded("\"refund\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture77Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getChargesChargeRefundsRefund decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetChargesSearchGetChargesSearchReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.charges.search.getCharges(
            query: decoded("\"query\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            page: decoded("\"page\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture78Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getChargesSearch decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCheckoutSessionsGetCheckoutSessionsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1CheckoutSessionsMethods.GetCheckoutSessionsOptions()
        options.created = try decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}")
        options.customer = try decoded("\"customer\"")
        options.customerAccount = try decoded("\"customer_account\"")
        options.customerDetails = try decoded("{\"email\":\"user@example.com\"}")
        options.endingBefore = try decoded("\"ending_before\"")
        options.expand = try decoded("[\"expand\"]")
        options.limit = try decoded("1")
        options.paymentIntent = try decoded("\"payment_intent\"")
        options.paymentLink = try decoded("\"https://example.com\"")
        options.startingAfter = try decoded("\"starting_after\"")
        options.status = try decoded("\"complete\"")
        options.subscription = try decoded("\"subscription\"")
        let actual = try await client.v1.checkout.sessions.getCheckout(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture79Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCheckoutSessions decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCheckoutSessionsSessionGetCheckoutSessionsSessionReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.checkout.sessions.lineItems.getCheckoutSessionsSession(
            session: decoded("\"session\""),
            endingBefore: decoded("\"sample\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("\"sample\""),
            startingAfter: decoded("\"sample\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture80Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCheckoutSessionsSession decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCheckoutSessionsSessionLineItemsGetCheckoutSessionsSessionLineItemsReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.checkout.sessions.lineItems.getCheckoutSessionsSession(
            session: decoded("\"session\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture81Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCheckoutSessionsSessionLineItems decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetClimateOrdersGetClimateOrdersReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.climate.orders.getClimate(
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture82Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getClimateOrders decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetClimateOrdersOrderGetClimateOrdersOrderReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.climate.orders.getClimateOrder(
            order: decoded("\"order\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture83Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getClimateOrdersOrder decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetClimateProductsGetClimateProductsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.climate.products.getClimate(
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture84Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getClimateProducts decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetClimateProductsProductGetClimateProductsProductReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.climate.products.getClimateProduct(
            product: decoded("\"product\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture85Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getClimateProductsProduct decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetClimateSuppliersGetClimateSuppliersReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.climate.suppliers.getClimate(
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture86Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getClimateSuppliers decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetClimateSuppliersSupplierGetClimateSuppliersSupplierReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.climate.suppliers.getClimateSupplier(
            supplier: decoded("\"supplier\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture87Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getClimateSuppliersSupplier decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetConfirmationTokensConfirmationTokenGetConfirmationTokensCoXe5ce5a4c01ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.confirmationTokens.getConfirmationToken(
            confirmationToken: decoded("\"confirmation_token\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture88Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getConfirmationTokensConfirmationToken decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCountrySpecsGetCountrySpecsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.countrySpecs.get(
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture89Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCountrySpecs decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCountrySpecsCountryGetCountrySpecsCountryReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.countrySpecs.getCountry(
            country: decoded("\"country\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture90Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCountrySpecsCountry decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCouponsGetCouponsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.coupons.get(
            created: decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}"),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture91Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCoupons decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCouponsCouponGetCouponsCouponReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.coupons.getCoupon(
            coupon: decoded("\"coupon\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture92Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCouponsCoupon decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCreditNotesGetCreditNotesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1CreditNotesPreviewMethods.GetCreditNotesPreviewOptions(invoice: decoded("\"invoice\""))
        options.expand = try decoded("[\"expand\"]")
        let actual = try await client.v1.creditNotes.preview.getCreditNotes(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture93Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCreditNotes decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCreditNotesCreditNoteLinesGetCreditNotesCreditNoteLinesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.creditNotes.lines.getCreditNotesCreditNote(
            creditNote: decoded("\"credit_note\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture94Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCreditNotesCreditNoteLines decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCreditNotesIdGetCreditNotesIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.creditNotes.getId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture95Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCreditNotesId decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCreditNotesPreviewGetCreditNotesPreviewReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1CreditNotesPreviewLinesMethods
            .GetCreditNotesPreviewLinesOptions(invoice: decoded("\"invoice\""))
        options.amount = try decoded("100")
        options.creditAmount = try decoded("100")
        options.effectiveAt = try decoded("1")
        options.emailType = try decoded("\"credit_note\"")
        options.expand = try decoded("[\"expand\"]")
        options.lines = try decoded(sdkMethodFixture96Value1)
        options.memo = try decoded("\"memo\"")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.outOfBandAmount = try decoded("100")
        options.reason = try decoded("\"duplicate\"")
        options.refundAmount = try decoded("100")
        options.refunds = try decoded(sdkMethodFixture96Value2)
        options.shippingCost = try decoded("{\"shipping_rate\":\"shipping_rate\"}")
        let actual = try await client.v1.creditNotes.preview.lines.getCreditNotesPreview(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture96Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCreditNotesPreview decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCreditNotesPreviewLinesGetCreditNotesPreviewLinesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1CreditNotesPreviewLinesMethods
            .GetCreditNotesPreviewLinesOptions(invoice: decoded("\"invoice\""))
        options.amount = try decoded("100")
        options.creditAmount = try decoded("100")
        options.effectiveAt = try decoded("1")
        options.emailType = try decoded("\"credit_note\"")
        options.endingBefore = try decoded("\"ending_before\"")
        options.expand = try decoded("[\"expand\"]")
        options.limit = try decoded("1")
        options.lines = try decoded(sdkMethodFixture97Value1)
        options.memo = try decoded("\"memo\"")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.outOfBandAmount = try decoded("100")
        options.reason = try decoded("\"duplicate\"")
        options.refundAmount = try decoded("100")
        options.refunds = try decoded(sdkMethodFixture97Value2)
        options.shippingCost = try decoded("{\"shipping_rate\":\"shipping_rate\"}")
        options.startingAfter = try decoded("\"starting_after\"")
        let actual = try await client.v1.creditNotes.preview.lines.getCreditNotesPreview(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture97Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCreditNotesPreviewLines decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCustomersGetCustomersReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.search.getCustomers(
            query: decoded("\"sample\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            page: decoded("\"sample\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture98Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCustomers decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCustomersCustomerGetCustomersCustomerReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.balanceTransactions.getCustomersCustomer(
            customer: decoded("\"customer\""),
            created: decoded("\"sample\""),
            endingBefore: decoded("\"sample\""),
            expand: decoded("[\"expand\"]"),
            invoice: decoded("\"sample\""),
            limit: decoded("\"sample\""),
            startingAfter: decoded("\"sample\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture99Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCustomersCustomer decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetCustomersCustomerBalanceTransactionsGetCustomersCustomerBaX73cd380f2bReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.balanceTransactions.getCustomersCustomer(
            customer: decoded("\"customer\""),
            created: decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}"),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            invoice: decoded("\"invoice\""),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture100Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getCustomersCustomerBalanceTransactions decoded a response " +
                "that differs from the mock"
        )
    }
}
