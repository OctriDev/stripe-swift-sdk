// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriStripe
import XCTest

extension SdkMethodTests {
    func testGetEntitlementsFeaturesIdGetEntitlementsFeaturesIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.entitlements.features.getEntitlementsId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture125Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getEntitlementsFeaturesId decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetEventsGetEventsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1EventsMethods.GetEventsOptions()
        options.created = try decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}")
        options.deliverySuccess = try decoded("false")
        options.endingBefore = try decoded("\"ending_before\"")
        options.expand = try decoded("[\"expand\"]")
        options.limit = try decoded("1")
        options.startingAfter = try decoded("\"starting_after\"")
        options.type = try decoded("\"type\"")
        options.types = try decoded("[\"types\"]")
        let actual = try await client.v1.events.get(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture126Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getEvents decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetEventsIdGetEventsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.events.getId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture127Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getEventsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetExchangeRatesGetExchangeRatesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.exchangeRates.get(
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture128Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getExchangeRates decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetExchangeRatesRateIdGetExchangeRatesRateIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.exchangeRates.getRateId(
            rateId: decoded("\"rate_-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture129Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getExchangeRatesRateId decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetFileLinksGetFileLinksReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.fileLinks.get(
            created: decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}"),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            expired: decoded("false"),
            file: decoded("\"file\""),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture130Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getFileLinks decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetFileLinksLinkGetFileLinksLinkReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.fileLinks.getLink(
            link: decoded("\"https://example.com\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture131Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getFileLinksLink decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetFilesGetFilesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.files.get(
            created: decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}"),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            purpose: decoded("\"account_requirement\""),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture132Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getFiles decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetFilesFileGetFilesFileReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.files.getFile(
            file: decoded("\"file\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture133Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getFilesFile decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetFinancialConnectionsAccountsGetFinancialConnectionsAccountsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.financialConnections.accounts.getFinancialConnections(
            accountHolder: decoded(sdkMethodFixture134Value1),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            session: decoded("\"session\""),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture134Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getFinancialConnectionsAccounts decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetFinancialConnectionsAccountsAccountGetFinancialConnectionsX9df8685ee6ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.financialConnections.accounts.owners.getFinancialConnectionsAccountsAccount(
            account: decoded("\"account\""),
            ownership: decoded("\"sample\""),
            endingBefore: decoded("\"sample\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("\"sample\""),
            startingAfter: decoded("\"sample\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture135Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getFinancialConnectionsAccountsAccount decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetFinancialConnectionsAccountsAccountOwnersGetFinancialConneX1c94b27312ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.financialConnections.accounts.owners.getFinancialConnectionsAccountsAccount(
            account: decoded("\"account\""),
            ownership: decoded("\"ownership\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture136Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getFinancialConnectionsAccountsAccountOwners decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetFinancialConnectionsSessionsSessionGetFinancialConnectionsXd328f442d6ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.financialConnections.sessions.getFinancialConnectionsSession(
            session: decoded("\"session\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture137Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getFinancialConnectionsSessionsSession decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetFinancialConnectionsTransactionsGetFinancialConnectionsTransactionsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.financialConnections.transactions.getFinancialConnections(
            account: decoded("\"account\""),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\""),
            transactedAt: decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}"),
            transactionRefresh: decoded("{\"after\":\"after\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture138Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getFinancialConnectionsTransactions decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetFinancialConnectionsTransactionsTransactionGetFinancialConXb6df86722dReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.financialConnections.transactions.getFinancialConnectionsTransaction(
            transaction: decoded("\"transaction\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture139Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getFinancialConnectionsTransactionsTransaction decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetForwardingRequestsGetForwardingRequestsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.forwarding.requests.getForwarding(
            created: decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}"),
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture140Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getForwardingRequests decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetForwardingRequestsIdGetForwardingRequestsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.forwarding.requests.getForwardingId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture141Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getForwardingRequestsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetIdentityVerificationReportsGetIdentityVerificationReportsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1IdentityVerificationReportsMethods.GetIdentityVerificationReportsOptions()
        options.clientReferenceId = try decoded("\"client_reference_-1\"")
        options.created = try decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}")
        options.endingBefore = try decoded("\"ending_before\"")
        options.expand = try decoded("[\"expand\"]")
        options.limit = try decoded("1")
        options.startingAfter = try decoded("\"starting_after\"")
        options.type = try decoded("\"document\"")
        options.verificationSession = try decoded("\"verification_session\"")
        let actual = try await client.v1.identity.verificationReports.getIdentity(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture142Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getIdentityVerificationReports decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetIdentityVerificationReportsReportGetIdentityVerificationReportsReportReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.identity.verificationReports.getIdentityReport(
            report: decoded("\"report\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture143Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getIdentityVerificationReportsReport decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetIdentityVerificationSessionsGetIdentityVerificationSessionsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1IdentityVerificationSessionsMethods.GetIdentityVerificationSessionsOptions()
        options.clientReferenceId = try decoded("\"client_reference_-1\"")
        options.created = try decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}")
        options.endingBefore = try decoded("\"ending_before\"")
        options.expand = try decoded("[\"expand\"]")
        options.limit = try decoded("1")
        options.relatedCustomer = try decoded("\"related_customer\"")
        options.relatedCustomerAccount = try decoded("\"related_customer_account\"")
        options.startingAfter = try decoded("\"starting_after\"")
        options.status = try decoded("\"canceled\"")
        let actual = try await client.v1.identity.verificationSessions.getIdentity(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture144Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getIdentityVerificationSessions decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetIdentityVerificationSessionsSessionGetIdentityVerificationX3176451fd5ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.identity.verificationSessions.getIdentitySession(
            session: decoded("\"session\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture145Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getIdentityVerificationSessionsSession decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetInvoiceitemsGetInvoiceitemsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1InvoiceitemsMethods.GetInvoiceitemsOptions()
        options.created = try decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}")
        options.customer = try decoded("\"customer\"")
        options.customerAccount = try decoded("\"customer_account\"")
        options.endingBefore = try decoded("\"ending_before\"")
        options.expand = try decoded("[\"expand\"]")
        options.invoice = try decoded("\"invoice\"")
        options.limit = try decoded("1")
        options.pending = try decoded("false")
        options.startingAfter = try decoded("\"starting_after\"")
        let actual = try await client.v1.invoiceitems.get(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture146Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getInvoiceitems decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetInvoiceitemsInvoiceitemGetInvoiceitemsInvoiceitemReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.invoiceitems.getInvoiceitem(
            invoiceitem: decoded("\"invoiceitem\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture147Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getInvoiceitemsInvoiceitem decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetInvoicePaymentsGetInvoicePaymentsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1InvoicePaymentsMethods.GetInvoicePaymentsOptions()
        options.created = try decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}")
        options.endingBefore = try decoded("\"ending_before\"")
        options.expand = try decoded("[\"expand\"]")
        options.invoice = try decoded("\"invoice\"")
        options.limit = try decoded("1")
        options.payment = try decoded(sdkMethodFixture148Value1)
        options.startingAfter = try decoded("\"starting_after\"")
        options.status = try decoded("\"canceled\"")
        let actual = try await client.v1.invoicePayments.get(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture148Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getInvoicePayments decoded a response " +
                "that differs from the mock"
        )
    }
}
