// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriStripe
import XCTest

extension SdkMethodTests {
    func testPostIssuingDisputesDisputePostIssuingDisputesDisputeReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.issuing.disputes.submit.postIssuingDisputesDispute(
            dispute: decoded("\"dispute\""),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture422Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postIssuingDisputesDispute decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostIssuingDisputesDisputeSubmitPostIssuingDisputesDisputeSubmitReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.issuing.disputes.submit.postIssuingDisputesDispute(
            dispute: decoded("\"dispute\""),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture423Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postIssuingDisputesDisputeSubmit decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostIssuingPersonalizationDesignsPostIssuingPersonalizationDesignsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1IssuingPersonalizationDesignsMethods.PostIssuingPersonalizationDesignsOptions(physicalBundle: decoded("\"physical_bundle\""))
        options.cardLogo = try decoded("\"card_logo\"")
        options.carrierText = try decoded(sdkMethodFixture424Value1)
        options.expand = try decoded("[\"expand\"]")
        options.lookupKey = try decoded("\"lookup_key\"")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.name = try decoded("\"Example Name\"")
        options.preferences = try decoded("{\"is_default\":false}")
        options.transferLookupKey = try decoded("false")
        let actual = try await client.v1.issuing.personalizationDesigns.postIssuing(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture424Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postIssuingPersonalizationDesigns decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostIssuingPersonalizationDesignsPersonalizationDesignPostIssX903375cca0ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1IssuingPersonalizationDesignsMethods.PostIssuingPersonalizationDesignsPersonalizationDesignOptions(personalizationDesign: decoded("\"personalization_design\""))
        options.cardLogo = try decoded("\"card_logo\"")
        options.carrierText = try decoded(sdkMethodFixture425Value1)
        options.expand = try decoded("[\"expand\"]")
        options.lookupKey = try decoded("\"lookup_key\"")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.name = try decoded("\"Example Name\"")
        options.physicalBundle = try decoded("\"physical_bundle\"")
        options.preferences = try decoded("{\"is_default\":false}")
        options.transferLookupKey = try decoded("false")
        let actual = try await client.v1.issuing.personalizationDesigns.postIssuingPersonalizationDesign(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture425Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postIssuingPersonalizationDesignsPersonalizationDesign decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostIssuingSettlementsSettlementPostIssuingSettlementsSettlementReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.issuing.settlements.postIssuingSettlement(
            settlement: decoded("\"settlement\""),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture426Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postIssuingSettlementsSettlement decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostIssuingTokensTokenPostIssuingTokensTokenReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.issuing.tokens.postIssuingToken(
            token: decoded("\"token\""),
            status: decoded("\"active\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture427Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postIssuingTokensToken decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostIssuingTransactionsTransactionPostIssuingTransactionsTransactionReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.issuing.transactions.postIssuingTransaction(
            transaction: decoded("\"transaction\""),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture428Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postIssuingTransactionsTransaction decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostLinkAccountSessionsPostLinkAccountSessionsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1LinkAccountSessionsMethods.PostLinkAccountSessionsOptions(accountHolder: decoded(sdkMethodFixture429Value1), permissions: decoded("[\"balances\"]"))
        options.expand = try decoded("[\"expand\"]")
        options.filters = try decoded(sdkMethodFixture429Value2)
        options.limits = try decoded("{\"accounts\":1}")
        options.manualEntry = try decoded("{\"mode\":\"automatic\"}")
        options.prefetch = try decoded("[\"balances\"]")
        options.returnUrl = try decoded("\"https://example.com\"")
        let actual = try await client.v1.linkAccountSessions.post(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture429Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postLinkAccountSessions decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostLinkedAccountsAccountDisconnectPostLinkedAccountsAccountDisconnectReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.linkedAccounts.disconnect.postLinkedAccountsAccount(
            account: decoded("\"account\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture430Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postLinkedAccountsAccountDisconnect decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostLinkedAccountsAccountRefreshPostLinkedAccountsAccountRefreshReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.linkedAccounts.refresh.postLinkedAccountsAccount(
            account: decoded("\"account\""),
            features: decoded("[\"balance\"]"),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture431Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postLinkedAccountsAccountRefresh decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostPaymentIntentsPostPaymentIntentsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1PaymentIntentsMethods.PostPaymentIntentsOptions(amount: decoded("100"), currency: decoded("\"currency\""))
        options.allowedPaymentMethodTypes = try decoded("[\"acss_debit\"]")
        options.amountDetails = try decoded(sdkMethodFixture432Value1)
        options.applicationFeeAmount = try decoded("100")
        options.automaticPaymentMethods = try decoded("{\"allow_redirects\":\"always\",\"enabled\":false}")
        options.captureMethod = try decoded("\"automatic\"")
        options.confirm = try decoded("false")
        options.confirmationMethod = try decoded("\"automatic\"")
        options.confirmationToken = try decoded("\"confirmation_token\"")
        options.customer = try decoded("\"customer\"")
        options.customerAccount = try decoded("\"customer_account\"")
        options.description = try decoded("\"example text\"")
        options.errorOnRequiresAction = try decoded("false")
        options.excludedPaymentMethodTypes = try decoded("[\"acss_debit\"]")
        options.expand = try decoded("[\"expand\"]")
        options.hooks = try decoded("{\"inputs\":{\"tax\":{\"calculation\":\"calculation\"}}}")
        options.mandate = try decoded("\"2024-01-01T00:00:00Z\"")
        options.mandateData = try decoded(sdkMethodFixture432Value2)
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.offSession = try decoded("false")
        options.onBehalfOf = try decoded("\"on_behalf_of\"")
        options.paymentDetails = try decoded(sdkMethodFixture432Value3)
        options.paymentMethod = try decoded("\"payment_method\"")
        options.paymentMethodConfiguration = try decoded("\"payment_method_configuration\"")
        options.paymentMethodData = try decoded(sdkMethodFixture432Value4)
        options.paymentMethodOptions = try decoded(sdkMethodFixture432Value5)
        options.paymentMethodTypes = try decoded("[\"payment_method_types\"]")
        options.radarOptions = try decoded("{\"referrer\":\"referrer\",\"session\":\"session\"}")
        options.receiptEmail = try decoded("\"user@example.com\"")
        options.returnUrl = try decoded("\"https://example.com\"")
        options.setupFutureUsage = try decoded("\"off_session\"")
        options.shipping = try decoded(sdkMethodFixture432Value6)
        options.statementDescriptor = try decoded("\"statement_descriptor\"")
        options.statementDescriptorSuffix = try decoded("\"statement_descriptor_s\"")
        options.transferData = try decoded(sdkMethodFixture432Value7)
        options.transferGroup = try decoded("\"transfer_group\"")
        options.useStripeSdk = try decoded("false")
        let actual = try await client.v1.paymentIntents.post(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture432Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postPaymentIntents decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostPaymentIntentsIntentPostPaymentIntentsIntentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.paymentIntents.applyCustomerBalance.postPaymentIntentsIntent(
            intent: decoded("\"intent\""),
            amount: decoded("100"),
            currency: decoded("\"currency\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture433Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postPaymentIntentsIntent decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostPaymentIntentsIntentApplyCustomerBalancePostPaymentIntentXf6fe986c2cReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.paymentIntents.applyCustomerBalance.postPaymentIntentsIntent(
            intent: decoded("\"intent\""),
            amount: decoded("100"),
            currency: decoded("\"currency\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture434Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postPaymentIntentsIntentApplyCustomerBalance decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostPaymentIntentsIntentCancelPostPaymentIntentsIntentCancelReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.paymentIntents.cancel.postPaymentIntentsIntent(
            intent: decoded("\"intent\""),
            cancellationReason: decoded("\"abandoned\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture435Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postPaymentIntentsIntentCancel decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostPaymentIntentsIntentCapturePostPaymentIntentsIntentCaptureReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1PaymentIntentsCaptureMethods.PostPaymentIntentsIntentCaptureOptions(intent: decoded("\"intent\""))
        options.amountDetails = try decoded(sdkMethodFixture436Value1)
        options.amountToCapture = try decoded("100")
        options.applicationFeeAmount = try decoded("100")
        options.expand = try decoded("[\"expand\"]")
        options.finalCapture = try decoded("false")
        options.hooks = try decoded("{\"inputs\":{\"tax\":{\"calculation\":\"calculation\"}}}")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.paymentDetails = try decoded(sdkMethodFixture436Value2)
        options.statementDescriptor = try decoded("\"statement_descriptor\"")
        options.statementDescriptorSuffix = try decoded("\"statement_descriptor_s\"")
        options.transferData = try decoded("{\"amount\":100}")
        let actual = try await client.v1.paymentIntents.capture.postPaymentIntentsIntent(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture436Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postPaymentIntentsIntentCapture decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostPaymentIntentsIntentConfirmPostPaymentIntentsIntentConfirmReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1PaymentIntentsConfirmMethods.PostPaymentIntentsIntentConfirmOptions(intent: decoded("\"intent\""))
        options.allowedPaymentMethodTypes = try decoded("[\"acss_debit\"]")
        options.amountDetails = try decoded(sdkMethodFixture437Value1)
        options.amountToConfirm = try decoded("100")
        options.captureMethod = try decoded("\"automatic\"")
        options.clientSecret = try decoded("\"client_secret\"")
        options.confirmationToken = try decoded("\"confirmation_token\"")
        options.errorOnRequiresAction = try decoded("false")
        options.excludedPaymentMethodTypes = try decoded("[\"acss_debit\"]")
        options.expand = try decoded("[\"expand\"]")
        options.hooks = try decoded("{\"inputs\":{\"tax\":{\"calculation\":\"calculation\"}}}")
        options.mandate = try decoded("\"2024-01-01T00:00:00Z\"")
        options.mandateData = try decoded(sdkMethodFixture437Value2)
        options.offSession = try decoded("false")
        options.paymentDetails = try decoded(sdkMethodFixture437Value3)
        options.paymentMethod = try decoded("\"payment_method\"")
        options.paymentMethodData = try decoded(sdkMethodFixture437Value4)
        options.paymentMethodOptions = try decoded(sdkMethodFixture437Value5)
        options.paymentMethodTypes = try decoded("[\"payment_method_types\"]")
        options.radarOptions = try decoded("{\"referrer\":\"referrer\",\"session\":\"session\"}")
        options.receiptEmail = try decoded("\"user@example.com\"")
        options.returnUrl = try decoded("\"https://example.com\"")
        options.setupFutureUsage = try decoded("\"\"")
        options.shipping = try decoded(sdkMethodFixture437Value6)
        options.useStripeSdk = try decoded("false")
        let actual = try await client.v1.paymentIntents.confirm.postPaymentIntentsIntent(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture437Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postPaymentIntentsIntentConfirm decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostPaymentIntentsIntentIncrementAuthorizationPostPaymentInteX088dcf4270ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1PaymentIntentsIncrementAuthorizationMethods.PostPaymentIntentsIntentIncrementAuthorizationOptions(intent: decoded("\"intent\""), amount: decoded("100"))
        options.amountDetails = try decoded(sdkMethodFixture438Value1)
        options.applicationFeeAmount = try decoded("100")
        options.description = try decoded("\"example text\"")
        options.expand = try decoded("[\"expand\"]")
        options.hooks = try decoded("{\"inputs\":{\"tax\":{\"calculation\":\"calculation\"}}}")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.paymentDetails = try decoded(sdkMethodFixture438Value2)
        options.statementDescriptor = try decoded("\"statement_descriptor\"")
        options.transferData = try decoded("{\"amount\":100}")
        let actual = try await client.v1.paymentIntents.incrementAuthorization.postPaymentIntentsIntent(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture438Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postPaymentIntentsIntentIncrementAuthorization decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostPaymentIntentsIntentVerifyMicrodepositsPostPaymentIntentsX62d22d6210ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.paymentIntents.verifyMicrodeposits.postPaymentIntentsIntent(
            intent: decoded("\"intent\""),
            amounts: decoded("[100]"),
            clientSecret: decoded("\"client_secret\""),
            descriptorCode: decoded("\"descriptor_code\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture439Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postPaymentIntentsIntentVerifyMicrodeposits decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostPaymentLinksPostPaymentLinksReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1PaymentLinksMethods.PostPaymentLinksOptions(lineItems: decoded(sdkMethodFixture440Value1))
        options.afterCompletion = try decoded(sdkMethodFixture440Value2)
        options.allowPromotionCodes = try decoded("false")
        options.applicationFeeAmount = try decoded("100")
        options.applicationFeePercent = try decoded("1")
        options.automaticTax = try decoded(sdkMethodFixture440Value3)
        options.billingAddressCollection = try decoded("\"auto\"")
        options.consentCollection = try decoded(sdkMethodFixture440Value4)
        options.currency = try decoded("\"currency\"")
        options.customFields = try decoded(sdkMethodFixture440Value5)
        options.customText = try decoded(sdkMethodFixture440Value6)
        options.customerCreation = try decoded("\"always\"")
        options.expand = try decoded("[\"expand\"]")
        options.inactiveMessage = try decoded("\"example text\"")
        options.invoiceCreation = try decoded(sdkMethodFixture440Value7)
        options.managedPayments = try decoded("{\"enabled\":false}")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.nameCollection = try decoded(sdkMethodFixture440Value8)
        options.onBehalfOf = try decoded("\"on_behalf_of\"")
        options.optionalItems = try decoded(sdkMethodFixture440Value9)
        options.paymentIntentData = try decoded(sdkMethodFixture440Value10)
        options.paymentMethodCollection = try decoded("\"always\"")
        options.paymentMethodOptions = try decoded(sdkMethodFixture440Value11)
        options.paymentMethodTypes = try decoded("[\"affirm\"]")
        options.phoneNumberCollection = try decoded("{\"enabled\":false}")
        options.restrictions = try decoded("{\"completed_sessions\":{\"limit\":1}}")
        options.shippingAddressCollection = try decoded("{\"allowed_countries\":[\"AC\"]}")
        options.shippingOptions = try decoded("[{\"shipping_rate\":\"shipping_rate\"}]")
        options.submitType = try decoded("\"auto\"")
        options.subscriptionData = try decoded(sdkMethodFixture440Value12)
        options.taxIdCollection = try decoded("{\"enabled\":false,\"required\":\"if_supported\"}")
        options.transferData = try decoded("{\"amount\":100,\"destination\":\"destination\"}")
        let actual = try await client.v1.paymentLinks.post(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture440Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postPaymentLinks decoded a response " +
                "that differs from the mock"
        )
    }
}
