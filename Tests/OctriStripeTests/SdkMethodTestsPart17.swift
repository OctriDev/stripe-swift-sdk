// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriStripe
import XCTest

extension SdkMethodTests {
    func testPostDisputesDisputePostDisputesDisputeReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.disputes.close.postDisputesDispute(
            dispute: decoded("\"dispute\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture378Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postDisputesDispute decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostDisputesDisputeClosePostDisputesDisputeCloseReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.disputes.close.postDisputesDispute(
            dispute: decoded("\"dispute\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture379Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postDisputesDisputeClose decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostEntitlementsFeaturesPostEntitlementsFeaturesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.entitlements.features.postEntitlements(
            lookupKey: decoded("\"lookup_key\""),
            name: decoded("\"Example Name\""),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture380Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postEntitlementsFeatures decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostEntitlementsFeaturesIdPostEntitlementsFeaturesIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.entitlements.features.postEntitlementsId(
            id: decoded("\"id-1\""),
            active: decoded("false"),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}"),
            name: decoded("\"Example Name\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture381Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postEntitlementsFeaturesId decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostEphemeralKeysPostEphemeralKeysReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.ephemeralKeys.post(
            customer: decoded("\"customer\""),
            expand: decoded("[\"expand\"]"),
            issuingCard: decoded("\"issuing_card\""),
            nonce: decoded("\"nonce\""),
            verificationSession: decoded("\"verification_session\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture382Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postEphemeralKeys decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostExternalAccountsIdPostExternalAccountsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1ExternalAccountsMethods.PostExternalAccountsIdOptions(id: decoded("\"id-1\""))
        options.accountHolderName = try decoded("\"Example Name\"")
        options.accountHolderType = try decoded("\"\"")
        options.accountType = try decoded("\"checking\"")
        options.addressCity = try decoded("\"address_city\"")
        options.addressCountry = try decoded("\"address_country\"")
        options.addressLine1 = try decoded("\"address_line1\"")
        options.addressLine2 = try decoded("\"address_line2\"")
        options.addressState = try decoded("\"address_state\"")
        options.addressZip = try decoded("\"address_zip\"")
        options.defaultForCurrency = try decoded("false")
        options.documents = try decoded("{\"bank_account_ownership_verification\":{\"files\":[\"files\"]}}")
        options.expMonth = try decoded("\"exp_month\"")
        options.expYear = try decoded("\"exp_year\"")
        options.expand = try decoded("[\"expand\"]")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.name = try decoded("\"Example Name\"")
        let actual = try await client.v1.externalAccounts.postId(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture383Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postExternalAccountsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostFileLinksPostFileLinksReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.fileLinks.post(
            file: decoded("\"file\""),
            expand: decoded("[\"expand\"]"),
            expiresAt: decoded("1"),
            metadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture384Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postFileLinks decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostFileLinksLinkPostFileLinksLinkReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.fileLinks.postLink(
            link: decoded("\"https://example.com\""),
            expand: decoded("[\"expand\"]"),
            expiresAt: decoded("\"now\""),
            metadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture385Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postFileLinksLink decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostFilesPostFilesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.files.post(
            file: SdkUploadFile(data: Data("sample".utf8), filename: "sample.bin"),
            purpose: decoded("\"account_requirement\""),
            expand: decoded("[\"expand\"]"),
            fileLinkData: decoded("{\"create\":false,\"expires_at\":1,\"metadata\":{\"key\":\"key\"}}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture386Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postFiles decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostFinancialConnectionsAccountsAccountDisconnectPostFinanciaX6b79ea8119ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.financialConnections.accounts.disconnect
            .postFinancialConnectionsAccountsAccount(
                account: decoded("\"account\""),
                expand: decoded("[\"expand\"]")
            )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture387Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postFinancialConnectionsAccountsAccountDisconnect decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostFinancialConnectionsAccountsAccountRefreshPostFinancialCoXa202d3ef51ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.financialConnections.accounts.refresh.postFinancialConnectionsAccountsAccount(
            account: decoded("\"account\""),
            features: decoded("[\"balance\"]"),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture388Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postFinancialConnectionsAccountsAccountRefresh decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostFinancialConnectionsAccountsAccountSubscribePostFinancialX50075c29cdReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.financialConnections.accounts.subscribe
            .postFinancialConnectionsAccountsAccount(
                account: decoded("\"account\""),
                features: decoded("[\"transactions\"]"),
                expand: decoded("[\"expand\"]")
            )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture389Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postFinancialConnectionsAccountsAccountSubscribe decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostFinancialConnectionsAccountsAccountUnsubscribePostFinanciXefcf8d873cReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.financialConnections.accounts.unsubscribe
            .postFinancialConnectionsAccountsAccount(
                account: decoded("\"account\""),
                features: decoded("[\"transactions\"]"),
                expand: decoded("[\"expand\"]")
            )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture390Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postFinancialConnectionsAccountsAccountUnsubscribe decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostFinancialConnectionsSessionsPostFinancialConnectionsSessionsReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1FinancialConnectionsSessionsMethods.PostFinancialConnectionsSessionsOptions(
            accountHolder: decoded(sdkMethodFixture391Value1),
            permissions: decoded("[\"balances\"]")
        )
        options.expand = try decoded("[\"expand\"]")
        options.filters = try decoded(sdkMethodFixture391Value2)
        options.limits = try decoded("{\"accounts\":1}")
        options.manualEntry = try decoded("{\"mode\":\"automatic\"}")
        options.prefetch = try decoded("[\"balances\"]")
        options.returnUrl = try decoded("\"https://example.com\"")
        let actual = try await client.v1.financialConnections.sessions.postFinancialConnections(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture391Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postFinancialConnectionsSessions decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostForwardingRequestsPostForwardingRequestsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.forwarding.requests.postForwarding(
            paymentMethod: decoded("\"payment_method\""),
            replacements: decoded("[\"card_cvc\"]"),
            url: decoded("\"https://example.com\""),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}"),
            request: decoded(sdkMethodFixture392Value1)
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture392Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postForwardingRequests decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostIdentityVerificationSessionsPostIdentityVerificationSessionsReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1IdentityVerificationSessionsMethods.PostIdentityVerificationSessionsOptions()
        options.clientReferenceId = try decoded("\"client_reference_-1\"")
        options.expand = try decoded("[\"expand\"]")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.options = try decoded(sdkMethodFixture393Value1)
        options.providedDetails = try decoded("{\"email\":\"user@example.com\",\"phone\":\"+15555550100\"}")
        options.relatedCustomer = try decoded("\"related_customer\"")
        options.relatedCustomerAccount = try decoded("\"related_customer_account\"")
        options.relatedPerson = try decoded("{\"account\":\"account\",\"person\":\"person\"}")
        options.returnUrl = try decoded("\"https://example.com\"")
        options.type = try decoded("\"document\"")
        options.verificationFlow = try decoded("\"verification_flow\"")
        let actual = try await client.v1.identity.verificationSessions.postIdentity(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture393Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postIdentityVerificationSessions decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostIdentityVerificationSessionsSessionPostIdentityVerificatiX364bc77db4ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.identity.verificationSessions.cancel.postIdentityVerificationSessionsSession(
            session: decoded("\"session\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture394Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postIdentityVerificationSessionsSession decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostIdentityVerificationSessionsSessionCancelPostIdentityVeriXb5be6eb645ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.identity.verificationSessions.cancel.postIdentityVerificationSessionsSession(
            session: decoded("\"session\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture395Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postIdentityVerificationSessionsSessionCancel decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostIdentityVerificationSessionsSessionRedactPostIdentityVeriXaefb77d547ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.identity.verificationSessions.redact.postIdentityVerificationSessionsSession(
            session: decoded("\"session\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture396Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postIdentityVerificationSessionsSessionRedact decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostInvoiceitemsPostInvoiceitemsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1InvoiceitemsMethods.PostInvoiceitemsOptions()
        options.amount = try decoded("100")
        options.currency = try decoded("\"currency\"")
        options.customer = try decoded("\"customer\"")
        options.customerAccount = try decoded("\"customer_account\"")
        options.description = try decoded("\"example text\"")
        options.discountable = try decoded("false")
        options.discounts = try decoded(sdkMethodFixture397Value1)
        options.expand = try decoded("[\"expand\"]")
        options.invoice = try decoded("\"invoice\"")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.period = try decoded("{\"end\":1,\"start\":1}")
        options.priceData = try decoded(sdkMethodFixture397Value2)
        options.pricing = try decoded("{\"price\":\"price\"}")
        options.quantity = try decoded("1")
        options.quantityDecimal = try decoded("\"quantity_decimal\"")
        options.subscription = try decoded("\"subscription\"")
        options.taxBehavior = try decoded("\"exclusive\"")
        options.taxCode = try decoded("\"tax_code\"")
        options.taxRates = try decoded("[\"tax_rates\"]")
        options.unitAmountDecimal = try decoded("\"unit_amount_decimal\"")
        let actual = try await client.v1.invoiceitems.post(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture397Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postInvoiceitems decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostInvoiceitemsInvoiceitemPostInvoiceitemsInvoiceitemReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1InvoiceitemsMethods
            .PostInvoiceitemsInvoiceitemOptions(invoiceitem: decoded("\"invoiceitem\""))
        options.amount = try decoded("100")
        options.description = try decoded("\"example text\"")
        options.discountable = try decoded("false")
        options.discounts = try decoded(sdkMethodFixture398Value1)
        options.expand = try decoded("[\"expand\"]")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.period = try decoded("{\"end\":1,\"start\":1}")
        options.priceData = try decoded(sdkMethodFixture398Value2)
        options.pricing = try decoded("{\"price\":\"price\"}")
        options.quantity = try decoded("1")
        options.quantityDecimal = try decoded("\"quantity_decimal\"")
        options.taxBehavior = try decoded("\"exclusive\"")
        options.taxCode = try decoded("\"tax_code\"")
        options.taxRates = try decoded("[\"tax_rates\"]")
        options.unitAmountDecimal = try decoded("\"unit_amount_decimal\"")
        let actual = try await client.v1.invoiceitems.postInvoiceitem(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture398Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postInvoiceitemsInvoiceitem decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostInvoiceRenderingTemplatesTemplateArchivePostInvoiceRenderX166b48fdfdReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.invoiceRenderingTemplates.archive.postInvoiceRenderingTemplatesTemplate(
            template: decoded("\"template\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture399Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postInvoiceRenderingTemplatesTemplateArchive decoded a response " +
                "that differs from the mock"
        )
    }
}
