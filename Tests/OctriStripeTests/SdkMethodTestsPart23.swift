// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriStripe
import XCTest

extension SdkMethodTests {
    func testPostSubscriptionsPostSubscriptionsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1SubscriptionsMethods.PostSubscriptionsOptions()
        options.addInvoiceItems = try decoded(sdkMethodFixture498Value1)
        options.applicationFeePercent = try decoded("1")
        options.automaticTax = try decoded(sdkMethodFixture498Value2)
        options.backdateStartDate = try decoded("1")
        options.billingCycleAnchor = try decoded("1")
        options
            .billingCycleAnchorConfig =
            try decoded("{\"day_of_month\":1,\"hour\":1,\"minute\":1,\"month\":1,\"second\":1}")
        options.billingMode = try decoded(sdkMethodFixture498Value3)
        options.billingSchedules = try decoded(sdkMethodFixture498Value4)
        options.billingThresholds = try decoded("{\"amount_gte\":100,\"reset_billing_cycle_anchor\":false}")
        options.cancelAt = try decoded("1")
        options.cancelAtPeriodEnd = try decoded("false")
        options.collectionMethod = try decoded("\"charge_automatically\"")
        options.currency = try decoded("\"currency\"")
        options.customer = try decoded("\"customer\"")
        options.customerAccount = try decoded("\"customer_account\"")
        options.daysUntilDue = try decoded("1")
        options.defaultPaymentMethod = try decoded("\"default_payment_method\"")
        options.defaultSource = try decoded("\"default_source\"")
        options.defaultTaxRates = try decoded("[\"default_tax_rates\"]")
        options.description = try decoded("\"example text\"")
        options.discounts = try decoded(sdkMethodFixture498Value5)
        options.expand = try decoded("[\"expand\"]")
        options.invoiceSettings = try decoded(sdkMethodFixture498Value6)
        options.items = try decoded(sdkMethodFixture498Value7)
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.offSession = try decoded("false")
        options.onBehalfOf = try decoded("\"on_behalf_of\"")
        options.paymentBehavior = try decoded("\"allow_incomplete\"")
        options.paymentSettings = try decoded(sdkMethodFixture498Value8)
        options.pendingInvoiceItemInterval = try decoded("{\"interval\":\"day\",\"interval_count\":1}")
        options.prorationBehavior = try decoded("\"always_invoice\"")
        options.transferData = try decoded("{\"amount_percent\":9.99,\"destination\":\"destination\"}")
        options.trialEnd = try decoded("\"now\"")
        options.trialFromPlan = try decoded("false")
        options.trialPeriodDays = try decoded("1")
        options.trialSettings = try decoded("{\"end_behavior\":{\"missing_payment_method\":\"cancel\"}}")
        let actual = try await client.v1.subscriptions.post(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture498Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postSubscriptions decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostSubscriptionSchedulesPostSubscriptionSchedulesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1SubscriptionSchedulesMethods.PostSubscriptionSchedulesOptions()
        options.billingMode = try decoded(sdkMethodFixture499Value1)
        options.customer = try decoded("\"customer\"")
        options.customerAccount = try decoded("\"customer_account\"")
        options.defaultSettings = try decoded(sdkMethodFixture499Value2)
        options.endBehavior = try decoded("\"cancel\"")
        options.expand = try decoded("[\"expand\"]")
        options.fromSubscription = try decoded("\"from_subscription\"")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.phases = try decoded(sdkMethodFixture499Value3)
        options.startDate = try decoded("1")
        let actual = try await client.v1.subscriptionSchedules.post(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture499Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postSubscriptionSchedules decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostSubscriptionSchedulesSchedulePostSubscriptionSchedulesScheduleReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.subscriptionSchedules.cancel.postSubscriptionSchedulesSchedule(
            schedule: decoded("\"schedule\""),
            expand: decoded("[\"expand\"]"),
            invoiceNow: decoded("\"sample\""),
            prorate: decoded("\"sample\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture500Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postSubscriptionSchedulesSchedule decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostSubscriptionSchedulesScheduleCancelPostSubscriptionScheduX57478f3fb9ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.subscriptionSchedules.cancel.postSubscriptionSchedulesSchedule(
            schedule: decoded("\"schedule\""),
            expand: decoded("[\"expand\"]"),
            invoiceNow: decoded("false"),
            prorate: decoded("false")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture501Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postSubscriptionSchedulesScheduleCancel decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostSubscriptionSchedulesScheduleReleasePostSubscriptionSchedX23b65a8cefReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.subscriptionSchedules.release.postSubscriptionSchedulesSchedule(
            schedule: decoded("\"schedule\""),
            expand: decoded("[\"expand\"]"),
            preserveCancelDate: decoded("false")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture502Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postSubscriptionSchedulesScheduleRelease decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostSubscriptionsSubscriptionExposedIdPostSubscriptionsSubscrX87d466788dReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1SubscriptionsMethods
            .PostSubscriptionsSubscriptionExposedIdOptions(
                subscriptionExposedId: decoded("\"subscription_exposed_-1\"")
            )
        options.addInvoiceItems = try decoded(sdkMethodFixture503Value1)
        options.applicationFeePercent = try decoded("1")
        options.automaticTax = try decoded(sdkMethodFixture503Value2)
        options.billingCycleAnchor = try decoded("\"now\"")
        options.billingSchedules = try decoded(sdkMethodFixture503Value3)
        options.billingThresholds = try decoded("{\"amount_gte\":100,\"reset_billing_cycle_anchor\":false}")
        options.cancelAt = try decoded("1")
        options.cancelAtPeriodEnd = try decoded("false")
        options.cancellationDetails = try decoded(sdkMethodFixture503Value4)
        options.collectionMethod = try decoded("\"charge_automatically\"")
        options.daysUntilDue = try decoded("1")
        options.defaultPaymentMethod = try decoded("\"default_payment_method\"")
        options.defaultSource = try decoded("\"default_source\"")
        options.defaultTaxRates = try decoded("[\"default_tax_rates\"]")
        options.description = try decoded("\"example text\"")
        options.discounts = try decoded(sdkMethodFixture503Value5)
        options.expand = try decoded("[\"expand\"]")
        options.invoiceSettings = try decoded(sdkMethodFixture503Value6)
        options.items = try decoded(sdkMethodFixture503Value7)
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.offSession = try decoded("false")
        options.onBehalfOf = try decoded("\"on_behalf_of\"")
        options.pauseCollection = try decoded("{\"behavior\":\"keep_as_draft\",\"resumes_at\":1}")
        options.paymentBehavior = try decoded("\"allow_incomplete\"")
        options.paymentSettings = try decoded(sdkMethodFixture503Value8)
        options.pendingInvoiceItemInterval = try decoded("{\"interval\":\"day\",\"interval_count\":1}")
        options.prorationBehavior = try decoded("\"always_invoice\"")
        options.prorationDate = try decoded("1")
        options.transferData = try decoded("{\"amount_percent\":9.99,\"destination\":\"destination\"}")
        options.trialEnd = try decoded("\"now\"")
        options.trialFromPlan = try decoded("false")
        options.trialSettings = try decoded("{\"end_behavior\":{\"missing_payment_method\":\"cancel\"}}")
        let actual = try await client.v1.subscriptions.postSubscriptionExposedId(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture503Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postSubscriptionsSubscriptionExposedId decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostSubscriptionsSubscriptionMigratePostSubscriptionsSubscriptionMigrateReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.subscriptions.migrate.postSubscriptionsSubscription(
            subscription: decoded("\"subscription\""),
            billingMode: decoded(sdkMethodFixture504Value1),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture504Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postSubscriptionsSubscriptionMigrate decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostSubscriptionsSubscriptionResumePostSubscriptionsSubscriptionResumeReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.subscriptions.resume.postSubscriptionsSubscription(
            subscription: decoded("\"subscription\""),
            billingCycleAnchor: decoded("\"now\""),
            expand: decoded("[\"expand\"]"),
            prorationBehavior: decoded("\"always_invoice\""),
            prorationDate: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture505Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postSubscriptionsSubscriptionResume decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTaxCalculationsPostTaxCalculationsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1TaxCalculationsMethods.PostTaxCalculationsOptions(
            currency: decoded("\"currency\""),
            lineItems: decoded(sdkMethodFixture506Value1)
        )
        options.customer = try decoded("\"customer\"")
        options.customerDetails = try decoded(sdkMethodFixture506Value2)
        options.expand = try decoded("[\"expand\"]")
        options.shipFromDetails = try decoded(sdkMethodFixture506Value3)
        options.shippingCost = try decoded(sdkMethodFixture506Value4)
        options.taxDate = try decoded("1")
        let actual = try await client.v1.tax.calculations.postTax(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture506Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTaxCalculations decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTaxIdsPostTaxIdsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.taxIds.post(
            type: decoded("\"ad_nrt\""),
            value: decoded("\"value\""),
            expand: decoded("[\"expand\"]"),
            owner: decoded(sdkMethodFixture507Value1)
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture507Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTaxIds decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTaxRatesPostTaxRatesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1TaxRatesMethods.PostTaxRatesOptions(
            displayName: decoded("\"Example Name\""),
            inclusive: decoded("false"),
            percentage: decoded("30")
        )
        options.active = try decoded("false")
        options.country = try decoded("\"country\"")
        options.description = try decoded("\"example text\"")
        options.expand = try decoded("[\"expand\"]")
        options.jurisdiction = try decoded("\"https://example.com\"")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.state = try decoded("\"state\"")
        options.taxType = try decoded("\"amusement_tax\"")
        let actual = try await client.v1.taxRates.post(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture508Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTaxRates decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTaxRatesTaxRatePostTaxRatesTaxRateReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1TaxRatesMethods.PostTaxRatesTaxRateOptions(taxRate: decoded("\"tax_rate\""))
        options.active = try decoded("false")
        options.country = try decoded("\"country\"")
        options.description = try decoded("\"example text\"")
        options.displayName = try decoded("\"Example Name\"")
        options.expand = try decoded("[\"expand\"]")
        options.jurisdiction = try decoded("\"https://example.com\"")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.state = try decoded("\"state\"")
        options.taxType = try decoded("\"amusement_tax\"")
        let actual = try await client.v1.taxRates.postTaxRate(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture509Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTaxRatesTaxRate decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTaxRegistrationsPostTaxRegistrationsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.tax.registrations.postTax(
            activeFrom: decoded("\"now\""),
            country: decoded("\"country\""),
            countryOptions: PostTaxRegistrationsRequestBodyCountryOptions(rawValue: decoded("\"sample\"")),
            expand: decoded("[\"expand\"]"),
            expiresAt: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture510Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTaxRegistrations decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTaxRegistrationsIdPostTaxRegistrationsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.tax.registrations.postTaxId(
            id: decoded("\"id-1\""),
            activeFrom: decoded("\"now\""),
            expand: decoded("[\"expand\"]"),
            expiresAt: decoded("\"now\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture511Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTaxRegistrationsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTaxSettingsPostTaxSettingsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.tax.settings.postTax(
            defaults: decoded("{\"tax_behavior\":\"exclusive\",\"tax_code\":\"tax_code\"}"),
            expand: decoded("[\"expand\"]"),
            headOffice: decoded(sdkMethodFixture512Value1)
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture512Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTaxSettings decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTaxTransactionsCreateFromCalculationPostTaxTransactionsCrX02bc1e46beReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.tax.transactions.createFromCalculation.postTaxTransactions(
            calculation: decoded("\"calculation\""),
            reference: decoded("\"reference\""),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}"),
            postedAt: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture513Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTaxTransactionsCreateFromCalculation decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTaxTransactionsCreateReversalPostTaxTransactionsCreateReversalReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1TaxTransactionsCreateReversalMethods.PostTaxTransactionsCreateReversalOptions(
            mode: decoded("\"full\""),
            originalTransaction: decoded("\"original_transaction\""),
            reference: decoded("\"reference\"")
        )
        options.expand = try decoded("[\"expand\"]")
        options.flatAmount = try decoded("100")
        options.lineItems = try decoded(sdkMethodFixture514Value1)
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.shippingCost = try decoded("{\"amount\":100,\"amount_tax\":100}")
        let actual = try await client.v1.tax.transactions.createReversal.postTaxTransactions(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture514Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTaxTransactionsCreateReversal decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTerminalConfigurationsPostTerminalConfigurationsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1TerminalConfigurationsMethods.PostTerminalConfigurationsOptions()
        options.bbposWisepad3 = try decoded("{\"splashscreen\":\"splashscreen\"}")
        options.bbposWiseposE = try decoded("{\"splashscreen\":\"splashscreen\"}")
        options.cellular = try decoded("{\"enabled\":false}")
        options.expand = try decoded("[\"expand\"]")
        options.name = try decoded("\"Example Name\"")
        options.offline = try decoded("{\"enabled\":false}")
        options.rebootWindow = try decoded("{\"end_hour\":1,\"start_hour\":1}")
        options.stripeS700 = try decoded("{\"splashscreen\":\"splashscreen\"}")
        options.stripeS710 = try decoded("{\"splashscreen\":\"splashscreen\"}")
        options.tipping = try decoded(sdkMethodFixture515Value1)
        options.verifoneM425 = try decoded("{\"splashscreen\":\"splashscreen\"}")
        options.verifoneP400 = try decoded("{\"splashscreen\":\"splashscreen\"}")
        options.verifoneP630 = try decoded("{\"splashscreen\":\"splashscreen\"}")
        options.verifoneUx700 = try decoded("{\"splashscreen\":\"splashscreen\"}")
        options.verifoneV660p = try decoded("{\"splashscreen\":\"splashscreen\"}")
        options.wifi = try decoded(sdkMethodFixture515Value2)
        let actual = try await client.v1.terminal.configurations.postTerminal(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture515Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTerminalConfigurations decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTerminalConfigurationsConfigurationPostTerminalConfiguratXfd0468699aReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1TerminalConfigurationsMethods
            .PostTerminalConfigurationsConfigurationOptions(configuration: decoded("\"configuration\""))
        options.bbposWisepad3 = try decoded("{\"splashscreen\":\"splashscreen\"}")
        options.bbposWiseposE = try decoded("{\"splashscreen\":\"splashscreen\"}")
        options.cellular = try decoded("{\"enabled\":false}")
        options.expand = try decoded("[\"expand\"]")
        options.name = try decoded("\"Example Name\"")
        options.offline = try decoded("{\"enabled\":false}")
        options.rebootWindow = try decoded("{\"end_hour\":1,\"start_hour\":1}")
        options.stripeS700 = try decoded("{\"splashscreen\":\"splashscreen\"}")
        options.stripeS710 = try decoded("{\"splashscreen\":\"splashscreen\"}")
        options.tipping = try decoded(sdkMethodFixture516Value1)
        options.verifoneM425 = try decoded("{\"splashscreen\":\"splashscreen\"}")
        options.verifoneP400 = try decoded("{\"splashscreen\":\"splashscreen\"}")
        options.verifoneP630 = try decoded("{\"splashscreen\":\"splashscreen\"}")
        options.verifoneUx700 = try decoded("{\"splashscreen\":\"splashscreen\"}")
        options.verifoneV660p = try decoded("{\"splashscreen\":\"splashscreen\"}")
        options.wifi = try decoded(sdkMethodFixture516Value2)
        let actual = try await client.v1.terminal.configurations.postTerminalConfiguration(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture516Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTerminalConfigurationsConfiguration decoded a response " +
                "that differs from the mock"
        )
    }
}
