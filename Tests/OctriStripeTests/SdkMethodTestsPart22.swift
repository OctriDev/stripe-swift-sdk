// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriStripe
import XCTest

extension SdkMethodTests {
    func testPostRadarValueListItemsPostRadarValueListItemsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.radar.valueListItems.postRadar(
            value: decoded("\"value\""),
            valueList: decoded("\"value_list\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture477Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postRadarValueListItems decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostRadarValueListsPostRadarValueListsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.radar.valueLists.postRadar(
            alias: decoded("\"alias\""),
            name: decoded("\"Example Name\""),
            expand: decoded("[\"expand\"]"),
            itemType: decoded("\"account\""),
            metadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture478Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postRadarValueLists decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostRadarValueListsValueListPostRadarValueListsValueListReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.radar.valueLists.postRadarValueList(
            valueList: decoded("\"value_list\""),
            alias: decoded("\"alias\""),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}"),
            name: decoded("\"Example Name\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture479Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postRadarValueListsValueList decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostRefundsPostRefundsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1RefundsMethods.PostRefundsOptions()
        options.amount = try decoded("100")
        options.charge = try decoded("\"charge\"")
        options.currency = try decoded("\"currency\"")
        options.customer = try decoded("\"customer\"")
        options.expand = try decoded("[\"expand\"]")
        options.instructionsEmail = try decoded("\"user@example.com\"")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.origin = try decoded("\"customer_balance\"")
        options.paymentIntent = try decoded("\"payment_intent\"")
        options.reason = try decoded("\"duplicate\"")
        options.refundApplicationFee = try decoded("false")
        options.reverseTransfer = try decoded("false")
        let actual = try await client.v1.refunds.post(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture480Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postRefunds decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostRefundsRefundPostRefundsRefundReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.refunds.cancel.postRefundsRefund(
            refund: decoded("\"refund\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture481Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postRefundsRefund decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostRefundsRefundCancelPostRefundsRefundCancelReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.refunds.cancel.postRefundsRefund(
            refund: decoded("\"refund\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture482Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postRefundsRefundCancel decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostReportingReportRunsPostReportingReportRunsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.reporting.reportRuns.postReporting(
            reportType: decoded("\"report_type\""),
            expand: decoded("[\"expand\"]"),
            parameters: decoded(sdkMethodFixture483Value1)
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture483Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postReportingReportRuns decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostReviewsReviewApprovePostReviewsReviewApproveReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.reviews.approve.postReviewsReview(
            review: decoded("\"review\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture484Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postReviewsReviewApprove decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostSetupIntentsPostSetupIntentsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1SetupIntentsMethods.PostSetupIntentsOptions()
        options.allowedPaymentMethodTypes = try decoded("[\"acss_debit\"]")
        options.attachToSelf = try decoded("false")
        options.automaticPaymentMethods = try decoded("{\"allow_redirects\":\"always\",\"enabled\":false}")
        options.confirm = try decoded("false")
        options.confirmationToken = try decoded("\"confirmation_token\"")
        options.customer = try decoded("\"customer\"")
        options.customerAccount = try decoded("\"customer_account\"")
        options.description = try decoded("\"example text\"")
        options.excludedPaymentMethodTypes = try decoded("[\"acss_debit\"]")
        options.expand = try decoded("[\"expand\"]")
        options.flowDirections = try decoded("[\"inbound\"]")
        options.mandateData = try decoded(sdkMethodFixture485Value1)
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.onBehalfOf = try decoded("\"on_behalf_of\"")
        options.paymentMethod = try decoded("\"payment_method\"")
        options.paymentMethodConfiguration = try decoded("\"payment_method_configuration\"")
        options.paymentMethodData = try decoded(sdkMethodFixture485Value2)
        options.paymentMethodOptions = try decoded(sdkMethodFixture485Value3)
        options.paymentMethodTypes = try decoded("[\"payment_method_types\"]")
        options.returnUrl = try decoded("\"https://example.com\"")
        options.singleUse = try decoded("{\"amount\":100,\"currency\":\"currency\"}")
        options.usage = try decoded("\"off_session\"")
        options.useStripeSdk = try decoded("false")
        let actual = try await client.v1.setupIntents.post(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture485Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postSetupIntents decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostSetupIntentsIntentPostSetupIntentsIntentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.setupIntents.cancel.postSetupIntentsIntent(
            intent: decoded("\"intent\""),
            cancellationReason: decoded("\"sample\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture486Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postSetupIntentsIntent decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostSetupIntentsIntentCancelPostSetupIntentsIntentCancelReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.setupIntents.cancel.postSetupIntentsIntent(
            intent: decoded("\"intent\""),
            cancellationReason: decoded("\"abandoned\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture487Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postSetupIntentsIntentCancel decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostSetupIntentsIntentConfirmPostSetupIntentsIntentConfirmReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1SetupIntentsConfirmMethods.PostSetupIntentsIntentConfirmOptions(intent: decoded("\"intent\""))
        options.allowedPaymentMethodTypes = try decoded("[\"acss_debit\"]")
        options.clientSecret = try decoded("\"client_secret\"")
        options.confirmationToken = try decoded("\"confirmation_token\"")
        options.expand = try decoded("[\"expand\"]")
        options.mandateData = try decoded(sdkMethodFixture488Value1)
        options.paymentMethod = try decoded("\"payment_method\"")
        options.paymentMethodData = try decoded(sdkMethodFixture488Value2)
        options.paymentMethodOptions = try decoded(sdkMethodFixture488Value3)
        options.returnUrl = try decoded("\"https://example.com\"")
        options.useStripeSdk = try decoded("false")
        let actual = try await client.v1.setupIntents.confirm.postSetupIntentsIntent(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture488Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postSetupIntentsIntentConfirm decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostSetupIntentsIntentVerifyMicrodepositsPostSetupIntentsInteX3f7879e560ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.setupIntents.verifyMicrodeposits.postSetupIntentsIntent(
            intent: decoded("\"intent\""),
            amounts: decoded("[100]"),
            clientSecret: decoded("\"client_secret\""),
            descriptorCode: decoded("\"descriptor_code\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture489Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postSetupIntentsIntentVerifyMicrodeposits decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostShippingRatesPostShippingRatesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1ShippingRatesMethods.PostShippingRatesOptions(displayName: decoded("\"Example Name\""))
        options.deliveryEstimate = try decoded(sdkMethodFixture490Value1)
        options.expand = try decoded("[\"expand\"]")
        options.fixedAmount = try decoded(sdkMethodFixture490Value2)
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.taxBehavior = try decoded("\"exclusive\"")
        options.taxCode = try decoded("\"tax_code\"")
        options.type = try decoded("\"fixed_amount\"")
        let actual = try await client.v1.shippingRates.post(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture490Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postShippingRates decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostShippingRatesShippingRateTokenPostShippingRatesShippingRateTokenReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.shippingRates.postShippingRateToken(
            shippingRateToken: decoded("\"shipping_rate_token\""),
            active: decoded("false"),
            expand: decoded("[\"expand\"]"),
            fixedAmount: decoded(sdkMethodFixture491Value1),
            metadata: decoded("{\"key\":\"key\"}"),
            taxBehavior: decoded("\"exclusive\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture491Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postShippingRatesShippingRateToken decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostSigmaSavedQueriesIdPostSigmaSavedQueriesIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.sigma.savedQueries.postSigmaId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]"),
            name: decoded("\"Example Name\""),
            sql: decoded("\"sql\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture492Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postSigmaSavedQueriesId decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostSourcesPostSourcesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1SourcesMethods.PostSourcesOptions()
        options.amount = try decoded("100")
        options.currency = try decoded("\"currency\"")
        options.customer = try decoded("\"customer\"")
        options.expand = try decoded("[\"expand\"]")
        options.flow = try decoded("\"code_verification\"")
        options.mandate = try decoded(sdkMethodFixture493Value1)
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.originalSource = try decoded("\"original_source\"")
        options.owner = try decoded(sdkMethodFixture493Value2)
        options.receiver = try decoded("{\"refund_attributes_method\":\"email\"}")
        options.redirect = try decoded("{\"return_url\":\"https://example.com\"}")
        options.sourceOrder = try decoded(sdkMethodFixture493Value3)
        options.statementDescriptor = try decoded("\"statement_descriptor\"")
        options.token = try decoded("\"token\"")
        options.type = try decoded("\"type\"")
        options.usage = try decoded("\"reusable\"")
        let actual = try await client.v1.sources.post(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture493Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postSources decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostSourcesSourcePostSourcesSourceReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.sources.verify.postSourcesSource(
            source: decoded("\"source\""),
            values: decoded("\"sample\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture494Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postSourcesSource decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostSourcesSourceVerifyPostSourcesSourceVerifyReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.sources.verify.postSourcesSource(
            source: decoded("\"source\""),
            values: decoded("[\"values\"]"),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture495Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postSourcesSourceVerify decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostSubscriptionItemsPostSubscriptionItemsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1SubscriptionItemsMethods.PostSubscriptionItemsOptions(subscription: decoded("\"subscription\""))
        options.billingThresholds = try decoded("{\"usage_gte\":30}")
        options.discounts = try decoded(sdkMethodFixture496Value1)
        options.expand = try decoded("[\"expand\"]")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.paymentBehavior = try decoded("\"allow_incomplete\"")
        options.price = try decoded("\"price\"")
        options.priceData = try decoded(sdkMethodFixture496Value2)
        options.prorationBehavior = try decoded("\"always_invoice\"")
        options.prorationDate = try decoded("1")
        options.quantity = try decoded("1")
        options.taxRates = try decoded("[\"tax_rates\"]")
        let actual = try await client.v1.subscriptionItems.post(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture496Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postSubscriptionItems decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostSubscriptionItemsItemPostSubscriptionItemsItemReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1SubscriptionItemsMethods.PostSubscriptionItemsItemOptions(item: decoded("\"item\""))
        options.billingThresholds = try decoded("{\"usage_gte\":30}")
        options.discounts = try decoded(sdkMethodFixture497Value1)
        options.expand = try decoded("[\"expand\"]")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.offSession = try decoded("false")
        options.paymentBehavior = try decoded("\"allow_incomplete\"")
        options.price = try decoded("\"price\"")
        options.priceData = try decoded(sdkMethodFixture497Value2)
        options.prorationBehavior = try decoded("\"always_invoice\"")
        options.prorationDate = try decoded("1")
        options.quantity = try decoded("1")
        options.taxRates = try decoded("[\"tax_rates\"]")
        let actual = try await client.v1.subscriptionItems.postItem(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture497Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postSubscriptionItemsItem decoded a response " +
                "that differs from the mock"
        )
    }
}
