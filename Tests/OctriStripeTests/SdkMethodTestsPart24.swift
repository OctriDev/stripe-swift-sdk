// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriStripe
import XCTest

extension SdkMethodTests {
    func testPostTerminalConnectionTokensPostTerminalConnectionTokensReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.terminal.connectionTokens.postTerminal(
            expand: decoded("[\"expand\"]"),
            location: decoded("\"location\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture517Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTerminalConnectionTokens decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTerminalLocationsPostTerminalLocationsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1TerminalLocationsMethods.PostTerminalLocationsOptions()
        options.address = try decoded(sdkMethodFixture518Value1)
        options.addressKana = try decoded(sdkMethodFixture518Value2)
        options.addressKanji = try decoded(sdkMethodFixture518Value3)
        options.configurationOverrides = try decoded("\"configuration_overrides\"")
        options.displayName = try decoded("\"Example Name\"")
        options.displayNameKana = try decoded("\"Example Name\"")
        options.displayNameKanji = try decoded("\"Example Name\"")
        options.expand = try decoded("[\"expand\"]")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.phone = try decoded("\"+15555550100\"")
        let actual = try await client.v1.terminal.locations.postTerminal(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture518Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTerminalLocations decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTerminalLocationsLocationPostTerminalLocationsLocationReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1TerminalLocationsMethods
            .PostTerminalLocationsLocationOptions(location: decoded("\"location\""))
        options.address = try decoded(sdkMethodFixture519Value1)
        options.addressKana = try decoded(sdkMethodFixture519Value2)
        options.addressKanji = try decoded(sdkMethodFixture519Value3)
        options.configurationOverrides = try decoded("\"configuration_overrides\"")
        options.displayName = try decoded("\"Example Name\"")
        options.displayNameKana = try decoded("\"Example Name\"")
        options.displayNameKanji = try decoded("\"Example Name\"")
        options.expand = try decoded("[\"expand\"]")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.phone = try decoded("\"+15555550100\"")
        let actual = try await client.v1.terminal.locations.postTerminalLocation(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture519Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTerminalLocationsLocation decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTerminalOnboardingLinksPostTerminalOnboardingLinksReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.terminal.onboardingLinks.postTerminal(
            linkOptions: PostTerminalOnboardingLinksRequestBodyLinkOptions(),
            linkType: decoded("\"apple_terms_and_conditions\""),
            expand: decoded("[\"expand\"]"),
            onBehalfOf: decoded("\"on_behalf_of\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture520Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTerminalOnboardingLinks decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTerminalReadersPostTerminalReadersReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.terminal.readers.postTerminal(
            registrationCode: decoded("\"registration_code\""),
            expand: decoded("[\"expand\"]"),
            label: decoded("\"label\""),
            location: decoded("\"location\""),
            metadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture521Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTerminalReaders decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTerminalReadersReaderPostTerminalReadersReaderReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.terminal.readers.cancelAction.postTerminalReadersReader(
            reader: decoded("\"reader\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture522Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTerminalReadersReader decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTerminalReadersReaderCancelActionPostTerminalReadersReadeX30dc641690ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.terminal.readers.cancelAction.postTerminalReadersReader(
            reader: decoded("\"reader\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture523Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTerminalReadersReaderCancelAction decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTerminalReadersReaderCollectInputsPostTerminalReadersReadX0336d95d77ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.terminal.readers.collectInputs.postTerminalReadersReader(
            reader: decoded("\"reader\""),
            inputs: decoded(sdkMethodFixture524Value1),
            expand: decoded("[\"expand\"]"),
            metadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture524Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTerminalReadersReaderCollectInputs decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTerminalReadersReaderCollectPaymentMethodPostTerminalReadX1ce9b40944ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.terminal.readers.collectPaymentMethod.postTerminalReadersReader(
            reader: decoded("\"reader\""),
            paymentIntent: decoded("\"payment_intent\""),
            collectConfig: decoded(sdkMethodFixture525Value1),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture525Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTerminalReadersReaderCollectPaymentMethod decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTerminalReadersReaderConfirmPaymentIntentPostTerminalReadX8a197c3bd3ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.terminal.readers.confirmPaymentIntent.postTerminalReadersReader(
            reader: decoded("\"reader\""),
            paymentIntent: decoded("\"payment_intent\""),
            confirmConfig: decoded("{\"return_url\":\"https://example.com\"}"),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture526Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTerminalReadersReaderConfirmPaymentIntent decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTerminalReadersReaderProcessPaymentIntentPostTerminalReadX5f08760247ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.terminal.readers.processPaymentIntent.postTerminalReadersReader(
            reader: decoded("\"reader\""),
            paymentIntent: decoded("\"payment_intent\""),
            expand: decoded("[\"expand\"]"),
            processConfig: decoded(sdkMethodFixture527Value1)
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture527Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTerminalReadersReaderProcessPaymentIntent decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTerminalReadersReaderProcessSetupIntentPostTerminalReaderX530cc903afReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.terminal.readers.processSetupIntent.postTerminalReadersReader(
            reader: decoded("\"reader\""),
            allowRedisplay: decoded("\"always\""),
            setupIntent: decoded("\"setup_intent\""),
            expand: decoded("[\"expand\"]"),
            processConfig: decoded("{\"enable_customer_cancellation\":false}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture528Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTerminalReadersReaderProcessSetupIntent decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTerminalReadersReaderRefundPaymentPostTerminalReadersReadXb2550c0067ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1TerminalReadersRefundPaymentMethods
            .PostTerminalReadersReaderRefundPaymentOptions(reader: decoded("\"reader\""))
        options.amount = try decoded("100")
        options.charge = try decoded("\"charge\"")
        options.expand = try decoded("[\"expand\"]")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.paymentIntent = try decoded("\"payment_intent\"")
        options.refundApplicationFee = try decoded("false")
        options.refundPaymentConfig = try decoded("{\"enable_customer_cancellation\":false}")
        options.reverseTransfer = try decoded("false")
        let actual = try await client.v1.terminal.readers.refundPayment.postTerminalReadersReader(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture529Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTerminalReadersReaderRefundPayment decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTerminalReadersReaderSetReaderDisplayPostTerminalReadersRXed8b08191cReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.terminal.readers.setReaderDisplay.postTerminalReadersReader(
            reader: decoded("\"reader\""),
            type: decoded("\"cart\""),
            cart: decoded(sdkMethodFixture530Value1),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture530Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTerminalReadersReaderSetReaderDisplay decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTerminalRefundsPostTerminalRefundsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.terminal.refunds.postTerminal(
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture531Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTerminalRefunds decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTestHelpersConfirmationTokensPostTestHelpersConfirmationTokensReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.testHelpers.confirmationTokens.postTestHelpers(
            expand: decoded("[\"expand\"]"),
            paymentMethod: decoded("\"payment_method\""),
            paymentMethodData: decoded(sdkMethodFixture532Value1),
            paymentMethodOptions: decoded(sdkMethodFixture532Value2),
            returnUrl: decoded("\"https://example.com\""),
            setupFutureUsage: decoded("\"off_session\""),
            shipping: decoded(sdkMethodFixture532Value3)
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture532Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTestHelpersConfirmationTokens decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTestHelpersCustomersCustomerFundCashBalancePostTestHelperXea2df9d64dReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.testHelpers.customers.fundCashBalance.postTestHelpersCustomersCustomer(
            customer: decoded("\"customer\""),
            amount: decoded("100"),
            currency: decoded("\"currency\""),
            expand: decoded("[\"expand\"]"),
            reference: decoded("\"reference\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture533Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTestHelpersCustomersCustomerFundCashBalance decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTestHelpersIssuingAuthorizationsPostTestHelpersIssuingAuthorizationsReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1TestHelpersIssuingAuthorizationsMethods
            .PostTestHelpersIssuingAuthorizationsOptions(card: decoded("\"card\""))
        options.amount = try decoded("100")
        options.amountDetails = try decoded("{\"atm_fee\":1,\"cashback_amount\":100}")
        options.authorizationMethod = try decoded("\"chip\"")
        options.currency = try decoded("\"currency\"")
        options.expand = try decoded("[\"expand\"]")
        options.fleet = try decoded(sdkMethodFixture534Value1)
        options.fraudDisputabilityLikelihood = try decoded("\"neutral\"")
        options.fuel = try decoded(sdkMethodFixture534Value2)
        options.isAmountControllable = try decoded("false")
        options.merchantAmount = try decoded("100")
        options.merchantCurrency = try decoded("\"merchant_currency\"")
        options.merchantData = try decoded(sdkMethodFixture534Value3)
        options.networkData = try decoded("{\"acquiring_institution_id\":\"acquiring_institution_-1\"}")
        options.riskAssessment = try decoded(sdkMethodFixture534Value4)
        options.verificationData = try decoded(sdkMethodFixture534Value5)
        options.wallet = try decoded("\"apple_pay\"")
        let actual = try await client.v1.testHelpers.issuing.authorizations.postTestHelpersIssuing(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture534Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTestHelpersIssuingAuthorizations decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTestHelpersIssuingAuthorizationsAuthorizationCapturePostTX4aff045737ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.testHelpers.issuing.authorizations.capture
            .postTestHelpersIssuingAuthorizationsAuthorization(
                authorization: decoded("\"authorization\""),
                captureAmount: decoded("100"),
                closeAuthorization: decoded("false"),
                expand: decoded("[\"expand\"]"),
                purchaseDetails: decoded(sdkMethodFixture535Value1)
            )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture535Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTestHelpersIssuingAuthorizationsAuthorizationCapture decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTestHelpersIssuingAuthorizationsAuthorizationExpirePostTeXbb3c69d1faReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.testHelpers.issuing.authorizations.expire
            .postTestHelpersIssuingAuthorizationsAuthorization(
                authorization: decoded("\"authorization\""),
                expand: decoded("[\"expand\"]")
            )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture536Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTestHelpersIssuingAuthorizationsAuthorizationExpire decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTestHelpersIssuingAuthorizationsAuthorizationFinalizeAmouXb8b98b5086ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.testHelpers.issuing.authorizations.finalizeAmount
            .postTestHelpersIssuingAuthorizationsAuthorization(
                authorization: decoded("\"authorization\""),
                finalAmount: decoded("100"),
                expand: decoded("[\"expand\"]"),
                fleet: decoded(sdkMethodFixture537Value1),
                fuel: decoded(sdkMethodFixture537Value2)
            )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture537Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTestHelpersIssuingAuthorizationsAuthorizationFinalizeAmount decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostTestHelpersIssuingAuthorizationsAuthorizationFraudChallenX065bc410a2ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.testHelpers.issuing.authorizations.fraudChallenges.respond
            .postTestHelpersIssuingAuthorizationsAuthorizationFraudChallenges(
                authorization: decoded("\"authorization\""),
                confirmed: decoded("false"),
                expand: decoded("[\"expand\"]")
            )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture538Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postTestHelpersIssuingAuthorizationsAuthorizationFraudChallengesRespond decoded a response " +
                "that differs from the mock"
        )
    }
}
