// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriStripe
import XCTest

extension SdkMethodTests {
    func testGetTreasuryTransactionsGetTreasuryTransactionsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1TreasuryTransactionsMethods.GetTreasuryTransactionsOptions(financialAccount: decoded("\"financial_account\""))
        options.created = try decoded("{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}")
        options.endingBefore = try decoded("\"ending_before\"")
        options.expand = try decoded("[\"expand\"]")
        options.limit = try decoded("1")
        options.orderBy = try decoded("\"created\"")
        options.startingAfter = try decoded("\"starting_after\"")
        options.status = try decoded("\"open\"")
        options.statusTransitions = try decoded("{\"posted_at\":{\"gt\":1,\"gte\":1,\"lt\":1,\"lte\":1}}")
        let actual = try await client.v1.treasury.transactions.getTreasury(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture294Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTreasuryTransactions decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetTreasuryTransactionsIdGetTreasuryTransactionsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.treasury.transactions.getTreasuryId(
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture295Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getTreasuryTransactionsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetWebhookEndpointsGetWebhookEndpointsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.webhookEndpoints.get(
            endingBefore: decoded("\"ending_before\""),
            expand: decoded("[\"expand\"]"),
            limit: decoded("1"),
            startingAfter: decoded("\"starting_after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture296Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getWebhookEndpoints decoded a response " +
                "that differs from the mock"
        )
    }

    func testGetWebhookEndpointsWebhookEndpointGetWebhookEndpointsWebhookEndpointReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.webhookEndpoints.getWebhookEndpoint(
            webhookEndpoint: decoded("\"webhook_endpoint\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture297Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "getWebhookEndpointsWebhookEndpoint decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostAccountLinksPostAccountLinksReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.accountLinks.post(
            account: decoded("\"account\""),
            type: decoded("\"account_onboarding\""),
            collect: decoded("\"currently_due\""),
            collectionOptions: decoded("{\"fields\":\"currently_due\",\"future_requirements\":\"include\"}"),
            expand: decoded("[\"expand\"]"),
            refreshUrl: decoded("\"https://example.com\""),
            returnUrl: decoded("\"https://example.com\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture298Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postAccountLinks decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostAccountsPostAccountsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = V1AccountsMethods.PostAccountsOptions()
        options.accountToken = try decoded("\"account_token\"")
        options.bankAccount = try decoded(sdkMethodFixture299Value1)
        options.businessProfile = try decoded(sdkMethodFixture299Value2)
        options.businessType = try decoded("\"company\"")
        options.capabilities = try decoded(sdkMethodFixture299Value3)
        options.company = try decoded(sdkMethodFixture299Value4)
        options.controller = try decoded(sdkMethodFixture299Value5)
        options.country = try decoded("\"country\"")
        options.defaultCurrency = try decoded("\"default_currency\"")
        options.documents = try decoded(sdkMethodFixture299Value6)
        options.email = try decoded("\"user@example.com\"")
        options.expand = try decoded("[\"expand\"]")
        options.externalAccount = try decoded("\"external_account\"")
        options.groups = try decoded("{\"payments_pricing\":\"payments_pricing\"}")
        options.individual = try decoded(sdkMethodFixture299Value7)
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.settings = try decoded(sdkMethodFixture299Value8)
        options.tosAcceptance = try decoded(sdkMethodFixture299Value9)
        options.type = try decoded("\"custom\"")
        let actual = try await client.v1.accounts.post(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture299Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postAccounts decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostAccountsAccountPostAccountsAccountReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.accounts.bankAccounts.postAccountsAccount(
            account: decoded("\"account\""),
            bankAccount: decoded("\"sample\""),
            defaultForCurrency: decoded("\"sample\""),
            expand: decoded("[\"expand\"]"),
            externalAccount: decoded("\"external_account\""),
            metadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture300Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postAccountsAccount decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostAccountsAccountBankAccountsPostAccountsAccountBankAccountsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.accounts.bankAccounts.postAccountsAccount(
            account: decoded("\"account\""),
            bankAccount: decoded(sdkMethodFixture301Value1),
            defaultForCurrency: decoded("false"),
            expand: decoded("[\"expand\"]"),
            externalAccount: decoded("\"external_account\""),
            metadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture301Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postAccountsAccountBankAccounts decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostAccountsAccountBankAccountsIdPostAccountsAccountBankAccountsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1AccountsBankAccountsMethods.PostAccountsAccountBankAccountsIdOptions(account: decoded("\"account\""), id: decoded("\"id-1\""))
        options.accountHolderName = try decoded("\"Example Name\"")
        options.accountHolderType = try decoded("\"\"")
        options.accountType = try decoded("\"checking\"")
        options.addressCity = try decoded("\"address_city\"")
        options.addressCountry = try decoded("\"address_country\"")
        options.addressLine1 = try decoded("\"address_line1\"")
        options.addressLine2 = try decoded("\"address_line2\"")
        options.addressState = try decoded("\"address_state\"")
        options.addressZip = try decoded("\"address_zip\"")
        options.defaultForCurrency = try decoded("false")
        options.documents = try decoded("{\"bank_account_ownership_verification\":{\"files\":[\"files\"]}}")
        options.expMonth = try decoded("\"exp_month\"")
        options.expYear = try decoded("\"exp_year\"")
        options.expand = try decoded("[\"expand\"]")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.name = try decoded("\"Example Name\"")
        let actual = try await client.v1.accounts.bankAccounts.postAccountsAccountId(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture302Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postAccountsAccountBankAccountsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostAccountsAccountCapabilitiesCapabilityPostAccountsAccountCX950edbf102ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.accounts.capabilities.postAccountsAccountCapability(
            account: decoded("\"account\""),
            capability: decoded("\"capability\""),
            expand: decoded("[\"expand\"]"),
            requested: decoded("false")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture303Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postAccountsAccountCapabilitiesCapability decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostAccountsAccountExternalAccountsPostAccountsAccountExternalAccountsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.accounts.externalAccounts.postAccountsAccount(
            account: decoded("\"account\""),
            bankAccount: decoded(sdkMethodFixture304Value1),
            defaultForCurrency: decoded("false"),
            expand: decoded("[\"expand\"]"),
            externalAccount: decoded("\"external_account\""),
            metadata: decoded("{\"key\":\"key\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture304Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postAccountsAccountExternalAccounts decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostAccountsAccountExternalAccountsIdPostAccountsAccountExterX26c22b08aeReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1AccountsExternalAccountsMethods.PostAccountsAccountExternalAccountsIdOptions(account: decoded("\"account\""), id: decoded("\"id-1\""))
        options.accountHolderName = try decoded("\"Example Name\"")
        options.accountHolderType = try decoded("\"\"")
        options.accountType = try decoded("\"checking\"")
        options.addressCity = try decoded("\"address_city\"")
        options.addressCountry = try decoded("\"address_country\"")
        options.addressLine1 = try decoded("\"address_line1\"")
        options.addressLine2 = try decoded("\"address_line2\"")
        options.addressState = try decoded("\"address_state\"")
        options.addressZip = try decoded("\"address_zip\"")
        options.defaultForCurrency = try decoded("false")
        options.documents = try decoded("{\"bank_account_ownership_verification\":{\"files\":[\"files\"]}}")
        options.expMonth = try decoded("\"exp_month\"")
        options.expYear = try decoded("\"exp_year\"")
        options.expand = try decoded("[\"expand\"]")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.name = try decoded("\"Example Name\"")
        let actual = try await client.v1.accounts.externalAccounts.postAccountsAccountId(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture305Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postAccountsAccountExternalAccountsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostAccountsAccountLoginLinksPostAccountsAccountLoginLinksReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.accounts.loginLinks.postAccountsAccount(
            account: decoded("\"account\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture306Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postAccountsAccountLoginLinks decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostAccountsAccountPeoplePostAccountsAccountPeopleReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1AccountsPeopleMethods.PostAccountsAccountPeopleOptions(account: decoded("\"account\""))
        options.additionalTosAcceptances = try decoded("{\"account\":{\"date\":1,\"ip\":\"ip\",\"user_agent\":\"user_agent\"}}")
        options.address = try decoded(sdkMethodFixture307Value1)
        options.addressKana = try decoded(sdkMethodFixture307Value2)
        options.addressKanji = try decoded(sdkMethodFixture307Value3)
        options.dob = try decoded("{\"day\":1,\"month\":1,\"year\":1}")
        options.documents = try decoded(sdkMethodFixture307Value4)
        options.email = try decoded("\"user@example.com\"")
        options.expand = try decoded("[\"expand\"]")
        options.firstName = try decoded("\"Example Name\"")
        options.firstNameKana = try decoded("\"Example Name\"")
        options.firstNameKanji = try decoded("\"Example Name\"")
        options.fullNameAliases = try decoded("[\"Example Name\"]")
        options.gender = try decoded("\"gender\"")
        options.idNumber = try decoded("\"id_number\"")
        options.idNumberSecondary = try decoded("\"id_number_secondary\"")
        options.lastName = try decoded("\"Example Name\"")
        options.lastNameKana = try decoded("\"Example Name\"")
        options.lastNameKanji = try decoded("\"Example Name\"")
        options.maidenName = try decoded("\"Example Name\"")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.nationality = try decoded("\"nationality\"")
        options.personToken = try decoded("\"person_token\"")
        options.phone = try decoded("\"+15555550100\"")
        options.politicalExposure = try decoded("\"existing\"")
        options.registeredAddress = try decoded(sdkMethodFixture307Value5)
        options.relationship = try decoded(sdkMethodFixture307Value6)
        options.ssnLast4 = try decoded("\"ssn_last_4\"")
        options.usCfpbData = try decoded(sdkMethodFixture307Value7)
        options.verification = try decoded(sdkMethodFixture307Value8)
        let actual = try await client.v1.accounts.people.postAccountsAccount(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture307Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postAccountsAccountPeople decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostAccountsAccountPeoplePersonPostAccountsAccountPeoplePersonReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1AccountsPeopleMethods.PostAccountsAccountPeoplePersonOptions(account: decoded("\"account\""), person: decoded("\"person\""))
        options.additionalTosAcceptances = try decoded("{\"account\":{\"date\":1,\"ip\":\"ip\",\"user_agent\":\"user_agent\"}}")
        options.address = try decoded(sdkMethodFixture308Value1)
        options.addressKana = try decoded(sdkMethodFixture308Value2)
        options.addressKanji = try decoded(sdkMethodFixture308Value3)
        options.dob = try decoded("{\"day\":1,\"month\":1,\"year\":1}")
        options.documents = try decoded(sdkMethodFixture308Value4)
        options.email = try decoded("\"user@example.com\"")
        options.expand = try decoded("[\"expand\"]")
        options.firstName = try decoded("\"Example Name\"")
        options.firstNameKana = try decoded("\"Example Name\"")
        options.firstNameKanji = try decoded("\"Example Name\"")
        options.fullNameAliases = try decoded("[\"Example Name\"]")
        options.gender = try decoded("\"gender\"")
        options.idNumber = try decoded("\"id_number\"")
        options.idNumberSecondary = try decoded("\"id_number_secondary\"")
        options.lastName = try decoded("\"Example Name\"")
        options.lastNameKana = try decoded("\"Example Name\"")
        options.lastNameKanji = try decoded("\"Example Name\"")
        options.maidenName = try decoded("\"Example Name\"")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.nationality = try decoded("\"nationality\"")
        options.personToken = try decoded("\"person_token\"")
        options.phone = try decoded("\"+15555550100\"")
        options.politicalExposure = try decoded("\"existing\"")
        options.registeredAddress = try decoded(sdkMethodFixture308Value5)
        options.relationship = try decoded(sdkMethodFixture308Value6)
        options.ssnLast4 = try decoded("\"ssn_last_4\"")
        options.usCfpbData = try decoded(sdkMethodFixture308Value7)
        options.verification = try decoded(sdkMethodFixture308Value8)
        let actual = try await client.v1.accounts.people.postAccountsAccountPerson(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture308Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postAccountsAccountPeoplePerson decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostAccountsAccountPersonsPostAccountsAccountPersonsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1AccountsPersonsMethods.PostAccountsAccountPersonsOptions(account: decoded("\"account\""))
        options.additionalTosAcceptances = try decoded("{\"account\":{\"date\":1,\"ip\":\"ip\",\"user_agent\":\"user_agent\"}}")
        options.address = try decoded(sdkMethodFixture309Value1)
        options.addressKana = try decoded(sdkMethodFixture309Value2)
        options.addressKanji = try decoded(sdkMethodFixture309Value3)
        options.dob = try decoded("{\"day\":1,\"month\":1,\"year\":1}")
        options.documents = try decoded(sdkMethodFixture309Value4)
        options.email = try decoded("\"user@example.com\"")
        options.expand = try decoded("[\"expand\"]")
        options.firstName = try decoded("\"Example Name\"")
        options.firstNameKana = try decoded("\"Example Name\"")
        options.firstNameKanji = try decoded("\"Example Name\"")
        options.fullNameAliases = try decoded("[\"Example Name\"]")
        options.gender = try decoded("\"gender\"")
        options.idNumber = try decoded("\"id_number\"")
        options.idNumberSecondary = try decoded("\"id_number_secondary\"")
        options.lastName = try decoded("\"Example Name\"")
        options.lastNameKana = try decoded("\"Example Name\"")
        options.lastNameKanji = try decoded("\"Example Name\"")
        options.maidenName = try decoded("\"Example Name\"")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.nationality = try decoded("\"nationality\"")
        options.personToken = try decoded("\"person_token\"")
        options.phone = try decoded("\"+15555550100\"")
        options.politicalExposure = try decoded("\"existing\"")
        options.registeredAddress = try decoded(sdkMethodFixture309Value5)
        options.relationship = try decoded(sdkMethodFixture309Value6)
        options.ssnLast4 = try decoded("\"ssn_last_4\"")
        options.usCfpbData = try decoded(sdkMethodFixture309Value7)
        options.verification = try decoded(sdkMethodFixture309Value8)
        let actual = try await client.v1.accounts.persons.postAccountsAccount(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture309Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postAccountsAccountPersons decoded a response " +
                "that differs from the mock"
        )
    }

    func testPostAccountsAccountPersonsPersonPostAccountsAccountPersonsPersonReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try V1AccountsPersonsMethods.PostAccountsAccountPersonsPersonOptions(account: decoded("\"account\""), person: decoded("\"person\""))
        options.additionalTosAcceptances = try decoded("{\"account\":{\"date\":1,\"ip\":\"ip\",\"user_agent\":\"user_agent\"}}")
        options.address = try decoded(sdkMethodFixture310Value1)
        options.addressKana = try decoded(sdkMethodFixture310Value2)
        options.addressKanji = try decoded(sdkMethodFixture310Value3)
        options.dob = try decoded("{\"day\":1,\"month\":1,\"year\":1}")
        options.documents = try decoded(sdkMethodFixture310Value4)
        options.email = try decoded("\"user@example.com\"")
        options.expand = try decoded("[\"expand\"]")
        options.firstName = try decoded("\"Example Name\"")
        options.firstNameKana = try decoded("\"Example Name\"")
        options.firstNameKanji = try decoded("\"Example Name\"")
        options.fullNameAliases = try decoded("[\"Example Name\"]")
        options.gender = try decoded("\"gender\"")
        options.idNumber = try decoded("\"id_number\"")
        options.idNumberSecondary = try decoded("\"id_number_secondary\"")
        options.lastName = try decoded("\"Example Name\"")
        options.lastNameKana = try decoded("\"Example Name\"")
        options.lastNameKanji = try decoded("\"Example Name\"")
        options.maidenName = try decoded("\"Example Name\"")
        options.metadata = try decoded("{\"key\":\"key\"}")
        options.nationality = try decoded("\"nationality\"")
        options.personToken = try decoded("\"person_token\"")
        options.phone = try decoded("\"+15555550100\"")
        options.politicalExposure = try decoded("\"existing\"")
        options.registeredAddress = try decoded(sdkMethodFixture310Value5)
        options.relationship = try decoded(sdkMethodFixture310Value6)
        options.ssnLast4 = try decoded("\"ssn_last_4\"")
        options.usCfpbData = try decoded(sdkMethodFixture310Value7)
        options.verification = try decoded(sdkMethodFixture310Value8)
        let actual = try await client.v1.accounts.persons.postAccountsAccountPerson(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture310Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "postAccountsAccountPersonsPerson decoded a response " +
                "that differs from the mock"
        )
    }
}
