// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriStripe
import XCTest

extension SdkMethodTests {
    func testDeleteAccountsAccountDeleteAccountsAccountReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.accounts.deleteAccount(
            account: decoded("\"account\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "deleteAccountsAccount decoded a response " +
                "that differs from the mock"
        )
    }

    func testDeleteAccountsAccountBankAccountsIdDeleteAccountsAccountBankAccountsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.accounts.bankAccounts.deleteAccountsAccountId(
            account: decoded("\"account\""),
            id: decoded("\"id-1\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture2Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "deleteAccountsAccountBankAccountsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testDeleteAccountsAccountExternalAccountsIdDeleteAccountsAccountEX546ea69430ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.accounts.externalAccounts.deleteAccountsAccountId(
            account: decoded("\"account\""),
            id: decoded("\"id-1\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture3Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "deleteAccountsAccountExternalAccountsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testDeleteAccountsAccountPeoplePersonDeleteAccountsAccountPeoplePersonReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.accounts.people.deleteAccountsAccountPerson(
            account: decoded("\"account\""),
            person: decoded("\"person\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture4Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "deleteAccountsAccountPeoplePerson decoded a response " +
                "that differs from the mock"
        )
    }

    func testDeleteAccountsAccountPersonsPersonDeleteAccountsAccountPersonsPersonReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.accounts.persons.deleteAccountsAccountPerson(
            account: decoded("\"account\""),
            person: decoded("\"person\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture5Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "deleteAccountsAccountPersonsPerson decoded a response " +
                "that differs from the mock"
        )
    }

    func testDeleteApplePayDomainsDomainDeleteApplePayDomainsDomainReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.applePay.domains.deleteApplePayDomain(
            domain: decoded("\"domain\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture6Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "deleteApplePayDomainsDomain decoded a response " +
                "that differs from the mock"
        )
    }

    func testDeleteCouponsCouponDeleteCouponsCouponReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.coupons.deleteCoupon(
            coupon: decoded("\"coupon\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture7Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "deleteCouponsCoupon decoded a response " +
                "that differs from the mock"
        )
    }

    func testDeleteCustomersCustomerDeleteCustomersCustomerReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.discount.deleteCustomersCustomer(
            customer: decoded("\"customer\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture8Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "deleteCustomersCustomer decoded a response " +
                "that differs from the mock"
        )
    }

    func testDeleteCustomersCustomerBankAccountsIdDeleteCustomersCustomerBX0f9aadf7ceReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.bankAccounts.deleteCustomersCustomerId(
            customer: decoded("\"customer\""),
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture9Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "deleteCustomersCustomerBankAccountsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testDeleteCustomersCustomerCardsIdDeleteCustomersCustomerCardsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.cards.deleteCustomersCustomerId(
            customer: decoded("\"customer\""),
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture10Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "deleteCustomersCustomerCardsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testDeleteCustomersCustomerDiscountDeleteCustomersCustomerDiscountReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.discount.deleteCustomersCustomer(
            customer: decoded("\"customer\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture11Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "deleteCustomersCustomerDiscount decoded a response " +
                "that differs from the mock"
        )
    }

    func testDeleteCustomersCustomerSourcesIdDeleteCustomersCustomerSourcesIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.sources.deleteCustomersCustomerId(
            customer: decoded("\"customer\""),
            id: decoded("\"id-1\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture12Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "deleteCustomersCustomerSourcesId decoded a response " +
                "that differs from the mock"
        )
    }

    func testDeleteCustomersCustomerSubscriptionsSubscriptionExposedIdDeleX21f3f9ce0eReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.subscriptions.discount.deleteCustomersCustomerSubscriptionsSubscriptionExposedId(
            customer: decoded("\"customer\""),
            subscriptionExposedId: decoded("\"subscription_exposed_-1\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture13Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "deleteCustomersCustomerSubscriptionsSubscriptionExposedId decoded a response " +
                "that differs from the mock"
        )
    }

    func testDeleteCustomersCustomerSubscriptionsSubscriptionExposedIdDiscXd110b2f829ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.subscriptions.discount.deleteCustomersCustomerSubscriptionsSubscriptionExposedId(
            customer: decoded("\"customer\""),
            subscriptionExposedId: decoded("\"subscription_exposed_-1\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture14Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "deleteCustomersCustomerSubscriptionsSubscriptionExposedIdDiscount decoded a response " +
                "that differs from the mock"
        )
    }

    func testDeleteCustomersCustomerTaxIdsIdDeleteCustomersCustomerTaxIdsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.customers.taxIds.deleteCustomersCustomerId(
            customer: decoded("\"customer\""),
            id: decoded("\"id-1\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture15Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "deleteCustomersCustomerTaxIdsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testDeleteEphemeralKeysKeyDeleteEphemeralKeysKeyReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.ephemeralKeys.deleteKey(
            key: decoded("\"key\""),
            expand: decoded("[\"expand\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture16Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "deleteEphemeralKeysKey decoded a response " +
                "that differs from the mock"
        )
    }

    func testDeleteInvoiceitemsInvoiceitemDeleteInvoiceitemsInvoiceitemReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.invoiceitems.deleteInvoiceitem(
            invoiceitem: decoded("\"invoiceitem\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture17Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "deleteInvoiceitemsInvoiceitem decoded a response " +
                "that differs from the mock"
        )
    }

    func testDeleteInvoicesInvoiceDeleteInvoicesInvoiceReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.invoices.deleteInvoice(
            invoice: decoded("\"invoice\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture18Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "deleteInvoicesInvoice decoded a response " +
                "that differs from the mock"
        )
    }

    func testDeletePlansPlanDeletePlansPlanReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.plans.deletePlan(
            plan: decoded("\"plan\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture19Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "deletePlansPlan decoded a response " +
                "that differs from the mock"
        )
    }

    func testDeleteProductsIdDeleteProductsIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.products.deleteId(
            id: decoded("\"id-1\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture20Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "deleteProductsId decoded a response " +
                "that differs from the mock"
        )
    }

    func testDeleteProductsProductFeaturesIdDeleteProductsProductFeaturesIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.products.features.deleteProductsProductId(
            id: decoded("\"id-1\""),
            product: decoded("\"product\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture21Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "deleteProductsProductFeaturesId decoded a response " +
                "that differs from the mock"
        )
    }

    func testDeleteRadarValueListItemsItemDeleteRadarValueListItemsItemReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.radar.valueListItems.deleteRadarItem(
            item: decoded("\"item\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture22Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "deleteRadarValueListItemsItem decoded a response " +
                "that differs from the mock"
        )
    }

    func testDeleteRadarValueListsValueListDeleteRadarValueListsValueListReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.radar.valueLists.deleteRadarValueList(
            valueList: decoded("\"value_list\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture23Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "deleteRadarValueListsValueList decoded a response " +
                "that differs from the mock"
        )
    }

    func testDeleteSubscriptionItemsItemDeleteSubscriptionItemsItemReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.subscriptionItems.deleteItem(
            item: decoded("\"item\""),
            clearUsage: decoded("false"),
            paymentBehavior: decoded("\"allow_incomplete\""),
            prorationBehavior: decoded("\"always_invoice\""),
            prorationDate: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture24Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "deleteSubscriptionItemsItem decoded a response " +
                "that differs from the mock"
        )
    }

    func testDeleteSubscriptionsSubscriptionExposedIdDeleteSubscriptionsSuXe9dad48ebfReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.subscriptions.discount.deleteSubscriptionsSubscriptionExposedId(
            subscriptionExposedId: decoded("\"subscription_exposed_-1\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture25Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "deleteSubscriptionsSubscriptionExposedId decoded a response " +
                "that differs from the mock"
        )
    }

    func testDeleteSubscriptionsSubscriptionExposedIdDiscountDeleteSubscriXf5722f3461ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.v1.subscriptions.discount.deleteSubscriptionsSubscriptionExposedId(
            subscriptionExposedId: decoded("\"subscription_exposed_-1\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture26Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "deleteSubscriptionsSubscriptionExposedIdDiscount decoded a response " +
                "that differs from the mock"
        )
    }
}
