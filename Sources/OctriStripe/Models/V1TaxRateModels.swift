// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

/// V1TaxRate domain models
/// Tax rates can be applied to invoices, subscriptions and Checkout Sessions to collect tax. Related guide: Tax
/// rates
public struct TaxRate: Codable {
    /// Defaults to `true`. When set to `false`, this tax rate cannot be used with new applications or Checkout
    /// Sessions, but will still work for subscriptions and invoices that already have it set.
    public var active: Bool
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// The display name of the tax rates as it will appear to your customer on their receipt email, PDF, and the
    /// hosted invoice page.
    public var displayName: String
    /// Unique identifier for the object.
    public var id: String
    /// This specifies if the tax rate is inclusive or exclusive.
    public var inclusive: Bool
    /// If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is
    /// `false`.
    public var livemode: Bool
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: TaxRateObject
    /// Tax rate percentage out of 100. For tax calculations with automatic_tax[enabled]=true, this percentage
    /// includes the statutory tax rate of non-taxable jurisdictions.
    public var percentage: Double
    /// Two-letter country code (ISO 3166-1 alpha-2).
    public var country: String?
    /// An arbitrary string attached to the tax rate for your internal use only. It will not be visible to your
    /// customers.
    public var description: String?
    /// Actual/effective tax rate percentage out of 100. For tax calculations with automatic_tax[enabled]=true, this
    /// percentage reflects the rate actually used to calculate tax based on the product's taxability and whether
    /// the user is registered to collect taxes in the corresponding jurisdiction.
    public var effectivePercentage: Double?
    /// The amount of the tax rate when the `rate_type` is `flat_amount`. Tax rates with `rate_type` `percentage`
    /// can vary based on the transaction, resulting in this field being `null`. This field exposes the amount and
    /// currency of the flat tax rate.
    public var flatAmount: TaxRateFlatAmountX8c668128?
    /// The jurisdiction for the tax rate. You can use this label field for tax reporting purposes. It also appears
    /// on your customer’s invoice.
    public var jurisdiction: String?
    /// The level of the jurisdiction that imposes this tax rate. Will be `null` for manually defined tax rates.
    public var jurisdictionLevel: TaxRateJurisdictionLevel?
    /// Set of key-value pairs that you can attach to an object. This can be useful for storing additional
    /// information about the object in a structured format.
    public var metadata: [String: String]?
    /// Indicates the type of tax rate applied to the taxable amount. This value can be `null` when no tax applies
    /// to the location. This field is only present for TaxRates created by Stripe Tax.
    public var rateType: TaxRateRateType?
    /// ISO 3166-2 subdivision code, without country prefix. For example, "NY" for New York, United States.
    public var state: String?
    /// The high-level tax type, such as `vat` or `sales_tax`.
    public var taxType: TaxRateTaxType?

    enum CodingKeys: String, CodingKey {
        case active
        case created
        case displayName = "display_name"
        case id
        case inclusive
        case livemode
        case object
        case percentage
        case country
        case description
        case effectivePercentage = "effective_percentage"
        case flatAmount = "flat_amount"
        case jurisdiction
        case jurisdictionLevel = "jurisdiction_level"
        case metadata
        case rateType = "rate_type"
        case state
        case taxType = "tax_type"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension TaxRate {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        active = try container.sdkDecodeRequired(.active)
        created = try container.sdkDecodeRequired(.created)
        displayName = try container.sdkDecodeRequired(.displayName)
        id = try container.sdkDecodeRequired(.id)
        inclusive = try container.sdkDecodeRequired(.inclusive)
        livemode = try container.sdkDecodeRequired(.livemode)
        object = try container.sdkDecodeRequired(.object)
        percentage = try container.sdkDecodeRequired(.percentage)
        country = try container.sdkDecodeIfPresent(.country)
        description = try container.sdkDecodeIfPresent(.description)
        effectivePercentage = try container.sdkDecodeIfPresent(.effectivePercentage)
        flatAmount = try container.sdkDecodeIfPresent(.flatAmount)
        jurisdiction = try container.sdkDecodeIfPresent(.jurisdiction)
        jurisdictionLevel = try container.sdkDecodeIfPresent(.jurisdictionLevel)
        metadata = try container.sdkDecodeIfPresent(.metadata)
        rateType = try container.sdkDecodeIfPresent(.rateType)
        state = try container.sdkDecodeIfPresent(.state)
        taxType = try container.sdkDecodeIfPresent(.taxType)
        try validateLength("display_name", displayName, min: nil, max: 5000)
        try validateLength("id", id, min: nil, max: 5000)
        if let value = country {
            try validateLength("country", value, min: nil, max: 5000)
        }
        if let value = description {
            try validateLength("description", value, min: nil, max: 5000)
        }
        if let value = jurisdiction {
            try validateLength("jurisdiction", value, min: nil, max: 5000)
        }
        if let value = state {
            try validateLength("state", value, min: nil, max: 5000)
        }
    }
}

public extension TaxRate {
    init(
        active: Bool,
        created: Int,
        displayName: String,
        id: String,
        inclusive: Bool,
        livemode: Bool,
        object: TaxRateObject,
        percentage: Double,
        country: String? = nil,
        description: String? = nil,
        effectivePercentage: Double? = nil,
        flatAmount: TaxRateFlatAmountX8c668128? = nil,
        jurisdiction: String? = nil,
        jurisdictionLevel: TaxRateJurisdictionLevel? = nil,
        metadata: [String: String]? = nil,
        rateType: TaxRateRateType? = nil,
        state: String? = nil,
        taxType: TaxRateTaxType? = nil
    ) throws {
        (self.active, self.created) = (active, created)
        (self.displayName, self.id) = (displayName, id)
        (self.inclusive, self.livemode) = (inclusive, livemode)
        (self.object, self.percentage) = (object, percentage)
        (self.country, self.description) = (country, description)
        (self.effectivePercentage, self.flatAmount) = (effectivePercentage, flatAmount)
        (self.jurisdiction, self.jurisdictionLevel) = (jurisdiction, jurisdictionLevel)
        (self.metadata, self.rateType) = (metadata, rateType)
        (self.state, self.taxType) = (state, taxType)
        try validateLength("display_name", self.displayName, min: nil, max: 5000)
        try validateLength("id", self.id, min: nil, max: 5000)
        if let value = self.country {
            try validateLength("country", value, min: nil, max: 5000)
        }
        if let value = self.description {
            try validateLength("description", value, min: nil, max: 5000)
        }
        if let value = self.jurisdiction {
            try validateLength("jurisdiction", value, min: nil, max: 5000)
        }
        if let value = self.state {
            try validateLength("state", value, min: nil, max: 5000)
        }
    }
}

public enum TaxRateFlatAmountX8c668128 {
    case taxRateFlatAmount(TaxRateFlatAmount)
}

extension TaxRateFlatAmountX8c668128: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for TaxRateFlatAmountX8c668128"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(TaxRateFlatAmount.self) {
            return .taxRateFlatAmount(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .taxRateFlatAmount(value): try container.encode(value); return true
        }
    }
}

/// The amount of the tax rate when the `rate_type`` is `flat_amount`. Tax rates with `rate_type` `percentage` can
/// vary based on the transaction, resulting in this field being `null`. This field exposes the amount and currency
/// of the flat tax rate.
public struct TaxRateFlatAmount: Codable {
    /// Amount of the tax when the `rate_type` is `flat_amount`. This positive integer represents how much to charge
    /// in the smallest currency unit (e.g., 100 cents to charge $1.00 or 100 to charge ¥100, a zero-decimal
    /// currency). The amount value supports up to eight digits (e.g., a value of 99999999 for a USD charge of
    /// $999,999.99).
    public var amount: Int
    /// Three-letter ISO currency code, in lowercase.
    public var currency: String

    enum CodingKeys: String, CodingKey {
        case amount
        case currency
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension TaxRateFlatAmount {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.amount) else {
            throw SdkValidationError(
                field: "amount",
                code: "required",
                message: "Validation failed for 'amount': value is required"
            )
        }
        guard container.contains(.currency) else {
            throw SdkValidationError(
                field: "currency",
                code: "required",
                message: "Validation failed for 'currency': value is required"
            )
        }
        amount = try container.sdkDecodeRequired(.amount)
        currency = try container.sdkDecodeRequired(.currency)
        try validateLength("currency", currency, min: nil, max: 5000)
    }
}

public extension TaxRateFlatAmount {
    init(amount: Int, currency: String) throws {
        (self.amount, self.currency) = (amount, currency)
        try validateLength("currency", self.currency, min: nil, max: 5000)
    }
}

/// The high-level tax type, such as `vat` or `sales_tax`.
public struct TaxRateTaxType: RawRepresentable, Hashable, Codable, Sendable, SdkWireConvertible {
    public let rawValue: String

    public init(rawValue: String) {
        self.rawValue = rawValue
    }

    public static let amusementTax = TaxRateTaxType(rawValue: "amusement_tax")
    public static let communicationsTax = TaxRateTaxType(rawValue: "communications_tax")
    public static let gst = TaxRateTaxType(rawValue: "gst")
    public static let hst = TaxRateTaxType(rawValue: "hst")
    public static let igst = TaxRateTaxType(rawValue: "igst")
    public static let jct = TaxRateTaxType(rawValue: "jct")
    public static let leaseTax = TaxRateTaxType(rawValue: "lease_tax")
    public static let massTransitParkingTax = TaxRateTaxType(rawValue: "mass_transit_parking_tax")
    public static let parkingTax = TaxRateTaxType(rawValue: "parking_tax")
    public static let pst = TaxRateTaxType(rawValue: "pst")
    public static let qst = TaxRateTaxType(rawValue: "qst")
    public static let retailDeliveryFee = TaxRateTaxType(rawValue: "retail_delivery_fee")
    public static let rst = TaxRateTaxType(rawValue: "rst")
    public static let salesTax = TaxRateTaxType(rawValue: "sales_tax")
    public static let serviceTax = TaxRateTaxType(rawValue: "service_tax")
    public static let vat = TaxRateTaxType(rawValue: "vat")

    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        try self.init(rawValue: container.decode(String.self))
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        try container.encode(rawValue)
    }
}

/// Indicates the type of tax rate applied to the taxable amount. This value can be `null` when no tax applies
/// to the location. This field is only present for TaxRates created by Stripe Tax.
public struct TaxRateRateType: RawRepresentable, Hashable, Codable, Sendable, SdkWireConvertible {
    public let rawValue: String

    public init(rawValue: String) {
        self.rawValue = rawValue
    }

    public static let flatAmount = TaxRateRateType(rawValue: "flat_amount")
    public static let percentage = TaxRateRateType(rawValue: "percentage")

    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        try self.init(rawValue: container.decode(String.self))
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        try container.encode(rawValue)
    }
}

/// The level of the jurisdiction that imposes this tax rate. Will be `null` for manually defined tax rates.
public struct TaxRateJurisdictionLevel: RawRepresentable, Hashable, Codable, Sendable, SdkWireConvertible {
    public let rawValue: String

    public init(rawValue: String) {
        self.rawValue = rawValue
    }

    public static let city = TaxRateJurisdictionLevel(rawValue: "city")
    public static let country = TaxRateJurisdictionLevel(rawValue: "country")
    public static let county = TaxRateJurisdictionLevel(rawValue: "county")
    public static let district = TaxRateJurisdictionLevel(rawValue: "district")
    public static let multiple = TaxRateJurisdictionLevel(rawValue: "multiple")
    public static let state = TaxRateJurisdictionLevel(rawValue: "state")

    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        try self.init(rawValue: container.decode(String.self))
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        try container.encode(rawValue)
    }
}

/// String representing the object's type. Objects of the same type share the same value.
public struct TaxRateObject: RawRepresentable, Hashable, Codable, Sendable, SdkWireConvertible {
    public let rawValue: String

    public init(rawValue: String) {
        self.rawValue = rawValue
    }

    public static let taxRate = TaxRateObject(rawValue: "tax_rate")

    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        try self.init(rawValue: container.decode(String.self))
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        try container.encode(rawValue)
    }
}
