// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

/// V1 domain models
extension SetupAttemptPaymentMethodDetailsIdealGeneratedSepaDebitMandate: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for SetupAttemptPaymentMethodDetailsIdealGeneratedSepaDebitMandate"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container.decode(Mandate.self) {
            return .mandate(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .mandate(value): try container.encode(value); return true
        }
    }
}

/// Typed representation of the `SetupAttemptPaymentMethodDetailsSofort` API schema.
public struct SetupAttemptPaymentMethodDetailsSofort: Codable {
    /// Bank code of bank associated with the bank account.
    public var bankCode: String?
    /// Name of the bank associated with the bank account.
    public var bankName: String?
    /// Bank Identifier Code of the bank associated with the bank account.
    public var bic: String?
    /// The ID of the SEPA Direct Debit PaymentMethod which was generated by this SetupAttempt.
    public var generatedSepaDebit: SetupAttemptPaymentMethodDetailsSofortGeneratedSepaDebit?
    /// The mandate for the SEPA Direct Debit PaymentMethod which was generated by this SetupAttempt.
    public var generatedSepaDebitMandate: SetupAttemptPaymentMethodDetailsSofortGeneratedSepaDebitMandate?
    /// Last four characters of the IBAN.
    public var ibanLast4: String?
    /// Preferred language of the Sofort authorization page that the customer is redirected to. Can be one of `en`,
    /// `de`, `fr`, or `nl`
    public var preferredLanguage: SetupAttemptPaymentMethodDetailsSofortPreferredLanguage?
    /// Owner's verified full name. Values are verified or provided by Sofort directly (if supported) at the time of
    /// authorization or settlement. They cannot be set or mutated.
    public var verifiedName: String?

    enum CodingKeys: String, CodingKey {
        case bankCode = "bank_code"
        case bankName = "bank_name"
        case bic
        case generatedSepaDebit = "generated_sepa_debit"
        case generatedSepaDebitMandate = "generated_sepa_debit_mandate"
        case ibanLast4 = "iban_last4"
        case preferredLanguage = "preferred_language"
        case verifiedName = "verified_name"
    }

    init() {
        (bankCode, bankName, bic, generatedSepaDebit, generatedSepaDebitMandate) = (nil, nil, nil, nil, nil)
        (ibanLast4, preferredLanguage, verifiedName) = (nil, nil, nil)
    }
}

public extension SetupAttemptPaymentMethodDetailsSofort {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        bankCode = try container.sdkDecodeIfPresent(.bankCode)
        bankName = try container.sdkDecodeIfPresent(.bankName)
        bic = try container.sdkDecodeIfPresent(.bic)
        generatedSepaDebit = try container.sdkDecodeIfPresent(.generatedSepaDebit)
        generatedSepaDebitMandate = try container.sdkDecodeIfPresent(.generatedSepaDebitMandate)
        ibanLast4 = try container.sdkDecodeIfPresent(.ibanLast4)
        preferredLanguage = try container.sdkDecodeIfPresent(.preferredLanguage)
        verifiedName = try container.sdkDecodeIfPresent(.verifiedName)
        if let value = bankCode {
            try validateLength("bank_code", value, min: nil, max: 5000)
        }
        if let value = bankName {
            try validateLength("bank_name", value, min: nil, max: 5000)
        }
        if let value = bic {
            try validateLength("bic", value, min: nil, max: 5000)
        }
        if let value = ibanLast4 {
            try validateLength("iban_last4", value, min: nil, max: 5000)
        }
        if let value = verifiedName {
            try validateLength("verified_name", value, min: nil, max: 5000)
        }
    }
}

public extension SetupAttemptPaymentMethodDetailsSofort {
    init(
        bankCode: String? = nil,
        bankName: String? = nil,
        bic: String? = nil,
        generatedSepaDebit: SetupAttemptPaymentMethodDetailsSofortGeneratedSepaDebit? = nil,
        generatedSepaDebitMandate: SetupAttemptPaymentMethodDetailsSofortGeneratedSepaDebitMandate? = nil,
        ibanLast4: String? = nil,
        preferredLanguage: SetupAttemptPaymentMethodDetailsSofortPreferredLanguage? = nil,
        verifiedName: String? = nil
    ) throws {
        self.init()
        (self.bankCode, self.bankName) = (bankCode, bankName)
        (self.bic, self.generatedSepaDebit) = (bic, generatedSepaDebit)
        (self.generatedSepaDebitMandate, self.ibanLast4) = (generatedSepaDebitMandate, ibanLast4)
        (self.preferredLanguage, self.verifiedName) = (preferredLanguage, verifiedName)
        if let value = self.bankCode {
            try validateLength("bank_code", value, min: nil, max: 5000)
        }
        if let value = self.bankName {
            try validateLength("bank_name", value, min: nil, max: 5000)
        }
        if let value = self.bic {
            try validateLength("bic", value, min: nil, max: 5000)
        }
        if let value = self.ibanLast4 {
            try validateLength("iban_last4", value, min: nil, max: 5000)
        }
        if let value = self.verifiedName {
            try validateLength("verified_name", value, min: nil, max: 5000)
        }
    }
}

public indirect enum SetupAttemptPaymentMethodDetailsSofortGeneratedSepaDebit {
    case stringValue(String)
    case paymentMethod(PaymentMethod)
}

extension SetupAttemptPaymentMethodDetailsSofortGeneratedSepaDebit: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for SetupAttemptPaymentMethodDetailsSofortGeneratedSepaDebit"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container.decode(PaymentMethod.self) {
            return .paymentMethod(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .paymentMethod(value): try container.encode(value); return true
        }
    }
}

public indirect enum SetupAttemptPaymentMethodDetailsSofortGeneratedSepaDebitMandate {
    case stringValue(String)
    case mandate(Mandate)
}

extension SetupAttemptPaymentMethodDetailsSofortGeneratedSepaDebitMandate: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for SetupAttemptPaymentMethodDetailsSofortGeneratedSepaDebitMandate"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container.decode(Mandate.self) {
            return .mandate(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .mandate(value): try container.encode(value); return true
        }
    }
}

/// A SetupIntent guides you through the process of setting up and saving a customer's payment credentials for
/// future payments. For example, you can use a SetupIntent to set up and save your customer's card without
/// immediately collecting a payment. Later, you can use PaymentIntents to drive the payment flow. Create a
/// SetupIntent when you're ready to collect your customer's payment credentials. Don't maintain long-lived,
/// unconfirmed SetupIntents because they might not be valid.…
public struct SetupIntent: Codable {
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// Unique identifier for the object.
    public var id: String
    /// If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is
    /// `false`.
    public var livemode: Bool
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: SetupIntentObject
    /// The list of payment method types (e.g. card) that this SetupIntent is allowed to set up. A list of valid
    /// payment method types can be found here.
    public var paymentMethodTypes: [String]
    /// Status of this SetupIntent, one of `requires_payment_method`, `requires_confirmation`, `requires_action`,
    /// `processing`, `canceled`, or `succeeded`.
    public var status: SetupIntentStatus
    /// Indicates how the payment method is intended to be used in the future. Use `on_session` if you intend to
    /// only reuse the payment method when the customer is in your checkout flow. Use `off_session` if your customer
    /// may or may not be in your checkout flow. If not provided, this value defaults to `off_session`.
    public var usage: String
    /// The list of payment method types to allow for this SetupIntent. Stripe will only use methods in this list
    /// when determining the payment methods to offer.
    public var allowedPaymentMethodTypes: [SetupIntentAllowedPaymentMethodTypesItem]?
    /// ID of the Connect application that created the SetupIntent.
    public var application: SetupIntentApplication?
    /// If present, the SetupIntent's payment method will be attached to the in-context Stripe Account. It can only
    /// be used for this Stripe Account’s own money movement flows like InboundTransfer and OutboundTransfers. It
    /// cannot be set to true when setting up a PaymentMethod for a Customer, and defaults to false when attaching a
    /// PaymentMethod to a Customer.
    public var attachToSelf: Bool?
    /// Settings for dynamic payment methods compatible with this Setup Intent
    public var automaticPaymentMethods: SetupIntentAutomaticPaymentMethods?
    /// Reason for cancellation of this SetupIntent, one of `abandoned`, `requested_by_customer`, or `duplicate`.
    public var cancellationReason: SetupIntentCancellationReason?
    /// The client secret of this SetupIntent. Used for client-side retrieval using a publishable key. The client
    /// secret can be used to complete payment setup from your frontend. It should not be stored, logged, or exposed
    /// to anyone other than the customer. Make sure that you have TLS enabled on any page that includes the client
    /// secret.
    public var clientSecret: String?
    /// ID of the Customer this SetupIntent belongs to, if one exists. If present, the SetupIntent's payment method
    /// will be attached to the Customer on successful setup. Payment methods attached to other Customers cannot be
    /// used with this SetupIntent.
    public var customer: SetupIntentCustomer?
    /// ID of the Account this SetupIntent belongs to, if one exists. If present, the SetupIntent's payment method
    /// will be attached to the Account on successful setup. Payment methods attached to other Accounts cannot be
    /// used with this SetupIntent.
    public var customerAccount: String?
    /// An arbitrary string attached to the object. Often useful for displaying to users.
    public var description: String?
    /// Payment method types that are excluded from this SetupIntent.
    public var excludedPaymentMethodTypes: [SetupIntentExcludedPaymentMethodTypesItem]?
    /// Indicates the directions of money movement for which this payment method is intended to be used. Include
    /// `inbound` if you intend to use the payment method as the origin to pull funds from. Include `outbound` if
    /// you intend to use the payment method as the destination to send funds to. You can include both if you intend
    /// to use the payment method for both purposes.
    public var flowDirections: [SetupIntentFlowDirectionsItem]?
    /// The error encountered in the previous SetupIntent confirmation.
    public var lastSetupError: SetupIntentLastSetupError?
    /// The most recent SetupAttempt for this SetupIntent.
    public var latestAttempt: SetupIntentLatestAttempt?
    /// Optional API value serialized in the `managed_payments` wire field.
    public var managedPayments: SetupIntentManagedPayments?
    /// ID of the multi use Mandate generated by the SetupIntent.
    public var mandate: SetupIntentMandate?
    /// Set of key-value pairs that you can attach to an object. This can be useful for storing additional
    /// information about the object in a structured format.
    public var metadata: [String: String]?
    /// If present, this property tells you what actions you need to take in order for your customer to continue
    /// payment setup.
    public var nextAction: SetupIntentNextActionXe0e117ee?
    /// The account (if any) for which the setup is intended.
    public var onBehalfOf: SetupIntentOnBehalfOf?
    /// ID of the payment method used with this SetupIntent. If the payment method is `card_present` and isn't a
    /// digital wallet, then the generated_card associated with the `latest_attempt` is attached to the Customer
    /// instead.
    public var paymentMethod: SetupIntentPaymentMethod?
    /// Information about the payment method configuration used for this Setup Intent.
    public var paymentMethodConfigurationDetails: SetupIntentPaymentMethodConfigurationDetails?
    /// Payment method-specific configuration for this SetupIntent.
    public var paymentMethodOptions: SetupIntentPaymentMethodOptionsX9e24e185?
    /// ID of the single_use Mandate generated by the SetupIntent.
    public var singleUseMandate: SetupIntentSingleUseMandate?

    enum CodingKeys: String, CodingKey {
        case created
        case id
        case livemode
        case object
        case paymentMethodTypes = "payment_method_types"
        case status
        case usage
        case allowedPaymentMethodTypes = "allowed_payment_method_types"
        case application
        case attachToSelf = "attach_to_self"
        case automaticPaymentMethods = "automatic_payment_methods"
        case cancellationReason = "cancellation_reason"
        case clientSecret = "client_secret"
        case customer
        case customerAccount = "customer_account"
        case description
        case excludedPaymentMethodTypes = "excluded_payment_method_types"
        case flowDirections = "flow_directions"
        case lastSetupError = "last_setup_error"
        case latestAttempt = "latest_attempt"
        case managedPayments = "managed_payments"
        case mandate
        case metadata
        case nextAction = "next_action"
        case onBehalfOf = "on_behalf_of"
        case paymentMethod = "payment_method"
        case paymentMethodConfigurationDetails = "payment_method_configuration_details"
        case paymentMethodOptions = "payment_method_options"
        case singleUseMandate = "single_use_mandate"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension SetupIntent {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.created) else {
            throw SdkValidationError(
                field: "created",
                code: "required",
                message: "Validation failed for 'created': value is required"
            )
        }
        guard container.contains(.id) else {
            throw SdkValidationError(
                field: "id",
                code: "required",
                message: "Validation failed for 'id': value is required"
            )
        }
        guard container.contains(.livemode) else {
            throw SdkValidationError(
                field: "livemode",
                code: "required",
                message: "Validation failed for 'livemode': value is required"
            )
        }
        guard container.contains(.object) else {
            throw SdkValidationError(
                field: "object",
                code: "required",
                message: "Validation failed for 'object': value is required"
            )
        }
        guard container.contains(.paymentMethodTypes) else {
            throw SdkValidationError(
                field: "payment_method_types",
                code: "required",
                message: "Validation failed for 'payment_method_types': value is required"
            )
        }
        guard container.contains(.status) else {
            throw SdkValidationError(
                field: "status",
                code: "required",
                message: "Validation failed for 'status': value is required"
            )
        }
        guard container.contains(.usage) else {
            throw SdkValidationError(
                field: "usage",
                code: "required",
                message: "Validation failed for 'usage': value is required"
            )
        }
        created = try container.sdkDecodeRequired(.created)
        id = try container.sdkDecodeRequired(.id)
        livemode = try container.sdkDecodeRequired(.livemode)
        object = try container.sdkDecodeRequired(.object)
        paymentMethodTypes = try container.sdkDecodeRequired(.paymentMethodTypes)
        status = try container.sdkDecodeRequired(.status)
        usage = try container.sdkDecodeRequired(.usage)
        allowedPaymentMethodTypes = try container.sdkDecodeIfPresent(.allowedPaymentMethodTypes)
        application = try container.sdkDecodeIfPresent(.application)
        attachToSelf = try container.sdkDecodeIfPresent(.attachToSelf)
        automaticPaymentMethods = try container.sdkDecodeIfPresent(.automaticPaymentMethods)
        cancellationReason = try container.sdkDecodeIfPresent(.cancellationReason)
        clientSecret = try container.sdkDecodeIfPresent(.clientSecret)
        customer = try container.sdkDecodeIfPresent(.customer)
        customerAccount = try container.sdkDecodeIfPresent(.customerAccount)
        description = try container.sdkDecodeIfPresent(.description)
        excludedPaymentMethodTypes = try container.sdkDecodeIfPresent(.excludedPaymentMethodTypes)
        flowDirections = try container.sdkDecodeIfPresent(.flowDirections)
        lastSetupError = try container.sdkDecodeIfPresent(.lastSetupError)
        latestAttempt = try container.sdkDecodeIfPresent(.latestAttempt)
        managedPayments = try container.sdkDecodeIfPresent(.managedPayments)
        mandate = try container.sdkDecodeIfPresent(.mandate)
        metadata = try container.sdkDecodeIfPresent(.metadata)
        nextAction = try container.sdkDecodeIfPresent(.nextAction)
        onBehalfOf = try container.sdkDecodeIfPresent(.onBehalfOf)
        paymentMethod = try container.sdkDecodeIfPresent(.paymentMethod)
        paymentMethodConfigurationDetails = try container.sdkDecodeIfPresent(.paymentMethodConfigurationDetails)
        paymentMethodOptions = try container.sdkDecodeIfPresent(.paymentMethodOptions)
        singleUseMandate = try container.sdkDecodeIfPresent(.singleUseMandate)
        try validateLength("id", id, min: nil, max: 5000)
        try validateLength("usage", usage, min: nil, max: 5000)
        if let value = clientSecret {
            try validateLength("client_secret", value, min: nil, max: 5000)
        }
        if let value = customerAccount {
            try validateLength("customer_account", value, min: nil, max: 5000)
        }
        if let value = description {
            try validateLength("description", value, min: nil, max: 5000)
        }
    }
}

public extension SetupIntent {
    init(
        created: Int,
        id: String,
        livemode: Bool,
        object: SetupIntentObject,
        paymentMethodTypes: [String],
        status: SetupIntentStatus,
        usage: String,
        allowedPaymentMethodTypes: [SetupIntentAllowedPaymentMethodTypesItem]? = nil,
        application: SetupIntentApplication? = nil,
        attachToSelf: Bool? = nil,
        automaticPaymentMethods: SetupIntentAutomaticPaymentMethods? = nil,
        cancellationReason: SetupIntentCancellationReason? = nil,
        clientSecret: String? = nil,
        customer: SetupIntentCustomer? = nil,
        customerAccount: String? = nil,
        description: String? = nil,
        excludedPaymentMethodTypes: [SetupIntentExcludedPaymentMethodTypesItem]? = nil,
        flowDirections: [SetupIntentFlowDirectionsItem]? = nil,
        lastSetupError: SetupIntentLastSetupError? = nil,
        latestAttempt: SetupIntentLatestAttempt? = nil,
        managedPayments: SetupIntentManagedPayments? = nil,
        mandate: SetupIntentMandate? = nil,
        metadata: [String: String]? = nil,
        nextAction: SetupIntentNextActionXe0e117ee? = nil,
        onBehalfOf: SetupIntentOnBehalfOf? = nil,
        paymentMethod: SetupIntentPaymentMethod? = nil,
        paymentMethodConfigurationDetails: SetupIntentPaymentMethodConfigurationDetails? = nil,
        paymentMethodOptions: SetupIntentPaymentMethodOptionsX9e24e185? = nil,
        singleUseMandate: SetupIntentSingleUseMandate? = nil
    ) throws {
        (self.created, self.id) = (created, id)
        (self.livemode, self.object) = (livemode, object)
        (self.paymentMethodTypes, self.status) = (paymentMethodTypes, status)
        (self.usage, self.allowedPaymentMethodTypes) = (usage, allowedPaymentMethodTypes)
        (self.application, self.attachToSelf) = (application, attachToSelf)
        self.automaticPaymentMethods = automaticPaymentMethods
        (self.cancellationReason, self.clientSecret) = (cancellationReason, clientSecret)
        (self.customer, self.customerAccount) = (customer, customerAccount)
        (self.description, self.excludedPaymentMethodTypes) = (description, excludedPaymentMethodTypes)
        (self.flowDirections, self.lastSetupError) = (flowDirections, lastSetupError)
        (self.latestAttempt, self.managedPayments) = (latestAttempt, managedPayments)
        (self.mandate, self.metadata) = (mandate, metadata)
        (self.nextAction, self.onBehalfOf) = (nextAction, onBehalfOf)
        self.paymentMethod = paymentMethod
        self.paymentMethodConfigurationDetails = paymentMethodConfigurationDetails
        (self.paymentMethodOptions, self.singleUseMandate) = (paymentMethodOptions, singleUseMandate)
        try validateLength("id", self.id, min: nil, max: 5000)
        try validateLength("usage", self.usage, min: nil, max: 5000)
        if let value = self.clientSecret {
            try validateLength("client_secret", value, min: nil, max: 5000)
        }
        if let value = self.customerAccount {
            try validateLength("customer_account", value, min: nil, max: 5000)
        }
        if let value = self.description {
            try validateLength("description", value, min: nil, max: 5000)
        }
    }
}

public enum SetupIntentApplication {
    case stringValue(String)
    case application(Application)
}

extension SetupIntentApplication: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for SetupIntentApplication"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container.decode(Application.self) {
            return .application(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .application(value): try container.encode(value); return true
        }
    }
}

public enum SetupIntentAutomaticPaymentMethods {
    case paymentFlowsAutomaticPaymentMethodsSetupIntent(PaymentFlowsAutomaticPaymentMethodsSetupIntent)
}

extension SetupIntentAutomaticPaymentMethods: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for SetupIntentAutomaticPaymentMethods"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(
            PaymentFlowsAutomaticPaymentMethodsSetupIntent.self
        ) {
            return .paymentFlowsAutomaticPaymentMethodsSetupIntent(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .paymentFlowsAutomaticPaymentMethodsSetupIntent(value): try container.encode(value); return true
        }
    }
}

public indirect enum SetupIntentCustomer {
    case stringValue(String)
    case customer(Customer)
    case deletedCustomer(DeletedCustomer)
}

extension SetupIntentCustomer: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for SetupIntentCustomer"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container.decode(Customer.self) {
            return .customer(value)
        }
        if let value = try? container.decode(DeletedCustomer.self) {
            return .deletedCustomer(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .customer(value): try container.encode(value); return true
        case let .deletedCustomer(value): try container.encode(value); return true
        }
    }
}

public enum SetupIntentLastSetupError {
    case apiErrors(ApiErrors)
}
