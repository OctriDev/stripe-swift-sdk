// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

/// V1 domain models
/// The `Charge` object represents a single attempt to move money into your Stripe account. PaymentIntent
/// confirmation is the most common way to create Charges, but Account Debits may also create Charges. Some legacy
/// payment flows create Charges directly, which is not recommended for new integrations.
public struct Charge: Codable {
    /// Amount intended to be collected by this payment. A positive integer representing how much to charge in the
    /// smallest currency unit (e.g., 100 cents to charge $1.00 or 100 to charge ¥100, a zero-decimal currency). The
    /// minimum amount is $0.50 US or equivalent in charge currency. The amount value supports up to eight digits
    /// (e.g., a value of 99999999 for a USD charge of $999,999.99).
    public var amount: Int
    /// Amount in cents (or local equivalent) captured (can be less than the amount attribute on the charge if a
    /// partial capture was made).
    public var amountCaptured: Int
    /// Amount in cents (or local equivalent) refunded (can be less than the amount attribute on the charge if a
    /// partial refund was issued).
    public var amountRefunded: Int
    /// Required object value serialized in the `billing_details` wire field.
    public var billingDetails: BillingDetails
    /// If the charge was created without capturing, this Boolean represents whether it is still uncaptured or has
    /// since been captured.
    public var captured: Bool
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// Three-letter ISO currency code, in lowercase. Must be a supported currency.
    public var currency: String
    /// Whether the charge has been disputed.
    public var disputed: Bool
    /// Unique identifier for the object.
    public var id: String
    /// If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is
    /// `false`.
    public var livemode: Bool
    /// Set of key-value pairs that you can attach to an object. This can be useful for storing additional
    /// information about the object in a structured format.
    public var metadata: [String: String]
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: ChargeObject
    /// `true` if the charge succeeded, or was successfully authorized for later capture.
    public var paid: Bool
    /// Whether the charge has been fully refunded. If the charge is only partially refunded, this attribute will
    /// still be false.
    public var refunded: Bool
    /// The status of the payment is either `succeeded`, `pending`, or `failed`.
    public var status: ChargeStatus
    /// ID of the Connect application that created the charge.
    public var application: ChargeApplication?
    /// The application fee (if any) for the charge. See the Connect documentation for details.
    public var applicationFee: ChargeApplicationFee?
    /// The amount of the application fee (if any) requested for the charge. See the Connect documentation for
    /// details.
    public var applicationFeeAmount: Int?
    /// ID of the balance transaction that describes the impact of this charge on your account balance (not
    /// including refunds or disputes).
    public var balanceTransaction: ChargeBalanceTransaction?
    /// The full statement descriptor that is passed to card networks, and that is displayed on your customers'
    /// credit card and bank statements. Allows you to see what the statement descriptor looks like after the static
    /// and dynamic portions are combined. This value only exists for card payments.
    public var calculatedStatementDescriptor: String?
    /// ID of the customer this charge is for if one exists.
    public var customer: ChargeCustomer?
    /// An arbitrary string attached to the object. Often useful for displaying to users.
    public var description: String?
    /// ID of the balance transaction that describes the reversal of the balance on your account due to payment
    /// failure.
    public var failureBalanceTransaction: ChargeFailureBalanceTransaction?
    /// Error code explaining reason for charge failure if available (see the errors section for a list of codes).
    public var failureCode: String?
    /// Message to user further explaining reason for charge failure if available.
    public var failureMessage: String?
    /// Information on fraud assessments for the charge.
    public var fraudDetails: ChargeFraudDetailsXd3933fa0?
    /// The account (if any) the charge was made on behalf of without triggering an automatic transfer. See the
    /// Connect documentation for details.
    public var onBehalfOf: ChargeOnBehalfOf?
    /// Details about whether the payment was accepted, and why. See understanding declines for details.
    public var outcome: ChargeOutcomeX98af5532?
    /// ID of the PaymentIntent associated with this charge, if one exists.
    public var paymentIntent: ChargePaymentIntent?
    /// ID of the payment method used in this charge.
    public var paymentMethod: String?
    /// Details about the payment method at the time of the transaction.
    public var paymentMethodDetails: ChargePaymentMethodDetails?
    /// Optional object value serialized in the `presentment_details` wire field.
    public var presentmentDetails: PaymentFlowsPaymentIntentPresentmentDetails?
    /// Options to configure Radar. See Radar Session for more information.
    public var radarOptions: RadarRadarOptions?
    /// This is the email address that the receipt for this charge was sent to.
    public var receiptEmail: String?
    /// This is the transaction number that appears on email receipts sent for this charge. This attribute will be
    /// `null` until a receipt has been sent.
    public var receiptNumber: String?
    /// This is the URL to view the receipt for this charge. The receipt is kept up-to-date to the latest state of
    /// the charge, including any refunds. If the charge is for an Invoice, the receipt will be stylized as an
    /// Invoice receipt.
    public var receiptUrl: String?
    /// A list of refunds that have been applied to the charge.
    public var refunds: ChargeRefunds?
    /// ID of the review associated with this charge if one exists.
    public var review: ChargeReview?
    /// Shipping information for the charge.
    public var shipping: ChargeShipping?
    /// The transfer ID which created this charge. Only present if the charge came from another Stripe account. See
    /// the Connect documentation for details.
    public var sourceTransfer: ChargeSourceTransfer?
    /// For a non-card charge, text that appears on the customer's statement as the statement descriptor. This value
    /// overrides the account's default statement descriptor. For information about requirements, including the
    /// 22-character limit, see the Statement Descriptor docs. For a card charge, this value is ignored unless you
    /// don't specify a `statement_descriptor_suffix`, in which case this value is used as the suffix.
    public var statementDescriptor: String?
    /// Provides information about a card charge. Concatenated to the account's statement descriptor prefix to form
    /// the complete statement descriptor that appears on the customer's statement. If the account has no prefix
    /// value, the suffix is concatenated to the account's statement descriptor.
    public var statementDescriptorSuffix: String?
    /// ID of the transfer to the `destination` account (only applicable if the charge was created using the
    /// `destination` parameter).
    public var transfer: ChargeTransfer?
    /// An optional dictionary including the account to automatically transfer to as part of a destination charge.
    /// See the Connect documentation for details.
    public var transferData: ChargeTransferDataXa9a536ab?
    /// A string that identifies this transaction as part of a group. See the Connect documentation for details.
    public var transferGroup: String?

    enum CodingKeys: String, CodingKey {
        case amount
        case amountCaptured = "amount_captured"
        case amountRefunded = "amount_refunded"
        case billingDetails = "billing_details"
        case captured
        case created
        case currency
        case disputed
        case id
        case livemode
        case metadata
        case object
        case paid
        case refunded
        case status
        case application
        case applicationFee = "application_fee"
        case applicationFeeAmount = "application_fee_amount"
        case balanceTransaction = "balance_transaction"
        case calculatedStatementDescriptor = "calculated_statement_descriptor"
        case customer
        case description
        case failureBalanceTransaction = "failure_balance_transaction"
        case failureCode = "failure_code"
        case failureMessage = "failure_message"
        case fraudDetails = "fraud_details"
        case onBehalfOf = "on_behalf_of"
        case outcome
        case paymentIntent = "payment_intent"
        case paymentMethod = "payment_method"
        case paymentMethodDetails = "payment_method_details"
        case presentmentDetails = "presentment_details"
        case radarOptions = "radar_options"
        case receiptEmail = "receipt_email"
        case receiptNumber = "receipt_number"
        case receiptUrl = "receipt_url"
        case refunds
        case review
        case shipping
        case sourceTransfer = "source_transfer"
        case statementDescriptor = "statement_descriptor"
        case statementDescriptorSuffix = "statement_descriptor_suffix"
        case transfer
        case transferData = "transfer_data"
        case transferGroup = "transfer_group"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension Charge {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        amount = try container.sdkDecodeRequired(.amount)
        amountCaptured = try container.sdkDecodeRequired(.amountCaptured)
        amountRefunded = try container.sdkDecodeRequired(.amountRefunded)
        billingDetails = try container.sdkDecodeRequired(.billingDetails)
        captured = try container.sdkDecodeRequired(.captured)
        created = try container.sdkDecodeRequired(.created)
        currency = try container.sdkDecodeRequired(.currency)
        disputed = try container.sdkDecodeRequired(.disputed)
        id = try container.sdkDecodeRequired(.id)
        livemode = try container.sdkDecodeRequired(.livemode)
        metadata = try container.sdkDecodeRequired(.metadata)
        object = try container.sdkDecodeRequired(.object)
        paid = try container.sdkDecodeRequired(.paid)
        refunded = try container.sdkDecodeRequired(.refunded)
        status = try container.sdkDecodeRequired(.status)
        application = try container.sdkDecodeIfPresent(.application)
        applicationFee = try container.sdkDecodeIfPresent(.applicationFee)
        applicationFeeAmount = try container.sdkDecodeIfPresent(.applicationFeeAmount)
        balanceTransaction = try container.sdkDecodeIfPresent(.balanceTransaction)
        calculatedStatementDescriptor = try container.sdkDecodeIfPresent(.calculatedStatementDescriptor)
        customer = try container.sdkDecodeIfPresent(.customer)
        description = try container.sdkDecodeIfPresent(.description)
        failureBalanceTransaction = try container.sdkDecodeIfPresent(.failureBalanceTransaction)
        failureCode = try container.sdkDecodeIfPresent(.failureCode)
        failureMessage = try container.sdkDecodeIfPresent(.failureMessage)
        fraudDetails = try container.sdkDecodeIfPresent(.fraudDetails)
        onBehalfOf = try container.sdkDecodeIfPresent(.onBehalfOf)
        outcome = try container.sdkDecodeIfPresent(.outcome)
        paymentIntent = try container.sdkDecodeIfPresent(.paymentIntent)
        paymentMethod = try container.sdkDecodeIfPresent(.paymentMethod)
        paymentMethodDetails = try container.sdkDecodeIfPresent(.paymentMethodDetails)
        presentmentDetails = try container.sdkDecodeIfPresent(.presentmentDetails)
        radarOptions = try container.sdkDecodeIfPresent(.radarOptions)
        receiptEmail = try container.sdkDecodeIfPresent(.receiptEmail)
        receiptNumber = try container.sdkDecodeIfPresent(.receiptNumber)
        receiptUrl = try container.sdkDecodeIfPresent(.receiptUrl)
        refunds = try container.sdkDecodeIfPresent(.refunds)
        review = try container.sdkDecodeIfPresent(.review)
        shipping = try container.sdkDecodeIfPresent(.shipping)
        sourceTransfer = try container.sdkDecodeIfPresent(.sourceTransfer)
        statementDescriptor = try container.sdkDecodeIfPresent(.statementDescriptor)
        statementDescriptorSuffix = try container.sdkDecodeIfPresent(.statementDescriptorSuffix)
        transfer = try container.sdkDecodeIfPresent(.transfer)
        transferData = try container.sdkDecodeIfPresent(.transferData)
        transferGroup = try container.sdkDecodeIfPresent(.transferGroup)
        try sdkValidateConstraints()
    }
}

public extension Charge {
    init(
        amount: Int,
        amountCaptured: Int,
        amountRefunded: Int,
        billingDetails: BillingDetails,
        captured: Bool,
        created: Int,
        currency: String,
        disputed: Bool,
        id: String,
        livemode: Bool,
        metadata: [String: String],
        object: ChargeObject,
        paid: Bool,
        refunded: Bool,
        status: ChargeStatus,
        application: ChargeApplication? = nil,
        applicationFee: ChargeApplicationFee? = nil,
        applicationFeeAmount: Int? = nil,
        balanceTransaction: ChargeBalanceTransaction? = nil,
        calculatedStatementDescriptor: String? = nil,
        customer: ChargeCustomer? = nil,
        description: String? = nil,
        failureBalanceTransaction: ChargeFailureBalanceTransaction? = nil,
        failureCode: String? = nil,
        failureMessage: String? = nil,
        fraudDetails: ChargeFraudDetailsXd3933fa0? = nil,
        onBehalfOf: ChargeOnBehalfOf? = nil,
        outcome: ChargeOutcomeX98af5532? = nil,
        paymentIntent: ChargePaymentIntent? = nil,
        paymentMethod: String? = nil,
        paymentMethodDetails: ChargePaymentMethodDetails? = nil,
        presentmentDetails: PaymentFlowsPaymentIntentPresentmentDetails? = nil,
        radarOptions: RadarRadarOptions? = nil,
        receiptEmail: String? = nil,
        receiptNumber: String? = nil,
        receiptUrl: String? = nil,
        refunds: ChargeRefunds? = nil,
        review: ChargeReview? = nil,
        shipping: ChargeShipping? = nil,
        sourceTransfer: ChargeSourceTransfer? = nil,
        statementDescriptor: String? = nil,
        statementDescriptorSuffix: String? = nil,
        transfer: ChargeTransfer? = nil,
        transferData: ChargeTransferDataXa9a536ab? = nil,
        transferGroup: String? = nil
    ) throws {
        (self.amount, self.amountCaptured) = (amount, amountCaptured)
        (self.amountRefunded, self.billingDetails) = (amountRefunded, billingDetails)
        (self.captured, self.created) = (captured, created)
        (self.currency, self.disputed) = (currency, disputed)
        (self.id, self.livemode) = (id, livemode)
        (self.metadata, self.object) = (metadata, object)
        (self.paid, self.refunded) = (paid, refunded)
        (self.status, self.application) = (status, application)
        (self.applicationFee, self.applicationFeeAmount) = (applicationFee, applicationFeeAmount)
        self.balanceTransaction = balanceTransaction
        (self.calculatedStatementDescriptor, self.customer) = (calculatedStatementDescriptor, customer)
        (self.description, self.failureBalanceTransaction) = (description, failureBalanceTransaction)
        (self.failureCode, self.failureMessage) = (failureCode, failureMessage)
        (self.fraudDetails, self.onBehalfOf) = (fraudDetails, onBehalfOf)
        (self.outcome, self.paymentIntent) = (outcome, paymentIntent)
        (self.paymentMethod, self.paymentMethodDetails) = (paymentMethod, paymentMethodDetails)
        (self.presentmentDetails, self.radarOptions) = (presentmentDetails, radarOptions)
        (self.receiptEmail, self.receiptNumber) = (receiptEmail, receiptNumber)
        (self.receiptUrl, self.refunds) = (receiptUrl, refunds)
        (self.review, self.shipping) = (review, shipping)
        (self.sourceTransfer, self.statementDescriptor) = (sourceTransfer, statementDescriptor)
        (self.statementDescriptorSuffix, self.transfer) = (statementDescriptorSuffix, transfer)
        (self.transferData, self.transferGroup) = (transferData, transferGroup)
        try sdkValidateConstraints()
    }
}

extension Charge {
    func sdkValidateConstraints() throws {
        try validateLength("id", id, min: nil, max: 5000)
        if let value = calculatedStatementDescriptor {
            try validateLength("calculated_statement_descriptor", value, min: nil, max: 5000)
        }
        if let value = description {
            try validateLength("description", value, min: nil, max: 40000)
        }
        if let value = failureCode {
            try validateLength("failure_code", value, min: nil, max: 5000)
        }
        if let value = failureMessage {
            try validateLength("failure_message", value, min: nil, max: 5000)
        }
        if let value = paymentMethod {
            try validateLength("payment_method", value, min: nil, max: 5000)
        }
        if let value = receiptEmail {
            try validateLength("receipt_email", value, min: nil, max: 5000)
        }
        if let value = receiptNumber {
            try validateLength("receipt_number", value, min: nil, max: 5000)
        }
        if let value = receiptUrl {
            try validateLength("receipt_url", value, min: nil, max: 5000)
        }
        if let value = statementDescriptor {
            try validateLength("statement_descriptor", value, min: nil, max: 5000)
        }
        if let value = statementDescriptorSuffix {
            try validateLength("statement_descriptor_suffix", value, min: nil, max: 5000)
        }
        if let value = transferGroup {
            try validateLength("transfer_group", value, min: nil, max: 5000)
        }
    }
}

public enum ChargeApplication {
    case stringValue(String)
    case application(Application)
}

extension ChargeApplication: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for ChargeApplication"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container.decode(Application.self) {
            return .application(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .application(value): try container.encode(value); return true
        }
    }
}

public indirect enum ChargeApplicationFee {
    case stringValue(String)
    case applicationFee(ApplicationFee)
}

extension ChargeApplicationFee: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for ChargeApplicationFee"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container.decode(ApplicationFee.self) {
            return .applicationFee(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .applicationFee(value): try container.encode(value); return true
        }
    }
}

public indirect enum ChargeBalanceTransaction {
    case stringValue(String)
    case balanceTransaction(BalanceTransaction)
}

extension ChargeBalanceTransaction: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for ChargeBalanceTransaction"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container.decode(BalanceTransaction.self) {
            return .balanceTransaction(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .balanceTransaction(value): try container.encode(value); return true
        }
    }
}

public indirect enum ChargeCustomer {
    case stringValue(String)
    case customer(Customer)
    case deletedCustomer(DeletedCustomer)
}

extension ChargeCustomer: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(in: container, debugDescription: "No variant matched for ChargeCustomer")
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container.decode(Customer.self) {
            return .customer(value)
        }
        if let value = try? container.decode(DeletedCustomer.self) {
            return .deletedCustomer(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .customer(value): try container.encode(value); return true
        case let .deletedCustomer(value): try container.encode(value); return true
        }
    }
}

public indirect enum ChargeFailureBalanceTransaction {
    case stringValue(String)
    case balanceTransaction(BalanceTransaction)
}

extension ChargeFailureBalanceTransaction: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for ChargeFailureBalanceTransaction"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container.decode(BalanceTransaction.self) {
            return .balanceTransaction(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .balanceTransaction(value): try container.encode(value); return true
        }
    }
}

public enum ChargeFraudDetailsXd3933fa0 {
    case chargeFraudDetails(ChargeFraudDetails)
}

extension ChargeFraudDetailsXd3933fa0: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for ChargeFraudDetailsXd3933fa0"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(ChargeFraudDetails.self) {
            return .chargeFraudDetails(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .chargeFraudDetails(value): try container.encode(value); return true
        }
    }
}

public enum ChargeOnBehalfOf {
    case stringValue(String)
    case account(Account)
}

extension ChargeOnBehalfOf: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for ChargeOnBehalfOf"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container.decode(Account.self) {
            return .account(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .account(value): try container.encode(value); return true
        }
    }
}

public enum ChargeOutcomeX98af5532 {
    case chargeOutcome(ChargeOutcome)
}

extension ChargeOutcomeX98af5532: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for ChargeOutcomeX98af5532"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(ChargeOutcome.self) {
            return .chargeOutcome(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .chargeOutcome(value): try container.encode(value); return true
        }
    }
}
