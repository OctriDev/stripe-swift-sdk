// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

/// V1 domain models
public extension SubscriptionScheduleAddInvoiceItem {
    init(
        discounts: [DiscountsResourceStackableDiscountWithDiscountEnd],
        period: SubscriptionScheduleAddInvoiceItemPeriod,
        price: SubscriptionScheduleAddInvoiceItemPrice,
        discountable: Bool? = nil,
        metadata: [String: String]? = nil,
        quantity: Int? = nil,
        taxRates: [TaxRate]? = nil
    ) {
        (self.discounts, self.period) = (discounts, period)
        (self.price, self.discountable) = (price, discountable)
        (self.metadata, self.quantity) = (metadata, quantity)
        self.taxRates = taxRates
    }
}

public enum SubscriptionScheduleAddInvoiceItemPrice {
    case stringValue(String)
    case price(Price)
    case deletedPrice(DeletedPrice)
}

extension SubscriptionScheduleAddInvoiceItemPrice: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for SubscriptionScheduleAddInvoiceItemPrice"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container.decode(Price.self) {
            return .price(value)
        }
        if let value = try? container.decode(DeletedPrice.self) {
            return .deletedPrice(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .price(value): try container.encode(value); return true
        case let .deletedPrice(value): try container.encode(value); return true
        }
    }
}

/// A phase item describes the price and quantity of a phase.
public struct SubscriptionScheduleConfigurationItem: Codable {
    /// The discounts applied to the subscription item. Subscription item discounts are applied before subscription
    /// discounts. Use `expand[]=discounts` to expand each discount.
    public var discounts: [StackableDiscountWithDiscountSettings]
    /// ID of the price to which the customer should be subscribed.
    public var price: SubscriptionScheduleConfigurationItemPrice
    /// Define thresholds at which an invoice will be sent, and the related subscription advanced to a new billing
    /// period
    public var billingThresholds: SubscriptionScheduleConfigurationItemBillingThresholds?
    /// Set of key-value pairs that you can attach to an item. Metadata on this item will update the underlying
    /// subscription item's `metadata` when the phase is entered.
    public var metadata: [String: String]?
    /// Quantity of the plan to which the customer should be subscribed.
    public var quantity: Int?
    /// The tax rates which apply to this `phase_item`. When set, the `default_tax_rates` on the phase do not apply
    /// to this `phase_item`.
    public var taxRates: [TaxRate]?

    enum CodingKeys: String, CodingKey {
        case discounts
        case price
        case billingThresholds = "billing_thresholds"
        case metadata
        case quantity
        case taxRates = "tax_rates"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension SubscriptionScheduleConfigurationItem {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.discounts) else {
            throw SdkValidationError(
                field: "discounts",
                code: "required",
                message: "Validation failed for 'discounts': value is required"
            )
        }
        guard container.contains(.price) else {
            throw SdkValidationError(
                field: "price",
                code: "required",
                message: "Validation failed for 'price': value is required"
            )
        }
        discounts = try container.sdkDecodeRequired(.discounts)
        price = try container.sdkDecodeRequired(.price)
        billingThresholds = try container.sdkDecodeIfPresent(.billingThresholds)
        metadata = try container.sdkDecodeIfPresent(.metadata)
        quantity = try container.sdkDecodeIfPresent(.quantity)
        taxRates = try container.sdkDecodeIfPresent(.taxRates)
    }
}

public extension SubscriptionScheduleConfigurationItem {
    init(
        discounts: [StackableDiscountWithDiscountSettings],
        price: SubscriptionScheduleConfigurationItemPrice,
        billingThresholds: SubscriptionScheduleConfigurationItemBillingThresholds? = nil,
        metadata: [String: String]? = nil,
        quantity: Int? = nil,
        taxRates: [TaxRate]? = nil
    ) {
        (self.discounts, self.price) = (discounts, price)
        (self.billingThresholds, self.metadata) = (billingThresholds, metadata)
        (self.quantity, self.taxRates) = (quantity, taxRates)
    }
}

public enum SubscriptionScheduleConfigurationItemBillingThresholds {
    case subscriptionItemBillingThresholds(SubscriptionItemBillingThresholds)
}

extension SubscriptionScheduleConfigurationItemBillingThresholds: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for SubscriptionScheduleConfigurationItemBillingThresholds"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container
            .decode(SubscriptionItemBillingThresholds.self) {
            return .subscriptionItemBillingThresholds(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .subscriptionItemBillingThresholds(value): try container.encode(value); return true
        }
    }
}

public enum SubscriptionScheduleConfigurationItemPrice {
    case stringValue(String)
    case price(Price)
    case deletedPrice(DeletedPrice)
}

extension SubscriptionScheduleConfigurationItemPrice: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for SubscriptionScheduleConfigurationItemPrice"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container.decode(Price.self) {
            return .price(value)
        }
        if let value = try? container.decode(DeletedPrice.self) {
            return .deletedPrice(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .price(value): try container.encode(value); return true
        case let .deletedPrice(value): try container.encode(value); return true
        }
    }
}

/// A phase describes the plans, coupon, and trialing status of a subscription for a predefined time period.
public struct SubscriptionSchedulePhaseConfiguration: Codable {
    /// A list of prices and quantities that will generate invoice items appended to the next invoice for this
    /// phase.
    public var addInvoiceItems: [SubscriptionScheduleAddInvoiceItem]
    /// Three-letter ISO currency code, in lowercase. Must be a supported currency.
    public var currency: String
    /// The stackable discounts that will be applied to the subscription on this phase. Subscription item discounts
    /// are applied before subscription discounts.
    public var discounts: [StackableDiscountWithDiscountSettingsAndDiscountEnd]
    /// The end of this phase of the subscription schedule.
    public var endDate: Int
    /// Subscription items to configure the subscription to during this phase of the subscription schedule.
    public var items: [SubscriptionScheduleConfigurationItem]
    /// When transitioning phases, controls how prorations are handled (if any). Possible values are
    /// `create_prorations`, `none`, and `always_invoice`.
    public var prorationBehavior: SubscriptionSchedulePhaseConfigurationProrationBehavior
    /// The start of this phase of the subscription schedule.
    public var startDate: Int
    /// A non-negative decimal between 0 and 100, with at most two decimal places. This represents the percentage of
    /// the subscription invoice total that will be transferred to the application owner's Stripe account during
    /// this phase of the schedule.
    public var applicationFeePercent: Double?
    /// Optional object value serialized in the `automatic_tax` wire field.
    public var automaticTax: SchedulesPhaseAutomaticTax?
    /// Possible values are `phase_start` or `automatic`. If `phase_start` then billing cycle anchor of the
    /// subscription is set to the start of the phase when entering the phase. If `automatic` then the billing cycle
    /// anchor is automatically modified as needed when entering the phase. For more information, see the billing
    /// cycle documentation.
    public var billingCycleAnchor: SubscriptionSchedulePhaseConfigurationBillingCycleAnchor?
    /// Define thresholds at which an invoice will be sent, and the subscription advanced to a new billing period
    public var billingThresholds: SubscriptionSchedulePhaseConfigurationBillingThresholds?
    /// Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay
    /// the underlying subscription at the end of each billing cycle using the default source attached to the
    /// customer. When sending an invoice, Stripe will email your customer an invoice with payment instructions and
    /// mark the subscription as `active`.
    public var collectionMethod: SubscriptionSchedulePhaseConfigurationCollectionMethod?
    /// ID of the default payment method for the subscription schedule. It must belong to the customer associated
    /// with the subscription schedule. If not set, invoices will use the default payment method in the customer's
    /// invoice settings.
    public var defaultPaymentMethod: SubscriptionSchedulePhaseConfigurationDefaultPaymentMethod?
    /// The default tax rates to apply to the subscription during this phase of the subscription schedule.
    public var defaultTaxRates: [TaxRate]?
    /// Subscription description, meant to be displayable to the customer. Use this field to optionally store an
    /// explanation of the subscription for rendering in Stripe surfaces and certain local payment methods UIs.
    public var description: String?
    /// The invoice settings applicable during this phase.
    public var invoiceSettings: SubscriptionSchedulePhaseConfigurationInvoiceSettings?
    /// Set of key-value pairs that you can attach to a phase. Metadata on a schedule's phase will update the
    /// underlying subscription's `metadata` when the phase is entered. Updating the underlying subscription's
    /// `metadata` directly will not affect the current phase's `metadata`.
    public var metadata: [String: String]?
    /// The account (if any) the charge was made on behalf of for charges associated with the schedule's
    /// subscription. See the Connect documentation for details.
    public var onBehalfOf: SubscriptionSchedulePhaseConfigurationOnBehalfOf?
    /// The account (if any) the associated subscription's payments will be attributed to for tax reporting, and
    /// where funds from each payment will be transferred to for each of the subscription's invoices.
    public var transferData: SubscriptionSchedulePhaseConfigurationTransferData?
    /// If set to true the entire phase is counted as a trial and the customer will not be charged for any fees.
    public var trial: Bool?
    /// When the trial ends within the phase.
    public var trialEnd: Int?

    enum CodingKeys: String, CodingKey {
        case addInvoiceItems = "add_invoice_items"
        case currency
        case discounts
        case endDate = "end_date"
        case items
        case prorationBehavior = "proration_behavior"
        case startDate = "start_date"
        case applicationFeePercent = "application_fee_percent"
        case automaticTax = "automatic_tax"
        case billingCycleAnchor = "billing_cycle_anchor"
        case billingThresholds = "billing_thresholds"
        case collectionMethod = "collection_method"
        case defaultPaymentMethod = "default_payment_method"
        case defaultTaxRates = "default_tax_rates"
        case description
        case invoiceSettings = "invoice_settings"
        case metadata
        case onBehalfOf = "on_behalf_of"
        case transferData = "transfer_data"
        case trial
        case trialEnd = "trial_end"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension SubscriptionSchedulePhaseConfiguration {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.addInvoiceItems) else {
            throw SdkValidationError(
                field: "add_invoice_items",
                code: "required",
                message: "Validation failed for 'add_invoice_items': value is required"
            )
        }
        guard container.contains(.currency) else {
            throw SdkValidationError(
                field: "currency",
                code: "required",
                message: "Validation failed for 'currency': value is required"
            )
        }
        guard container.contains(.discounts) else {
            throw SdkValidationError(
                field: "discounts",
                code: "required",
                message: "Validation failed for 'discounts': value is required"
            )
        }
        guard container.contains(.endDate) else {
            throw SdkValidationError(
                field: "end_date",
                code: "required",
                message: "Validation failed for 'end_date': value is required"
            )
        }
        guard container.contains(.items) else {
            throw SdkValidationError(
                field: "items",
                code: "required",
                message: "Validation failed for 'items': value is required"
            )
        }
        guard container.contains(.prorationBehavior) else {
            throw SdkValidationError(
                field: "proration_behavior",
                code: "required",
                message: "Validation failed for 'proration_behavior': value is required"
            )
        }
        guard container.contains(.startDate) else {
            throw SdkValidationError(
                field: "start_date",
                code: "required",
                message: "Validation failed for 'start_date': value is required"
            )
        }
        addInvoiceItems = try container.sdkDecodeRequired(.addInvoiceItems)
        currency = try container.sdkDecodeRequired(.currency)
        discounts = try container.sdkDecodeRequired(.discounts)
        endDate = try container.sdkDecodeRequired(.endDate)
        items = try container.sdkDecodeRequired(.items)
        prorationBehavior = try container.sdkDecodeRequired(.prorationBehavior)
        startDate = try container.sdkDecodeRequired(.startDate)
        applicationFeePercent = try container.sdkDecodeIfPresent(.applicationFeePercent)
        automaticTax = try container.sdkDecodeIfPresent(.automaticTax)
        billingCycleAnchor = try container.sdkDecodeIfPresent(.billingCycleAnchor)
        billingThresholds = try container.sdkDecodeIfPresent(.billingThresholds)
        collectionMethod = try container.sdkDecodeIfPresent(.collectionMethod)
        defaultPaymentMethod = try container.sdkDecodeIfPresent(.defaultPaymentMethod)
        defaultTaxRates = try container.sdkDecodeIfPresent(.defaultTaxRates)
        description = try container.sdkDecodeIfPresent(.description)
        invoiceSettings = try container.sdkDecodeIfPresent(.invoiceSettings)
        metadata = try container.sdkDecodeIfPresent(.metadata)
        onBehalfOf = try container.sdkDecodeIfPresent(.onBehalfOf)
        transferData = try container.sdkDecodeIfPresent(.transferData)
        trial = try container.sdkDecodeIfPresent(.trial)
        trialEnd = try container.sdkDecodeIfPresent(.trialEnd)
        if let value = description {
            try validateLength("description", value, min: nil, max: 5000)
        }
    }
}

public extension SubscriptionSchedulePhaseConfiguration {
    init(
        addInvoiceItems: [SubscriptionScheduleAddInvoiceItem],
        currency: String,
        discounts: [StackableDiscountWithDiscountSettingsAndDiscountEnd],
        endDate: Int,
        items: [SubscriptionScheduleConfigurationItem],
        prorationBehavior: SubscriptionSchedulePhaseConfigurationProrationBehavior,
        startDate: Int,
        applicationFeePercent: Double? = nil,
        automaticTax: SchedulesPhaseAutomaticTax? = nil,
        billingCycleAnchor: SubscriptionSchedulePhaseConfigurationBillingCycleAnchor? = nil,
        billingThresholds: SubscriptionSchedulePhaseConfigurationBillingThresholds? = nil,
        collectionMethod: SubscriptionSchedulePhaseConfigurationCollectionMethod? = nil,
        defaultPaymentMethod: SubscriptionSchedulePhaseConfigurationDefaultPaymentMethod? = nil,
        defaultTaxRates: [TaxRate]? = nil,
        description: String? = nil,
        invoiceSettings: SubscriptionSchedulePhaseConfigurationInvoiceSettings? = nil,
        metadata: [String: String]? = nil,
        onBehalfOf: SubscriptionSchedulePhaseConfigurationOnBehalfOf? = nil,
        transferData: SubscriptionSchedulePhaseConfigurationTransferData? = nil,
        trial: Bool? = nil,
        trialEnd: Int? = nil
    ) throws {
        (self.addInvoiceItems, self.currency) = (addInvoiceItems, currency)
        (self.discounts, self.endDate) = (discounts, endDate)
        (self.items, self.prorationBehavior) = (items, prorationBehavior)
        (self.startDate, self.applicationFeePercent) = (startDate, applicationFeePercent)
        (self.automaticTax, self.billingCycleAnchor) = (automaticTax, billingCycleAnchor)
        (self.billingThresholds, self.collectionMethod) = (billingThresholds, collectionMethod)
        (self.defaultPaymentMethod, self.defaultTaxRates) = (defaultPaymentMethod, defaultTaxRates)
        (self.description, self.invoiceSettings) = (description, invoiceSettings)
        (self.metadata, self.onBehalfOf) = (metadata, onBehalfOf)
        (self.transferData, self.trial) = (transferData, trial)
        self.trialEnd = trialEnd
        if let value = self.description {
            try validateLength("description", value, min: nil, max: 5000)
        }
    }
}

public enum SubscriptionSchedulePhaseConfigurationBillingThresholds {
    case subscriptionBillingThresholds(SubscriptionBillingThresholds)
}

extension SubscriptionSchedulePhaseConfigurationBillingThresholds: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for SubscriptionSchedulePhaseConfigurationBillingThresholds"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container
            .decode(SubscriptionBillingThresholds.self) {
            return .subscriptionBillingThresholds(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .subscriptionBillingThresholds(value): try container.encode(value); return true
        }
    }
}

public enum SubscriptionSchedulePhaseConfigurationDefaultPaymentMethod {
    case stringValue(String)
    case paymentMethod(PaymentMethod)
}

extension SubscriptionSchedulePhaseConfigurationDefaultPaymentMethod: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for SubscriptionSchedulePhaseConfigurationDefaultPaymentMethod"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container.decode(PaymentMethod.self) {
            return .paymentMethod(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .paymentMethod(value): try container.encode(value); return true
        }
    }
}

public enum SubscriptionSchedulePhaseConfigurationInvoiceSettings {
    case invoiceSettingSubscriptionSchedulePhaseSetting(InvoiceSettingSubscriptionSchedulePhaseSetting)
}

extension SubscriptionSchedulePhaseConfigurationInvoiceSettings: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for SubscriptionSchedulePhaseConfigurationInvoiceSettings"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(
            InvoiceSettingSubscriptionSchedulePhaseSetting.self
        ) {
            return .invoiceSettingSubscriptionSchedulePhaseSetting(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .invoiceSettingSubscriptionSchedulePhaseSetting(value): try container.encode(value); return true
        }
    }
}

public enum SubscriptionSchedulePhaseConfigurationOnBehalfOf {
    case stringValue(String)
    case account(Account)
}

extension SubscriptionSchedulePhaseConfigurationOnBehalfOf: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for SubscriptionSchedulePhaseConfigurationOnBehalfOf"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container.decode(Account.self) {
            return .account(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .account(value): try container.encode(value); return true
        }
    }
}

public enum SubscriptionSchedulePhaseConfigurationTransferData {
    case subscriptionTransferData(SubscriptionTransferData)
}

extension SubscriptionSchedulePhaseConfigurationTransferData: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for SubscriptionSchedulePhaseConfigurationTransferData"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(SubscriptionTransferData.self) {
            return .subscriptionTransferData(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .subscriptionTransferData(value): try container.encode(value); return true
        }
    }
}
