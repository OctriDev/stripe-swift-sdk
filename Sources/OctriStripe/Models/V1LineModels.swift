// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

/// V1Line domain models
/// Typed representation of the `LineItemsAdjustableQuantity` API schema.
public struct LineItemsAdjustableQuantity: Codable {
    /// Required `boolean` value serialized in the `enabled` wire field.
    public var enabled: Bool
    /// Optional `integer` value serialized in the `maximum` wire field.
    public var maximum: Int?
    /// Optional `integer` value serialized in the `minimum` wire field.
    public var minimum: Int?

    enum CodingKeys: String, CodingKey {
        case enabled
        case maximum
        case minimum
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension LineItemsAdjustableQuantity {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.enabled) else {
            throw SdkValidationError(
                field: "enabled",
                code: "required",
                message: "Validation failed for 'enabled': value is required"
            )
        }
        enabled = try container.sdkDecodeRequired(.enabled)
        maximum = try container.sdkDecodeIfPresent(.maximum)
        minimum = try container.sdkDecodeIfPresent(.minimum)
    }
}

public extension LineItemsAdjustableQuantity {
    init(enabled: Bool, maximum: Int? = nil, minimum: Int? = nil) {
        (self.enabled, self.maximum) = (enabled, maximum)
        self.minimum = minimum
    }
}

/// Typed representation of the `LineItemsDiscountAmount` API schema.
public struct LineItemsDiscountAmount: Codable {
    /// The amount discounted.
    public var amount: Int
    /// A discount represents the actual application of a coupon or promotion code. It contains information about
    /// when the discount began, when it will end, and what it is applied to. Related guide: Applying discounts to
    /// subscriptions
    public var discount: Discount

    enum CodingKeys: String, CodingKey {
        case amount
        case discount
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension LineItemsDiscountAmount {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.amount) else {
            throw SdkValidationError(
                field: "amount",
                code: "required",
                message: "Validation failed for 'amount': value is required"
            )
        }
        guard container.contains(.discount) else {
            throw SdkValidationError(
                field: "discount",
                code: "required",
                message: "Validation failed for 'discount': value is required"
            )
        }
        amount = try container.sdkDecodeRequired(.amount)
        discount = try container.sdkDecodeRequired(.discount)
    }
}

public extension LineItemsDiscountAmount {
    init(amount: Int, discount: Discount) {
        (self.amount, self.discount) = (amount, discount)
    }
}

/// Typed representation of the `LineItemsTaxAmount` API schema.
public struct LineItemsTaxAmount: Codable {
    /// Amount of tax applied for this rate.
    public var amount: Int
    /// Tax rates can be applied to invoices, subscriptions and Checkout Sessions to collect tax. Related guide: Tax
    /// rates
    public var rate: TaxRate
    /// The reasoning behind this tax, for example, if the product is tax exempt. The possible values for this field
    /// may be extended as new tax rules are supported.
    public var taxabilityReason: LineItemsTaxAmountTaxabilityReason?
    /// The amount on which tax is calculated, in cents (or local equivalent).
    public var taxableAmount: Int?

    enum CodingKeys: String, CodingKey {
        case amount
        case rate
        case taxabilityReason = "taxability_reason"
        case taxableAmount = "taxable_amount"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension LineItemsTaxAmount {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.amount) else {
            throw SdkValidationError(
                field: "amount",
                code: "required",
                message: "Validation failed for 'amount': value is required"
            )
        }
        guard container.contains(.rate) else {
            throw SdkValidationError(
                field: "rate",
                code: "required",
                message: "Validation failed for 'rate': value is required"
            )
        }
        amount = try container.sdkDecodeRequired(.amount)
        rate = try container.sdkDecodeRequired(.rate)
        taxabilityReason = try container.sdkDecodeIfPresent(.taxabilityReason)
        taxableAmount = try container.sdkDecodeIfPresent(.taxableAmount)
    }
}

public extension LineItemsTaxAmount {
    init(
        amount: Int,
        rate: TaxRate,
        taxabilityReason: LineItemsTaxAmountTaxabilityReason? = nil,
        taxableAmount: Int? = nil
    ) {
        (self.amount, self.rate) = (amount, rate)
        (self.taxabilityReason, self.taxableAmount) = (taxabilityReason, taxableAmount)
    }
}

/// The reasoning behind this tax, for example, if the product is tax exempt. The possible values for this field
/// may be extended as new tax rules are supported.
public struct LineItemsTaxAmountTaxabilityReason: RawRepresentable, Hashable, Codable, Sendable, SdkWireConvertible {
    public let rawValue: String

    public init(rawValue: String) {
        self.rawValue = rawValue
    }

    public static let customerExempt = LineItemsTaxAmountTaxabilityReason(rawValue: "customer_exempt")
    public static let notCollecting = LineItemsTaxAmountTaxabilityReason(rawValue: "not_collecting")
    public static let notSubjectToTax = LineItemsTaxAmountTaxabilityReason(rawValue: "not_subject_to_tax")
    public static let notSupported = LineItemsTaxAmountTaxabilityReason(rawValue: "not_supported")
    public static let portionProductExempt = LineItemsTaxAmountTaxabilityReason(rawValue: "portion_product_exempt")
    public static let portionReducedRated = LineItemsTaxAmountTaxabilityReason(rawValue: "portion_reduced_rated")
    public static let portionStandardRated = LineItemsTaxAmountTaxabilityReason(rawValue: "portion_standard_rated")
    public static let productExempt = LineItemsTaxAmountTaxabilityReason(rawValue: "product_exempt")
    public static let productExemptHoliday = LineItemsTaxAmountTaxabilityReason(rawValue: "product_exempt_holiday")
    public static let proportionallyRated = LineItemsTaxAmountTaxabilityReason(rawValue: "proportionally_rated")
    public static let reducedRated = LineItemsTaxAmountTaxabilityReason(rawValue: "reduced_rated")
    public static let reverseCharge = LineItemsTaxAmountTaxabilityReason(rawValue: "reverse_charge")
    public static let standardRated = LineItemsTaxAmountTaxabilityReason(rawValue: "standard_rated")
    public static let taxableBasisReduced = LineItemsTaxAmountTaxabilityReason(rawValue: "taxable_basis_reduced")
    public static let zeroRated = LineItemsTaxAmountTaxabilityReason(rawValue: "zero_rated")

    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        try self.init(rawValue: container.decode(String.self))
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        try container.encode(rawValue)
    }
}
