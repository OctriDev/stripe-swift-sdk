// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

/// V1 domain models
extension AccountBusinessProfileXc496d4ba: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for AccountBusinessProfileXc496d4ba"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(AccountBusinessProfile.self) {
            return .accountBusinessProfile(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .accountBusinessProfile(value): try container.encode(value); return true
        }
    }
}

/// External accounts (bank accounts and debit cards) currently attached to this account. External accounts are only
/// returned for requests where `controller[is_controller]` is true.
public struct AccountExternalAccounts: Codable {
    /// The list contains all external accounts that have been attached to the Stripe account. These may be bank
    /// accounts or cards.
    public var data: [AccountExternalAccountsDataItem]
    /// True if this list has another page of items after this one that can be fetched.
    public var hasMore: Bool
    /// String representing the object's type. Objects of the same type share the same value. Always has the value
    /// `list`.
    public var object: AccountExternalAccountsObject
    /// The URL where this list can be accessed.
    public var url: String

    enum CodingKeys: String, CodingKey {
        case data
        case hasMore = "has_more"
        case object
        case url
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension AccountExternalAccounts {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.data) else {
            throw SdkValidationError(
                field: "data",
                code: "required",
                message: "Validation failed for 'data': value is required"
            )
        }
        guard container.contains(.hasMore) else {
            throw SdkValidationError(
                field: "has_more",
                code: "required",
                message: "Validation failed for 'has_more': value is required"
            )
        }
        guard container.contains(.object) else {
            throw SdkValidationError(
                field: "object",
                code: "required",
                message: "Validation failed for 'object': value is required"
            )
        }
        guard container.contains(.url) else {
            throw SdkValidationError(
                field: "url",
                code: "required",
                message: "Validation failed for 'url': value is required"
            )
        }
        data = try container.sdkDecodeRequired(.data)
        hasMore = try container.sdkDecodeRequired(.hasMore)
        object = try container.sdkDecodeRequired(.object)
        url = try container.sdkDecodeRequired(.url)
        try validateLength("url", url, min: nil, max: 5000)
    }
}

public extension AccountExternalAccounts {
    init(
        data: [AccountExternalAccountsDataItem],
        hasMore: Bool,
        object: AccountExternalAccountsObject,
        url: String
    ) throws {
        (self.data, self.hasMore) = (data, hasMore)
        (self.object, self.url) = (object, url)
        try validateLength("url", self.url, min: nil, max: 5000)
    }
}

public enum AccountGroups {
    case accountGroupMembership(AccountGroupMembership)
}

extension AccountGroups: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(in: container, debugDescription: "No variant matched for AccountGroups")
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(AccountGroupMembership.self) {
            return .accountGroupMembership(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .accountGroupMembership(value): try container.encode(value); return true
        }
    }
}

public enum AccountSettingsXf131fa57 {
    case accountSettings(AccountSettings)
}

extension AccountSettingsXf131fa57: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for AccountSettingsXf131fa57"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(AccountSettings.self) {
            return .accountSettings(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .accountSettings(value): try container.encode(value); return true
        }
    }
}

/// Typed representation of the `AccountInvoicesSettings` API schema.
public struct AccountInvoicesSettings: Codable {
    /// The list of default Account Tax IDs to automatically include on invoices. Account Tax IDs get added when an
    /// invoice is finalized.
    public var defaultAccountTaxIds: [AccountInvoicesSettingsDefaultAccountTaxIdsItem]?
    /// Whether to save the payment method after a payment is completed for a one-time invoice or a subscription
    /// invoice when the customer already has a default payment method on the hosted invoice page.
    public var hostedPaymentMethodSave: AccountInvoicesSettingsHostedPaymentMethodSave?

    enum CodingKeys: String, CodingKey {
        case defaultAccountTaxIds = "default_account_tax_ids"
        case hostedPaymentMethodSave = "hosted_payment_method_save"
    }

    init() {
        (defaultAccountTaxIds, hostedPaymentMethodSave) = (nil, nil)
    }
}

public extension AccountInvoicesSettings {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        defaultAccountTaxIds = try container.sdkDecodeIfPresent(.defaultAccountTaxIds)
        hostedPaymentMethodSave = try container.sdkDecodeIfPresent(.hostedPaymentMethodSave)
    }
}

public extension AccountInvoicesSettings {
    init(
        defaultAccountTaxIds: [AccountInvoicesSettingsDefaultAccountTaxIdsItem]? = nil,
        hostedPaymentMethodSave: AccountInvoicesSettingsHostedPaymentMethodSave? = nil
    ) {
        self.init()
        self.defaultAccountTaxIds = defaultAccountTaxIds
        self.hostedPaymentMethodSave = hostedPaymentMethodSave
    }
}

public enum AccountInvoicesSettingsDefaultAccountTaxIdsItem {
    case stringValue(String)
    case taxId(TaxId)
}

extension AccountInvoicesSettingsDefaultAccountTaxIdsItem: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for AccountInvoicesSettingsDefaultAccountTaxIdsItem"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container.decode(TaxId.self) {
            return .taxId(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .taxId(value): try container.encode(value); return true
        }
    }
}

/// Typed representation of the `AccountSettings` API schema.
public struct AccountSettings: Codable {
    /// Required object value serialized in the `branding` wire field.
    public var branding: AccountBrandingSettings
    /// Required object value serialized in the `card_payments` wire field.
    public var cardPayments: AccountCardPaymentsSettings
    /// Required object value serialized in the `dashboard` wire field.
    public var dashboard: AccountDashboardSettings
    /// Required object value serialized in the `payments` wire field.
    public var payments: AccountPaymentsSettings
    /// Optional object value serialized in the `bacs_debit_payments` wire field.
    public var bacsDebitPayments: AccountBacsDebitPaymentsSettings?
    /// Optional object value serialized in the `card_issuing` wire field.
    public var cardIssuing: AccountCardIssuingSettings?
    /// Optional object value serialized in the `invoices` wire field.
    public var invoices: AccountInvoicesSettings?
    /// Optional object value serialized in the `payouts` wire field.
    public var payouts: AccountPayoutSettings?
    /// Optional object value serialized in the `sepa_debit_payments` wire field.
    public var sepaDebitPayments: AccountSepaDebitPaymentsSettings?
    /// Optional object value serialized in the `treasury` wire field.
    public var treasury: AccountTreasurySettings?

    enum CodingKeys: String, CodingKey {
        case branding
        case cardPayments = "card_payments"
        case dashboard
        case payments
        case bacsDebitPayments = "bacs_debit_payments"
        case cardIssuing = "card_issuing"
        case invoices
        case payouts
        case sepaDebitPayments = "sepa_debit_payments"
        case treasury
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension AccountSettings {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.branding) else {
            throw SdkValidationError(
                field: "branding",
                code: "required",
                message: "Validation failed for 'branding': value is required"
            )
        }
        guard container.contains(.cardPayments) else {
            throw SdkValidationError(
                field: "card_payments",
                code: "required",
                message: "Validation failed for 'card_payments': value is required"
            )
        }
        guard container.contains(.dashboard) else {
            throw SdkValidationError(
                field: "dashboard",
                code: "required",
                message: "Validation failed for 'dashboard': value is required"
            )
        }
        guard container.contains(.payments) else {
            throw SdkValidationError(
                field: "payments",
                code: "required",
                message: "Validation failed for 'payments': value is required"
            )
        }
        branding = try container.sdkDecodeRequired(.branding)
        cardPayments = try container.sdkDecodeRequired(.cardPayments)
        dashboard = try container.sdkDecodeRequired(.dashboard)
        payments = try container.sdkDecodeRequired(.payments)
        bacsDebitPayments = try container.sdkDecodeIfPresent(.bacsDebitPayments)
        cardIssuing = try container.sdkDecodeIfPresent(.cardIssuing)
        invoices = try container.sdkDecodeIfPresent(.invoices)
        payouts = try container.sdkDecodeIfPresent(.payouts)
        sepaDebitPayments = try container.sdkDecodeIfPresent(.sepaDebitPayments)
        treasury = try container.sdkDecodeIfPresent(.treasury)
    }
}

public extension AccountSettings {
    init(
        branding: AccountBrandingSettings,
        cardPayments: AccountCardPaymentsSettings,
        dashboard: AccountDashboardSettings,
        payments: AccountPaymentsSettings,
        bacsDebitPayments: AccountBacsDebitPaymentsSettings? = nil,
        cardIssuing: AccountCardIssuingSettings? = nil,
        invoices: AccountInvoicesSettings? = nil,
        payouts: AccountPayoutSettings? = nil,
        sepaDebitPayments: AccountSepaDebitPaymentsSettings? = nil,
        treasury: AccountTreasurySettings? = nil
    ) {
        (self.branding, self.cardPayments) = (branding, cardPayments)
        (self.dashboard, self.payments) = (dashboard, payments)
        (self.bacsDebitPayments, self.cardIssuing) = (bacsDebitPayments, cardIssuing)
        (self.invoices, self.payouts) = (invoices, payouts)
        (self.sepaDebitPayments, self.treasury) = (sepaDebitPayments, treasury)
    }
}

/// Typed representation of the `AmazonPayUnderlyingPaymentMethodFundingDetails` API schema.
public struct AmazonPayUnderlyingPaymentMethodFundingDetails: Codable {
    /// Optional object value serialized in the `card` wire field.
    public var card: PaymentMethodDetailsPassthroughCard?
    /// funding type of the underlying payment method.
    public var type: AmazonPayUnderlyingPaymentMethodFundingDetailsType?

    enum CodingKeys: String, CodingKey {
        case card
        case type
    }

    init() {
        (card, type) = (nil, nil)
    }
}

public extension AmazonPayUnderlyingPaymentMethodFundingDetails {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        card = try container.sdkDecodeIfPresent(.card)
        type = try container.sdkDecodeIfPresent(.type)
    }
}

public extension AmazonPayUnderlyingPaymentMethodFundingDetails {
    init(
        card: PaymentMethodDetailsPassthroughCard? = nil,
        type: AmazonPayUnderlyingPaymentMethodFundingDetailsType? = nil
    ) {
        self.init()
        (self.card, self.type) = (card, type)
    }
}

/// Typed representation of the `ApiErrors` API schema.
public struct ApiErrors: Codable {
    /// The type of error returned. One of `api_error`, `card_error`, `idempotency_error`, or
    /// `invalid_request_error`
    public var type: ApiErrorsType
    /// For card errors resulting from a card issuer decline, a short string indicating how to proceed with an error
    /// if they provide one.
    public var adviceCode: String?
    /// For card errors, the ID of the failed charge.
    public var charge: String?
    /// For some errors that could be handled programmatically, a short string indicating the error code reported.
    public var code: String?
    /// For card errors resulting from a card issuer decline, a short string indicating the card issuer's reason for
    /// the decline if they provide one.
    public var declineCode: String?
    /// A URL to more information about the error code reported.
    public var docUrl: String?
    /// A human-readable message providing more details about the error. For card errors, these messages can be
    /// shown to your users.
    public var message: String?
    /// For card errors resulting from a card issuer decline, a 2 digit code which indicates the advice given to
    /// merchant by the card network on how to proceed with an error.
    public var networkAdviceCode: String?
    /// For payments declined by the network, an alphanumeric code which indicates the reason the payment failed.
    public var networkDeclineCode: String?
    /// If the error is parameter-specific, the parameter related to the error. For example, you can use this to
    /// display a message near the correct form field.
    public var param: String?
    /// A PaymentIntent guides you through the process of collecting a payment from your customer. We recommend that
    /// you create exactly one PaymentIntent for each order or customer session in your system. You can reference
    /// the PaymentIntent later to see the history of payment attempts for a particular session. A PaymentIntent
    /// transitions through multiple statuses throughout its lifetime as it interfaces with Stripe.js to perform
    /// authentication flows and ultimately creates at most…
    public var paymentIntent: SdkBox<PaymentIntent>?
    /// PaymentMethod objects represent your customer's payment instruments. You can use them with PaymentIntents to
    /// collect payments or save them to Customer objects to store instrument details for future payments. Related
    /// guides: Payment Methods and More Payment Scenarios.
    public var paymentMethod: PaymentMethod?
    /// If the error is specific to the type of payment method, the payment method type that had a problem. This
    /// field is only populated for invoice-related errors.
    public var paymentMethodType: String?
    /// A URL to the request log entry in your dashboard.
    public var requestLogUrl: String?
    /// A SetupIntent guides you through the process of setting up and saving a customer's payment credentials for
    /// future payments. For example, you can use a SetupIntent to set up and save your customer's card without
    /// immediately collecting a payment. Later, you can use PaymentIntents to drive the payment flow. Create a
    /// SetupIntent when you're ready to collect your customer's payment credentials. Don't maintain long-lived,
    /// unconfirmed SetupIntents because they might not be valid.…
    public var setupIntent: SdkBox<SetupIntent>?
    /// The source object for errors returned on a request involving a source.
    public var source: ApiErrorsSource?

    enum CodingKeys: String, CodingKey {
        case type
        case adviceCode = "advice_code"
        case charge
        case code
        case declineCode = "decline_code"
        case docUrl = "doc_url"
        case message
        case networkAdviceCode = "network_advice_code"
        case networkDeclineCode = "network_decline_code"
        case param
        case paymentIntent = "payment_intent"
        case paymentMethod = "payment_method"
        case paymentMethodType = "payment_method_type"
        case requestLogUrl = "request_log_url"
        case setupIntent = "setup_intent"
        case source
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension ApiErrors {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.type) else {
            throw SdkValidationError(
                field: "type",
                code: "required",
                message: "Validation failed for 'type': value is required"
            )
        }
        type = try container.sdkDecodeRequired(.type)
        adviceCode = try container.sdkDecodeIfPresent(.adviceCode)
        charge = try container.sdkDecodeIfPresent(.charge)
        code = try container.sdkDecodeIfPresent(.code)
        declineCode = try container.sdkDecodeIfPresent(.declineCode)
        docUrl = try container.sdkDecodeIfPresent(.docUrl)
        message = try container.sdkDecodeIfPresent(.message)
        networkAdviceCode = try container.sdkDecodeIfPresent(.networkAdviceCode)
        networkDeclineCode = try container.sdkDecodeIfPresent(.networkDeclineCode)
        param = try container.sdkDecodeIfPresent(.param)
        paymentIntent = try container.sdkDecodeIfPresent(.paymentIntent)
        paymentMethod = try container.sdkDecodeIfPresent(.paymentMethod)
        paymentMethodType = try container.sdkDecodeIfPresent(.paymentMethodType)
        requestLogUrl = try container.sdkDecodeIfPresent(.requestLogUrl)
        setupIntent = try container.sdkDecodeIfPresent(.setupIntent)
        source = try container.sdkDecodeIfPresent(.source)
        try sdkValidateConstraints()
    }
}

public extension ApiErrors {
    init(
        type: ApiErrorsType,
        adviceCode: String? = nil,
        charge: String? = nil,
        code: String? = nil,
        declineCode: String? = nil,
        docUrl: String? = nil,
        message: String? = nil,
        networkAdviceCode: String? = nil,
        networkDeclineCode: String? = nil,
        param: String? = nil,
        paymentIntent: SdkBox<PaymentIntent>? = nil,
        paymentMethod: PaymentMethod? = nil,
        paymentMethodType: String? = nil,
        requestLogUrl: String? = nil,
        setupIntent: SdkBox<SetupIntent>? = nil,
        source: ApiErrorsSource? = nil
    ) throws {
        (self.type, self.adviceCode) = (type, adviceCode)
        (self.charge, self.code) = (charge, code)
        (self.declineCode, self.docUrl) = (declineCode, docUrl)
        (self.message, self.networkAdviceCode) = (message, networkAdviceCode)
        (self.networkDeclineCode, self.param) = (networkDeclineCode, param)
        (self.paymentIntent, self.paymentMethod) = (paymentIntent, paymentMethod)
        (self.paymentMethodType, self.requestLogUrl) = (paymentMethodType, requestLogUrl)
        (self.setupIntent, self.source) = (setupIntent, source)
        try sdkValidateConstraints()
    }
}

extension ApiErrors {
    func sdkValidateConstraints() throws {
        if let value = adviceCode {
            try validateLength("advice_code", value, min: nil, max: 5000)
        }
        if let value = charge {
            try validateLength("charge", value, min: nil, max: 5000)
        }
        if let value = code {
            try validateLength("code", value, min: nil, max: 5000)
        }
        if let value = declineCode {
            try validateLength("decline_code", value, min: nil, max: 5000)
        }
        if let value = docUrl {
            try validateLength("doc_url", value, min: nil, max: 5000)
        }
        if let value = message {
            try validateLength("message", value, min: nil, max: 40000)
        }
        if let value = networkAdviceCode {
            try validateLength("network_advice_code", value, min: nil, max: 5000)
        }
        if let value = networkDeclineCode {
            try validateLength("network_decline_code", value, min: nil, max: 5000)
        }
        if let value = param {
            try validateLength("param", value, min: nil, max: 5000)
        }
        if let value = paymentMethodType {
            try validateLength("payment_method_type", value, min: nil, max: 5000)
        }
        if let value = requestLogUrl {
            try validateLength("request_log_url", value, min: nil, max: 5000)
        }
    }
}

/// Typed representation of the `ApplePayDomain` API schema.
public struct ApplePayDomain: Codable {
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// Required `string` value serialized in the `domain_name` wire field.
    public var domainName: String
    /// Unique identifier for the object.
    public var id: String
    /// If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is
    /// `false`.
    public var livemode: Bool
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: ApplePayDomainObject

    enum CodingKeys: String, CodingKey {
        case created
        case domainName = "domain_name"
        case id
        case livemode
        case object
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}
