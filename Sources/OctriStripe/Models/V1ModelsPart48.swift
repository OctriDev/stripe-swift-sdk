// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

/// V1 domain models
/// To top up your Stripe balance, you create a top-up object. You can retrieve individual top-ups, as well as list
/// all top-ups. Top-ups are identified by a unique, random ID. Related guide: Topping up your platform account
public struct Topup: Codable {
    /// Amount transferred.
    public var amount: Int
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// Three-letter ISO currency code, in lowercase. Must be a supported currency.
    public var currency: String
    /// Unique identifier for the object.
    public var id: String
    /// If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is
    /// `false`.
    public var livemode: Bool
    /// Set of key-value pairs that you can attach to an object. This can be useful for storing additional
    /// information about the object in a structured format.
    public var metadata: [String: String]
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: TopupObject
    /// The status of the top-up is either `canceled`, `failed`, `pending`, `reversed`, or `succeeded`.
    public var status: TopupStatus
    /// ID of the balance transaction that describes the impact of this top-up on your account balance. May not be
    /// specified depending on status of top-up.
    public var balanceTransaction: TopupBalanceTransaction?
    /// An arbitrary string attached to the object. Often useful for displaying to users.
    public var description: String?
    /// Date the funds are expected to arrive in your Stripe account for payouts. This factors in delays like
    /// weekends or bank holidays. May not be specified depending on status of top-up.
    public var expectedAvailabilityDate: Int?
    /// Error code explaining reason for top-up failure if available (see the errors section for a list of codes).
    public var failureCode: String?
    /// Message to user further explaining reason for top-up failure if available.
    public var failureMessage: String?
    /// Indicates whether the top-up was initiated by Stripe or by the user.
    public var initiatedBy: TopupInitiatedBy?
    /// The ID of a PaymentMethod representing the payment method used for the top-up. A PaymentMethod of type
    /// `us_bank_account` can be used.
    public var paymentMethod: TopupPaymentMethod?
    /// Payment-method-specific configuration for this top-up.
    public var paymentMethodOptions: TopupPaymentMethodOptions?
    /// The source field is deprecated. It might not always be present in the API response.
    public var source: TopupSource?
    /// Extra information about a top-up. This will appear on your source's bank statement. It must contain at least
    /// one letter.
    public var statementDescriptor: String?
    /// A string that identifies this top-up as part of a group.
    public var transferGroup: String?

    enum CodingKeys: String, CodingKey {
        case amount
        case created
        case currency
        case id
        case livemode
        case metadata
        case object
        case status
        case balanceTransaction = "balance_transaction"
        case description
        case expectedAvailabilityDate = "expected_availability_date"
        case failureCode = "failure_code"
        case failureMessage = "failure_message"
        case initiatedBy = "initiated_by"
        case paymentMethod = "payment_method"
        case paymentMethodOptions = "payment_method_options"
        case source
        case statementDescriptor = "statement_descriptor"
        case transferGroup = "transfer_group"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension Topup {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        amount = try container.sdkDecodeRequired(.amount)
        created = try container.sdkDecodeRequired(.created)
        currency = try container.sdkDecodeRequired(.currency)
        id = try container.sdkDecodeRequired(.id)
        livemode = try container.sdkDecodeRequired(.livemode)
        metadata = try container.sdkDecodeRequired(.metadata)
        object = try container.sdkDecodeRequired(.object)
        status = try container.sdkDecodeRequired(.status)
        balanceTransaction = try container.sdkDecodeIfPresent(.balanceTransaction)
        description = try container.sdkDecodeIfPresent(.description)
        expectedAvailabilityDate = try container.sdkDecodeIfPresent(.expectedAvailabilityDate)
        failureCode = try container.sdkDecodeIfPresent(.failureCode)
        failureMessage = try container.sdkDecodeIfPresent(.failureMessage)
        initiatedBy = try container.sdkDecodeIfPresent(.initiatedBy)
        paymentMethod = try container.sdkDecodeIfPresent(.paymentMethod)
        paymentMethodOptions = try container.sdkDecodeIfPresent(.paymentMethodOptions)
        source = try container.sdkDecodeIfPresent(.source)
        statementDescriptor = try container.sdkDecodeIfPresent(.statementDescriptor)
        transferGroup = try container.sdkDecodeIfPresent(.transferGroup)
        try sdkValidateConstraints()
    }
}

public extension Topup {
    init(
        amount: Int,
        created: Int,
        currency: String,
        id: String,
        livemode: Bool,
        metadata: [String: String],
        object: TopupObject,
        status: TopupStatus,
        balanceTransaction: TopupBalanceTransaction? = nil,
        description: String? = nil,
        expectedAvailabilityDate: Int? = nil,
        failureCode: String? = nil,
        failureMessage: String? = nil,
        initiatedBy: TopupInitiatedBy? = nil,
        paymentMethod: TopupPaymentMethod? = nil,
        paymentMethodOptions: TopupPaymentMethodOptions? = nil,
        source: TopupSource? = nil,
        statementDescriptor: String? = nil,
        transferGroup: String? = nil
    ) throws {
        (self.amount, self.created) = (amount, created)
        (self.currency, self.id) = (currency, id)
        (self.livemode, self.metadata) = (livemode, metadata)
        (self.object, self.status) = (object, status)
        (self.balanceTransaction, self.description) = (balanceTransaction, description)
        (self.expectedAvailabilityDate, self.failureCode) = (expectedAvailabilityDate, failureCode)
        (self.failureMessage, self.initiatedBy) = (failureMessage, initiatedBy)
        (self.paymentMethod, self.paymentMethodOptions) = (paymentMethod, paymentMethodOptions)
        (self.source, self.statementDescriptor) = (source, statementDescriptor)
        self.transferGroup = transferGroup
        try sdkValidateConstraints()
    }
}

extension Topup {
    func sdkValidateConstraints() throws {
        try validateLength("currency", currency, min: nil, max: 5000)
        try validateLength("id", id, min: nil, max: 5000)
        if let value = description {
            try validateLength("description", value, min: nil, max: 5000)
        }
        if let value = failureCode {
            try validateLength("failure_code", value, min: nil, max: 5000)
        }
        if let value = failureMessage {
            try validateLength("failure_message", value, min: nil, max: 5000)
        }
        if let value = statementDescriptor {
            try validateLength("statement_descriptor", value, min: nil, max: 5000)
        }
        if let value = transferGroup {
            try validateLength("transfer_group", value, min: nil, max: 5000)
        }
    }
}

public indirect enum TopupBalanceTransaction {
    case stringValue(String)
    case balanceTransaction(BalanceTransaction)
}

extension TopupBalanceTransaction: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for TopupBalanceTransaction"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container.decode(BalanceTransaction.self) {
            return .balanceTransaction(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .balanceTransaction(value): try container.encode(value); return true
        }
    }
}

public indirect enum TopupPaymentMethod {
    case stringValue(String)
    case paymentMethod(PaymentMethod)
}

extension TopupPaymentMethod: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for TopupPaymentMethod"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container.decode(PaymentMethod.self) {
            return .paymentMethod(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .paymentMethod(value): try container.encode(value); return true
        }
    }
}

public enum TopupPaymentMethodOptions {
    case topupResourcePaymentMethodOptions(TopupResourcePaymentMethodOptions)
}

extension TopupPaymentMethodOptions: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for TopupPaymentMethodOptions"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container
            .decode(TopupResourcePaymentMethodOptions.self) {
            return .topupResourcePaymentMethodOptions(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .topupResourcePaymentMethodOptions(value): try container.encode(value); return true
        }
    }
}

public enum TopupSource {
    case source(Source)
}

extension TopupSource: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(in: container, debugDescription: "No variant matched for TopupSource")
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(Source.self) {
            return .source(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .source(value): try container.encode(value); return true
        }
    }
}

/// A `Transfer` object is created when you move funds between Stripe accounts as part of Connect. Before April 6,
/// 2017, transfers also represented movement of funds from a Stripe account to a card or bank account. This
/// behavior has since been split out into a Payout object, with corresponding payout endpoints. For more
/// information, read about the transfer/payout split. Related guide: Creating separate charges and transfers
public struct Transfer: Codable {
    /// Amount in cents (or local equivalent) to be transferred.
    public var amount: Int
    /// Amount in cents (or local equivalent) reversed (can be less than the amount attribute on the transfer if a
    /// partial reversal was issued).
    public var amountReversed: Int
    /// Time that this record of the transfer was first created.
    public var created: Int
    /// Three-letter ISO currency code, in lowercase. Must be a supported currency.
    public var currency: String
    /// Unique identifier for the object.
    public var id: String
    /// If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is
    /// `false`.
    public var livemode: Bool
    /// Set of key-value pairs that you can attach to an object. This can be useful for storing additional
    /// information about the object in a structured format.
    public var metadata: [String: String]
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: TransferObject
    /// A list of reversals that have been applied to the transfer.
    public var reversals: TransferReversals
    /// Whether the transfer has been fully reversed. If the transfer is only partially reversed, this attribute
    /// will still be false.
    public var reversed: Bool
    /// Balance transaction that describes the impact of this transfer on your account balance.
    public var balanceTransaction: TransferBalanceTransaction?
    /// An arbitrary string attached to the object. Often useful for displaying to users.
    public var description: String?
    /// ID of the Stripe account the transfer was sent to.
    public var destination: TransferDestination?
    /// If the destination is a Stripe account, this will be the ID of the payment that the destination account
    /// received for the transfer.
    public var destinationPayment: TransferDestinationPayment?
    /// ID of the charge that was used to fund the transfer. If null, the transfer was funded from the available
    /// balance.
    public var sourceTransaction: TransferSourceTransaction?
    /// The source balance this transfer came from. One of `card`, `fpx`, or `bank_account`.
    public var sourceType: String?
    /// A string that identifies this transaction as part of a group. See the Connect documentation for details.
    public var transferGroup: String?

    enum CodingKeys: String, CodingKey {
        case amount
        case amountReversed = "amount_reversed"
        case created
        case currency
        case id
        case livemode
        case metadata
        case object
        case reversals
        case reversed
        case balanceTransaction = "balance_transaction"
        case description
        case destination
        case destinationPayment = "destination_payment"
        case sourceTransaction = "source_transaction"
        case sourceType = "source_type"
        case transferGroup = "transfer_group"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension Transfer {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        amount = try container.sdkDecodeRequired(.amount)
        amountReversed = try container.sdkDecodeRequired(.amountReversed)
        created = try container.sdkDecodeRequired(.created)
        currency = try container.sdkDecodeRequired(.currency)
        id = try container.sdkDecodeRequired(.id)
        livemode = try container.sdkDecodeRequired(.livemode)
        metadata = try container.sdkDecodeRequired(.metadata)
        object = try container.sdkDecodeRequired(.object)
        reversals = try container.sdkDecodeRequired(.reversals)
        reversed = try container.sdkDecodeRequired(.reversed)
        balanceTransaction = try container.sdkDecodeIfPresent(.balanceTransaction)
        description = try container.sdkDecodeIfPresent(.description)
        destination = try container.sdkDecodeIfPresent(.destination)
        destinationPayment = try container.sdkDecodeIfPresent(.destinationPayment)
        sourceTransaction = try container.sdkDecodeIfPresent(.sourceTransaction)
        sourceType = try container.sdkDecodeIfPresent(.sourceType)
        transferGroup = try container.sdkDecodeIfPresent(.transferGroup)
        try validateLength("id", id, min: nil, max: 5000)
        if let value = description {
            try validateLength("description", value, min: nil, max: 5000)
        }
        if let value = sourceType {
            try validateLength("source_type", value, min: nil, max: 5000)
        }
        if let value = transferGroup {
            try validateLength("transfer_group", value, min: nil, max: 5000)
        }
    }
}

public extension Transfer {
    init(
        amount: Int,
        amountReversed: Int,
        created: Int,
        currency: String,
        id: String,
        livemode: Bool,
        metadata: [String: String],
        object: TransferObject,
        reversals: TransferReversals,
        reversed: Bool,
        balanceTransaction: TransferBalanceTransaction? = nil,
        description: String? = nil,
        destination: TransferDestination? = nil,
        destinationPayment: TransferDestinationPayment? = nil,
        sourceTransaction: TransferSourceTransaction? = nil,
        sourceType: String? = nil,
        transferGroup: String? = nil
    ) throws {
        (self.amount, self.amountReversed) = (amount, amountReversed)
        (self.created, self.currency) = (created, currency)
        (self.id, self.livemode) = (id, livemode)
        (self.metadata, self.object) = (metadata, object)
        (self.reversals, self.reversed) = (reversals, reversed)
        (self.balanceTransaction, self.description) = (balanceTransaction, description)
        (self.destination, self.destinationPayment) = (destination, destinationPayment)
        (self.sourceTransaction, self.sourceType) = (sourceTransaction, sourceType)
        self.transferGroup = transferGroup
        try validateLength("id", self.id, min: nil, max: 5000)
        if let value = self.description {
            try validateLength("description", value, min: nil, max: 5000)
        }
        if let value = self.sourceType {
            try validateLength("source_type", value, min: nil, max: 5000)
        }
        if let value = self.transferGroup {
            try validateLength("transfer_group", value, min: nil, max: 5000)
        }
    }
}

public indirect enum TransferBalanceTransaction {
    case stringValue(String)
    case balanceTransaction(BalanceTransaction)
}

extension TransferBalanceTransaction: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for TransferBalanceTransaction"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container.decode(BalanceTransaction.self) {
            return .balanceTransaction(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .balanceTransaction(value): try container.encode(value); return true
        }
    }
}

public enum TransferDestination {
    case stringValue(String)
    case account(Account)
}

extension TransferDestination: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for TransferDestination"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container.decode(Account.self) {
            return .account(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .account(value): try container.encode(value); return true
        }
    }
}

public indirect enum TransferDestinationPayment {
    case stringValue(String)
    case charge(Charge)
}

extension TransferDestinationPayment: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for TransferDestinationPayment"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container.decode(Charge.self) {
            return .charge(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .charge(value): try container.encode(value); return true
        }
    }
}

/// A list of reversals that have been applied to the transfer.
public struct TransferReversals: Codable {
    /// Details about each object.
    public var data: [TransferReversal]
    /// True if this list has another page of items after this one that can be fetched.
    public var hasMore: Bool
    /// String representing the object's type. Objects of the same type share the same value. Always has the value
    /// `list`.
    public var object: TransferReversalsObject
    /// The URL where this list can be accessed.
    public var url: String

    enum CodingKeys: String, CodingKey {
        case data
        case hasMore = "has_more"
        case object
        case url
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension TransferReversals {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.data) else {
            throw SdkValidationError(
                field: "data",
                code: "required",
                message: "Validation failed for 'data': value is required"
            )
        }
        guard container.contains(.hasMore) else {
            throw SdkValidationError(
                field: "has_more",
                code: "required",
                message: "Validation failed for 'has_more': value is required"
            )
        }
        guard container.contains(.object) else {
            throw SdkValidationError(
                field: "object",
                code: "required",
                message: "Validation failed for 'object': value is required"
            )
        }
        guard container.contains(.url) else {
            throw SdkValidationError(
                field: "url",
                code: "required",
                message: "Validation failed for 'url': value is required"
            )
        }
        data = try container.sdkDecodeRequired(.data)
        hasMore = try container.sdkDecodeRequired(.hasMore)
        object = try container.sdkDecodeRequired(.object)
        url = try container.sdkDecodeRequired(.url)
        try validateLength("url", url, min: nil, max: 5000)
    }
}

public extension TransferReversals {
    init(data: [TransferReversal], hasMore: Bool, object: TransferReversalsObject, url: String) throws {
        (self.data, self.hasMore) = (data, hasMore)
        (self.object, self.url) = (object, url)
        try validateLength("url", self.url, min: nil, max: 5000)
    }
}

public indirect enum TransferSourceTransaction {
    case stringValue(String)
    case charge(Charge)
}
