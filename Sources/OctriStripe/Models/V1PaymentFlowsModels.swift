// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

/// V1PaymentFlows domain models
/// Typed representation of the `PaymentFlowsAmountDetails` API schema.
public struct PaymentFlowsAmountDetails: Codable {
    /// The total discount applied on the transaction represented in the smallest currency unit. An integer greater
    /// than 0. This field is mutually exclusive with the `amount_details[line_items][#][discount_amount]` field.
    public var discountAmount: Int?
    /// Optional object value serialized in the `error` wire field.
    public var error: PaymentFlowsAmountDetailsResourceError?
    /// A list of line items, each containing information about a product in the PaymentIntent. There is a maximum
    /// of 200 line items.
    public var lineItems: PaymentFlowsAmountDetailsLineItems?
    /// Optional object value serialized in the `shipping` wire field.
    public var shipping: PaymentFlowsAmountDetailsResourceShipping?
    /// Optional object value serialized in the `tax` wire field.
    public var tax: PaymentFlowsAmountDetailsResourceTax?
    /// Optional object value serialized in the `tip` wire field.
    public var tip: PaymentFlowsAmountDetailsClientResourceTip?

    enum CodingKeys: String, CodingKey {
        case discountAmount = "discount_amount"
        case error
        case lineItems = "line_items"
        case shipping
        case tax
        case tip
    }

    init() {
        (discountAmount, error, lineItems, shipping, tax) = (nil, nil, nil, nil, nil)
        tip = nil
    }
}

public extension PaymentFlowsAmountDetails {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        discountAmount = try container.sdkDecodeIfPresent(.discountAmount)
        error = try container.sdkDecodeIfPresent(.error)
        lineItems = try container.sdkDecodeIfPresent(.lineItems)
        shipping = try container.sdkDecodeIfPresent(.shipping)
        tax = try container.sdkDecodeIfPresent(.tax)
        tip = try container.sdkDecodeIfPresent(.tip)
    }
}

public extension PaymentFlowsAmountDetails {
    init(
        discountAmount: Int? = nil,
        error: PaymentFlowsAmountDetailsResourceError? = nil,
        lineItems: PaymentFlowsAmountDetailsLineItems? = nil,
        shipping: PaymentFlowsAmountDetailsResourceShipping? = nil,
        tax: PaymentFlowsAmountDetailsResourceTax? = nil,
        tip: PaymentFlowsAmountDetailsClientResourceTip? = nil
    ) {
        self.init()
        (self.discountAmount, self.error) = (discountAmount, error)
        (self.lineItems, self.shipping) = (lineItems, shipping)
        (self.tax, self.tip) = (tax, tip)
    }
}

/// A list of line items, each containing information about a product in the PaymentIntent. There is a maximum of
/// 200 line items.
public struct PaymentFlowsAmountDetailsLineItems: Codable {
    /// Details about each object.
    public var data: [PaymentIntentAmountDetailsLineItem]
    /// True if this list has another page of items after this one that can be fetched.
    public var hasMore: Bool
    /// String representing the object's type. Objects of the same type share the same value. Always has the value
    /// `list`.
    public var object: PaymentFlowsAmountDetailsLineItemsObject
    /// The URL where this list can be accessed.
    public var url: String

    enum CodingKeys: String, CodingKey {
        case data
        case hasMore = "has_more"
        case object
        case url
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension PaymentFlowsAmountDetailsLineItems {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.data) else {
            throw SdkValidationError(
                field: "data",
                code: "required",
                message: "Validation failed for 'data': value is required"
            )
        }
        guard container.contains(.hasMore) else {
            throw SdkValidationError(
                field: "has_more",
                code: "required",
                message: "Validation failed for 'has_more': value is required"
            )
        }
        guard container.contains(.object) else {
            throw SdkValidationError(
                field: "object",
                code: "required",
                message: "Validation failed for 'object': value is required"
            )
        }
        guard container.contains(.url) else {
            throw SdkValidationError(
                field: "url",
                code: "required",
                message: "Validation failed for 'url': value is required"
            )
        }
        data = try container.sdkDecodeRequired(.data)
        hasMore = try container.sdkDecodeRequired(.hasMore)
        object = try container.sdkDecodeRequired(.object)
        url = try container.sdkDecodeRequired(.url)
        try validateLength("url", url, min: nil, max: 5000)
    }
}

public extension PaymentFlowsAmountDetailsLineItems {
    init(
        data: [PaymentIntentAmountDetailsLineItem],
        hasMore: Bool,
        object: PaymentFlowsAmountDetailsLineItemsObject,
        url: String
    ) throws {
        (self.data, self.hasMore) = (data, hasMore)
        (self.object, self.url) = (object, url)
        try validateLength("url", self.url, min: nil, max: 5000)
    }
}

/// Typed representation of the `PaymentFlowsAmountDetailsClient` API schema.
public struct PaymentFlowsAmountDetailsClient: Codable {
    /// Optional object value serialized in the `tip` wire field.
    public var tip: PaymentFlowsAmountDetailsClientResourceTip?

    enum CodingKeys: String, CodingKey {
        case tip
    }

    init() {
        tip = nil
    }
}

public extension PaymentFlowsAmountDetailsClient {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        tip = try container.sdkDecodeIfPresent(.tip)
    }
}

public extension PaymentFlowsAmountDetailsClient {
    init(tip: PaymentFlowsAmountDetailsClientResourceTip? = nil) {
        self.init()
        self.tip = tip
    }
}

/// Typed representation of the `PaymentFlowsAmountDetailsClientResourceTip` API schema.
public struct PaymentFlowsAmountDetailsClientResourceTip: Codable {
    /// Portion of the amount that corresponds to a tip.
    public var amount: Int?

    enum CodingKeys: String, CodingKey {
        case amount
    }

    init() {
        amount = nil
    }
}

public extension PaymentFlowsAmountDetailsClientResourceTip {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        amount = try container.sdkDecodeIfPresent(.amount)
    }
}

public extension PaymentFlowsAmountDetailsClientResourceTip {
    init(amount: Int? = nil) {
        self.init()
        self.amount = amount
    }
}

/// Typed representation of the `PaymentFlowsAmountDetailsResourceError` API schema.
public struct PaymentFlowsAmountDetailsResourceError: Codable {
    /// The code of the error that occurred when validating the current amount details.
    public var code: PaymentFlowsAmountDetailsResourceErrorCode?
    /// A message providing more details about the error.
    public var message: String?

    enum CodingKeys: String, CodingKey {
        case code
        case message
    }

    init() {
        (code, message) = (nil, nil)
    }
}

public extension PaymentFlowsAmountDetailsResourceError {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        code = try container.sdkDecodeIfPresent(.code)
        message = try container.sdkDecodeIfPresent(.message)
        if let value = message {
            try validateLength("message", value, min: nil, max: 5000)
        }
    }
}

public extension PaymentFlowsAmountDetailsResourceError {
    init(code: PaymentFlowsAmountDetailsResourceErrorCode? = nil, message: String? = nil) throws {
        self.init()
        (self.code, self.message) = (code, message)
        if let value = self.message {
            try validateLength("message", value, min: nil, max: 5000)
        }
    }
}

/// Typed representation of the
/// `PaymentFlowsAmountDetailsResourceLineItemsListResourceLineItemResourcePaymentMethodOptions` API schema.
public struct PaymentFlowsAmountDetailsResourceLineItemsListResourceLineIteX05de09564a: Codable {
    /// Optional object value serialized in the `card` wire field.
    public var card: PaymentFlowsPrivatePaymentMethodsCardPaymentIntentAmountDetaiX7f988d235b?
    /// Optional object value serialized in the `card_present` wire field.
    public var cardPresent: PaymentFlowsPrivatePaymentMethodsCardPresentAmountDetailsLineX6a4e57089d?
    /// Optional object value serialized in the `klarna` wire field.
    public var klarna: PaymentFlowsPrivatePaymentMethodsKlarnaPaymentIntentAmountDetXc7e39e34a8?
    /// Optional object value serialized in the `paypal` wire field.
    public var paypal: PaymentFlowsPrivatePaymentMethodsPaypalAmountDetailsLineItemPX52b4174653?

    enum CodingKeys: String, CodingKey {
        case card
        case cardPresent = "card_present"
        case klarna
        case paypal
    }

    init() {
        (card, cardPresent, klarna, paypal) = (nil, nil, nil, nil)
    }
}

public extension PaymentFlowsAmountDetailsResourceLineItemsListResourceLineIteX05de09564a {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        card = try container.sdkDecodeIfPresent(.card)
        cardPresent = try container.sdkDecodeIfPresent(.cardPresent)
        klarna = try container.sdkDecodeIfPresent(.klarna)
        paypal = try container.sdkDecodeIfPresent(.paypal)
    }
}

public extension PaymentFlowsAmountDetailsResourceLineItemsListResourceLineIteX05de09564a {
    init(
        card: PaymentFlowsPrivatePaymentMethodsCardPaymentIntentAmountDetaiX7f988d235b? = nil,
        cardPresent: PaymentFlowsPrivatePaymentMethodsCardPresentAmountDetailsLineX6a4e57089d? = nil,
        klarna: PaymentFlowsPrivatePaymentMethodsKlarnaPaymentIntentAmountDetXc7e39e34a8? = nil,
        paypal: PaymentFlowsPrivatePaymentMethodsPaypalAmountDetailsLineItemPX52b4174653? = nil
    ) {
        self.init()
        (self.card, self.cardPresent) = (card, cardPresent)
        (self.klarna, self.paypal) = (klarna, paypal)
    }
}

/// Typed representation of the `PaymentFlowsAmountDetailsResourceLineItemsListResourceLineItemResourceTax` API
/// schema.
public struct PaymentFlowsAmountDetailsResourceLineItemsListResourceLineIteXf95fd11ec2: Codable {
    /// The total amount of tax on the transaction represented in the smallest currency unit. Required for L2 rates.
    /// An integer greater than or equal to 0. This field is mutually exclusive with the
    /// `amount_details[line_items][#][tax][total_tax_amount]` field.
    public var totalTaxAmount: Int

    enum CodingKeys: String, CodingKey {
        case totalTaxAmount = "total_tax_amount"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension PaymentFlowsAmountDetailsResourceLineItemsListResourceLineIteXf95fd11ec2 {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.totalTaxAmount) else {
            throw SdkValidationError(
                field: "total_tax_amount",
                code: "required",
                message: "Validation failed for 'total_tax_amount': value is required"
            )
        }
        totalTaxAmount = try container.sdkDecodeRequired(.totalTaxAmount)
    }
}

public extension PaymentFlowsAmountDetailsResourceLineItemsListResourceLineIteXf95fd11ec2 {
    init(totalTaxAmount: Int) {
        self.totalTaxAmount = totalTaxAmount
    }
}

/// Typed representation of the `PaymentFlowsAmountDetailsResourceShipping` API schema.
public struct PaymentFlowsAmountDetailsResourceShipping: Codable {
    /// If a physical good is being shipped, the cost of shipping represented in the smallest currency unit. An
    /// integer greater than or equal to 0.
    public var amount: Int?
    /// If a physical good is being shipped, the postal code of where it is being shipped from. At most 10
    /// alphanumeric characters long, hyphens and spaces are allowed.
    public var fromPostalCode: String?
    /// If a physical good is being shipped, the postal code of where it is being shipped to. At most 10
    /// alphanumeric characters long, hyphens and spaces are allowed.
    public var toPostalCode: String?

    enum CodingKeys: String, CodingKey {
        case amount
        case fromPostalCode = "from_postal_code"
        case toPostalCode = "to_postal_code"
    }

    init() {
        (amount, fromPostalCode, toPostalCode) = (nil, nil, nil)
    }
}

public extension PaymentFlowsAmountDetailsResourceShipping {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        amount = try container.sdkDecodeIfPresent(.amount)
        fromPostalCode = try container.sdkDecodeIfPresent(.fromPostalCode)
        toPostalCode = try container.sdkDecodeIfPresent(.toPostalCode)
        if let value = fromPostalCode {
            try validateLength("from_postal_code", value, min: nil, max: 5000)
        }
        if let value = toPostalCode {
            try validateLength("to_postal_code", value, min: nil, max: 5000)
        }
    }
}

public extension PaymentFlowsAmountDetailsResourceShipping {
    init(amount: Int? = nil, fromPostalCode: String? = nil, toPostalCode: String? = nil) throws {
        self.init()
        (self.amount, self.fromPostalCode) = (amount, fromPostalCode)
        self.toPostalCode = toPostalCode
        if let value = self.fromPostalCode {
            try validateLength("from_postal_code", value, min: nil, max: 5000)
        }
        if let value = self.toPostalCode {
            try validateLength("to_postal_code", value, min: nil, max: 5000)
        }
    }
}

/// Typed representation of the `PaymentFlowsAmountDetailsResourceTax` API schema.
public struct PaymentFlowsAmountDetailsResourceTax: Codable {
    /// The total amount of tax on the transaction represented in the smallest currency unit. Required for L2 rates.
    /// An integer greater than or equal to 0. This field is mutually exclusive with the
    /// `amount_details[line_items][#][tax][total_tax_amount]` field.
    public var totalTaxAmount: Int?

    enum CodingKeys: String, CodingKey {
        case totalTaxAmount = "total_tax_amount"
    }

    init() {
        totalTaxAmount = nil
    }
}

public extension PaymentFlowsAmountDetailsResourceTax {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        totalTaxAmount = try container.sdkDecodeIfPresent(.totalTaxAmount)
    }
}

public extension PaymentFlowsAmountDetailsResourceTax {
    init(totalTaxAmount: Int? = nil) {
        self.init()
        self.totalTaxAmount = totalTaxAmount
    }
}

/// Typed representation of the `PaymentFlowsAutomaticPaymentMethodsPaymentIntent` API schema.
public struct PaymentFlowsAutomaticPaymentMethodsPaymentIntent: Codable {
    /// Automatically calculates compatible payment methods
    public var enabled: Bool
    /// Controls whether this PaymentIntent will accept redirect-based payment methods. Redirect-based payment
    /// methods may require your customer to be redirected to a payment method's app or site for authentication or
    /// additional steps. To confirm this PaymentIntent, you may be required to provide a `return_url` to redirect
    /// customers back to your site after they authenticate or complete the payment.
    public var allowRedirects: PaymentFlowsAutomaticPaymentMethodsPaymentIntentAllowRedirects?

    enum CodingKeys: String, CodingKey {
        case enabled
        case allowRedirects = "allow_redirects"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension PaymentFlowsAutomaticPaymentMethodsPaymentIntent {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.enabled) else {
            throw SdkValidationError(
                field: "enabled",
                code: "required",
                message: "Validation failed for 'enabled': value is required"
            )
        }
        enabled = try container.sdkDecodeRequired(.enabled)
        allowRedirects = try container.sdkDecodeIfPresent(.allowRedirects)
    }
}

public extension PaymentFlowsAutomaticPaymentMethodsPaymentIntent {
    init(enabled: Bool, allowRedirects: PaymentFlowsAutomaticPaymentMethodsPaymentIntentAllowRedirects? = nil) {
        (self.enabled, self.allowRedirects) = (enabled, allowRedirects)
    }
}

/// Typed representation of the `PaymentFlowsAutomaticPaymentMethodsSetupIntent` API schema.
public struct PaymentFlowsAutomaticPaymentMethodsSetupIntent: Codable {
    /// Controls whether this SetupIntent will accept redirect-based payment methods. Redirect-based payment methods
    /// may require your customer to be redirected to a payment method's app or site for authentication or
    /// additional steps. To confirm this SetupIntent, you may be required to provide a `return_url` to redirect
    /// customers back to your site after they authenticate or complete the setup.
    public var allowRedirects: PaymentFlowsAutomaticPaymentMethodsSetupIntentAllowRedirects?
    /// Automatically calculates compatible payment methods
    public var enabled: Bool?

    enum CodingKeys: String, CodingKey {
        case allowRedirects = "allow_redirects"
        case enabled
    }

    init() {
        (allowRedirects, enabled) = (nil, nil)
    }
}

public extension PaymentFlowsAutomaticPaymentMethodsSetupIntent {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        allowRedirects = try container.sdkDecodeIfPresent(.allowRedirects)
        enabled = try container.sdkDecodeIfPresent(.enabled)
    }
}

public extension PaymentFlowsAutomaticPaymentMethodsSetupIntent {
    init(allowRedirects: PaymentFlowsAutomaticPaymentMethodsSetupIntentAllowRedirects? = nil, enabled: Bool? = nil) {
        self.init()
        (self.allowRedirects, self.enabled) = (allowRedirects, enabled)
    }
}

/// Typed representation of the `PaymentFlowsInstallmentOptions` API schema.
public struct PaymentFlowsInstallmentOptions: Codable {
    /// Required `boolean` value serialized in the `enabled` wire field.
    public var enabled: Bool
    /// Optional object value serialized in the `plan` wire field.
    public var plan: PaymentMethodDetailsCardInstallmentsPlan?

    enum CodingKeys: String, CodingKey {
        case enabled
        case plan
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension PaymentFlowsInstallmentOptions {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.enabled) else {
            throw SdkValidationError(
                field: "enabled",
                code: "required",
                message: "Validation failed for 'enabled': value is required"
            )
        }
        enabled = try container.sdkDecodeRequired(.enabled)
        plan = try container.sdkDecodeIfPresent(.plan)
    }
}

public extension PaymentFlowsInstallmentOptions {
    init(enabled: Bool, plan: PaymentMethodDetailsCardInstallmentsPlan? = nil) {
        (self.enabled, self.plan) = (enabled, plan)
    }
}

/// Typed representation of the `PaymentFlowsPaymentDetails` API schema.
public struct PaymentFlowsPaymentDetails: Codable {
    /// A unique value to identify the customer. This field is available only for card payments. This field is
    /// truncated to 25 alphanumeric characters, excluding spaces, before being sent to card networks.
    public var customerReference: String?
    /// A unique value assigned by the business to identify the transaction. Required for L2 and L3 rates. For
    /// Cards, this field is truncated to 25 alphanumeric characters, excluding spaces, before being sent to card
    /// networks. For Klarna, this field is truncated to 255 characters and is visible to customers when they view
    /// the order in the Klarna app.
    public var orderReference: String?

    enum CodingKeys: String, CodingKey {
        case customerReference = "customer_reference"
        case orderReference = "order_reference"
    }

    init() {
        (customerReference, orderReference) = (nil, nil)
    }
}

public extension PaymentFlowsPaymentDetails {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        customerReference = try container.sdkDecodeIfPresent(.customerReference)
        orderReference = try container.sdkDecodeIfPresent(.orderReference)
        if let value = customerReference {
            try validateLength("customer_reference", value, min: nil, max: 5000)
        }
        if let value = orderReference {
            try validateLength("order_reference", value, min: nil, max: 5000)
        }
    }
}

public extension PaymentFlowsPaymentDetails {
    init(customerReference: String? = nil, orderReference: String? = nil) throws {
        self.init()
        (self.customerReference, self.orderReference) = (customerReference, orderReference)
        if let value = self.customerReference {
            try validateLength("customer_reference", value, min: nil, max: 5000)
        }
        if let value = self.orderReference {
            try validateLength("order_reference", value, min: nil, max: 5000)
        }
    }
}

/// Typed representation of the `PaymentFlowsPaymentIntentAsyncWorkflows` API schema.
public struct PaymentFlowsPaymentIntentAsyncWorkflows: Codable {
    /// Optional object value serialized in the `inputs` wire field.
    public var inputs: PaymentFlowsPaymentIntentAsyncWorkflowsResourceInputs?

    enum CodingKeys: String, CodingKey {
        case inputs
    }

    init() {
        inputs = nil
    }
}

public extension PaymentFlowsPaymentIntentAsyncWorkflows {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        inputs = try container.sdkDecodeIfPresent(.inputs)
    }
}

public extension PaymentFlowsPaymentIntentAsyncWorkflows {
    init(inputs: PaymentFlowsPaymentIntentAsyncWorkflowsResourceInputs? = nil) {
        self.init()
        self.inputs = inputs
    }
}

/// Typed representation of the `PaymentFlowsPaymentIntentAsyncWorkflowsResourceInputs` API schema.
public struct PaymentFlowsPaymentIntentAsyncWorkflowsResourceInputs: Codable {
    /// Optional object value serialized in the `tax` wire field.
    public var tax: PaymentFlowsPaymentIntentAsyncWorkflowsResourceInputsResourceTax?

    enum CodingKeys: String, CodingKey {
        case tax
    }

    init() {
        tax = nil
    }
}

public extension PaymentFlowsPaymentIntentAsyncWorkflowsResourceInputs {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        tax = try container.sdkDecodeIfPresent(.tax)
    }
}

public extension PaymentFlowsPaymentIntentAsyncWorkflowsResourceInputs {
    init(tax: PaymentFlowsPaymentIntentAsyncWorkflowsResourceInputsResourceTax? = nil) {
        self.init()
        self.tax = tax
    }
}
