// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

// V1 domain models
/// This object represents a customer of your business. Use it to create recurring charges, save payment and contact
/// information, and track payments that belong to the same customer.
public struct Customer: Codable {
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// Unique identifier for the object.
    public var id: String
    /// If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is
    /// `false`.
    public var livemode: Bool
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: CustomerObject
    /// The customer's address.
    public var address: CustomerAddress?
    /// The current balance, if any, that's stored on the customer in their default currency. If negative, the
    /// customer has credit to apply to their next invoice. If positive, the customer has an amount owed that's
    /// added to their next invoice. The balance only considers amounts that Stripe hasn't successfully applied to
    /// any invoice. It doesn't reflect unpaid invoices. This balance is only taken into account after invoices
    /// finalize. For multi-currency balances, see…
    public var balance: Int?
    /// The customer's business name.
    public var businessName: String?
    /// The current funds being held by Stripe on behalf of the customer. You can apply these funds towards payment
    /// intents when the source is "cash_balance". The `settings[reconciliation_mode]` field describes if these
    /// funds apply to these payment intents manually or automatically.
    public var cashBalance: CustomerCashBalance?
    /// Three-letter ISO code for the currency the customer can be charged in for recurring billing purposes.
    public var currency: String?
    /// The ID of an Account representing a customer. You can use this ID with any v1 API that accepts a
    /// customer_account parameter.
    public var customerAccount: String?
    /// ID of the default payment source for the customer. If you use payment methods created through the
    /// PaymentMethods API, see the invoice_settings.default_payment_method field instead.
    public var defaultSource: CustomerDefaultSource?
    /// Tracks the most recent state change on any invoice belonging to the customer. Paying an invoice or marking
    /// it uncollectible via the API will set this field to false. An automatic payment failure or passing the
    /// `invoice.due_date` will set this field to `true`. If an invoice becomes uncollectible by dunning,
    /// `delinquent` doesn't reset to `false`. If you care whether the customer has paid their most recent
    /// subscription invoice, use `subscription.status` instead. Paying or…
    public var delinquent: Bool?
    /// An arbitrary string attached to the object. Often useful for displaying to users.
    public var description: String?
    /// Describes the current discount active on the customer, if there is one.
    public var discount: CustomerDiscount?
    /// The customer's email address.
    public var email: String?
    /// The customer's individual name.
    public var individualName: String?
    /// The current multi-currency balances, if any, that's stored on the customer. If positive in a currency, the
    /// customer has a credit to apply to their next invoice denominated in that currency. If negative, the customer
    /// has an amount owed that's added to their next invoice denominated in that currency. These balances don't
    /// apply to unpaid invoices. They solely track amounts that Stripe hasn't successfully applied to any invoice.
    /// Stripe only applies a balance in a specific…
    public var invoiceCreditBalance: [String: Int]?
    /// The prefix for the customer used to generate unique invoice numbers.
    public var invoicePrefix: String?
    /// Optional object value serialized in the `invoice_settings` wire field.
    public var invoiceSettings: InvoiceSettingCustomerSetting?
    /// Set of key-value pairs that you can attach to an object. This can be useful for storing additional
    /// information about the object in a structured format.
    public var metadata: [String: String]?
    /// The customer's full name or business name.
    public var name: String?
    /// The suffix of the customer's next invoice number (for example, 0001). When the account uses account level
    /// sequencing, this parameter is ignored in API requests and the field omitted in API responses.
    public var nextInvoiceSequence: Int?
    /// The customer's phone number.
    public var phone: String?
    /// The customer's preferred locales (languages), ordered by preference.
    public var preferredLocales: [String]?
    /// Mailing and shipping address for the customer. Appears on invoices emailed to this customer.
    public var shipping: CustomerShipping?
    /// The customer's payment sources, if any.
    public var sources: CustomerSources?
    /// The customer's current subscriptions, if any.
    public var subscriptions: CustomerSubscriptions?
    /// Optional object value serialized in the `tax` wire field.
    public var tax: CustomerTax?
    /// Describes the customer's tax exemption status, which is `none`, `exempt`, or `reverse`. When set to
    /// `reverse`, invoice and receipt PDFs include the following text: **"Reverse charge"**.
    public var taxExempt: CustomerTaxExempt?
    /// The customer's tax IDs.
    public var taxIds: CustomerTaxIds?
    /// ID of the test clock that this customer belongs to.
    public var testClock: CustomerTestClock?

    enum CodingKeys: String, CodingKey {
        case created
        case id
        case livemode
        case object
        case address
        case balance
        case businessName = "business_name"
        case cashBalance = "cash_balance"
        case currency
        case customerAccount = "customer_account"
        case defaultSource = "default_source"
        case delinquent
        case description
        case discount
        case email
        case individualName = "individual_name"
        case invoiceCreditBalance = "invoice_credit_balance"
        case invoicePrefix = "invoice_prefix"
        case invoiceSettings = "invoice_settings"
        case metadata
        case name
        case nextInvoiceSequence = "next_invoice_sequence"
        case phone
        case preferredLocales = "preferred_locales"
        case shipping
        case sources
        case subscriptions
        case tax
        case taxExempt = "tax_exempt"
        case taxIds = "tax_ids"
        case testClock = "test_clock"
    }

    private init(sdkCopy value: Self) { self = value }
}

extension Customer {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.created) else {
            throw SdkValidationError(field: "created", code: "required", message: "Validation failed for 'created': value is required")
        }
        guard container.contains(.id) else {
            throw SdkValidationError(field: "id", code: "required", message: "Validation failed for 'id': value is required")
        }
        guard container.contains(.livemode) else {
            throw SdkValidationError(field: "livemode", code: "required", message: "Validation failed for 'livemode': value is required")
        }
        guard container.contains(.object) else {
            throw SdkValidationError(field: "object", code: "required", message: "Validation failed for 'object': value is required")
        }
        self.created = try container.sdkDecodeRequired(.created)
        self.id = try container.sdkDecodeRequired(.id)
        self.livemode = try container.sdkDecodeRequired(.livemode)
        self.object = try container.sdkDecodeRequired(.object)
        self.address = try container.sdkDecodeIfPresent(.address)
        self.balance = try container.sdkDecodeIfPresent(.balance)
        self.businessName = try container.sdkDecodeIfPresent(.businessName)
        self.cashBalance = try container.sdkDecodeIfPresent(.cashBalance)
        self.currency = try container.sdkDecodeIfPresent(.currency)
        self.customerAccount = try container.sdkDecodeIfPresent(.customerAccount)
        self.defaultSource = try container.sdkDecodeIfPresent(.defaultSource)
        self.delinquent = try container.sdkDecodeIfPresent(.delinquent)
        self.description = try container.sdkDecodeIfPresent(.description)
        self.discount = try container.sdkDecodeIfPresent(.discount)
        self.email = try container.sdkDecodeIfPresent(.email)
        self.individualName = try container.sdkDecodeIfPresent(.individualName)
        self.invoiceCreditBalance = try container.sdkDecodeIfPresent(.invoiceCreditBalance)
        self.invoicePrefix = try container.sdkDecodeIfPresent(.invoicePrefix)
        self.invoiceSettings = try container.sdkDecodeIfPresent(.invoiceSettings)
        self.metadata = try container.sdkDecodeIfPresent(.metadata)
        self.name = try container.sdkDecodeIfPresent(.name)
        self.nextInvoiceSequence = try container.sdkDecodeIfPresent(.nextInvoiceSequence)
        self.phone = try container.sdkDecodeIfPresent(.phone)
        self.preferredLocales = try container.sdkDecodeIfPresent(.preferredLocales)
        self.shipping = try container.sdkDecodeIfPresent(.shipping)
        self.sources = try container.sdkDecodeIfPresent(.sources)
        self.subscriptions = try container.sdkDecodeIfPresent(.subscriptions)
        self.tax = try container.sdkDecodeIfPresent(.tax)
        self.taxExempt = try container.sdkDecodeIfPresent(.taxExempt)
        self.taxIds = try container.sdkDecodeIfPresent(.taxIds)
        self.testClock = try container.sdkDecodeIfPresent(.testClock)
        try sdkValidateConstraints()
    }
}

extension Customer {
    public init(created: Int, id: String, livemode: Bool, object: CustomerObject, address: CustomerAddress? = nil, balance: Int? = nil, businessName: String? = nil, cashBalance: CustomerCashBalance? = nil, currency: String? = nil, customerAccount: String? = nil, defaultSource: CustomerDefaultSource? = nil, delinquent: Bool? = nil, description: String? = nil, discount: CustomerDiscount? = nil, email: String? = nil, individualName: String? = nil, invoiceCreditBalance: [String: Int]? = nil, invoicePrefix: String? = nil, invoiceSettings: InvoiceSettingCustomerSetting? = nil, metadata: [String: String]? = nil, name: String? = nil, nextInvoiceSequence: Int? = nil, phone: String? = nil, preferredLocales: [String]? = nil, shipping: CustomerShipping? = nil, sources: CustomerSources? = nil, subscriptions: CustomerSubscriptions? = nil, tax: CustomerTax? = nil, taxExempt: CustomerTaxExempt? = nil, taxIds: CustomerTaxIds? = nil, testClock: CustomerTestClock? = nil) throws {
        (self.created, self.id) = (created, id)
        (self.livemode, self.object) = (livemode, object)
        (self.address, self.balance) = (address, balance)
        (self.businessName, self.cashBalance) = (businessName, cashBalance)
        (self.currency, self.customerAccount) = (currency, customerAccount)
        (self.defaultSource, self.delinquent) = (defaultSource, delinquent)
        (self.description, self.discount) = (description, discount)
        (self.email, self.individualName) = (email, individualName)
        (self.invoiceCreditBalance, self.invoicePrefix) = (invoiceCreditBalance, invoicePrefix)
        (self.invoiceSettings, self.metadata) = (invoiceSettings, metadata)
        (self.name, self.nextInvoiceSequence) = (name, nextInvoiceSequence)
        (self.phone, self.preferredLocales) = (phone, preferredLocales)
        (self.shipping, self.sources) = (shipping, sources)
        (self.subscriptions, self.tax) = (subscriptions, tax)
        (self.taxExempt, self.taxIds) = (taxExempt, taxIds)
        self.testClock = testClock
        try sdkValidateConstraints()
    }
}

extension Customer {
    func sdkValidateConstraints() throws {
            try validateLength("id", self.id, min: nil, max: 5000)
        if let value = self.businessName {
            try validateLength("business_name", value, min: nil, max: 150)
        }
        if let value = self.currency {
            try validateLength("currency", value, min: nil, max: 5000)
        }
        if let value = self.customerAccount {
            try validateLength("customer_account", value, min: nil, max: 5000)
        }
        if let value = self.description {
            try validateLength("description", value, min: nil, max: 5000)
        }
        if let value = self.email {
            try validateLength("email", value, min: nil, max: 5000)
        }
        if let value = self.individualName {
            try validateLength("individual_name", value, min: nil, max: 150)
        }
        if let value = self.invoicePrefix {
            try validateLength("invoice_prefix", value, min: nil, max: 5000)
        }
        if let value = self.name {
            try validateLength("name", value, min: nil, max: 5000)
        }
        if let value = self.phone {
            try validateLength("phone", value, min: nil, max: 5000)
        }
    }
}

public enum CustomerAddress {
    case address(Address)
}

extension CustomerAddress: Codable {

    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) { self = value; return }
        throw DecodingError.dataCorruptedError(in: container, debugDescription: "No variant matched for CustomerAddress")
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(Address.self) { return .address(value) }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) { return }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .address(value): try container.encode(value); return true
        }
    }

}

public enum CustomerCashBalance {
    case cashBalance(CashBalance)
}

extension CustomerCashBalance: Codable {

    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) { self = value; return }
        throw DecodingError.dataCorruptedError(in: container, debugDescription: "No variant matched for CustomerCashBalance")
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(CashBalance.self) { return .cashBalance(value) }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) { return }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .cashBalance(value): try container.encode(value); return true
        }
    }

}

public enum CustomerDiscount {
    case discount(Discount)
}

extension CustomerDiscount: Codable {

    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) { self = value; return }
        throw DecodingError.dataCorruptedError(in: container, debugDescription: "No variant matched for CustomerDiscount")
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(Discount.self) { return .discount(value) }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) { return }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .discount(value): try container.encode(value); return true
        }
    }

}

public enum CustomerShipping {
    case shipping(Shipping)
}

extension CustomerShipping: Codable {

    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) { self = value; return }
        throw DecodingError.dataCorruptedError(in: container, debugDescription: "No variant matched for CustomerShipping")
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(Shipping.self) { return .shipping(value) }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) { return }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .shipping(value): try container.encode(value); return true
        }
    }

}

/// The customer's payment sources, if any.
public struct CustomerSources: Codable {
    /// Details about each object.
    public var data: [CustomerSourcesDataItem]
    /// True if this list has another page of items after this one that can be fetched.
    public var hasMore: Bool
    /// String representing the object's type. Objects of the same type share the same value. Always has the value
    /// `list`.
    public var object: CustomerSourcesObject
    /// The URL where this list can be accessed.
    public var url: String

    enum CodingKeys: String, CodingKey {
        case data
        case hasMore = "has_more"
        case object
        case url
    }

    private init(sdkCopy value: Self) { self = value }
}

extension CustomerSources {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.data) else {
            throw SdkValidationError(field: "data", code: "required", message: "Validation failed for 'data': value is required")
        }
        guard container.contains(.hasMore) else {
            throw SdkValidationError(field: "has_more", code: "required", message: "Validation failed for 'has_more': value is required")
        }
        guard container.contains(.object) else {
            throw SdkValidationError(field: "object", code: "required", message: "Validation failed for 'object': value is required")
        }
        guard container.contains(.url) else {
            throw SdkValidationError(field: "url", code: "required", message: "Validation failed for 'url': value is required")
        }
        self.data = try container.sdkDecodeRequired(.data)
        self.hasMore = try container.sdkDecodeRequired(.hasMore)
        self.object = try container.sdkDecodeRequired(.object)
        self.url = try container.sdkDecodeRequired(.url)
            try validateLength("url", self.url, min: nil, max: 5000)
    }
}

extension CustomerSources {
    public init(data: [CustomerSourcesDataItem], hasMore: Bool, object: CustomerSourcesObject, url: String) throws {
        (self.data, self.hasMore) = (data, hasMore)
        (self.object, self.url) = (object, url)
            try validateLength("url", self.url, min: nil, max: 5000)
    }
}

/// The customer's current subscriptions, if any.
public struct CustomerSubscriptions: Codable {
    /// Details about each object.
    public var data: [Subscription]
    /// True if this list has another page of items after this one that can be fetched.
    public var hasMore: Bool
    /// String representing the object's type. Objects of the same type share the same value. Always has the value
    /// `list`.
    public var object: CustomerSubscriptionsObject
    /// The URL where this list can be accessed.
    public var url: String

    enum CodingKeys: String, CodingKey {
        case data
        case hasMore = "has_more"
        case object
        case url
    }

    private init(sdkCopy value: Self) { self = value }
}

extension CustomerSubscriptions {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.data) else {
            throw SdkValidationError(field: "data", code: "required", message: "Validation failed for 'data': value is required")
        }
        guard container.contains(.hasMore) else {
            throw SdkValidationError(field: "has_more", code: "required", message: "Validation failed for 'has_more': value is required")
        }
        guard container.contains(.object) else {
            throw SdkValidationError(field: "object", code: "required", message: "Validation failed for 'object': value is required")
        }
        guard container.contains(.url) else {
            throw SdkValidationError(field: "url", code: "required", message: "Validation failed for 'url': value is required")
        }
        self.data = try container.sdkDecodeRequired(.data)
        self.hasMore = try container.sdkDecodeRequired(.hasMore)
        self.object = try container.sdkDecodeRequired(.object)
        self.url = try container.sdkDecodeRequired(.url)
            try validateLength("url", self.url, min: nil, max: 5000)
    }
}

extension CustomerSubscriptions {
    public init(data: [Subscription], hasMore: Bool, object: CustomerSubscriptionsObject, url: String) throws {
        (self.data, self.hasMore) = (data, hasMore)
        (self.object, self.url) = (object, url)
            try validateLength("url", self.url, min: nil, max: 5000)
    }
}

/// The customer's tax IDs.
public struct CustomerTaxIds: Codable {
    /// Details about each object.
    public var data: [TaxId]
    /// True if this list has another page of items after this one that can be fetched.
    public var hasMore: Bool
    /// String representing the object's type. Objects of the same type share the same value. Always has the value
    /// `list`.
    public var object: CustomerTaxIdsObject
    /// The URL where this list can be accessed.
    public var url: String

    enum CodingKeys: String, CodingKey {
        case data
        case hasMore = "has_more"
        case object
        case url
    }

    private init(sdkCopy value: Self) { self = value }
}

extension CustomerTaxIds {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.data) else {
            throw SdkValidationError(field: "data", code: "required", message: "Validation failed for 'data': value is required")
        }
        guard container.contains(.hasMore) else {
            throw SdkValidationError(field: "has_more", code: "required", message: "Validation failed for 'has_more': value is required")
        }
        guard container.contains(.object) else {
            throw SdkValidationError(field: "object", code: "required", message: "Validation failed for 'object': value is required")
        }
        guard container.contains(.url) else {
            throw SdkValidationError(field: "url", code: "required", message: "Validation failed for 'url': value is required")
        }
        self.data = try container.sdkDecodeRequired(.data)
        self.hasMore = try container.sdkDecodeRequired(.hasMore)
        self.object = try container.sdkDecodeRequired(.object)
        self.url = try container.sdkDecodeRequired(.url)
            try validateLength("url", self.url, min: nil, max: 5000)
    }
}

extension CustomerTaxIds {
    public init(data: [TaxId], hasMore: Bool, object: CustomerTaxIdsObject, url: String) throws {
        (self.data, self.hasMore) = (data, hasMore)
        (self.object, self.url) = (object, url)
            try validateLength("url", self.url, min: nil, max: 5000)
    }
}

public enum CustomerTestClock {
    case stringValue(String)
    case testHelpersTestClock(TestHelpersTestClock)
}
