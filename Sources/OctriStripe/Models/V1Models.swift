// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

/// V1 domain models
public enum AccountExternalAccountsDataItem {
    case bankAccount(BankAccount)
    case card(Card)
}

extension AccountExternalAccountsDataItem: Codable {
    private enum CodingKeys: String, CodingKey {
        case discriminator = "object"
    }

    public init(from decoder: Decoder) throws {
        let tagged = try decoder.container(keyedBy: CodingKeys.self)
        let discriminator = try tagged.decode(String.self, forKey: .discriminator)
        if let value = try Self.decodeGroup1(discriminator, from: decoder) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            forKey: .discriminator,
            in: tagged,
            debugDescription: "Unknown discriminator for AccountExternalAccountsDataItem"
        )
    }

    private static func decodeGroup1(_ discriminator: String, from decoder: Decoder) throws -> Self? {
        switch discriminator {
        case "bank_account": try .bankAccount(BankAccount(from: decoder))
        case "card": try .card(Card(from: decoder))
        default: nil
        }
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .bankAccount(value): try container.encode(value); return true
        case let .card(value): try container.encode(value); return true
        }
    }
}

public enum ApiErrorsSource {
    case bankAccount(BankAccount)
    case card(Card)
    case source(Source)
}

extension ApiErrorsSource: Codable {
    private enum CodingKeys: String, CodingKey {
        case discriminator = "object"
    }

    public init(from decoder: Decoder) throws {
        let tagged = try decoder.container(keyedBy: CodingKeys.self)
        let discriminator = try tagged.decode(String.self, forKey: .discriminator)
        if let value = try Self.decodeGroup1(discriminator, from: decoder) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            forKey: .discriminator,
            in: tagged,
            debugDescription: "Unknown discriminator for ApiErrorsSource"
        )
    }

    private static func decodeGroup1(_ discriminator: String, from decoder: Decoder) throws -> Self? {
        switch discriminator {
        case "bank_account": try .bankAccount(BankAccount(from: decoder))
        case "card": try .card(Card(from: decoder))
        case "source": try .source(Source(from: decoder))
        default: nil
        }
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .bankAccount(value): try container.encode(value); return true
        case let .card(value): try container.encode(value); return true
        case let .source(value): try container.encode(value); return true
        }
    }
}

public enum BalanceTransactionSource {
    case stringValue(String)
    case applicationFee(ApplicationFee)
    case charge(Charge)
    case connectCollectionTransfer(ConnectCollectionTransfer)
    case customerCashBalanceTransaction(CustomerCashBalanceTransaction)
    case dispute(Dispute)
    case feeRefund(FeeRefund)
    case issuingAuthorization(IssuingAuthorization)
    case issuingDispute(IssuingDispute)
    case issuingTransaction(IssuingTransaction)
    case payout(Payout)
    case refund(Refund)
    case reserveTransaction(ReserveTransaction)
    case taxDeductedAtSource(TaxDeductedAtSource)
    case topup(Topup)
    case transfer(Transfer)
    case transferReversal(TransferReversal)
}

extension BalanceTransactionSource: Codable {
    private enum CodingKeys: String, CodingKey {
        case discriminator = "object"
    }

    public init(from decoder: Decoder) throws {
        if let tagged = try? decoder.container(keyedBy: CodingKeys.self),
           let discriminator = try? tagged.decode(String.self, forKey: .discriminator) {
            if let value = try Self.decodeGroup1(discriminator, from: decoder) {
                self = value; return
            }
        }
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeUntaggedGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for BalanceTransactionSource"
        )
    }

    private static func decodeGroup1(_ discriminator: String, from decoder: Decoder) throws -> Self? {
        switch discriminator {
        case "application_fee": try .applicationFee(ApplicationFee(from: decoder))
        case "charge": try .charge(Charge(from: decoder))
        case "connect_collection_transfer": try .connectCollectionTransfer(ConnectCollectionTransfer(from: decoder))
        case "customer_cash_balance_transaction": try .customerCashBalanceTransaction(
                CustomerCashBalanceTransaction(from: decoder)
            )
        case "dispute": try .dispute(Dispute(from: decoder))
        case "fee_refund": try .feeRefund(FeeRefund(from: decoder))
        case "issuing.authorization": try .issuingAuthorization(IssuingAuthorization(from: decoder))
        case "issuing.dispute": try .issuingDispute(IssuingDispute(from: decoder))
        case "issuing.transaction": try .issuingTransaction(IssuingTransaction(from: decoder))
        case "payout": try .payout(Payout(from: decoder))
        case "refund": try .refund(Refund(from: decoder))
        case "reserve_transaction": try .reserveTransaction(ReserveTransaction(from: decoder))
        case "tax_deducted_at_source": try .taxDeductedAtSource(TaxDeductedAtSource(from: decoder))
        case "topup": try .topup(Topup(from: decoder))
        case "transfer": try .transfer(Transfer(from: decoder))
        case "transfer_reversal": try .transferReversal(TransferReversal(from: decoder))
        default: nil
        }
    }

    private static func decodeUntaggedGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
        if try encodeGroup2(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .applicationFee(value): try container.encode(value); return true
        case let .charge(value): try container.encode(value); return true
        case let .connectCollectionTransfer(value): try container.encode(value); return true
        case let .customerCashBalanceTransaction(value): try container.encode(value); return true
        case let .dispute(value): try container.encode(value); return true
        case let .feeRefund(value): try container.encode(value); return true
        case let .issuingAuthorization(value): try container.encode(value); return true
        case let .issuingDispute(value): try container.encode(value); return true
        case let .issuingTransaction(value): try container.encode(value); return true
        case let .payout(value): try container.encode(value); return true
        case let .refund(value): try container.encode(value); return true
        default: return false
        }
    }

    private func encodeGroup2(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .reserveTransaction(value): try container.encode(value); return true
        case let .taxDeductedAtSource(value): try container.encode(value); return true
        case let .topup(value): try container.encode(value); return true
        case let .transfer(value): try container.encode(value); return true
        case let .transferReversal(value): try container.encode(value); return true
        default: return false
        }
    }
}

public enum CustomerDefaultSource {
    case stringValue(String)
    case bankAccount(BankAccount)
    case card(Card)
    case source(Source)
}

extension CustomerDefaultSource: Codable {
    private enum CodingKeys: String, CodingKey {
        case discriminator = "object"
    }

    public init(from decoder: Decoder) throws {
        if let tagged = try? decoder.container(keyedBy: CodingKeys.self),
           let discriminator = try? tagged.decode(String.self, forKey: .discriminator) {
            if let value = try Self.decodeGroup1(discriminator, from: decoder) {
                self = value; return
            }
        }
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeUntaggedGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for CustomerDefaultSource"
        )
    }

    private static func decodeGroup1(_ discriminator: String, from decoder: Decoder) throws -> Self? {
        switch discriminator {
        case "bank_account": try .bankAccount(BankAccount(from: decoder))
        case "card": try .card(Card(from: decoder))
        case "source": try .source(Source(from: decoder))
        default: nil
        }
    }

    private static func decodeUntaggedGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .bankAccount(value): try container.encode(value); return true
        case let .card(value): try container.encode(value); return true
        case let .source(value): try container.encode(value); return true
        }
    }
}

public enum CustomerSourcesDataItem {
    case bankAccount(BankAccount)
    case card(Card)
    case source(Source)
}

extension CustomerSourcesDataItem: Codable {
    private enum CodingKeys: String, CodingKey {
        case discriminator = "object"
    }

    public init(from decoder: Decoder) throws {
        let tagged = try decoder.container(keyedBy: CodingKeys.self)
        let discriminator = try tagged.decode(String.self, forKey: .discriminator)
        if let value = try Self.decodeGroup1(discriminator, from: decoder) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            forKey: .discriminator,
            in: tagged,
            debugDescription: "Unknown discriminator for CustomerSourcesDataItem"
        )
    }

    private static func decodeGroup1(_ discriminator: String, from decoder: Decoder) throws -> Self? {
        switch discriminator {
        case "bank_account": try .bankAccount(BankAccount(from: decoder))
        case "card": try .card(Card(from: decoder))
        case "source": try .source(Source(from: decoder))
        default: nil
        }
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .bankAccount(value): try container.encode(value); return true
        case let .card(value): try container.encode(value); return true
        case let .source(value): try container.encode(value); return true
        }
    }
}

public enum InvoiceDefaultSource {
    case stringValue(String)
    case bankAccount(BankAccount)
    case card(Card)
    case source(Source)
}

extension InvoiceDefaultSource: Codable {
    private enum CodingKeys: String, CodingKey {
        case discriminator = "object"
    }

    public init(from decoder: Decoder) throws {
        if let tagged = try? decoder.container(keyedBy: CodingKeys.self),
           let discriminator = try? tagged.decode(String.self, forKey: .discriminator) {
            if let value = try Self.decodeGroup1(discriminator, from: decoder) {
                self = value; return
            }
        }
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeUntaggedGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for InvoiceDefaultSource"
        )
    }

    private static func decodeGroup1(_ discriminator: String, from decoder: Decoder) throws -> Self? {
        switch discriminator {
        case "bank_account": try .bankAccount(BankAccount(from: decoder))
        case "card": try .card(Card(from: decoder))
        case "source": try .source(Source(from: decoder))
        default: nil
        }
    }

    private static func decodeUntaggedGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .bankAccount(value): try container.encode(value); return true
        case let .card(value): try container.encode(value); return true
        case let .source(value): try container.encode(value); return true
        }
    }
}

public enum SubscriptionDefaultSource {
    case stringValue(String)
    case bankAccount(BankAccount)
    case card(Card)
    case source(Source)
}

extension SubscriptionDefaultSource: Codable {
    private enum CodingKeys: String, CodingKey {
        case discriminator = "object"
    }

    public init(from decoder: Decoder) throws {
        if let tagged = try? decoder.container(keyedBy: CodingKeys.self),
           let discriminator = try? tagged.decode(String.self, forKey: .discriminator) {
            if let value = try Self.decodeGroup1(discriminator, from: decoder) {
                self = value; return
            }
        }
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeUntaggedGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for SubscriptionDefaultSource"
        )
    }

    private static func decodeGroup1(_ discriminator: String, from decoder: Decoder) throws -> Self? {
        switch discriminator {
        case "bank_account": try .bankAccount(BankAccount(from: decoder))
        case "card": try .card(Card(from: decoder))
        case "source": try .source(Source(from: decoder))
        default: nil
        }
    }

    private static func decodeUntaggedGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .bankAccount(value): try container.encode(value); return true
        case let .card(value): try container.encode(value); return true
        case let .source(value): try container.encode(value); return true
        }
    }
}

/// For new integrations, we recommend using the Accounts v2 API, in place of /v1/accounts and /v1/customers to
/// represent a user. This is an object representing a Stripe account. You can retrieve it to see properties on the
/// account like its current requirements or if the account is enabled to make live charges or receive payouts. For
/// accounts where controller.requirement_collection is `application`, which includes Custom accounts, the
/// properties below are always returned. For…
public struct Account: Codable {
    /// Unique identifier for the object.
    public var id: String
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: AccountObject
    /// Business information about the account.
    public var businessProfile: AccountBusinessProfileXc496d4ba?
    /// The business type.
    public var businessType: AccountBusinessType?
    /// Optional object value serialized in the `capabilities` wire field.
    public var capabilities: AccountCapabilities?
    /// Whether the account can process charges.
    public var chargesEnabled: Bool?
    /// Optional object value serialized in the `company` wire field.
    public var company: LegalEntityCompany?
    /// Optional object value serialized in the `controller` wire field.
    public var controller: AccountUnificationAccountController?
    /// The account's country.
    public var country: String?
    /// Time at which the account was connected. Measured in seconds since the Unix epoch.
    public var created: Int?
    /// Three-letter ISO currency code representing the default currency for the account. This must be a currency
    /// that Stripe supports in the account's country.
    public var defaultCurrency: String?
    /// Whether account details have been submitted. Accounts with Stripe Dashboard access, which includes Standard
    /// accounts, cannot receive payouts before this is true. Accounts where this is false should be directed to an
    /// onboarding flow to finish submitting account details.
    public var detailsSubmitted: Bool?
    /// An email address associated with the account. It's not used for authentication and Stripe doesn't market to
    /// this field without explicit approval from the platform.
    public var email: String?
    /// External accounts (bank accounts and debit cards) currently attached to this account. External accounts are
    /// only returned for requests where `controller[is_controller]` is true.
    public var externalAccounts: AccountExternalAccounts?
    /// Optional object value serialized in the `future_requirements` wire field.
    public var futureRequirements: AccountFutureRequirements?
    /// The groups associated with the account.
    public var groups: AccountGroups?
    /// This is an object representing a person associated with a Stripe account. A platform can only access a
    /// subset of data in a person for an account where account.controller.requirement_collection is `stripe`, which
    /// includes Standard and Express accounts, after creating an Account Link or Account Session to start Connect
    /// onboarding. See the Standard onboarding or Express onboarding documentation for information about prefilling
    /// information and account onboarding steps. Learn…
    public var individual: Person?
    /// Set of key-value pairs that you can attach to an object. This can be useful for storing additional
    /// information about the object in a structured format.
    public var metadata: [String: String]?
    /// Whether the funds in this account can be paid out.
    public var payoutsEnabled: Bool?
    /// Optional object value serialized in the `requirements` wire field.
    public var requirements: AccountRequirements?
    /// Options for customizing how the account functions within Stripe.
    public var settings: AccountSettingsXf131fa57?
    /// Optional object value serialized in the `tos_acceptance` wire field.
    public var tosAcceptance: AccountTosAcceptance?
    /// The Stripe account type. Can be `standard`, `express`, `custom`, or `none`.
    public var type: AccountType?

    enum CodingKeys: String, CodingKey {
        case id
        case object
        case businessProfile = "business_profile"
        case businessType = "business_type"
        case capabilities
        case chargesEnabled = "charges_enabled"
        case company
        case controller
        case country
        case created
        case defaultCurrency = "default_currency"
        case detailsSubmitted = "details_submitted"
        case email
        case externalAccounts = "external_accounts"
        case futureRequirements = "future_requirements"
        case groups
        case individual
        case metadata
        case payoutsEnabled = "payouts_enabled"
        case requirements
        case settings
        case tosAcceptance = "tos_acceptance"
        case type
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension Account {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.id) else {
            throw SdkValidationError(
                field: "id",
                code: "required",
                message: "Validation failed for 'id': value is required"
            )
        }
        guard container.contains(.object) else {
            throw SdkValidationError(
                field: "object",
                code: "required",
                message: "Validation failed for 'object': value is required"
            )
        }
        id = try container.sdkDecodeRequired(.id)
        object = try container.sdkDecodeRequired(.object)
        businessProfile = try container.sdkDecodeIfPresent(.businessProfile)
        businessType = try container.sdkDecodeIfPresent(.businessType)
        capabilities = try container.sdkDecodeIfPresent(.capabilities)
        chargesEnabled = try container.sdkDecodeIfPresent(.chargesEnabled)
        company = try container.sdkDecodeIfPresent(.company)
        controller = try container.sdkDecodeIfPresent(.controller)
        country = try container.sdkDecodeIfPresent(.country)
        created = try container.sdkDecodeIfPresent(.created)
        defaultCurrency = try container.sdkDecodeIfPresent(.defaultCurrency)
        detailsSubmitted = try container.sdkDecodeIfPresent(.detailsSubmitted)
        email = try container.sdkDecodeIfPresent(.email)
        externalAccounts = try container.sdkDecodeIfPresent(.externalAccounts)
        futureRequirements = try container.sdkDecodeIfPresent(.futureRequirements)
        groups = try container.sdkDecodeIfPresent(.groups)
        individual = try container.sdkDecodeIfPresent(.individual)
        metadata = try container.sdkDecodeIfPresent(.metadata)
        payoutsEnabled = try container.sdkDecodeIfPresent(.payoutsEnabled)
        requirements = try container.sdkDecodeIfPresent(.requirements)
        settings = try container.sdkDecodeIfPresent(.settings)
        tosAcceptance = try container.sdkDecodeIfPresent(.tosAcceptance)
        type = try container.sdkDecodeIfPresent(.type)
        try validateLength("id", id, min: nil, max: 5000)
        if let value = country {
            try validateLength("country", value, min: nil, max: 5000)
        }
        if let value = defaultCurrency {
            try validateLength("default_currency", value, min: nil, max: 5000)
        }
        if let value = email {
            try validateLength("email", value, min: nil, max: 5000)
        }
    }
}

public extension Account {
    init(
        id: String,
        object: AccountObject,
        businessProfile: AccountBusinessProfileXc496d4ba? = nil,
        businessType: AccountBusinessType? = nil,
        capabilities: AccountCapabilities? = nil,
        chargesEnabled: Bool? = nil,
        company: LegalEntityCompany? = nil,
        controller: AccountUnificationAccountController? = nil,
        country: String? = nil,
        created: Int? = nil,
        defaultCurrency: String? = nil,
        detailsSubmitted: Bool? = nil,
        email: String? = nil,
        externalAccounts: AccountExternalAccounts? = nil,
        futureRequirements: AccountFutureRequirements? = nil,
        groups: AccountGroups? = nil,
        individual: Person? = nil,
        metadata: [String: String]? = nil,
        payoutsEnabled: Bool? = nil,
        requirements: AccountRequirements? = nil,
        settings: AccountSettingsXf131fa57? = nil,
        tosAcceptance: AccountTosAcceptance? = nil,
        type: AccountType? = nil
    ) throws {
        (self.id, self.object) = (id, object)
        (self.businessProfile, self.businessType) = (businessProfile, businessType)
        (self.capabilities, self.chargesEnabled) = (capabilities, chargesEnabled)
        (self.company, self.controller) = (company, controller)
        (self.country, self.created) = (country, created)
        (self.defaultCurrency, self.detailsSubmitted) = (defaultCurrency, detailsSubmitted)
        (self.email, self.externalAccounts) = (email, externalAccounts)
        (self.futureRequirements, self.groups) = (futureRequirements, groups)
        (self.individual, self.metadata) = (individual, metadata)
        (self.payoutsEnabled, self.requirements) = (payoutsEnabled, requirements)
        (self.settings, self.tosAcceptance) = (settings, tosAcceptance)
        self.type = type
        try validateLength("id", self.id, min: nil, max: 5000)
        if let value = self.country {
            try validateLength("country", value, min: nil, max: 5000)
        }
        if let value = self.defaultCurrency {
            try validateLength("default_currency", value, min: nil, max: 5000)
        }
        if let value = self.email {
            try validateLength("email", value, min: nil, max: 5000)
        }
    }
}

public enum AccountBusinessProfileXc496d4ba {
    case accountBusinessProfile(AccountBusinessProfile)
}
