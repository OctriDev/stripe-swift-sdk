// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

/// V1 domain models
public indirect enum PayoutReversedBy {
    case stringValue(String)
    case payout(Payout)
}

extension PayoutReversedBy: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for PayoutReversedBy"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container.decode(Payout.self) {
            return .payout(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .payout(value): try container.encode(value); return true
        }
    }
}

public enum PayoutTraceId {
    case payoutsTraceId(PayoutsTraceId)
}

extension PayoutTraceId: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(in: container, debugDescription: "No variant matched for PayoutTraceId")
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(PayoutsTraceId.self) {
            return .payoutsTraceId(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .payoutsTraceId(value): try container.encode(value); return true
        }
    }
}

/// Typed representation of the `PayoutsTraceId` API schema.
public struct PayoutsTraceId: Codable {
    /// Possible values are `pending`, `supported`, and `unsupported`. When `payout.status` is `pending` or
    /// `in_transit`, this will be `pending`. When the payout transitions to `paid`, `failed`, or `canceled`, this
    /// status will become `supported` or `unsupported` shortly after in most cases. In some cases, this may appear
    /// as `pending` for up to 10 days after `arrival_date` until transitioning to `supported` or `unsupported`.
    public var status: String
    /// The trace ID value if `trace_id.status` is `supported`, otherwise `nil`.
    public var value: String?

    enum CodingKeys: String, CodingKey {
        case status
        case value
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension PayoutsTraceId {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.status) else {
            throw SdkValidationError(
                field: "status",
                code: "required",
                message: "Validation failed for 'status': value is required"
            )
        }
        status = try container.sdkDecodeRequired(.status)
        value = try container.sdkDecodeIfPresent(.value)
        try validateLength("status", status, min: nil, max: 5000)
        if let value {
            try validateLength("value", value, min: nil, max: 5000)
        }
    }
}

public extension PayoutsTraceId {
    init(status: String, value: String? = nil) throws {
        (self.status, self.value) = (status, value)
        try validateLength("status", self.status, min: nil, max: 5000)
        if let value = self.value {
            try validateLength("value", value, min: nil, max: 5000)
        }
    }
}

/// Typed representation of the `PaypalSellerProtection` API schema.
public struct PaypalSellerProtection: Codable {
    /// Indicates whether the transaction is eligible for PayPal's seller protection.
    public var status: PaypalSellerProtectionStatus
    /// An array of conditions that are covered for the transaction, if applicable.
    public var disputeCategories: [PaypalSellerProtectionDisputeCategoriesItem]?

    enum CodingKeys: String, CodingKey {
        case status
        case disputeCategories = "dispute_categories"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension PaypalSellerProtection {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.status) else {
            throw SdkValidationError(
                field: "status",
                code: "required",
                message: "Validation failed for 'status': value is required"
            )
        }
        status = try container.sdkDecodeRequired(.status)
        disputeCategories = try container.sdkDecodeIfPresent(.disputeCategories)
    }
}

public extension PaypalSellerProtection {
    init(
        status: PaypalSellerProtectionStatus,
        disputeCategories: [PaypalSellerProtectionDisputeCategoriesItem]? = nil
    ) {
        (self.status, self.disputeCategories) = (status, disputeCategories)
    }
}

/// Typed representation of the `PlatformEarningFeeSource` API schema.
public struct PlatformEarningFeeSource: Codable {
    /// Type of object that created the application fee.
    public var type: PlatformEarningFeeSourceType
    /// Charge ID that created this application fee.
    public var charge: String?
    /// Payout ID that created this application fee.
    public var payout: String?

    enum CodingKeys: String, CodingKey {
        case type
        case charge
        case payout
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension PlatformEarningFeeSource {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.type) else {
            throw SdkValidationError(
                field: "type",
                code: "required",
                message: "Validation failed for 'type': value is required"
            )
        }
        type = try container.sdkDecodeRequired(.type)
        charge = try container.sdkDecodeIfPresent(.charge)
        payout = try container.sdkDecodeIfPresent(.payout)
        if let value = charge {
            try validateLength("charge", value, min: nil, max: 5000)
        }
        if let value = payout {
            try validateLength("payout", value, min: nil, max: 5000)
        }
    }
}

public extension PlatformEarningFeeSource {
    init(type: PlatformEarningFeeSourceType, charge: String? = nil, payout: String? = nil) throws {
        (self.type, self.charge) = (type, charge)
        self.payout = payout
        if let value = self.charge {
            try validateLength("charge", value, min: nil, max: 5000)
        }
        if let value = self.payout {
            try validateLength("payout", value, min: nil, max: 5000)
        }
    }
}

/// Prices define the unit cost, currency, and (optional) billing cycle for both recurring and one-time purchases of
/// products. Products help you track inventory or provisioning, and prices help you track payment terms. Different
/// physical goods or levels of service should be represented by products, and pricing options should be represented
/// by prices. This approach lets you change prices without having to change your provisioning scheme. For example,
/// you might have a single…
public struct Price: Codable {
    /// Whether the price can be used for new purchases.
    public var active: Bool
    /// Describes how to compute the price per period. Either `per_unit` or `tiered`. `per_unit` indicates that the
    /// fixed amount (specified in `unit_amount` or `unit_amount_decimal`) will be charged per unit in `quantity`
    /// (for prices with `usage_type=licensed`), or per unit of total usage (for prices with `usage_type=metered`).
    /// `tiered` indicates that the unit pricing will be computed using a tiering strategy as defined using the
    /// `tiers` and `tiers_mode` attributes.
    public var billingScheme: PriceBillingScheme
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// Three-letter ISO currency code, in lowercase. Must be a supported currency.
    public var currency: String
    /// Unique identifier for the object.
    public var id: String
    /// If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is
    /// `false`.
    public var livemode: Bool
    /// Set of key-value pairs that you can attach to an object. This can be useful for storing additional
    /// information about the object in a structured format.
    public var metadata: [String: String]
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: PriceObject
    /// The ID of the product this price is associated with.
    public var product: PriceProduct
    /// One of `one_time` or `recurring` depending on whether the price is for a one-time purchase or a recurring
    /// (subscription) purchase.
    public var type: PriceType
    /// Prices defined in each available currency option. Each key must be a three-letter ISO currency code and a
    /// supported currency.
    public var currencyOptions: [String: CurrencyOption]?
    /// When set, provides configuration for the amount to be adjusted by the customer during Checkout Sessions and
    /// Payment Links.
    public var customUnitAmount: PriceCustomUnitAmount?
    /// A lookup key used to retrieve prices dynamically from a static string. This may be up to 200 characters.
    public var lookupKey: String?
    /// A brief description of the price, hidden from customers.
    public var nickname: String?
    /// The recurring components of a price such as `interval` and `usage_type`.
    public var recurring: PriceRecurring?
    /// Only required if a default tax behavior) was not provided in the Stripe Tax settings. Specifies whether the
    /// price is considered inclusive of taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or
    /// `unspecified`. Once specified as either `inclusive` or `exclusive`, it cannot be changed.
    public var taxBehavior: PriceTaxBehavior?
    /// Each element represents a pricing tier. This parameter requires `billing_scheme` to be set to `tiered`. See
    /// also the documentation for `billing_scheme`.
    public var tiers: [PriceTier]?
    /// Defines if the tiering price should be `graduated` or `volume` based. In `volume`-based tiering, the maximum
    /// quantity within a period determines the per unit price. In `graduated` tiering, pricing can change as the
    /// quantity grows.
    public var tiersMode: PriceTiersMode?
    /// Apply a transformation to the reported usage or set quantity before computing the amount billed. Cannot be
    /// combined with `tiers`.
    public var transformQuantity: PriceTransformQuantity?
    /// The unit amount in cents (or local equivalent) to be charged, represented as a whole integer if possible.
    /// Only set if `billing_scheme=per_unit`.
    public var unitAmount: Int?
    /// The unit amount in cents (or local equivalent) to be charged, represented as a decimal string with at most
    /// 12 decimal places. Only set if `billing_scheme=per_unit`.
    public var unitAmountDecimal: String?

    enum CodingKeys: String, CodingKey {
        case active
        case billingScheme = "billing_scheme"
        case created
        case currency
        case id
        case livemode
        case metadata
        case object
        case product
        case type
        case currencyOptions = "currency_options"
        case customUnitAmount = "custom_unit_amount"
        case lookupKey = "lookup_key"
        case nickname
        case recurring
        case taxBehavior = "tax_behavior"
        case tiers
        case tiersMode = "tiers_mode"
        case transformQuantity = "transform_quantity"
        case unitAmount = "unit_amount"
        case unitAmountDecimal = "unit_amount_decimal"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension Price {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        active = try container.sdkDecodeRequired(.active)
        billingScheme = try container.sdkDecodeRequired(.billingScheme)
        created = try container.sdkDecodeRequired(.created)
        currency = try container.sdkDecodeRequired(.currency)
        id = try container.sdkDecodeRequired(.id)
        livemode = try container.sdkDecodeRequired(.livemode)
        metadata = try container.sdkDecodeRequired(.metadata)
        object = try container.sdkDecodeRequired(.object)
        product = try container.sdkDecodeRequired(.product)
        type = try container.sdkDecodeRequired(.type)
        currencyOptions = try container.sdkDecodeIfPresent(.currencyOptions)
        customUnitAmount = try container.sdkDecodeIfPresent(.customUnitAmount)
        lookupKey = try container.sdkDecodeIfPresent(.lookupKey)
        nickname = try container.sdkDecodeIfPresent(.nickname)
        recurring = try container.sdkDecodeIfPresent(.recurring)
        taxBehavior = try container.sdkDecodeIfPresent(.taxBehavior)
        tiers = try container.sdkDecodeIfPresent(.tiers)
        tiersMode = try container.sdkDecodeIfPresent(.tiersMode)
        transformQuantity = try container.sdkDecodeIfPresent(.transformQuantity)
        unitAmount = try container.sdkDecodeIfPresent(.unitAmount)
        unitAmountDecimal = try container.sdkDecodeIfPresent(.unitAmountDecimal)
        try validateLength("id", id, min: nil, max: 5000)
        if let value = lookupKey {
            try validateLength("lookup_key", value, min: nil, max: 5000)
        }
        if let value = nickname {
            try validateLength("nickname", value, min: nil, max: 5000)
        }
    }
}

public extension Price {
    init(
        active: Bool,
        billingScheme: PriceBillingScheme,
        created: Int,
        currency: String,
        id: String,
        livemode: Bool,
        metadata: [String: String],
        object: PriceObject,
        product: PriceProduct,
        type: PriceType,
        currencyOptions: [String: CurrencyOption]? = nil,
        customUnitAmount: PriceCustomUnitAmount? = nil,
        lookupKey: String? = nil,
        nickname: String? = nil,
        recurring: PriceRecurring? = nil,
        taxBehavior: PriceTaxBehavior? = nil,
        tiers: [PriceTier]? = nil,
        tiersMode: PriceTiersMode? = nil,
        transformQuantity: PriceTransformQuantity? = nil,
        unitAmount: Int? = nil,
        unitAmountDecimal: String? = nil
    ) throws {
        (self.active, self.billingScheme) = (active, billingScheme)
        (self.created, self.currency) = (created, currency)
        (self.id, self.livemode) = (id, livemode)
        (self.metadata, self.object) = (metadata, object)
        (self.product, self.type) = (product, type)
        (self.currencyOptions, self.customUnitAmount) = (currencyOptions, customUnitAmount)
        (self.lookupKey, self.nickname) = (lookupKey, nickname)
        (self.recurring, self.taxBehavior) = (recurring, taxBehavior)
        (self.tiers, self.tiersMode) = (tiers, tiersMode)
        (self.transformQuantity, self.unitAmount) = (transformQuantity, unitAmount)
        self.unitAmountDecimal = unitAmountDecimal
        try validateLength("id", self.id, min: nil, max: 5000)
        if let value = self.lookupKey {
            try validateLength("lookup_key", value, min: nil, max: 5000)
        }
        if let value = self.nickname {
            try validateLength("nickname", value, min: nil, max: 5000)
        }
    }
}

public enum PriceCustomUnitAmount {
    case customUnitAmount(CustomUnitAmount)
}

extension PriceCustomUnitAmount: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for PriceCustomUnitAmount"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(CustomUnitAmount.self) {
            return .customUnitAmount(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .customUnitAmount(value): try container.encode(value); return true
        }
    }
}

public indirect enum PriceProduct {
    case stringValue(String)
    case product(Product)
    case deletedProduct(DeletedProduct)
}

extension PriceProduct: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(in: container, debugDescription: "No variant matched for PriceProduct")
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container.decode(Product.self) {
            return .product(value)
        }
        if let value = try? container.decode(DeletedProduct.self) {
            return .deletedProduct(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .product(value): try container.encode(value); return true
        case let .deletedProduct(value): try container.encode(value); return true
        }
    }
}

public enum PriceRecurring {
    case recurring(Recurring)
}

extension PriceRecurring: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(in: container, debugDescription: "No variant matched for PriceRecurring")
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(Recurring.self) {
            return .recurring(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .recurring(value): try container.encode(value); return true
        }
    }
}

public enum PriceTransformQuantity {
    case transformQuantity(TransformQuantity)
}

extension PriceTransformQuantity: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for PriceTransformQuantity"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(TransformQuantity.self) {
            return .transformQuantity(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .transformQuantity(value): try container.encode(value); return true
        }
    }
}

/// Products describe the specific goods or services you offer to your customers. For example, you might offer a
/// Standard and Premium version of your goods or service; each version would be a separate Product. They can be
/// used in conjunction with Prices to configure pricing in Payment Links, Checkout, and Subscriptions. Related
/// guides: Set up a subscription, share a Payment Link, accept payments with Checkout, and more about Products and
/// Prices
public struct Product: Codable {
    /// Whether the product is currently available for purchase.
    public var active: Bool
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// Unique identifier for the object.
    public var id: String
    /// A list of up to 8 URLs of images for this product, meant to be displayable to the customer.
    public var images: [String]
    /// If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is
    /// `false`.
    public var livemode: Bool
    /// A list of up to 15 marketing features for this product. These are displayed in pricing tables.
    public var marketingFeatures: [ProductMarketingFeature]
    /// Set of key-value pairs that you can attach to an object. This can be useful for storing additional
    /// information about the object in a structured format.
    public var metadata: [String: String]
    /// The product's name, meant to be displayable to the customer.
    public var name: String
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: ProductObject
    /// Time at which the object was last updated. Measured in seconds since the Unix epoch.
    public var updated: Int
    /// The ID of the Price object that is the default price for this product.
    public var defaultPrice: ProductDefaultPrice?
    /// The product's description, meant to be displayable to the customer. Use this field to optionally store a
    /// long form explanation of the product being sold for your own rendering purposes.
    public var description: String?
    /// The dimensions of this product for shipping purposes.
    public var packageDimensions: ProductPackageDimensions?
    /// Whether this product is shipped (i.e., physical goods).
    public var shippable: Bool?
    /// Extra information about a product which will appear on your customer's credit card statement. In the case
    /// that multiple products are billed at once, the first statement descriptor will be used. Only used for
    /// subscription payments.
    public var statementDescriptor: String?
    /// A tax code ID.
    public var taxCode: ProductTaxCode?
    /// A label that represents units of this product. When set, this will be included in customers' receipts,
    /// invoices, Checkout, and the customer portal.
    public var unitLabel: String?
    /// A URL of a publicly-accessible webpage for this product.
    public var url: String?

    enum CodingKeys: String, CodingKey {
        case active
        case created
        case id
        case images
        case livemode
        case marketingFeatures = "marketing_features"
        case metadata
        case name
        case object
        case updated
        case defaultPrice = "default_price"
        case description
        case packageDimensions = "package_dimensions"
        case shippable
        case statementDescriptor = "statement_descriptor"
        case taxCode = "tax_code"
        case unitLabel = "unit_label"
        case url
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension Product {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        active = try container.sdkDecodeRequired(.active)
        created = try container.sdkDecodeRequired(.created)
        id = try container.sdkDecodeRequired(.id)
        images = try container.sdkDecodeRequired(.images)
        livemode = try container.sdkDecodeRequired(.livemode)
        marketingFeatures = try container.sdkDecodeRequired(.marketingFeatures)
        metadata = try container.sdkDecodeRequired(.metadata)
        name = try container.sdkDecodeRequired(.name)
        object = try container.sdkDecodeRequired(.object)
        updated = try container.sdkDecodeRequired(.updated)
        defaultPrice = try container.sdkDecodeIfPresent(.defaultPrice)
        description = try container.sdkDecodeIfPresent(.description)
        packageDimensions = try container.sdkDecodeIfPresent(.packageDimensions)
        shippable = try container.sdkDecodeIfPresent(.shippable)
        statementDescriptor = try container.sdkDecodeIfPresent(.statementDescriptor)
        taxCode = try container.sdkDecodeIfPresent(.taxCode)
        unitLabel = try container.sdkDecodeIfPresent(.unitLabel)
        url = try container.sdkDecodeIfPresent(.url)
        try validateLength("id", id, min: nil, max: 5000)
        try validateLength("name", name, min: nil, max: 5000)
        if let value = description {
            try validateLength("description", value, min: nil, max: 5000)
        }
        if let value = statementDescriptor {
            try validateLength("statement_descriptor", value, min: nil, max: 5000)
        }
        if let value = unitLabel {
            try validateLength("unit_label", value, min: nil, max: 5000)
        }
        if let value = url {
            try validateLength("url", value, min: nil, max: 2048)
        }
    }
}
