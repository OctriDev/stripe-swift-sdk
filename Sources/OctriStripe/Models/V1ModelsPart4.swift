// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

/// V1 domain models
public extension AutomaticTax {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.enabled) else {
            throw SdkValidationError(
                field: "enabled",
                code: "required",
                message: "Validation failed for 'enabled': value is required"
            )
        }
        enabled = try container.sdkDecodeRequired(.enabled)
        disabledReason = try container.sdkDecodeIfPresent(.disabledReason)
        liability = try container.sdkDecodeIfPresent(.liability)
        provider = try container.sdkDecodeIfPresent(.provider)
        status = try container.sdkDecodeIfPresent(.status)
        if let value = provider {
            try validateLength("provider", value, min: nil, max: 5000)
        }
    }
}

public extension AutomaticTax {
    init(
        enabled: Bool,
        disabledReason: AutomaticTaxDisabledReason? = nil,
        liability: AutomaticTaxLiability? = nil,
        provider: String? = nil,
        status: AutomaticTaxStatus? = nil
    ) throws {
        (self.enabled, self.disabledReason) = (enabled, disabledReason)
        (self.liability, self.provider) = (liability, provider)
        self.status = status
        if let value = self.provider {
            try validateLength("provider", value, min: nil, max: 5000)
        }
    }
}

public enum AutomaticTaxLiability {
    case connectAccountReference(ConnectAccountReference)
}

extension AutomaticTaxLiability: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for AutomaticTaxLiability"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(ConnectAccountReference.self) {
            return .connectAccountReference(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .connectAccountReference(value): try container.encode(value); return true
        }
    }
}

/// Balance transactions represent funds moving through your Stripe account. Stripe creates them for every type of
/// transaction that enters or leaves your Stripe account balance. Related guide: Balance transaction types
public struct BalanceTransaction: Codable {
    /// Gross amount of this transaction (in cents (or local equivalent)). A positive value represents funds charged
    /// to another party, and a negative value represents funds sent to another party.
    public var amount: Int
    /// The date that the transaction's net funds become available in the Stripe balance.
    public var availableOn: Int
    /// The balance that this transaction impacts.
    public var balanceType: BalanceTransactionBalanceType
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// Three-letter ISO currency code, in lowercase. Must be a supported currency.
    public var currency: String
    /// Fees (in cents (or local equivalent)) paid for this transaction. Represented as a positive integer when
    /// assessed.
    public var fee: Int
    /// Detailed breakdown of fees (in cents (or local equivalent)) paid for this transaction.
    public var feeDetails: [Fee]
    /// Unique identifier for the object.
    public var id: String
    /// Net impact to a Stripe balance (in cents (or local equivalent)). A positive value represents incrementing a
    /// Stripe balance, and a negative value decrementing a Stripe balance. You can calculate the net impact of a
    /// transaction on a balance by `amount` - `fee`
    public var net: Int
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: BalanceTransactionObject
    /// Learn more about how reporting categories can help you understand balance transactions from an accounting
    /// perspective.
    public var reportingCategory: String
    /// The transaction's net funds status in the Stripe balance, which are either `available` or `pending`.
    public var status: String
    /// Transaction type: `tax_fund`, `adjustment`, `advance`, `advance_funding`, `anticipation_repayment`,
    /// `application_fee`, `application_fee_refund`, `charge`, `climate_order_purchase`, `climate_order_refund`,
    /// `connect_collection_transfer`, `contribution`, `inbound_transfer`, `inbound_transfer_reversal`,
    /// `issuing_authorization_hold`, `issuing_authorization_release`, `issuing_dispute`, `issuing_transaction`,
    /// `obligation_outbound`, `obligation_reversal_inbound`, `payment`,…
    public var type: BalanceTransactionType
    /// An arbitrary string attached to the object. Often useful for displaying to users.
    public var description: String?
    /// If applicable, this transaction uses an exchange rate. If money converts from currency A to currency B, then
    /// the `amount` in currency A, multiplied by the `exchange_rate`, equals the `amount` in currency B. For
    /// example, if you charge a customer 10.00 EUR, the PaymentIntent's `amount` is `1000` and `currency` is `eur`.
    /// If this converts to 12.34 USD in your Stripe account, the BalanceTransaction's `amount` is `1234`, its
    /// `currency` is `usd`, and the `exchange_rate` is `1.234`.
    public var exchangeRate: Double?
    /// This transaction relates to the Stripe object.
    public var source: BalanceTransactionSource?

    enum CodingKeys: String, CodingKey {
        case amount
        case availableOn = "available_on"
        case balanceType = "balance_type"
        case created
        case currency
        case fee
        case feeDetails = "fee_details"
        case id
        case net
        case object
        case reportingCategory = "reporting_category"
        case status
        case type
        case description
        case exchangeRate = "exchange_rate"
        case source
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension BalanceTransaction {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        amount = try container.sdkDecodeRequired(.amount)
        availableOn = try container.sdkDecodeRequired(.availableOn)
        balanceType = try container.sdkDecodeRequired(.balanceType)
        created = try container.sdkDecodeRequired(.created)
        currency = try container.sdkDecodeRequired(.currency)
        fee = try container.sdkDecodeRequired(.fee)
        feeDetails = try container.sdkDecodeRequired(.feeDetails)
        id = try container.sdkDecodeRequired(.id)
        net = try container.sdkDecodeRequired(.net)
        object = try container.sdkDecodeRequired(.object)
        reportingCategory = try container.sdkDecodeRequired(.reportingCategory)
        status = try container.sdkDecodeRequired(.status)
        type = try container.sdkDecodeRequired(.type)
        description = try container.sdkDecodeIfPresent(.description)
        exchangeRate = try container.sdkDecodeIfPresent(.exchangeRate)
        source = try container.sdkDecodeIfPresent(.source)
        try validateLength("id", id, min: nil, max: 5000)
        try validateLength("reporting_category", reportingCategory, min: nil, max: 5000)
        try validateLength("status", status, min: nil, max: 5000)
        if let value = description {
            try validateLength("description", value, min: nil, max: 5000)
        }
    }
}

public extension BalanceTransaction {
    init(
        amount: Int,
        availableOn: Int,
        balanceType: BalanceTransactionBalanceType,
        created: Int,
        currency: String,
        fee: Int,
        feeDetails: [Fee],
        id: String,
        net: Int,
        object: BalanceTransactionObject,
        reportingCategory: String,
        status: String,
        type: BalanceTransactionType,
        description: String? = nil,
        exchangeRate: Double? = nil,
        source: BalanceTransactionSource? = nil
    ) throws {
        (self.amount, self.availableOn) = (amount, availableOn)
        (self.balanceType, self.created) = (balanceType, created)
        (self.currency, self.fee) = (currency, fee)
        (self.feeDetails, self.id) = (feeDetails, id)
        (self.net, self.object) = (net, object)
        (self.reportingCategory, self.status) = (reportingCategory, status)
        (self.type, self.description) = (type, description)
        (self.exchangeRate, self.source) = (exchangeRate, source)
        try validateLength("id", self.id, min: nil, max: 5000)
        try validateLength("reporting_category", self.reportingCategory, min: nil, max: 5000)
        try validateLength("status", self.status, min: nil, max: 5000)
        if let value = self.description {
            try validateLength("description", value, min: nil, max: 5000)
        }
    }
}

/// These bank accounts are payment methods on `Customer` objects. On the other hand External Accounts are transfer
/// destinations on `Account` objects for connected accounts. They can be bank accounts or debit cards as well, and
/// are documented in the links above. Related guide: Bank debits and transfers
public struct BankAccount: Codable {
    /// Two-letter ISO code representing the country the bank account is located in.
    public var country: String
    /// Three-letter ISO code for the currency paid out to the bank account.
    public var currency: String
    /// Unique identifier for the object.
    public var id: String
    /// The last four digits of the bank account number.
    public var last4: String
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: BankAccountObject
    /// For bank accounts, possible values are `new`, `validated`, `verified`, `verification_failed`,
    /// `tokenized_account_number_deactivated` or `errored`. A bank account that hasn't had any activity or
    /// validation performed is `new`. If Stripe can determine that the bank account exists, its status will be
    /// `validated`. Note that there often isn’t enough information to know (e.g., for smaller credit unions), and
    /// the validation is not always run. If customer bank account verification…
    public var status: String
    /// The account this bank account belongs to. Only applicable on Accounts (not customers or recipients) This
    /// property is only available when returned as an External Account where controller.is_controller is `true`.
    public var account: BankAccountAccount?
    /// The name of the person or business that owns the bank account.
    public var accountHolderName: String?
    /// The type of entity that holds the account. This can be either `individual` or `company`.
    public var accountHolderType: String?
    /// The bank account type. This can only be `checking` or `savings` in most countries. In Japan, this can only
    /// be `futsu` or `toza`.
    public var accountType: String?
    /// A set of available payout methods for this bank account. Only values from this set should be passed as the
    /// `method` when creating a payout.
    public var availablePayoutMethods: [BankAccountAvailablePayoutMethodsItem]?
    /// Name of the bank associated with the routing number (e.g., `WELLS FARGO`).
    public var bankName: String?
    /// The ID of the customer that the bank account is associated with.
    public var customer: BankAccountCustomer?
    /// Whether this bank account is the default external account for its currency.
    public var defaultForCurrency: Bool?
    /// Uniquely identifies this particular bank account. You can use this attribute to check whether two bank
    /// accounts are the same.
    public var fingerprint: String?
    /// Information about the upcoming new requirements for the bank account, including what information needs to be
    /// collected, and by when.
    public var futureRequirements: BankAccountFutureRequirements?
    /// Set of key-value pairs that you can attach to an object. This can be useful for storing additional
    /// information about the object in a structured format.
    public var metadata: [String: String]?
    /// Information about the requirements for the bank account, including what information needs to be collected.
    public var requirements: BankAccountRequirements?
    /// The routing transit number for the bank account.
    public var routingNumber: String?

    enum CodingKeys: String, CodingKey {
        case country
        case currency
        case id
        case last4
        case object
        case status
        case account
        case accountHolderName = "account_holder_name"
        case accountHolderType = "account_holder_type"
        case accountType = "account_type"
        case availablePayoutMethods = "available_payout_methods"
        case bankName = "bank_name"
        case customer
        case defaultForCurrency = "default_for_currency"
        case fingerprint
        case futureRequirements = "future_requirements"
        case metadata
        case requirements
        case routingNumber = "routing_number"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension BankAccount {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.country) else {
            throw SdkValidationError(
                field: "country",
                code: "required",
                message: "Validation failed for 'country': value is required"
            )
        }
        guard container.contains(.currency) else {
            throw SdkValidationError(
                field: "currency",
                code: "required",
                message: "Validation failed for 'currency': value is required"
            )
        }
        guard container.contains(.id) else {
            throw SdkValidationError(
                field: "id",
                code: "required",
                message: "Validation failed for 'id': value is required"
            )
        }
        guard container.contains(.last4) else {
            throw SdkValidationError(
                field: "last4",
                code: "required",
                message: "Validation failed for 'last4': value is required"
            )
        }
        guard container.contains(.object) else {
            throw SdkValidationError(
                field: "object",
                code: "required",
                message: "Validation failed for 'object': value is required"
            )
        }
        guard container.contains(.status) else {
            throw SdkValidationError(
                field: "status",
                code: "required",
                message: "Validation failed for 'status': value is required"
            )
        }
        country = try container.sdkDecodeRequired(.country)
        currency = try container.sdkDecodeRequired(.currency)
        id = try container.sdkDecodeRequired(.id)
        last4 = try container.sdkDecodeRequired(.last4)
        object = try container.sdkDecodeRequired(.object)
        status = try container.sdkDecodeRequired(.status)
        account = try container.sdkDecodeIfPresent(.account)
        accountHolderName = try container.sdkDecodeIfPresent(.accountHolderName)
        accountHolderType = try container.sdkDecodeIfPresent(.accountHolderType)
        accountType = try container.sdkDecodeIfPresent(.accountType)
        availablePayoutMethods = try container.sdkDecodeIfPresent(.availablePayoutMethods)
        bankName = try container.sdkDecodeIfPresent(.bankName)
        customer = try container.sdkDecodeIfPresent(.customer)
        defaultForCurrency = try container.sdkDecodeIfPresent(.defaultForCurrency)
        fingerprint = try container.sdkDecodeIfPresent(.fingerprint)
        futureRequirements = try container.sdkDecodeIfPresent(.futureRequirements)
        metadata = try container.sdkDecodeIfPresent(.metadata)
        requirements = try container.sdkDecodeIfPresent(.requirements)
        routingNumber = try container.sdkDecodeIfPresent(.routingNumber)
        try sdkValidateConstraints()
    }
}

public extension BankAccount {
    init(
        country: String,
        currency: String,
        id: String,
        last4: String,
        object: BankAccountObject,
        status: String,
        account: BankAccountAccount? = nil,
        accountHolderName: String? = nil,
        accountHolderType: String? = nil,
        accountType: String? = nil,
        availablePayoutMethods: [BankAccountAvailablePayoutMethodsItem]? = nil,
        bankName: String? = nil,
        customer: BankAccountCustomer? = nil,
        defaultForCurrency: Bool? = nil,
        fingerprint: String? = nil,
        futureRequirements: BankAccountFutureRequirements? = nil,
        metadata: [String: String]? = nil,
        requirements: BankAccountRequirements? = nil,
        routingNumber: String? = nil
    ) throws {
        (self.country, self.currency) = (country, currency)
        (self.id, self.last4) = (id, last4)
        (self.object, self.status) = (object, status)
        (self.account, self.accountHolderName) = (account, accountHolderName)
        (self.accountHolderType, self.accountType) = (accountHolderType, accountType)
        (self.availablePayoutMethods, self.bankName) = (availablePayoutMethods, bankName)
        (self.customer, self.defaultForCurrency) = (customer, defaultForCurrency)
        (self.fingerprint, self.futureRequirements) = (fingerprint, futureRequirements)
        (self.metadata, self.requirements) = (metadata, requirements)
        self.routingNumber = routingNumber
        try sdkValidateConstraints()
    }
}

extension BankAccount {
    func sdkValidateConstraints() throws {
        try validateLength("country", country, min: nil, max: 5000)
        try validateLength("id", id, min: nil, max: 5000)
        try validateLength("last4", last4, min: nil, max: 5000)
        try validateLength("status", status, min: nil, max: 5000)
        if let value = accountHolderName {
            try validateLength("account_holder_name", value, min: nil, max: 5000)
        }
        if let value = accountHolderType {
            try validateLength("account_holder_type", value, min: nil, max: 5000)
        }
        if let value = accountType {
            try validateLength("account_type", value, min: nil, max: 5000)
        }
        if let value = bankName {
            try validateLength("bank_name", value, min: nil, max: 5000)
        }
        if let value = fingerprint {
            try validateLength("fingerprint", value, min: nil, max: 5000)
        }
        if let value = routingNumber {
            try validateLength("routing_number", value, min: nil, max: 5000)
        }
    }
}

public enum BankAccountAccount {
    case stringValue(String)
    case account(Account)
}

extension BankAccountAccount: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for BankAccountAccount"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container.decode(Account.self) {
            return .account(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .account(value): try container.encode(value); return true
        }
    }
}

public indirect enum BankAccountCustomer {
    case stringValue(String)
    case customer(Customer)
    case deletedCustomer(DeletedCustomer)
}

extension BankAccountCustomer: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for BankAccountCustomer"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container.decode(Customer.self) {
            return .customer(value)
        }
        if let value = try? container.decode(DeletedCustomer.self) {
            return .deletedCustomer(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .customer(value): try container.encode(value); return true
        case let .deletedCustomer(value): try container.encode(value); return true
        }
    }
}

public enum BankAccountFutureRequirements {
    case externalAccountRequirements(ExternalAccountRequirements)
}

extension BankAccountFutureRequirements: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for BankAccountFutureRequirements"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container
            .decode(ExternalAccountRequirements.self) {
            return .externalAccountRequirements(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .externalAccountRequirements(value): try container.encode(value); return true
        }
    }
}

public enum BankAccountRequirements {
    case externalAccountRequirements(ExternalAccountRequirements)
}

extension BankAccountRequirements: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for BankAccountRequirements"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container
            .decode(ExternalAccountRequirements.self) {
            return .externalAccountRequirements(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .externalAccountRequirements(value): try container.encode(value); return true
        }
    }
}

/// Typed representation of the `BillingBillResourceInvoicingParentsInvoiceParent` API schema.
public struct BillingBillResourceInvoicingParentsInvoiceParent: Codable {
    /// The type of parent that generated this invoice
    public var type: BillingBillResourceInvoicingParentsInvoiceParentType
    /// Details about the quote that generated this invoice
    public var quoteDetails: BillingBillResourceInvoicingParentsInvoiceParentQuoteDetails?
    /// Details about the subscription that generated this invoice
    public var subscriptionDetails: BillingBillResourceInvoicingParentsInvoiceParentSubscriptionDetails?

    enum CodingKeys: String, CodingKey {
        case type
        case quoteDetails = "quote_details"
        case subscriptionDetails = "subscription_details"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension BillingBillResourceInvoicingParentsInvoiceParent {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.type) else {
            throw SdkValidationError(
                field: "type",
                code: "required",
                message: "Validation failed for 'type': value is required"
            )
        }
        type = try container.sdkDecodeRequired(.type)
        quoteDetails = try container.sdkDecodeIfPresent(.quoteDetails)
        subscriptionDetails = try container.sdkDecodeIfPresent(.subscriptionDetails)
    }
}

public extension BillingBillResourceInvoicingParentsInvoiceParent {
    init(
        type: BillingBillResourceInvoicingParentsInvoiceParentType,
        quoteDetails: BillingBillResourceInvoicingParentsInvoiceParentQuoteDetails? = nil,
        subscriptionDetails: BillingBillResourceInvoicingParentsInvoiceParentSubscriptionDetails? = nil
    ) {
        (self.type, self.quoteDetails) = (type, quoteDetails)
        self.subscriptionDetails = subscriptionDetails
    }
}

public enum BillingBillResourceInvoicingParentsInvoiceParentQuoteDetails {
    case billingBillResourceInvoicingParentsInvoiceQuoteParent(BillingBillResourceInvoicingParentsInvoiceQuoteParent)
}
