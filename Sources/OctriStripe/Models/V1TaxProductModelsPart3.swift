// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

/// V1TaxProduct domain models
public extension TaxProductRegistrationsResourceCountryOptionsSimplified {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.type) else {
            throw SdkValidationError(
                field: "type",
                code: "required",
                message: "Validation failed for 'type': value is required"
            )
        }
        type = try container.sdkDecodeRequired(.type)
    }
}

public extension TaxProductRegistrationsResourceCountryOptionsSimplified {
    init(type: TaxProductRegistrationsResourceCountryOptionsSimplifiedType) {
        self.type = type
    }
}

/// Typed representation of the `TaxProductRegistrationsResourceCountryOptionsThailand` API schema.
public struct TaxProductRegistrationsResourceCountryOptionsThailand: Codable {
    /// Type of registration in `country`.
    public var type: TaxProductRegistrationsResourceCountryOptionsThailandType

    enum CodingKeys: String, CodingKey {
        case type
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension TaxProductRegistrationsResourceCountryOptionsThailand {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.type) else {
            throw SdkValidationError(
                field: "type",
                code: "required",
                message: "Validation failed for 'type': value is required"
            )
        }
        type = try container.sdkDecodeRequired(.type)
    }
}

public extension TaxProductRegistrationsResourceCountryOptionsThailand {
    init(type: TaxProductRegistrationsResourceCountryOptionsThailandType) {
        self.type = type
    }
}

/// Typed representation of the `TaxProductRegistrationsResourceCountryOptionsUnitedStates` API schema.
public struct TaxProductRegistrationsResourceCountryOptionsUnitedStates: Codable {
    /// Two-letter US state code (ISO 3166-2).
    public var state: String
    /// Type of registration in the US.
    public var type: TaxProductRegistrationsResourceCountryOptionsUnitedStatesType
    /// Optional object value serialized in the `local_amusement_tax` wire field.
    public var localAmusementTax: TaxProductRegistrationsResourceCountryOptionsUsLocalAmusementTax?
    /// Optional object value serialized in the `local_lease_tax` wire field.
    public var localLeaseTax: TaxProductRegistrationsResourceCountryOptionsUsLocalLeaseTax?
    /// Optional object value serialized in the `mass_transit_parking_tax` wire field.
    public var massTransitParkingTax: TaxProductRegistrationsResourceCountryOptionsUsMassTransitParkingTax?
    /// Optional object value serialized in the `parking_tax` wire field.
    public var parkingTax: TaxProductRegistrationsResourceCountryOptionsUsParkingTax?
    /// Optional object value serialized in the `state_sales_tax` wire field.
    public var stateSalesTax: TaxProductRegistrationsResourceCountryOptionsUsStateSalesTax?

    enum CodingKeys: String, CodingKey {
        case state
        case type
        case localAmusementTax = "local_amusement_tax"
        case localLeaseTax = "local_lease_tax"
        case massTransitParkingTax = "mass_transit_parking_tax"
        case parkingTax = "parking_tax"
        case stateSalesTax = "state_sales_tax"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension TaxProductRegistrationsResourceCountryOptionsUnitedStates {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.state) else {
            throw SdkValidationError(
                field: "state",
                code: "required",
                message: "Validation failed for 'state': value is required"
            )
        }
        guard container.contains(.type) else {
            throw SdkValidationError(
                field: "type",
                code: "required",
                message: "Validation failed for 'type': value is required"
            )
        }
        state = try container.sdkDecodeRequired(.state)
        type = try container.sdkDecodeRequired(.type)
        localAmusementTax = try container.sdkDecodeIfPresent(.localAmusementTax)
        localLeaseTax = try container.sdkDecodeIfPresent(.localLeaseTax)
        massTransitParkingTax = try container.sdkDecodeIfPresent(.massTransitParkingTax)
        parkingTax = try container.sdkDecodeIfPresent(.parkingTax)
        stateSalesTax = try container.sdkDecodeIfPresent(.stateSalesTax)
        try validateLength("state", state, min: nil, max: 5000)
    }
}

public extension TaxProductRegistrationsResourceCountryOptionsUnitedStates {
    init(
        state: String,
        type: TaxProductRegistrationsResourceCountryOptionsUnitedStatesType,
        localAmusementTax: TaxProductRegistrationsResourceCountryOptionsUsLocalAmusementTax? = nil,
        localLeaseTax: TaxProductRegistrationsResourceCountryOptionsUsLocalLeaseTax? = nil,
        massTransitParkingTax: TaxProductRegistrationsResourceCountryOptionsUsMassTransitParkingTax? = nil,
        parkingTax: TaxProductRegistrationsResourceCountryOptionsUsParkingTax? = nil,
        stateSalesTax: TaxProductRegistrationsResourceCountryOptionsUsStateSalesTax? = nil
    ) throws {
        (self.state, self.type) = (state, type)
        (self.localAmusementTax, self.localLeaseTax) = (localAmusementTax, localLeaseTax)
        (self.massTransitParkingTax, self.parkingTax) = (massTransitParkingTax, parkingTax)
        self.stateSalesTax = stateSalesTax
        try validateLength("state", self.state, min: nil, max: 5000)
    }
}

/// Typed representation of the `TaxProductRegistrationsResourceCountryOptionsUsLocalAmusementTax` API schema.
public struct TaxProductRegistrationsResourceCountryOptionsUsLocalAmusementTax: Codable {
    /// A FIPS code representing the local jurisdiction.
    public var jurisdiction: String

    enum CodingKeys: String, CodingKey {
        case jurisdiction
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension TaxProductRegistrationsResourceCountryOptionsUsLocalAmusementTax {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.jurisdiction) else {
            throw SdkValidationError(
                field: "jurisdiction",
                code: "required",
                message: "Validation failed for 'jurisdiction': value is required"
            )
        }
        jurisdiction = try container.sdkDecodeRequired(.jurisdiction)
        try validateLength("jurisdiction", jurisdiction, min: nil, max: 5000)
    }
}

public extension TaxProductRegistrationsResourceCountryOptionsUsLocalAmusementTax {
    init(jurisdiction: String) throws {
        self.jurisdiction = jurisdiction
        try validateLength("jurisdiction", self.jurisdiction, min: nil, max: 5000)
    }
}

/// Typed representation of the `TaxProductRegistrationsResourceCountryOptionsUsLocalLeaseTax` API schema.
public struct TaxProductRegistrationsResourceCountryOptionsUsLocalLeaseTax: Codable {
    /// A FIPS code representing the local jurisdiction.
    public var jurisdiction: String

    enum CodingKeys: String, CodingKey {
        case jurisdiction
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension TaxProductRegistrationsResourceCountryOptionsUsLocalLeaseTax {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.jurisdiction) else {
            throw SdkValidationError(
                field: "jurisdiction",
                code: "required",
                message: "Validation failed for 'jurisdiction': value is required"
            )
        }
        jurisdiction = try container.sdkDecodeRequired(.jurisdiction)
        try validateLength("jurisdiction", jurisdiction, min: nil, max: 5000)
    }
}

public extension TaxProductRegistrationsResourceCountryOptionsUsLocalLeaseTax {
    init(jurisdiction: String) throws {
        self.jurisdiction = jurisdiction
        try validateLength("jurisdiction", self.jurisdiction, min: nil, max: 5000)
    }
}

/// Typed representation of the `TaxProductRegistrationsResourceCountryOptionsUsMassTransitParkingTax` API schema.
public struct TaxProductRegistrationsResourceCountryOptionsUsMassTransitParkingTax: Codable {
    /// A jurisdiction code representing the local jurisdiction.
    public var jurisdiction: String

    enum CodingKeys: String, CodingKey {
        case jurisdiction
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension TaxProductRegistrationsResourceCountryOptionsUsMassTransitParkingTax {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.jurisdiction) else {
            throw SdkValidationError(
                field: "jurisdiction",
                code: "required",
                message: "Validation failed for 'jurisdiction': value is required"
            )
        }
        jurisdiction = try container.sdkDecodeRequired(.jurisdiction)
        try validateLength("jurisdiction", jurisdiction, min: nil, max: 5000)
    }
}

public extension TaxProductRegistrationsResourceCountryOptionsUsMassTransitParkingTax {
    init(jurisdiction: String) throws {
        self.jurisdiction = jurisdiction
        try validateLength("jurisdiction", self.jurisdiction, min: nil, max: 5000)
    }
}

/// Typed representation of the `TaxProductRegistrationsResourceCountryOptionsUsParkingTax` API schema.
public struct TaxProductRegistrationsResourceCountryOptionsUsParkingTax: Codable {
    /// A jurisdiction code representing the local jurisdiction.
    public var jurisdiction: String

    enum CodingKeys: String, CodingKey {
        case jurisdiction
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension TaxProductRegistrationsResourceCountryOptionsUsParkingTax {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.jurisdiction) else {
            throw SdkValidationError(
                field: "jurisdiction",
                code: "required",
                message: "Validation failed for 'jurisdiction': value is required"
            )
        }
        jurisdiction = try container.sdkDecodeRequired(.jurisdiction)
        try validateLength("jurisdiction", jurisdiction, min: nil, max: 5000)
    }
}

public extension TaxProductRegistrationsResourceCountryOptionsUsParkingTax {
    init(jurisdiction: String) throws {
        self.jurisdiction = jurisdiction
        try validateLength("jurisdiction", self.jurisdiction, min: nil, max: 5000)
    }
}

public typealias TaxProductRegistrationsResourceCountryOptionsUsStateSalesTaxEXdd31906a0e =
    [TaxProductRegistrationsResourceCountryOptionsUsStateSalesTaxElection]

/// Typed representation of the `TaxProductRegistrationsResourceCountryOptionsUsStateSalesTax` API schema.
public struct TaxProductRegistrationsResourceCountryOptionsUsStateSalesTax: Codable {
    /// Elections for the state sales tax registration.
    public var elections: TaxProductRegistrationsResourceCountryOptionsUsStateSalesTaxEXdd31906a0e?

    enum CodingKeys: String, CodingKey {
        case elections
    }

    init() {
        elections = nil
    }
}

public extension TaxProductRegistrationsResourceCountryOptionsUsStateSalesTax {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        elections = try container.sdkDecodeIfPresent(.elections)
    }
}

public extension TaxProductRegistrationsResourceCountryOptionsUsStateSalesTax {
    init(elections: TaxProductRegistrationsResourceCountryOptionsUsStateSalesTaxEXdd31906a0e? = nil) {
        self.init()
        self.elections = elections
    }
}

/// Typed representation of the `TaxProductRegistrationsResourceCountryOptionsUsStateSalesTaxElection` API schema.
public struct TaxProductRegistrationsResourceCountryOptionsUsStateSalesTaxElection: Codable {
    /// The type of the election for the state sales tax registration.
    public var type: TaxProductRegistrationsResourceCountryOptionsUsStateSalesTaxElectionType
    /// A FIPS code representing the local jurisdiction.
    public var jurisdiction: String?

    enum CodingKeys: String, CodingKey {
        case type
        case jurisdiction
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension TaxProductRegistrationsResourceCountryOptionsUsStateSalesTaxElection {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.type) else {
            throw SdkValidationError(
                field: "type",
                code: "required",
                message: "Validation failed for 'type': value is required"
            )
        }
        type = try container.sdkDecodeRequired(.type)
        jurisdiction = try container.sdkDecodeIfPresent(.jurisdiction)
        if let value = jurisdiction {
            try validateLength("jurisdiction", value, min: nil, max: 5000)
        }
    }
}

public extension TaxProductRegistrationsResourceCountryOptionsUsStateSalesTaxElection {
    init(
        type: TaxProductRegistrationsResourceCountryOptionsUsStateSalesTaxElectionType,
        jurisdiction: String? = nil
    ) throws {
        (self.type, self.jurisdiction) = (type, jurisdiction)
        if let value = self.jurisdiction {
            try validateLength("jurisdiction", value, min: nil, max: 5000)
        }
    }
}

/// Typed representation of the `TaxProductResourceCustomerDetails` API schema.
public struct TaxProductResourceCustomerDetails: Codable {
    /// The customer's tax IDs (for example, EU VAT numbers).
    public var taxIds: [TaxProductResourceCustomerDetailsResourceTaxId]
    /// The taxability override used for taxation.
    public var taxabilityOverride: TaxProductResourceCustomerDetailsTaxabilityOverride
    /// The customer's postal address (for example, home or business location).
    public var address: TaxProductResourceCustomerDetailsAddress?
    /// The type of customer address provided.
    public var addressSource: TaxProductResourceCustomerDetailsAddressSource?
    /// The customer's IP address (IPv4 or IPv6).
    public var ipAddress: String?

    enum CodingKeys: String, CodingKey {
        case taxIds = "tax_ids"
        case taxabilityOverride = "taxability_override"
        case address
        case addressSource = "address_source"
        case ipAddress = "ip_address"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension TaxProductResourceCustomerDetails {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.taxIds) else {
            throw SdkValidationError(
                field: "tax_ids",
                code: "required",
                message: "Validation failed for 'tax_ids': value is required"
            )
        }
        guard container.contains(.taxabilityOverride) else {
            throw SdkValidationError(
                field: "taxability_override",
                code: "required",
                message: "Validation failed for 'taxability_override': value is required"
            )
        }
        taxIds = try container.sdkDecodeRequired(.taxIds)
        taxabilityOverride = try container.sdkDecodeRequired(.taxabilityOverride)
        address = try container.sdkDecodeIfPresent(.address)
        addressSource = try container.sdkDecodeIfPresent(.addressSource)
        ipAddress = try container.sdkDecodeIfPresent(.ipAddress)
        if let value = ipAddress {
            try validateLength("ip_address", value, min: nil, max: 5000)
        }
    }
}

public extension TaxProductResourceCustomerDetails {
    init(
        taxIds: [TaxProductResourceCustomerDetailsResourceTaxId],
        taxabilityOverride: TaxProductResourceCustomerDetailsTaxabilityOverride,
        address: TaxProductResourceCustomerDetailsAddress? = nil,
        addressSource: TaxProductResourceCustomerDetailsAddressSource? = nil,
        ipAddress: String? = nil
    ) throws {
        (self.taxIds, self.taxabilityOverride) = (taxIds, taxabilityOverride)
        (self.address, self.addressSource) = (address, addressSource)
        self.ipAddress = ipAddress
        if let value = self.ipAddress {
            try validateLength("ip_address", value, min: nil, max: 5000)
        }
    }
}

public enum TaxProductResourceCustomerDetailsAddress {
    case taxProductResourcePostalAddress(TaxProductResourcePostalAddress)
}

extension TaxProductResourceCustomerDetailsAddress: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for TaxProductResourceCustomerDetailsAddress"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container
            .decode(TaxProductResourcePostalAddress.self) {
            return .taxProductResourcePostalAddress(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .taxProductResourcePostalAddress(value): try container.encode(value); return true
        }
    }
}

/// Typed representation of the `TaxProductResourceCustomerDetailsResourceTaxId` API schema.
public struct TaxProductResourceCustomerDetailsResourceTaxId: Codable {
    /// The type of the tax ID, one of `ad_nrt`, `ar_cuit`, `eu_vat`, `bo_tin`, `br_cnpj`, `br_cpf`, `cn_tin`,
    /// `co_nit`, `cr_tin`, `do_rcn`, `ec_ruc`, `eu_oss_vat`, `hr_oib`, `pe_ruc`, `ro_tin`, `rs_pib`, `sv_nit`,
    /// `uy_ruc`, `ve_rif`, `vn_tin`, `gb_vat`, `nz_gst`, `au_abn`, `au_arn`, `in_gst`, `no_vat`, `no_voec`,
    /// `za_vat`, `ch_vat`, `mx_rfc`, `sg_uen`, `ru_inn`, `ru_kpp`, `ca_bn`, `hk_br`, `es_cif`, `pl_nip`, `it_cf`,
    /// `fo_vat`, `gi_tin`, `py_ruc`, `tw_vat`, `th_vat`, `jp_cn`,…
    public var type: TaxProductResourceCustomerDetailsResourceTaxIdType
    /// The value of the tax ID.
    public var value: String

    enum CodingKeys: String, CodingKey {
        case type
        case value
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension TaxProductResourceCustomerDetailsResourceTaxId {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.type) else {
            throw SdkValidationError(
                field: "type",
                code: "required",
                message: "Validation failed for 'type': value is required"
            )
        }
        guard container.contains(.value) else {
            throw SdkValidationError(
                field: "value",
                code: "required",
                message: "Validation failed for 'value': value is required"
            )
        }
        type = try container.sdkDecodeRequired(.type)
        value = try container.sdkDecodeRequired(.value)
        try validateLength("value", value, min: nil, max: 5000)
    }
}

public extension TaxProductResourceCustomerDetailsResourceTaxId {
    init(type: TaxProductResourceCustomerDetailsResourceTaxIdType, value: String) throws {
        (self.type, self.value) = (type, value)
        try validateLength("value", self.value, min: nil, max: 5000)
    }
}

/// Typed representation of the `TaxProductResourceJurisdiction` API schema.
public struct TaxProductResourceJurisdiction: Codable {
    /// Two-letter country code (ISO 3166-1 alpha-2).
    public var country: String
    /// A human-readable name for the jurisdiction imposing the tax.
    public var displayName: String
    /// Indicates the level of the jurisdiction imposing the tax.
    public var level: TaxProductResourceJurisdictionLevel
    /// ISO 3166-2 subdivision code, without country prefix. For example, "NY" for New York, United States.
    public var state: String?

    enum CodingKeys: String, CodingKey {
        case country
        case displayName = "display_name"
        case level
        case state
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension TaxProductResourceJurisdiction {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.country) else {
            throw SdkValidationError(
                field: "country",
                code: "required",
                message: "Validation failed for 'country': value is required"
            )
        }
        guard container.contains(.displayName) else {
            throw SdkValidationError(
                field: "display_name",
                code: "required",
                message: "Validation failed for 'display_name': value is required"
            )
        }
        guard container.contains(.level) else {
            throw SdkValidationError(
                field: "level",
                code: "required",
                message: "Validation failed for 'level': value is required"
            )
        }
        country = try container.sdkDecodeRequired(.country)
        displayName = try container.sdkDecodeRequired(.displayName)
        level = try container.sdkDecodeRequired(.level)
        state = try container.sdkDecodeIfPresent(.state)
        try validateLength("country", country, min: nil, max: 5000)
        try validateLength("display_name", displayName, min: nil, max: 5000)
        if let value = state {
            try validateLength("state", value, min: nil, max: 5000)
        }
    }
}

public extension TaxProductResourceJurisdiction {
    init(
        country: String,
        displayName: String,
        level: TaxProductResourceJurisdictionLevel,
        state: String? = nil
    ) throws {
        (self.country, self.displayName) = (country, displayName)
        (self.level, self.state) = (level, state)
        try validateLength("country", self.country, min: nil, max: 5000)
        try validateLength("display_name", self.displayName, min: nil, max: 5000)
        if let value = self.state {
            try validateLength("state", value, min: nil, max: 5000)
        }
    }
}

/// Typed representation of the `TaxProductResourceLineItemTaxBreakdown` API schema.
public struct TaxProductResourceLineItemTaxBreakdown: Codable {
    /// The amount of tax, in the smallest currency unit.
    public var amount: Int
    /// Required object value serialized in the `jurisdiction` wire field.
    public var jurisdiction: TaxProductResourceJurisdiction
    /// Indicates whether the jurisdiction was determined by the origin (merchant's address) or destination
    /// (customer's address).
    public var sourcing: TaxProductResourceLineItemTaxBreakdownSourcing
    /// The reasoning behind this tax, for example, if the product is tax exempt. The possible values for this field
    /// may be extended as new tax rules are supported.
    public var taxabilityReason: TaxProductResourceLineItemTaxBreakdownTaxabilityReason
    /// The amount on which tax is calculated, in the smallest currency unit.
    public var taxableAmount: Int
    /// Details regarding the rate for this tax. This field will be `null` when the tax is not imposed, for example
    /// if the product is exempt from tax.
    public var taxRateDetails: TaxProductResourceLineItemTaxBreakdownTaxRateDetails?

    enum CodingKeys: String, CodingKey {
        case amount
        case jurisdiction
        case sourcing
        case taxabilityReason = "taxability_reason"
        case taxableAmount = "taxable_amount"
        case taxRateDetails = "tax_rate_details"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}
