// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

/// V1Deleted domain models
public enum DeletedExternalAccount {
    case deletedBankAccount(DeletedBankAccount)
    case deletedCard(DeletedCard)
}

extension DeletedExternalAccount: Codable {
    private enum CodingKeys: String, CodingKey {
        case discriminator = "object"
    }

    public init(from decoder: Decoder) throws {
        let tagged = try decoder.container(keyedBy: CodingKeys.self)
        let discriminator = try tagged.decode(String.self, forKey: .discriminator)
        if let value = try Self.decodeGroup1(discriminator, from: decoder) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            forKey: .discriminator,
            in: tagged,
            debugDescription: "Unknown discriminator for DeletedExternalAccount"
        )
    }

    private static func decodeGroup1(_ discriminator: String, from decoder: Decoder) throws -> Self? {
        switch discriminator {
        case "bank_account": try .deletedBankAccount(DeletedBankAccount(from: decoder))
        case "card": try .deletedCard(DeletedCard(from: decoder))
        default: nil
        }
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .deletedBankAccount(value): try container.encode(value); return true
        case let .deletedCard(value): try container.encode(value); return true
        }
    }
}

public enum DeletedPaymentSource {
    case deletedBankAccount(DeletedBankAccount)
    case deletedCard(DeletedCard)
}

extension DeletedPaymentSource: Codable {
    private enum CodingKeys: String, CodingKey {
        case discriminator = "object"
    }

    public init(from decoder: Decoder) throws {
        let tagged = try decoder.container(keyedBy: CodingKeys.self)
        let discriminator = try tagged.decode(String.self, forKey: .discriminator)
        if let value = try Self.decodeGroup1(discriminator, from: decoder) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            forKey: .discriminator,
            in: tagged,
            debugDescription: "Unknown discriminator for DeletedPaymentSource"
        )
    }

    private static func decodeGroup1(_ discriminator: String, from decoder: Decoder) throws -> Self? {
        switch discriminator {
        case "bank_account": try .deletedBankAccount(DeletedBankAccount(from: decoder))
        case "card": try .deletedCard(DeletedCard(from: decoder))
        default: nil
        }
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .deletedBankAccount(value): try container.encode(value); return true
        case let .deletedCard(value): try container.encode(value); return true
        }
    }
}

/// Typed representation of the `DeletedAccount` API schema.
public struct DeletedAccount: Codable {
    /// Always true for a deleted object
    public var deleted: Bool
    /// Unique identifier for the object.
    public var id: String
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: DeletedAccountObject

    enum CodingKeys: String, CodingKey {
        case deleted
        case id
        case object
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension DeletedAccount {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.deleted) else {
            throw SdkValidationError(
                field: "deleted",
                code: "required",
                message: "Validation failed for 'deleted': value is required"
            )
        }
        guard container.contains(.id) else {
            throw SdkValidationError(
                field: "id",
                code: "required",
                message: "Validation failed for 'id': value is required"
            )
        }
        guard container.contains(.object) else {
            throw SdkValidationError(
                field: "object",
                code: "required",
                message: "Validation failed for 'object': value is required"
            )
        }
        deleted = try container.sdkDecodeRequired(.deleted)
        id = try container.sdkDecodeRequired(.id)
        object = try container.sdkDecodeRequired(.object)
        try validateLength("id", id, min: nil, max: 5000)
    }
}

public extension DeletedAccount {
    init(deleted: Bool, id: String, object: DeletedAccountObject) throws {
        (self.deleted, self.id) = (deleted, id)
        self.object = object
        try validateLength("id", self.id, min: nil, max: 5000)
    }
}

/// Typed representation of the `DeletedApplePayDomain` API schema.
public struct DeletedApplePayDomain: Codable {
    /// Always true for a deleted object
    public var deleted: Bool
    /// Unique identifier for the object.
    public var id: String
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: DeletedApplePayDomainObject

    enum CodingKeys: String, CodingKey {
        case deleted
        case id
        case object
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension DeletedApplePayDomain {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.deleted) else {
            throw SdkValidationError(
                field: "deleted",
                code: "required",
                message: "Validation failed for 'deleted': value is required"
            )
        }
        guard container.contains(.id) else {
            throw SdkValidationError(
                field: "id",
                code: "required",
                message: "Validation failed for 'id': value is required"
            )
        }
        guard container.contains(.object) else {
            throw SdkValidationError(
                field: "object",
                code: "required",
                message: "Validation failed for 'object': value is required"
            )
        }
        deleted = try container.sdkDecodeRequired(.deleted)
        id = try container.sdkDecodeRequired(.id)
        object = try container.sdkDecodeRequired(.object)
        try validateLength("id", id, min: nil, max: 5000)
    }
}

public extension DeletedApplePayDomain {
    init(deleted: Bool, id: String, object: DeletedApplePayDomainObject) throws {
        (self.deleted, self.id) = (deleted, id)
        self.object = object
        try validateLength("id", self.id, min: nil, max: 5000)
    }
}

/// Typed representation of the `DeletedApplication` API schema.
public struct DeletedApplication: Codable {
    /// Always true for a deleted object
    public var deleted: Bool
    /// Unique identifier for the object.
    public var id: String
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: DeletedApplicationObject
    /// The name of the application.
    public var name: String?

    enum CodingKeys: String, CodingKey {
        case deleted
        case id
        case object
        case name
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension DeletedApplication {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.deleted) else {
            throw SdkValidationError(
                field: "deleted",
                code: "required",
                message: "Validation failed for 'deleted': value is required"
            )
        }
        guard container.contains(.id) else {
            throw SdkValidationError(
                field: "id",
                code: "required",
                message: "Validation failed for 'id': value is required"
            )
        }
        guard container.contains(.object) else {
            throw SdkValidationError(
                field: "object",
                code: "required",
                message: "Validation failed for 'object': value is required"
            )
        }
        deleted = try container.sdkDecodeRequired(.deleted)
        id = try container.sdkDecodeRequired(.id)
        object = try container.sdkDecodeRequired(.object)
        name = try container.sdkDecodeIfPresent(.name)
        try validateLength("id", id, min: nil, max: 5000)
        if let value = name {
            try validateLength("name", value, min: nil, max: 5000)
        }
    }
}

public extension DeletedApplication {
    init(deleted: Bool, id: String, object: DeletedApplicationObject, name: String? = nil) throws {
        (self.deleted, self.id) = (deleted, id)
        (self.object, self.name) = (object, name)
        try validateLength("id", self.id, min: nil, max: 5000)
        if let value = self.name {
            try validateLength("name", value, min: nil, max: 5000)
        }
    }
}

/// Typed representation of the `DeletedBankAccount` API schema.
public struct DeletedBankAccount: Codable {
    /// Always true for a deleted object
    public var deleted: Bool
    /// Unique identifier for the object.
    public var id: String
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: DeletedBankAccountObject
    /// Three-letter ISO code for the currency paid out to the bank account.
    public var currency: String?

    enum CodingKeys: String, CodingKey {
        case deleted
        case id
        case object
        case currency
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension DeletedBankAccount {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.deleted) else {
            throw SdkValidationError(
                field: "deleted",
                code: "required",
                message: "Validation failed for 'deleted': value is required"
            )
        }
        guard container.contains(.id) else {
            throw SdkValidationError(
                field: "id",
                code: "required",
                message: "Validation failed for 'id': value is required"
            )
        }
        guard container.contains(.object) else {
            throw SdkValidationError(
                field: "object",
                code: "required",
                message: "Validation failed for 'object': value is required"
            )
        }
        deleted = try container.sdkDecodeRequired(.deleted)
        id = try container.sdkDecodeRequired(.id)
        object = try container.sdkDecodeRequired(.object)
        currency = try container.sdkDecodeIfPresent(.currency)
        try validateLength("id", id, min: nil, max: 5000)
        if let value = currency {
            try validateLength("currency", value, min: nil, max: 5000)
        }
    }
}

public extension DeletedBankAccount {
    init(deleted: Bool, id: String, object: DeletedBankAccountObject, currency: String? = nil) throws {
        (self.deleted, self.id) = (deleted, id)
        (self.object, self.currency) = (object, currency)
        try validateLength("id", self.id, min: nil, max: 5000)
        if let value = self.currency {
            try validateLength("currency", value, min: nil, max: 5000)
        }
    }
}

/// Typed representation of the `DeletedCard` API schema.
public struct DeletedCard: Codable {
    /// Always true for a deleted object
    public var deleted: Bool
    /// Unique identifier for the object.
    public var id: String
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: DeletedCardObject
    /// Three-letter ISO code for the currency paid out to the bank account.
    public var currency: String?

    enum CodingKeys: String, CodingKey {
        case deleted
        case id
        case object
        case currency
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension DeletedCard {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.deleted) else {
            throw SdkValidationError(
                field: "deleted",
                code: "required",
                message: "Validation failed for 'deleted': value is required"
            )
        }
        guard container.contains(.id) else {
            throw SdkValidationError(
                field: "id",
                code: "required",
                message: "Validation failed for 'id': value is required"
            )
        }
        guard container.contains(.object) else {
            throw SdkValidationError(
                field: "object",
                code: "required",
                message: "Validation failed for 'object': value is required"
            )
        }
        deleted = try container.sdkDecodeRequired(.deleted)
        id = try container.sdkDecodeRequired(.id)
        object = try container.sdkDecodeRequired(.object)
        currency = try container.sdkDecodeIfPresent(.currency)
        try validateLength("id", id, min: nil, max: 5000)
        if let value = currency {
            try validateLength("currency", value, min: nil, max: 5000)
        }
    }
}

public extension DeletedCard {
    init(deleted: Bool, id: String, object: DeletedCardObject, currency: String? = nil) throws {
        (self.deleted, self.id) = (deleted, id)
        (self.object, self.currency) = (object, currency)
        try validateLength("id", self.id, min: nil, max: 5000)
        if let value = self.currency {
            try validateLength("currency", value, min: nil, max: 5000)
        }
    }
}

/// Typed representation of the `DeletedCoupon` API schema.
public struct DeletedCoupon: Codable {
    /// Always true for a deleted object
    public var deleted: Bool
    /// Unique identifier for the object.
    public var id: String
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: DeletedCouponObject

    enum CodingKeys: String, CodingKey {
        case deleted
        case id
        case object
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension DeletedCoupon {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.deleted) else {
            throw SdkValidationError(
                field: "deleted",
                code: "required",
                message: "Validation failed for 'deleted': value is required"
            )
        }
        guard container.contains(.id) else {
            throw SdkValidationError(
                field: "id",
                code: "required",
                message: "Validation failed for 'id': value is required"
            )
        }
        guard container.contains(.object) else {
            throw SdkValidationError(
                field: "object",
                code: "required",
                message: "Validation failed for 'object': value is required"
            )
        }
        deleted = try container.sdkDecodeRequired(.deleted)
        id = try container.sdkDecodeRequired(.id)
        object = try container.sdkDecodeRequired(.object)
        try validateLength("id", id, min: nil, max: 5000)
    }
}

public extension DeletedCoupon {
    init(deleted: Bool, id: String, object: DeletedCouponObject) throws {
        (self.deleted, self.id) = (deleted, id)
        self.object = object
        try validateLength("id", self.id, min: nil, max: 5000)
    }
}

/// Typed representation of the `DeletedCustomer` API schema.
public struct DeletedCustomer: Codable {
    /// Always true for a deleted object
    public var deleted: Bool
    /// Unique identifier for the object.
    public var id: String
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: DeletedCustomerObject

    enum CodingKeys: String, CodingKey {
        case deleted
        case id
        case object
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension DeletedCustomer {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.deleted) else {
            throw SdkValidationError(
                field: "deleted",
                code: "required",
                message: "Validation failed for 'deleted': value is required"
            )
        }
        guard container.contains(.id) else {
            throw SdkValidationError(
                field: "id",
                code: "required",
                message: "Validation failed for 'id': value is required"
            )
        }
        guard container.contains(.object) else {
            throw SdkValidationError(
                field: "object",
                code: "required",
                message: "Validation failed for 'object': value is required"
            )
        }
        deleted = try container.sdkDecodeRequired(.deleted)
        id = try container.sdkDecodeRequired(.id)
        object = try container.sdkDecodeRequired(.object)
        try validateLength("id", id, min: nil, max: 5000)
    }
}

public extension DeletedCustomer {
    init(deleted: Bool, id: String, object: DeletedCustomerObject) throws {
        (self.deleted, self.id) = (deleted, id)
        self.object = object
        try validateLength("id", self.id, min: nil, max: 5000)
    }
}

/// Typed representation of the `DeletedInvoice` API schema.
public struct DeletedInvoice: Codable {
    /// Always true for a deleted object
    public var deleted: Bool
    /// Unique identifier for the object.
    public var id: String
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: DeletedInvoiceObject

    enum CodingKeys: String, CodingKey {
        case deleted
        case id
        case object
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension DeletedInvoice {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.deleted) else {
            throw SdkValidationError(
                field: "deleted",
                code: "required",
                message: "Validation failed for 'deleted': value is required"
            )
        }
        guard container.contains(.id) else {
            throw SdkValidationError(
                field: "id",
                code: "required",
                message: "Validation failed for 'id': value is required"
            )
        }
        guard container.contains(.object) else {
            throw SdkValidationError(
                field: "object",
                code: "required",
                message: "Validation failed for 'object': value is required"
            )
        }
        deleted = try container.sdkDecodeRequired(.deleted)
        id = try container.sdkDecodeRequired(.id)
        object = try container.sdkDecodeRequired(.object)
        try validateLength("id", id, min: nil, max: 5000)
    }
}

public extension DeletedInvoice {
    init(deleted: Bool, id: String, object: DeletedInvoiceObject) throws {
        (self.deleted, self.id) = (deleted, id)
        self.object = object
        try validateLength("id", self.id, min: nil, max: 5000)
    }
}

/// Typed representation of the `DeletedInvoiceitem` API schema.
public struct DeletedInvoiceitem: Codable {
    /// Always true for a deleted object
    public var deleted: Bool
    /// Unique identifier for the object.
    public var id: String
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: DeletedInvoiceitemObject

    enum CodingKeys: String, CodingKey {
        case deleted
        case id
        case object
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}
