// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

/// V1Connect domain models
/// Typed representation of the `ConnectEmbeddedInstantPayoutsPromotionConfig` API schema.
public struct ConnectEmbeddedInstantPayoutsPromotionConfig: Codable {
    /// Whether the embedded component is enabled.
    public var enabled: Bool
    /// Required object value serialized in the `features` wire field.
    public var features: ConnectEmbeddedInstantPayoutsPromotionFeatures

    enum CodingKeys: String, CodingKey {
        case enabled
        case features
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension ConnectEmbeddedInstantPayoutsPromotionConfig {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.enabled) else {
            throw SdkValidationError(
                field: "enabled",
                code: "required",
                message: "Validation failed for 'enabled': value is required"
            )
        }
        guard container.contains(.features) else {
            throw SdkValidationError(
                field: "features",
                code: "required",
                message: "Validation failed for 'features': value is required"
            )
        }
        enabled = try container.sdkDecodeRequired(.enabled)
        features = try container.sdkDecodeRequired(.features)
    }
}

public extension ConnectEmbeddedInstantPayoutsPromotionConfig {
    init(enabled: Bool, features: ConnectEmbeddedInstantPayoutsPromotionFeatures) {
        (self.enabled, self.features) = (enabled, features)
    }
}

/// Typed representation of the `ConnectEmbeddedInstantPayoutsPromotionFeatures` API schema.
public struct ConnectEmbeddedInstantPayoutsPromotionFeatures: Codable {
    /// Whether Stripe user authentication is disabled. This value can only be `true` for accounts where
    /// `controller.requirement_collection` is `application` for the account. The default value is the opposite of
    /// the `external_account_collection` value. For example, if you don't set `external_account_collection`, it
    /// defaults to `true` and `disable_stripe_user_authentication` defaults to `false`.
    public var disableStripeUserAuthentication: Bool
    /// Whether external account collection is enabled. This feature can only be `false` for accounts where you’re
    /// responsible for collecting updated information when requirements are due or change, like Custom accounts.
    /// The default value for this feature is `true`.
    public var externalAccountCollection: Bool
    /// Whether to allow creation of instant payouts. The default value is `enabled` when Stripe is responsible for
    /// negative account balances, and `use_dashboard_rules` otherwise.
    public var instantPayouts: Bool

    enum CodingKeys: String, CodingKey {
        case disableStripeUserAuthentication = "disable_stripe_user_authentication"
        case externalAccountCollection = "external_account_collection"
        case instantPayouts = "instant_payouts"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension ConnectEmbeddedInstantPayoutsPromotionFeatures {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.disableStripeUserAuthentication) else {
            throw SdkValidationError(
                field: "disable_stripe_user_authentication",
                code: "required",
                message: "Validation failed for 'disable_stripe_user_authentication': value is required"
            )
        }
        guard container.contains(.externalAccountCollection) else {
            throw SdkValidationError(
                field: "external_account_collection",
                code: "required",
                message: "Validation failed for 'external_account_collection': value is required"
            )
        }
        guard container.contains(.instantPayouts) else {
            throw SdkValidationError(
                field: "instant_payouts",
                code: "required",
                message: "Validation failed for 'instant_payouts': value is required"
            )
        }
        disableStripeUserAuthentication = try container.sdkDecodeRequired(.disableStripeUserAuthentication)
        externalAccountCollection = try container.sdkDecodeRequired(.externalAccountCollection)
        instantPayouts = try container.sdkDecodeRequired(.instantPayouts)
    }
}

public extension ConnectEmbeddedInstantPayoutsPromotionFeatures {
    init(disableStripeUserAuthentication: Bool, externalAccountCollection: Bool, instantPayouts: Bool) {
        self.disableStripeUserAuthentication = disableStripeUserAuthentication
        (self.externalAccountCollection, self.instantPayouts) = (externalAccountCollection, instantPayouts)
    }
}

/// Typed representation of the `ConnectEmbeddedIssuingCardConfigClaim` API schema.
public struct ConnectEmbeddedIssuingCardConfigClaim: Codable {
    /// Whether the embedded component is enabled.
    public var enabled: Bool
    /// Required object value serialized in the `features` wire field.
    public var features: ConnectEmbeddedIssuingCardFeatures

    enum CodingKeys: String, CodingKey {
        case enabled
        case features
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension ConnectEmbeddedIssuingCardConfigClaim {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.enabled) else {
            throw SdkValidationError(
                field: "enabled",
                code: "required",
                message: "Validation failed for 'enabled': value is required"
            )
        }
        guard container.contains(.features) else {
            throw SdkValidationError(
                field: "features",
                code: "required",
                message: "Validation failed for 'features': value is required"
            )
        }
        enabled = try container.sdkDecodeRequired(.enabled)
        features = try container.sdkDecodeRequired(.features)
    }
}

public extension ConnectEmbeddedIssuingCardConfigClaim {
    init(enabled: Bool, features: ConnectEmbeddedIssuingCardFeatures) {
        (self.enabled, self.features) = (enabled, features)
    }
}

/// Typed representation of the `ConnectEmbeddedIssuingCardFeatures` API schema.
public struct ConnectEmbeddedIssuingCardFeatures: Codable {
    /// Whether to allow card management features.
    public var cardManagement: Bool
    /// Whether to allow card spend dispute management features.
    public var cardSpendDisputeManagement: Bool
    /// Whether to allow cardholder management features.
    public var cardholderManagement: Bool
    /// Whether to allow spend control management features.
    public var spendControlManagement: Bool

    enum CodingKeys: String, CodingKey {
        case cardManagement = "card_management"
        case cardSpendDisputeManagement = "card_spend_dispute_management"
        case cardholderManagement = "cardholder_management"
        case spendControlManagement = "spend_control_management"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension ConnectEmbeddedIssuingCardFeatures {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.cardManagement) else {
            throw SdkValidationError(
                field: "card_management",
                code: "required",
                message: "Validation failed for 'card_management': value is required"
            )
        }
        guard container.contains(.cardSpendDisputeManagement) else {
            throw SdkValidationError(
                field: "card_spend_dispute_management",
                code: "required",
                message: "Validation failed for 'card_spend_dispute_management': value is required"
            )
        }
        guard container.contains(.cardholderManagement) else {
            throw SdkValidationError(
                field: "cardholder_management",
                code: "required",
                message: "Validation failed for 'cardholder_management': value is required"
            )
        }
        guard container.contains(.spendControlManagement) else {
            throw SdkValidationError(
                field: "spend_control_management",
                code: "required",
                message: "Validation failed for 'spend_control_management': value is required"
            )
        }
        cardManagement = try container.sdkDecodeRequired(.cardManagement)
        cardSpendDisputeManagement = try container.sdkDecodeRequired(.cardSpendDisputeManagement)
        cardholderManagement = try container.sdkDecodeRequired(.cardholderManagement)
        spendControlManagement = try container.sdkDecodeRequired(.spendControlManagement)
    }
}

public extension ConnectEmbeddedIssuingCardFeatures {
    init(
        cardManagement: Bool,
        cardSpendDisputeManagement: Bool,
        cardholderManagement: Bool,
        spendControlManagement: Bool
    ) {
        self.cardManagement = cardManagement
        self.cardSpendDisputeManagement = cardSpendDisputeManagement
        self.cardholderManagement = cardholderManagement
        self.spendControlManagement = spendControlManagement
    }
}

/// Typed representation of the `ConnectEmbeddedIssuingCardsListConfigClaim` API schema.
public struct ConnectEmbeddedIssuingCardsListConfigClaim: Codable {
    /// Whether the embedded component is enabled.
    public var enabled: Bool
    /// Required object value serialized in the `features` wire field.
    public var features: ConnectEmbeddedIssuingCardsListFeatures

    enum CodingKeys: String, CodingKey {
        case enabled
        case features
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension ConnectEmbeddedIssuingCardsListConfigClaim {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.enabled) else {
            throw SdkValidationError(
                field: "enabled",
                code: "required",
                message: "Validation failed for 'enabled': value is required"
            )
        }
        guard container.contains(.features) else {
            throw SdkValidationError(
                field: "features",
                code: "required",
                message: "Validation failed for 'features': value is required"
            )
        }
        enabled = try container.sdkDecodeRequired(.enabled)
        features = try container.sdkDecodeRequired(.features)
    }
}

public extension ConnectEmbeddedIssuingCardsListConfigClaim {
    init(enabled: Bool, features: ConnectEmbeddedIssuingCardsListFeatures) {
        (self.enabled, self.features) = (enabled, features)
    }
}

/// Typed representation of the `ConnectEmbeddedIssuingCardsListFeatures` API schema.
public struct ConnectEmbeddedIssuingCardsListFeatures: Codable {
    /// Whether to allow card management features.
    public var cardManagement: Bool
    /// Whether to allow card spend dispute management features.
    public var cardSpendDisputeManagement: Bool
    /// Whether to allow cardholder management features.
    public var cardholderManagement: Bool
    /// Whether Stripe user authentication is disabled. This value can only be `true` for accounts where
    /// `controller.requirement_collection` is `application` for the account. The default value is the opposite of
    /// the `external_account_collection` value. For example, if you don't set `external_account_collection`, it
    /// defaults to `true` and `disable_stripe_user_authentication` defaults to `false`.
    public var disableStripeUserAuthentication: Bool
    /// Whether to allow spend control management features.
    public var spendControlManagement: Bool

    enum CodingKeys: String, CodingKey {
        case cardManagement = "card_management"
        case cardSpendDisputeManagement = "card_spend_dispute_management"
        case cardholderManagement = "cardholder_management"
        case disableStripeUserAuthentication = "disable_stripe_user_authentication"
        case spendControlManagement = "spend_control_management"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension ConnectEmbeddedIssuingCardsListFeatures {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.cardManagement) else {
            throw SdkValidationError(
                field: "card_management",
                code: "required",
                message: "Validation failed for 'card_management': value is required"
            )
        }
        guard container.contains(.cardSpendDisputeManagement) else {
            throw SdkValidationError(
                field: "card_spend_dispute_management",
                code: "required",
                message: "Validation failed for 'card_spend_dispute_management': value is required"
            )
        }
        guard container.contains(.cardholderManagement) else {
            throw SdkValidationError(
                field: "cardholder_management",
                code: "required",
                message: "Validation failed for 'cardholder_management': value is required"
            )
        }
        guard container.contains(.disableStripeUserAuthentication) else {
            throw SdkValidationError(
                field: "disable_stripe_user_authentication",
                code: "required",
                message: "Validation failed for 'disable_stripe_user_authentication': value is required"
            )
        }
        guard container.contains(.spendControlManagement) else {
            throw SdkValidationError(
                field: "spend_control_management",
                code: "required",
                message: "Validation failed for 'spend_control_management': value is required"
            )
        }
        cardManagement = try container.sdkDecodeRequired(.cardManagement)
        cardSpendDisputeManagement = try container.sdkDecodeRequired(.cardSpendDisputeManagement)
        cardholderManagement = try container.sdkDecodeRequired(.cardholderManagement)
        disableStripeUserAuthentication = try container.sdkDecodeRequired(.disableStripeUserAuthentication)
        spendControlManagement = try container.sdkDecodeRequired(.spendControlManagement)
    }
}

public extension ConnectEmbeddedIssuingCardsListFeatures {
    init(
        cardManagement: Bool,
        cardSpendDisputeManagement: Bool,
        cardholderManagement: Bool,
        disableStripeUserAuthentication: Bool,
        spendControlManagement: Bool
    ) {
        self.cardManagement = cardManagement
        self.cardSpendDisputeManagement = cardSpendDisputeManagement
        self.cardholderManagement = cardholderManagement
        self.disableStripeUserAuthentication = disableStripeUserAuthentication
        self.spendControlManagement = spendControlManagement
    }
}

/// Typed representation of the `ConnectEmbeddedPaymentDisputesConfig` API schema.
public struct ConnectEmbeddedPaymentDisputesConfig: Codable {
    /// Whether the embedded component is enabled.
    public var enabled: Bool
    /// Required object value serialized in the `features` wire field.
    public var features: ConnectEmbeddedPaymentDisputesFeatures

    enum CodingKeys: String, CodingKey {
        case enabled
        case features
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension ConnectEmbeddedPaymentDisputesConfig {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.enabled) else {
            throw SdkValidationError(
                field: "enabled",
                code: "required",
                message: "Validation failed for 'enabled': value is required"
            )
        }
        guard container.contains(.features) else {
            throw SdkValidationError(
                field: "features",
                code: "required",
                message: "Validation failed for 'features': value is required"
            )
        }
        enabled = try container.sdkDecodeRequired(.enabled)
        features = try container.sdkDecodeRequired(.features)
    }
}

public extension ConnectEmbeddedPaymentDisputesConfig {
    init(enabled: Bool, features: ConnectEmbeddedPaymentDisputesFeatures) {
        (self.enabled, self.features) = (enabled, features)
    }
}

/// Typed representation of the `ConnectEmbeddedPaymentDisputesFeatures` API schema.
public struct ConnectEmbeddedPaymentDisputesFeatures: Codable {
    /// Whether connected accounts can manage destination charges that are created on behalf of them. This is
    /// `false` by default.
    public var destinationOnBehalfOfChargeManagement: Bool
    /// Whether responding to disputes is enabled, including submitting evidence and accepting disputes. This is
    /// `true` by default.
    public var disputeManagement: Bool
    /// Whether sending refunds is enabled. This is `true` by default.
    public var refundManagement: Bool
    /// Whether to allow connected accounts to submit disputes using Smart Disputes. Defaults to the value of
    /// `dispute_management`.
    public var smartDisputesManagement: Bool

    enum CodingKeys: String, CodingKey {
        case destinationOnBehalfOfChargeManagement = "destination_on_behalf_of_charge_management"
        case disputeManagement = "dispute_management"
        case refundManagement = "refund_management"
        case smartDisputesManagement = "smart_disputes_management"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension ConnectEmbeddedPaymentDisputesFeatures {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.destinationOnBehalfOfChargeManagement) else {
            let field = "destination_on_behalf_of_charge_management"
            throw SdkValidationError(
                field: field,
                code: "required",
                message: "Validation failed for '\(field)': value is required"
            )
        }
        guard container.contains(.disputeManagement) else {
            throw SdkValidationError(
                field: "dispute_management",
                code: "required",
                message: "Validation failed for 'dispute_management': value is required"
            )
        }
        guard container.contains(.refundManagement) else {
            throw SdkValidationError(
                field: "refund_management",
                code: "required",
                message: "Validation failed for 'refund_management': value is required"
            )
        }
        guard container.contains(.smartDisputesManagement) else {
            throw SdkValidationError(
                field: "smart_disputes_management",
                code: "required",
                message: "Validation failed for 'smart_disputes_management': value is required"
            )
        }
        destinationOnBehalfOfChargeManagement = try container.sdkDecodeRequired(.destinationOnBehalfOfChargeManagement)
        disputeManagement = try container.sdkDecodeRequired(.disputeManagement)
        refundManagement = try container.sdkDecodeRequired(.refundManagement)
        smartDisputesManagement = try container.sdkDecodeRequired(.smartDisputesManagement)
    }
}

public extension ConnectEmbeddedPaymentDisputesFeatures {
    init(
        destinationOnBehalfOfChargeManagement: Bool,
        disputeManagement: Bool,
        refundManagement: Bool,
        smartDisputesManagement: Bool
    ) {
        self.destinationOnBehalfOfChargeManagement = destinationOnBehalfOfChargeManagement
        (self.disputeManagement, self.refundManagement) = (disputeManagement, refundManagement)
        self.smartDisputesManagement = smartDisputesManagement
    }
}

/// Typed representation of the `ConnectEmbeddedPaymentMethodSettingsConfigClaim` API schema.
public struct ConnectEmbeddedPaymentMethodSettingsConfigClaim: Codable {
    /// Whether the embedded component is enabled.
    public var enabled: Bool
    /// Required object value serialized in the `features` wire field.
    public var features: ConnectEmbeddedPaymentMethodSettingsFeatures

    enum CodingKeys: String, CodingKey {
        case enabled
        case features
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension ConnectEmbeddedPaymentMethodSettingsConfigClaim {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.enabled) else {
            throw SdkValidationError(
                field: "enabled",
                code: "required",
                message: "Validation failed for 'enabled': value is required"
            )
        }
        guard container.contains(.features) else {
            throw SdkValidationError(
                field: "features",
                code: "required",
                message: "Validation failed for 'features': value is required"
            )
        }
        enabled = try container.sdkDecodeRequired(.enabled)
        features = try container.sdkDecodeRequired(.features)
    }
}

public extension ConnectEmbeddedPaymentMethodSettingsConfigClaim {
    init(enabled: Bool, features: ConnectEmbeddedPaymentMethodSettingsFeatures) {
        (self.enabled, self.features) = (enabled, features)
    }
}

/// Typed representation of the `ConnectEmbeddedPaymentMethodSettingsFeatures` API schema.
public struct ConnectEmbeddedPaymentMethodSettingsFeatures: Codable {
    /// Whether Stripe user authentication is disabled. This value can only be `true` for accounts where
    /// `controller.requirement_collection` is `application` for the account. This is `false` by default.
    public var disableStripeUserAuthentication: Bool

    enum CodingKeys: String, CodingKey {
        case disableStripeUserAuthentication = "disable_stripe_user_authentication"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension ConnectEmbeddedPaymentMethodSettingsFeatures {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.disableStripeUserAuthentication) else {
            throw SdkValidationError(
                field: "disable_stripe_user_authentication",
                code: "required",
                message: "Validation failed for 'disable_stripe_user_authentication': value is required"
            )
        }
        disableStripeUserAuthentication = try container.sdkDecodeRequired(.disableStripeUserAuthentication)
    }
}

public extension ConnectEmbeddedPaymentMethodSettingsFeatures {
    init(disableStripeUserAuthentication: Bool) {
        self.disableStripeUserAuthentication = disableStripeUserAuthentication
    }
}

/// Typed representation of the `ConnectEmbeddedPaymentsConfigClaim` API schema.
public struct ConnectEmbeddedPaymentsConfigClaim: Codable {
    /// Whether the embedded component is enabled.
    public var enabled: Bool
    /// Required object value serialized in the `features` wire field.
    public var features: ConnectEmbeddedPaymentsFeatures

    enum CodingKeys: String, CodingKey {
        case enabled
        case features
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension ConnectEmbeddedPaymentsConfigClaim {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.enabled) else {
            throw SdkValidationError(
                field: "enabled",
                code: "required",
                message: "Validation failed for 'enabled': value is required"
            )
        }
        guard container.contains(.features) else {
            throw SdkValidationError(
                field: "features",
                code: "required",
                message: "Validation failed for 'features': value is required"
            )
        }
        enabled = try container.sdkDecodeRequired(.enabled)
        features = try container.sdkDecodeRequired(.features)
    }
}

public extension ConnectEmbeddedPaymentsConfigClaim {
    init(enabled: Bool, features: ConnectEmbeddedPaymentsFeatures) {
        (self.enabled, self.features) = (enabled, features)
    }
}

/// Typed representation of the `ConnectEmbeddedPaymentsFeatures` API schema.
public struct ConnectEmbeddedPaymentsFeatures: Codable {
    /// Whether to allow capturing and cancelling payment intents. This is `true` by default.
    public var capturePayments: Bool
    /// Whether connected accounts can manage destination charges that are created on behalf of them. This is
    /// `false` by default.
    public var destinationOnBehalfOfChargeManagement: Bool
    /// Whether responding to disputes is enabled, including submitting evidence and accepting disputes. This is
    /// `true` by default.
    public var disputeManagement: Bool
    /// Whether sending refunds is enabled. This is `true` by default.
    public var refundManagement: Bool
    /// Whether to allow connected accounts to submit disputes using Smart Disputes. Defaults to the value of
    /// `dispute_management`.
    public var smartDisputesManagement: Bool

    enum CodingKeys: String, CodingKey {
        case capturePayments = "capture_payments"
        case destinationOnBehalfOfChargeManagement = "destination_on_behalf_of_charge_management"
        case disputeManagement = "dispute_management"
        case refundManagement = "refund_management"
        case smartDisputesManagement = "smart_disputes_management"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}
