// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

/// V1 domain models
extension InvoicesPaymentsInvoicePaymentAssociatedPaymentPaymentIntent: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for InvoicesPaymentsInvoicePaymentAssociatedPaymentPaymentIntent"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container.decode(PaymentIntent.self) {
            return .paymentIntent(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .paymentIntent(value): try container.encode(value); return true
        }
    }
}

public enum InvoicesPaymentsInvoicePaymentAssociatedPaymentPaymentRecord {
    case stringValue(String)
    case paymentRecord(PaymentRecord)
}

extension InvoicesPaymentsInvoicePaymentAssociatedPaymentPaymentRecord: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for InvoicesPaymentsInvoicePaymentAssociatedPaymentPaymentRecord"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container.decode(PaymentRecord.self) {
            return .paymentRecord(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .paymentRecord(value): try container.encode(value); return true
        }
    }
}

/// Typed representation of the `InvoicesResourceFromInvoice` API schema.
public struct InvoicesResourceFromInvoice: Codable {
    /// The relation between this invoice and the cloned invoice
    public var action: String
    /// The invoice that was cloned.
    public var invoice: InvoicesResourceFromInvoiceInvoice

    enum CodingKeys: String, CodingKey {
        case action
        case invoice
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension InvoicesResourceFromInvoice {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.action) else {
            throw SdkValidationError(
                field: "action",
                code: "required",
                message: "Validation failed for 'action': value is required"
            )
        }
        guard container.contains(.invoice) else {
            throw SdkValidationError(
                field: "invoice",
                code: "required",
                message: "Validation failed for 'invoice': value is required"
            )
        }
        action = try container.sdkDecodeRequired(.action)
        invoice = try container.sdkDecodeRequired(.invoice)
        try validateLength("action", action, min: nil, max: 5000)
    }
}

public extension InvoicesResourceFromInvoice {
    init(action: String, invoice: InvoicesResourceFromInvoiceInvoice) throws {
        (self.action, self.invoice) = (action, invoice)
        try validateLength("action", self.action, min: nil, max: 5000)
    }
}

public enum InvoicesResourceFromInvoiceInvoice {
    case stringValue(String)
    case invoice(Invoice)
}

extension InvoicesResourceFromInvoiceInvoice: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for InvoicesResourceFromInvoiceInvoice"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container.decode(Invoice.self) {
            return .invoice(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .invoice(value): try container.encode(value); return true
        }
    }
}

/// Typed representation of the `InvoicesResourcePretaxCreditAmount` API schema.
public struct InvoicesResourcePretaxCreditAmount: Codable {
    /// The amount, in cents (or local equivalent), of the pretax credit amount.
    public var amount: Int
    /// Type of the pretax credit amount referenced.
    public var type: InvoicesResourcePretaxCreditAmountType
    /// The credit balance transaction that was applied to get this pretax credit amount.
    public var creditBalanceTransaction: InvoicesResourcePretaxCreditAmountCreditBalanceTransaction?
    /// The discount that was applied to get this pretax credit amount.
    public var discount: InvoicesResourcePretaxCreditAmountDiscount?

    enum CodingKeys: String, CodingKey {
        case amount
        case type
        case creditBalanceTransaction = "credit_balance_transaction"
        case discount
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension InvoicesResourcePretaxCreditAmount {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.amount) else {
            throw SdkValidationError(
                field: "amount",
                code: "required",
                message: "Validation failed for 'amount': value is required"
            )
        }
        guard container.contains(.type) else {
            throw SdkValidationError(
                field: "type",
                code: "required",
                message: "Validation failed for 'type': value is required"
            )
        }
        amount = try container.sdkDecodeRequired(.amount)
        type = try container.sdkDecodeRequired(.type)
        creditBalanceTransaction = try container.sdkDecodeIfPresent(.creditBalanceTransaction)
        discount = try container.sdkDecodeIfPresent(.discount)
    }
}

public extension InvoicesResourcePretaxCreditAmount {
    init(
        amount: Int,
        type: InvoicesResourcePretaxCreditAmountType,
        creditBalanceTransaction: InvoicesResourcePretaxCreditAmountCreditBalanceTransaction? = nil,
        discount: InvoicesResourcePretaxCreditAmountDiscount? = nil
    ) {
        (self.amount, self.type) = (amount, type)
        (self.creditBalanceTransaction, self.discount) = (creditBalanceTransaction, discount)
    }
}

public enum InvoicesResourcePretaxCreditAmountCreditBalanceTransaction {
    case stringValue(String)
    case billingCreditBalanceTransaction(BillingCreditBalanceTransaction)
}

extension InvoicesResourcePretaxCreditAmountCreditBalanceTransaction: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for InvoicesResourcePretaxCreditAmountCreditBalanceTransaction"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container
            .decode(BillingCreditBalanceTransaction.self) {
            return .billingCreditBalanceTransaction(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .billingCreditBalanceTransaction(value): try container.encode(value); return true
        }
    }
}

public enum InvoicesResourcePretaxCreditAmountDiscount {
    case stringValue(String)
    case discount(Discount)
    case deletedDiscount(DeletedDiscount)
}

extension InvoicesResourcePretaxCreditAmountDiscount: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for InvoicesResourcePretaxCreditAmountDiscount"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container.decode(Discount.self) {
            return .discount(value)
        }
        if let value = try? container.decode(DeletedDiscount.self) {
            return .deletedDiscount(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .discount(value): try container.encode(value); return true
        case let .deletedDiscount(value): try container.encode(value); return true
        }
    }
}

/// When an issued card is used to make a purchase, an Issuing `Authorization` object is created. Authorizations
/// must be approved for the purchase to be completed successfully. Related guide: Issued card authorizations
public struct IssuingAuthorization: Codable {
    /// The total amount that was authorized or rejected. This amount is in `currency` and in the smallest currency
    /// unit. `amount` should be the same as `merchant_amount`, unless `currency` and `merchant_currency` are
    /// different.
    public var amount: Int
    /// Whether the authorization has been approved.
    public var approved: Bool
    /// How the card details were provided.
    public var authorizationMethod: IssuingAuthorizationAuthorizationMethod
    /// List of balance transactions associated with this authorization.
    public var balanceTransactions: [BalanceTransaction]
    /// You can create physical or virtual cards that are issued to cardholders.
    public var card: IssuingCard
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// The currency of the cardholder. This currency can be different from the currency presented at authorization
    /// and the `merchant_currency` field on this authorization. Three-letter ISO currency code, in lowercase. Must
    /// be a supported currency.
    public var currency: String
    /// Unique identifier for the object.
    public var id: String
    /// If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is
    /// `false`.
    public var livemode: Bool
    /// The total amount that was authorized or rejected. This amount is in the `merchant_currency` and in the
    /// smallest currency unit. `merchant_amount` should be the same as `amount`, unless `merchant_currency` and
    /// `currency` are different.
    public var merchantAmount: Int
    /// The local currency that was presented to the cardholder for the authorization. This currency can be
    /// different from the cardholder currency and the `currency` field on this authorization. Three-letter ISO
    /// currency code, in lowercase. Must be a supported currency.
    public var merchantCurrency: String
    /// Required object value serialized in the `merchant_data` wire field.
    public var merchantData: IssuingAuthorizationMerchantData
    /// Set of key-value pairs that you can attach to an object. This can be useful for storing additional
    /// information about the object in a structured format.
    public var metadata: [String: String]
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: IssuingAuthorizationObject
    /// History of every time a `pending_request` authorization was approved/declined, either by you directly or by
    /// Stripe (e.g. based on your spending_controls). If the merchant changes the authorization by performing an
    /// incremental authorization, you can look at this field to see the previous requests for the authorization.
    /// This field can be helpful in determining why a given authorization was approved/declined.
    public var requestHistory: [IssuingAuthorizationRequest]
    /// The current status of the authorization in its lifecycle.
    public var status: IssuingAuthorizationStatus
    /// List of transactions associated with this authorization.
    public var transactions: [IssuingTransaction]
    /// Required object value serialized in the `verification_data` wire field.
    public var verificationData: IssuingAuthorizationVerificationData
    /// Detailed breakdown of amount components. These amounts are denominated in `currency` and in the smallest
    /// currency unit.
    public var amountDetails: IssuingAuthorizationAmountDetailsX260791fd?
    /// Whether the card was present at the point of sale for the authorization.
    public var cardPresence: IssuingAuthorizationCardPresence?
    /// The cardholder to whom this authorization belongs.
    public var cardholder: IssuingAuthorizationCardholder?
    /// Fleet-specific information for authorizations using Fleet cards.
    public var fleet: IssuingAuthorizationFleet?
    /// Fraud challenges sent to the cardholder, if this authorization was declined for fraud risk reasons.
    public var fraudChallenges: [IssuingAuthorizationFraudChallenge]?
    /// Information about fuel that was purchased with this transaction. Typically this information is received from
    /// the merchant after the authorization has been approved and the fuel dispensed.
    public var fuel: IssuingAuthorizationFuel?
    /// Details about the authorization, such as identifiers, set by the card network.
    public var networkData: IssuingAuthorizationNetworkDataX998ecb79?
    /// The pending authorization request. This field will only be non-null during an
    /// `issuing_authorization.request` webhook.
    public var pendingRequest: IssuingAuthorizationPendingRequestXdbedaf6e?
    /// Token object used for this authorization. If a network token was not used for this authorization, this field
    /// will be null.
    public var token: IssuingAuthorizationToken?
    /// Treasury details related to this authorization if it was created on a FinancialAccount.
    public var treasury: IssuingAuthorizationTreasuryX33e1dc1b?
    /// Whether the authorization bypassed fraud risk checks because the cardholder has previously completed a fraud
    /// challenge on a similar high-risk authorization from the same merchant.
    public var verifiedByFraudChallenge: Bool?
    /// The digital wallet used for this transaction. One of `apple_pay`, `google_pay`, or `samsung_pay`. Will
    /// populate as `null` when no digital wallet was utilized.
    public var wallet: String?

    enum CodingKeys: String, CodingKey {
        case amount
        case approved
        case authorizationMethod = "authorization_method"
        case balanceTransactions = "balance_transactions"
        case card
        case created
        case currency
        case id
        case livemode
        case merchantAmount = "merchant_amount"
        case merchantCurrency = "merchant_currency"
        case merchantData = "merchant_data"
        case metadata
        case object
        case requestHistory = "request_history"
        case status
        case transactions
        case verificationData = "verification_data"
        case amountDetails = "amount_details"
        case cardPresence = "card_presence"
        case cardholder
        case fleet
        case fraudChallenges = "fraud_challenges"
        case fuel
        case networkData = "network_data"
        case pendingRequest = "pending_request"
        case token
        case treasury
        case verifiedByFraudChallenge = "verified_by_fraud_challenge"
        case wallet
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension IssuingAuthorization {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        amount = try container.sdkDecodeRequired(.amount)
        approved = try container.sdkDecodeRequired(.approved)
        authorizationMethod = try container.sdkDecodeRequired(.authorizationMethod)
        balanceTransactions = try container.sdkDecodeRequired(.balanceTransactions)
        card = try container.sdkDecodeRequired(.card)
        created = try container.sdkDecodeRequired(.created)
        currency = try container.sdkDecodeRequired(.currency)
        id = try container.sdkDecodeRequired(.id)
        livemode = try container.sdkDecodeRequired(.livemode)
        merchantAmount = try container.sdkDecodeRequired(.merchantAmount)
        merchantCurrency = try container.sdkDecodeRequired(.merchantCurrency)
        merchantData = try container.sdkDecodeRequired(.merchantData)
        metadata = try container.sdkDecodeRequired(.metadata)
        object = try container.sdkDecodeRequired(.object)
        requestHistory = try container.sdkDecodeRequired(.requestHistory)
        status = try container.sdkDecodeRequired(.status)
        transactions = try container.sdkDecodeRequired(.transactions)
        verificationData = try container.sdkDecodeRequired(.verificationData)
        amountDetails = try container.sdkDecodeIfPresent(.amountDetails)
        cardPresence = try container.sdkDecodeIfPresent(.cardPresence)
        cardholder = try container.sdkDecodeIfPresent(.cardholder)
        fleet = try container.sdkDecodeIfPresent(.fleet)
        fraudChallenges = try container.sdkDecodeIfPresent(.fraudChallenges)
        fuel = try container.sdkDecodeIfPresent(.fuel)
        networkData = try container.sdkDecodeIfPresent(.networkData)
        pendingRequest = try container.sdkDecodeIfPresent(.pendingRequest)
        token = try container.sdkDecodeIfPresent(.token)
        treasury = try container.sdkDecodeIfPresent(.treasury)
        verifiedByFraudChallenge = try container.sdkDecodeIfPresent(.verifiedByFraudChallenge)
        wallet = try container.sdkDecodeIfPresent(.wallet)
        try validateLength("id", id, min: nil, max: 5000)
        if let value = wallet {
            try validateLength("wallet", value, min: nil, max: 5000)
        }
    }
}

public extension IssuingAuthorization {
    init(
        amount: Int,
        approved: Bool,
        authorizationMethod: IssuingAuthorizationAuthorizationMethod,
        balanceTransactions: [BalanceTransaction],
        card: IssuingCard,
        created: Int,
        currency: String,
        id: String,
        livemode: Bool,
        merchantAmount: Int,
        merchantCurrency: String,
        merchantData: IssuingAuthorizationMerchantData,
        metadata: [String: String],
        object: IssuingAuthorizationObject,
        requestHistory: [IssuingAuthorizationRequest],
        status: IssuingAuthorizationStatus,
        transactions: [IssuingTransaction],
        verificationData: IssuingAuthorizationVerificationData,
        amountDetails: IssuingAuthorizationAmountDetailsX260791fd? = nil,
        cardPresence: IssuingAuthorizationCardPresence? = nil,
        cardholder: IssuingAuthorizationCardholder? = nil,
        fleet: IssuingAuthorizationFleet? = nil,
        fraudChallenges: [IssuingAuthorizationFraudChallenge]? = nil,
        fuel: IssuingAuthorizationFuel? = nil,
        networkData: IssuingAuthorizationNetworkDataX998ecb79? = nil,
        pendingRequest: IssuingAuthorizationPendingRequestXdbedaf6e? = nil,
        token: IssuingAuthorizationToken? = nil,
        treasury: IssuingAuthorizationTreasuryX33e1dc1b? = nil,
        verifiedByFraudChallenge: Bool? = nil,
        wallet: String? = nil
    ) throws {
        (self.amount, self.approved) = (amount, approved)
        (self.authorizationMethod, self.balanceTransactions) = (authorizationMethod, balanceTransactions)
        (self.card, self.created) = (card, created)
        (self.currency, self.id) = (currency, id)
        (self.livemode, self.merchantAmount) = (livemode, merchantAmount)
        (self.merchantCurrency, self.merchantData) = (merchantCurrency, merchantData)
        (self.metadata, self.object) = (metadata, object)
        (self.requestHistory, self.status) = (requestHistory, status)
        (self.transactions, self.verificationData) = (transactions, verificationData)
        (self.amountDetails, self.cardPresence) = (amountDetails, cardPresence)
        (self.cardholder, self.fleet) = (cardholder, fleet)
        (self.fraudChallenges, self.fuel) = (fraudChallenges, fuel)
        (self.networkData, self.pendingRequest) = (networkData, pendingRequest)
        (self.token, self.treasury) = (token, treasury)
        (self.verifiedByFraudChallenge, self.wallet) = (verifiedByFraudChallenge, wallet)
        try validateLength("id", self.id, min: nil, max: 5000)
        if let value = self.wallet {
            try validateLength("wallet", value, min: nil, max: 5000)
        }
    }
}

public enum IssuingAuthorizationAmountDetailsX260791fd {
    case issuingAuthorizationAmountDetails(IssuingAuthorizationAmountDetails)
}

extension IssuingAuthorizationAmountDetailsX260791fd: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for IssuingAuthorizationAmountDetailsX260791fd"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container
            .decode(IssuingAuthorizationAmountDetails.self) {
            return .issuingAuthorizationAmountDetails(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .issuingAuthorizationAmountDetails(value): try container.encode(value); return true
        }
    }
}

public enum IssuingAuthorizationCardholder {
    case stringValue(String)
    case issuingCardholder(IssuingCardholder)
}

extension IssuingAuthorizationCardholder: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for IssuingAuthorizationCardholder"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container.decode(IssuingCardholder.self) {
            return .issuingCardholder(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .issuingCardholder(value): try container.encode(value); return true
        }
    }
}

public enum IssuingAuthorizationFleet {
    case issuingAuthorizationFleetData(IssuingAuthorizationFleetData)
}

extension IssuingAuthorizationFleet: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for IssuingAuthorizationFleet"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container
            .decode(IssuingAuthorizationFleetData.self) {
            return .issuingAuthorizationFleetData(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .issuingAuthorizationFleetData(value): try container.encode(value); return true
        }
    }
}

public enum IssuingAuthorizationFuel {
    case issuingAuthorizationFuelData(IssuingAuthorizationFuelData)
}
