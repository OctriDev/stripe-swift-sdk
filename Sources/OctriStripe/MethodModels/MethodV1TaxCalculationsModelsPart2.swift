// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif

/// Canonical v1TaxCalculations operation model declarations
extension PostTaxCalculationsRequestBodyCustomerDetailsAddressLine1: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for PostTaxCalculationsRequestBodyCustomerDetailsAddressLine1"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container.decode(String.self) {
            return .stringValue1(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .stringValue1(value): try container.encode(value); return true
        }
    }
}

public struct PostTaxCalculationsRequestBodyShipFromDetailsAddress: Codable {
    public var country: String
    public var city: PostTaxCalculationsRequestBodyShipFromDetailsAddressCity?
    public var line1: PostTaxCalculationsRequestBodyShipFromDetailsAddressLine1?
    public var line2: PostTaxCalculationsRequestBodyShipFromDetailsAddressLine2?
    public var postalCode: PostTaxCalculationsRequestBodyShipFromDetailsAddressPostalCode?
    public var state: PostTaxCalculationsRequestBodyShipFromDetailsAddressState?

    enum CodingKeys: String, CodingKey {
        case country
        case city
        case line1
        case line2
        case postalCode = "postal_code"
        case state
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension PostTaxCalculationsRequestBodyShipFromDetailsAddress {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.country) else {
            throw SdkValidationError(
                field: "country",
                code: "required",
                message: "Validation failed for 'country': value is required"
            )
        }
        country = try container.sdkDecodeRequired(.country)
        city = try container.sdkDecodeIfPresent(.city)
        line1 = try container.sdkDecodeIfPresent(.line1)
        line2 = try container.sdkDecodeIfPresent(.line2)
        postalCode = try container.sdkDecodeIfPresent(.postalCode)
        state = try container.sdkDecodeIfPresent(.state)
        try validateLength("country", country, min: nil, max: 5000)
    }
}

public extension PostTaxCalculationsRequestBodyShipFromDetailsAddress {
    init(
        country: String,
        city: PostTaxCalculationsRequestBodyShipFromDetailsAddressCity? = nil,
        line1: PostTaxCalculationsRequestBodyShipFromDetailsAddressLine1? = nil,
        line2: PostTaxCalculationsRequestBodyShipFromDetailsAddressLine2? = nil,
        postalCode: PostTaxCalculationsRequestBodyShipFromDetailsAddressPostalCode? = nil,
        state: PostTaxCalculationsRequestBodyShipFromDetailsAddressState? = nil
    ) throws {
        (self.country, self.city) = (country, city)
        (self.line1, self.line2) = (line1, line2)
        (self.postalCode, self.state) = (postalCode, state)
        try validateLength("country", self.country, min: nil, max: 5000)
    }
}

public enum PostTaxCalculationsRequestBodyShipFromDetailsAddressState {
    case stringValue(String)
    case stringValue1(String)
}

extension PostTaxCalculationsRequestBodyShipFromDetailsAddressState: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for PostTaxCalculationsRequestBodyShipFromDetailsAddressState"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(String.self) {
            return .stringValue(value)
        }
        if let value = try? container.decode(String.self) {
            return .stringValue1(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .stringValue(value): try container.encode(value); return true
        case let .stringValue1(value): try container.encode(value); return true
        }
    }
}
