// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
extension V1BalanceTransactionsMethods {
    public struct GetBalanceTransactionsOptions: Codable {
        public var created: GetBalanceTransactionsParameter?
        public var currency: String?
        public var endingBefore: String?
        public var expand: [String]?
        public var limit: Int?
        public var payout: String?
        public var source: String?
        public var startingAfter: String?
        public var type: String?

        public init() {}
    }

    /// Lists balance transactions that contribute to the account balance, with the newest transactions returned first. Use `created`, `currency`, `payout`, `source`, and `type` to filter results, and use cursor parameters to paginate the list.
    ///
    /// Returns a list of transactions that have contributed to the Stripe account balance (for example, charges, transfers, and so on). The transactions return in sorted order, with the most recent transactions appearing first. The previous name of this endpoint was “Balance history,” and it used the path /v1/balance/history .
    ///
    /// - Parameters:
    /// - created: Only return transactions that were created during the given date
    ///   interval.
    /// - currency: Only return transactions in a certain currency. Three-letter
    ///   [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in
    ///   lowercase. Must be a [supported
    ///   currency](https://stripe.com/docs/currencies).
    /// - endingBefore: A cursor for use in pagination. `ending_before` is an object
    ///   ID that defines your place in the list. For instance, if you make a list
    ///   request and receive 100 objects, starting with `obj_bar`, your subsequent
    ///   call can include `ending_before=obj_bar` in order to fetch the previous page
    ///   of the list.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - limit: A limit on the number of objects to be returned. Limit can range
    ///   between 1 and 100, and the default is 10.
    /// - payout: For automatic Stripe payouts only, only returns transactions that
    ///   were paid out on the specified payout ID.
    /// - source: Only returns transactions associated with the given object.
    /// - startingAfter: A cursor for use in pagination. `starting_after` is an
    ///   object ID that defines your place in the list. For instance, if you make a
    ///   list request and receive 100 objects, ending with `obj_foo`, your subsequent
    ///   call can include `starting_after=obj_foo` in order to fetch the next page of
    ///   the list.
    /// - type: Only returns transactions of the given type. One of: `tax_fund`,
    ///   `adjustment`, `advance`, `advance_funding`, `anticipation_repayment`,
    ///   `application_fee`, `application_fee_refund`, `charge`,
    ///   `climate_order_purchase`, `climate_order_refund`,
    ///   `connect_collection_transfer`, `contribution`, `inbound_transfer`,
    ///   `inbound_transfer_reversal`, `issuing_authorization_hold`,
    ///   `issuing_authorization_release`, `issuing_dispute`, `issuing_transaction`,
    ///   `obligation_outbound`, `obligation_reversal_inbound`, `payment`,
    ///   `payment_failure_refund`, `payment_network_reserve_hold`,
    ///   `payment_network_reserve_release`, `payment_refund`, `payment_reversal`,
    ///   `payment_unreconciled`, `payout`, `payout_cancel`, `payout_failure`,
    ///   `payout_minimum_balance_hold`, `payout_minimum_balance_release`, `refund`,
    ///   `refund_failure`, `reserve_transaction`, `reserved_funds`, `reserve_hold`,
    ///   `reserve_release`, `stripe_fee`, `stripe_fx_fee`,
    ///   `stripe_balance_payment_debit`, `stripe_balance_payment_debit_reversal`,
    ///   `tax_fee`, `topup`, `topup_reversal`, `transfer`, `transfer_cancel`,
    ///   `transfer_failure`, `transfer_refund`, or `fee_credit_funding`.
    public static func getBalanceTransactions(config: ClientConfig, options: GetBalanceTransactionsOptions) async throws -> GetBalanceTransactionsResponse {
        if let endingBefore = options.endingBefore {
            try validateLength("ending_before", endingBefore, max: 5000)
        }

        if let payout = options.payout {
            try validateLength("payout", payout, max: 5000)
        }

        if let source = options.source {
            try validateLength("source", source, max: 5000)
        }

        if let startingAfter = options.startingAfter {
            try validateLength("starting_after", startingAfter, max: 5000)
        }

        if let type = options.type {
            try validateLength("type", type, max: 5000)
        }

        return try (await sdkRequest("GET", "/v1/balance_transactions", config: config, query: [
            SdkQueryParameter("created", value: options.created),
            SdkQueryParameter("currency", value: options.currency),
            SdkQueryParameter("ending_before", value: options.endingBefore),
            SdkQueryParameter("expand", values: options.expand, style: "deepObject", explode: true),
            SdkQueryParameter("limit", value: options.limit),
            SdkQueryParameter("payout", value: options.payout),
            SdkQueryParameter("source", value: options.source),
            SdkQueryParameter("starting_after", value: options.startingAfter),
            SdkQueryParameter("type", value: options.type),
        ], decoder: .json, operationId: "GetBalanceTransactions")).data
    }
}
