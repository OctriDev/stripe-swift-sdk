// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
extension V1CustomersSubscriptionsMethods {
    public struct PostCustomersCustomerSubscriptionsSubscriptionExposedIdOptions: Codable {
        public var customer: String
        public var subscriptionExposedId: String
        public var addInvoiceItems: PostCustomersCustomerSubscriptionsSubscriptionExposedIdAddInvXce4d386374?
        public var applicationFeePercent: PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequesXb566b4cfb2?
        public var automaticTax: PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequesX061b2fba54?
        public var billingCycleAnchor: PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequesXcb3bf41713?
        public var billingThresholds: PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequesXc04826dcc2?
        public var cancelAt: PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequesXb61df81763?
        public var cancelAtPeriodEnd: Bool?
        public var cancellationDetails: PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequesX689f325c8b?
        public var collectionMethod: PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequesX891204e038?
        public var daysUntilDue: Int?
        public var defaultPaymentMethod: String?
        public var defaultSource: PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequesX481aa52569?
        public var defaultTaxRates: PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequesX17eea5324a?
        public var discounts: PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequesX4bd7c8f2a3?
        public var expand: [String]?
        public var invoiceSettings: PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequesX0c2f448881?
        public var items: PostCustomersCustomerSubscriptionsSubscriptionExposedIdItemsList?
        public var metadata: PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequesX93640c44b4?
        public var offSession: Bool?
        public var pauseCollection: PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequesX8ed1db46df?
        public var paymentBehavior: PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequesX110c2dc4e0?
        public var paymentSettings: PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequesX1a39e5e6a8?
        public var pendingInvoiceItemInterval: PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequesXaaee2e6a7c?
        public var prorationBehavior: PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequesX8b267a8692?
        public var prorationDate: Int?
        public var transferData: PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequesXae76096fdb?
        public var trialEnd: PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequesX4616b24d76?
        public var trialFromPlan: Bool?
        public var trialSettings: PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequesX24936f0c7f?

        public init(customer: String, subscriptionExposedId: String) {
            self.customer = customer
            self.subscriptionExposedId = subscriptionExposedId
        }
    }

    /// Updates an existing subscription on a customer using the supplied configuration. Use the subscription identifiers to select the resource and provide only the settings you want to change, such as items, billing behavior, discounts, or cancellation details. Changes to plans or quantities can affect proration and the next amount charged.
    ///
    /// Updates an existing subscription on a customer to match the specified parameters. When changing plans or quantities, we will optionally prorate the price we charge next month to make up for any price changes. To preview how the proration will be calculated, use the upcoming invoice endpoint.
    ///
    /// - Parameters:
    /// - addInvoiceItems: A list of prices and quantities that will generate
    ///   invoice items appended to the next invoice for this subscription. You may
    ///   pass up to 20 items.
    /// - applicationFeePercent: A non-negative decimal between 0 and 100, with at
    ///   most two decimal places. This represents the percentage of the subscription
    ///   invoice total that will be transferred to the application owner's Stripe
    ///   account. The request must be made by a platform account on a connected
    ///   account in order to set an application fee percentage. For more information,
    ///   see the application fees
    ///   [documentation](https://stripe.com/docs/connect/subscriptions#collecting-fee
    ///   s-on-subscriptions).
    /// - automaticTax: Automatic tax settings for this subscription. We recommend
    ///   you only include this parameter when the existing value is being changed.
    /// - billingCycleAnchor: Either `now` or `unchanged`. Setting the value to
    ///   `now` resets the subscription's billing cycle anchor to the current time.
    ///   For more information, see the billing cycle
    ///   [documentation](https://docs.stripe.com/billing/subscriptions/billing-cycle)
    ///   .
    /// - billingThresholds: Define thresholds at which an invoice will be sent, and
    ///   the subscription advanced to a new billing period. When updating, pass an
    ///   empty string to remove previously-defined thresholds.
    /// - cancelAt: A timestamp at which the subscription should cancel. If set to a
    ///   date before the current period ends, this will cause a proration if
    ///   prorations have been enabled using `proration_behavior`. If set during a
    ///   future period, this will always cause a proration for that period.
    /// - cancelAtPeriodEnd: Indicate whether this subscription should cancel at the
    ///   end of the current period (`current_period_end`).
    /// - cancellationDetails: Details about why this subscription was cancelled
    /// - collectionMethod: Either `charge_automatically`, or `send_invoice`. When
    ///   charging automatically, Stripe will attempt to pay this subscription at the
    ///   end of the cycle using the default source attached to the customer. When
    ///   sending an invoice, Stripe will email your customer an invoice with payment
    ///   instructions and mark the subscription as `active`. Defaults to
    ///   `charge_automatically`.
    /// - daysUntilDue: Number of days a customer has to pay invoices generated by
    ///   this subscription. Valid only for subscriptions where `collection_method` is
    ///   set to `send_invoice`.
    /// - defaultPaymentMethod: ID of the default payment method for the
    ///   subscription. It must belong to the customer associated with the
    ///   subscription. This takes precedence over `default_source`. If neither are
    ///   set, invoices will use the customer's
    ///   [invoice_settings.default_payment_method](https://docs.stripe.com/api/custom
    ///   ers/object#customer_object-invoice_settings-default_payment_method) or
    ///   [default_source](https://docs.stripe.com/api/customers/object#customer_objec
    ///   t-default_source).
    /// - defaultSource: ID of the default payment source for the subscription. It
    ///   must belong to the customer associated with the subscription and be in a
    ///   chargeable state. If `default_payment_method` is also set,
    ///   `default_payment_method` will take precedence. If neither are set, invoices
    ///   will use the customer's
    ///   [invoice_settings.default_payment_method](https://docs.stripe.com/api/custom
    ///   ers/object#customer_object-invoice_settings-default_payment_method) or
    ///   [default_source](https://docs.stripe.com/api/customers/object#customer_objec
    ///   t-default_source).
    /// - defaultTaxRates: The tax rates that will apply to any subscription item
    ///   that does not have `tax_rates` set. Invoices created will have their
    ///   `default_tax_rates` populated from the subscription. Pass an empty string to
    ///   remove previously-defined tax rates.
    /// - discounts: The coupons to redeem into discounts for the subscription. A
    ///   populated array overwrites the existing discounts on the subscription. If
    ///   not specified or empty array, it leaves the subscription's discounts
    ///   unchanged. If empty string, it clears the subscription's discounts.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - invoiceSettings: All invoices will be billed using the specified settings.
    /// - items: A list of up to 20 subscription items, each with an attached price.
    /// - metadata: Set of [key-value pairs](https://docs.stripe.com/api/metadata)
    ///   that you can attach to an object. This can be useful for storing additional
    ///   information about the object in a structured format. Individual keys can be
    ///   unset by posting an empty value to them. All keys can be unset by posting an
    ///   empty value to `metadata`.
    /// - offSession: Indicates if a customer is on or off-session while an invoice
    ///   payment is attempted. Defaults to `false` (on-session).
    /// - pauseCollection: If specified, payment collection for this subscription
    ///   will be paused. Note that the subscription status will be unchanged and will
    ///   not be updated to `paused`. Learn more about [pausing
    ///   collection](https://docs.stripe.com/billing/subscriptions/pause-payment).
    /// - paymentBehavior: Controls how Stripe handles payment when a subscription
    ///   update requires payment and `collection_method=charge_automatically`.
    /// - paymentSettings: Payment settings to pass to invoices created by the
    ///   subscription.
    /// - pendingInvoiceItemInterval: Specifies an interval for how often to bill
    ///   for any pending invoice items. It is analogous to calling Create an invoice
    ///   for the given subscription at the specified interval.
    /// - prorationBehavior: Determines how to handle
    ///   [prorations](https://docs.stripe.com/billing/subscriptions/prorations) when
    ///   the billing cycle changes (e.g., when switching plans, resetting
    ///   `billing_cycle_anchor=now`, or starting a trial), or if an item's `quantity`
    ///   changes. The default value is `create_prorations`.
    /// - prorationDate: If set, prorations will be calculated as though the
    ///   subscription was updated at the given time. This can be used to apply
    ///   exactly the same prorations that were previewed with the [create
    ///   preview](https://stripe.com/docs/api/invoices/create_preview) endpoint.
    ///   `proration_date` can also be used to implement custom proration logic, such
    ///   as prorating by day instead of by second, by providing the time that you
    ///   wish to use for proration calculations.
    /// - transferData: If specified, the funds from the subscription's invoices
    ///   will be transferred to the destination and the ID of the resulting transfers
    ///   will be found on the resulting charges. This will be unset if you POST an
    ///   empty value.
    /// - trialEnd: Unix timestamp representing the end of the trial period the
    ///   customer will get before being charged for the first time. This will always
    ///   overwrite any trials that might apply via a subscribed plan. If set,
    ///   trial_end will override the default trial period of the plan the customer is
    ///   being subscribed to. The special value `now` can be provided to end the
    ///   customer's trial immediately. Can be at most two years from
    ///   `billing_cycle_anchor`.
    /// - trialFromPlan: Indicates if a plan's `trial_period_days` should be applied
    ///   to the subscription. Setting `trial_end` per subscription is preferred, and
    ///   this defaults to `false`. Setting this flag to `true` together with
    ///   `trial_end` is not allowed. See [Using trial periods on
    ///   subscriptions](https://docs.stripe.com/billing/subscriptions/trials) to
    ///   learn more.
    /// - trialSettings: Settings related to subscription trials.
    public static func postCustomersCustomerSubscriptionsSubscriptionExposedId(config: ClientConfig, options: PostCustomersCustomerSubscriptionsSubscriptionExposedIdOptions) async throws -> Subscription {
        try validateLength("customer", options.customer, max: 5000)

        try validateLength("subscription_exposed_id", options.subscriptionExposedId, max: 5000)

        if let billingCycleAnchor = options.billingCycleAnchor {
            try validateLength("billing_cycle_anchor", billingCycleAnchor.rawValue, max: 5000)
        }

        if let defaultPaymentMethod = options.defaultPaymentMethod {
            try validateLength("default_payment_method", defaultPaymentMethod, max: 5000)
        }

        let requestBody = PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestBody(options: options)

        return try (await sdkRequest("POST", ["/v1/customers/", sdkEncodePathSegment(sdkWireString(options.customer)), "/subscriptions/", sdkEncodePathSegment(sdkWireString(options.subscriptionExposedId))].joined(), config: config, body: requestBody, contentType: "application/x-www-form-urlencoded", decoder: .json, operationId: "PostCustomersCustomerSubscriptionsSubscriptionExposedId")).data
    }
}
