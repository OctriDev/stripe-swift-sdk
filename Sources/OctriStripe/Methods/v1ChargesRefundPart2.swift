// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
extension V1ChargesRefundMethods {
    public struct PostChargesChargeRefundOptions: Codable {
        public var charge: String
        public var amount: Int?
        public var expand: [String]?
        public var instructionsEmail: String?
        public var metadata: PostChargesChargeRefundRequestBodyMetadata?
        public var paymentIntent: String?
        public var reason: PostChargesChargeRefundRequestBodyReason?
        public var refundApplicationFee: Bool?
        public var reverseTransfer: Bool?

        public init(charge: String) {
            self.charge = charge
        }
    }

    /// When you create a new refund, you must specify either a Charge or a PaymentIntent object. This action refunds a previously created charge that’s not refunded yet. Funds are refunded to the credit or debit card that’s originally charged. You can optionally refund only part of a charge. You can repeat this until the entire charge is refunded. After you entirely refund a charge, you can’t refund it again. This method raises an error when it’s called on an already-refunded charge, or when you attempt to refund more money than is left on a charge.
    ///
    /// - Parameters:
    /// - charge: The identifier of the charge to refund.
    /// - amount: A positive integer in the [smallest currency
    ///   unit](https://docs.stripe.com/currencies#zero-decimal) representing how much
    ///   of this charge to refund. Can refund only up to the remaining, unrefunded
    ///   amount of the charge.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - instructionsEmail: For payment methods without native refund support
    ///   (e.g., Konbini, PromptPay), use this email from the customer to receive
    ///   refund instructions.
    /// - metadata: Set of [key-value pairs](https://docs.stripe.com/api/metadata)
    ///   that you can attach to an object. This can be useful for storing additional
    ///   information about the object in a structured format. Individual keys can be
    ///   unset by posting an empty value to them. All keys can be unset by posting an
    ///   empty value to `metadata`.
    /// - paymentIntent: The identifier of the PaymentIntent to refund.
    /// - reason: String indicating the reason for the refund. If set, possible
    ///   values are `duplicate`, `fraudulent`, and `requested_by_customer`. If you
    ///   believe the charge to be fraudulent, specifying `fraudulent` as the reason
    ///   will add the associated card and email to your [block
    ///   lists](https://docs.stripe.com/radar/lists), and will also help us improve
    ///   our fraud detection algorithms.
    /// - refundApplicationFee: Boolean indicating whether the application fee
    ///   should be refunded when refunding this charge. If a full charge refund is
    ///   given, the full application fee will be refunded. Otherwise, the application
    ///   fee will be refunded in an amount proportional to the amount of the charge
    ///   refunded. An application fee can be refunded only by the application that
    ///   created the charge.
    /// - reverseTransfer: Boolean indicating whether the transfer should be
    ///   reversed when refunding this charge. The transfer will be reversed
    ///   proportionally to the amount being refunded (either the entire or partial
    ///   amount). A transfer can be reversed only by the application that created the
    ///   charge.
    public static func postChargesChargeRefund(config: ClientConfig, options: PostChargesChargeRefundOptions) async throws -> Charge {
        try validateLength("charge", options.charge, max: 5000)

        if let paymentIntent = options.paymentIntent {
            try validateLength("payment_intent", paymentIntent, max: 5000)
        }

        if let reason = options.reason {
            try validateLength("reason", reason.rawValue, max: 5000)
        }

        let requestBody = PostChargesChargeRefundRequestBody(options: options)

        return try (await sdkRequest("POST", ["/v1/charges/", sdkEncodePathSegment(sdkWireString(options.charge)), "/refund"].joined(), config: config, body: requestBody, contentType: "application/x-www-form-urlencoded", decoder: .json, operationId: "PostChargesChargeRefund")).data
    }
}
