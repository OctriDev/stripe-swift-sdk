// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
extension V1SubscriptionsMethods {
    public struct PostSubscriptionsOptions: Codable {
        public var addInvoiceItems: [PostSubscriptionsRequestBodyAddInvoiceItemsItem]?
        public var applicationFeePercent: PostSubscriptionsRequestBodyApplicationFeePercent?
        public var automaticTax: PostSubscriptionsRequestBodyAutomaticTax?
        public var backdateStartDate: Int?
        public var billingCycleAnchor: Int?
        public var billingCycleAnchorConfig: PostSubscriptionsRequestBodyBillingCycleAnchorConfig?
        public var billingMode: PostSubscriptionsRequestBodyBillingMode?
        public var billingSchedules: [PostSubscriptionsRequestBodyBillingSchedulesItem]?
        public var billingThresholds: PostSubscriptionsRequestBodyBillingThresholds?
        public var cancelAt: PostSubscriptionsRequestBodyCancelAt?
        public var cancelAtPeriodEnd: Bool?
        public var collectionMethod: PostSubscriptionsRequestBodyCollectionMethod?
        public var currency: String?
        public var customer: String?
        public var customerAccount: String?
        public var daysUntilDue: Int?
        public var defaultPaymentMethod: String?
        public var defaultSource: String?
        public var defaultTaxRates: PostSubscriptionsRequestBodyDefaultTaxRates?
        public var description: String?
        public var discounts: PostSubscriptionsRequestBodyDiscounts?
        public var expand: [String]?
        public var invoiceSettings: PostSubscriptionsRequestBodyInvoiceSettings?
        public var items: [PostSubscriptionsRequestBodyItemsItem]?
        public var metadata: PostSubscriptionsRequestBodyMetadata?
        public var offSession: Bool?
        public var onBehalfOf: String?
        public var paymentBehavior: PostSubscriptionsRequestBodyPaymentBehavior?
        public var paymentSettings: PostSubscriptionsRequestBodyPaymentSettings?
        public var pendingInvoiceItemInterval: PostSubscriptionsRequestBodyPendingInvoiceItemInterval?
        public var prorationBehavior: PostSubscriptionsRequestBodyProrationBehavior?
        public var transferData: PostSubscriptionsRequestBodyTransferData?
        public var trialEnd: PostSubscriptionsRequestBodyTrialEnd?
        public var trialFromPlan: Bool?
        public var trialPeriodDays: Int?
        public var trialSettings: PostSubscriptionsRequestBodyTrialSettings?

        public init() {}
    }

    /// Creates a new subscription on an existing customer. Each customer can have up to 500 active or scheduled subscriptions. When you create a subscription with collection_method=charge_automatically , the first invoice is finalized as part of the request. The payment_behavior parameter determines the exact behavior of the initial payment. To start subscriptions where the first invoice always begins in a draft status, use subscription schedules instead. Schedules provide the flexibility to model more complex billing configurations that change over time.
    ///
    /// - Parameters:
    /// - addInvoiceItems: A list of prices and quantities that will generate
    ///   invoice items appended to the next invoice for this subscription. You may
    ///   pass up to 20 items.
    /// - applicationFeePercent: A non-negative decimal between 0 and 100, with at
    ///   most two decimal places. This represents the percentage of the subscription
    ///   invoice total that will be transferred to the application owner's Stripe
    ///   account. The request must be made by a platform account on a connected
    ///   account in order to set an application fee percentage. For more information,
    ///   see the application fees
    ///   [documentation](https://stripe.com/docs/connect/subscriptions#collecting-fee
    ///   s-on-subscriptions).
    /// - automaticTax: Automatic tax settings for this subscription.
    /// - backdateStartDate: A past timestamp to backdate the subscription's start
    ///   date to. If set, the first invoice will contain line items for the timespan
    ///   between the start date and the current time. Can be combined with trials and
    ///   the billing cycle anchor.
    /// - billingCycleAnchor: A future timestamp in UTC format to anchor the
    ///   subscription's [billing
    ///   cycle](https://docs.stripe.com/subscriptions/billing-cycle). The anchor is
    ///   the reference point that aligns future billing cycle dates. It sets the day
    ///   of week for `week` intervals, the day of month for `month` and `year`
    ///   intervals, and the month of year for `year` intervals.
    /// - billingCycleAnchorConfig: Mutually exclusive with billing_cycle_anchor and
    ///   only valid with monthly and yearly price intervals. When provided, the
    ///   billing_cycle_anchor is set to the next occurrence of the day_of_month at
    ///   the hour, minute, and second UTC.
    /// - billingMode: Controls how prorations and invoices for subscriptions are
    ///   calculated and orchestrated.
    /// - billingSchedules: An array of billing schedules, which allow you to bill
    ///   customers in advance for multiple service periods. Requires flexible billing
    ///   mode and API version 2026-05-27.dahlia or later. Learn more about
    ///   [prebilling](https://docs.stripe.com/billing/subscriptions/prebilling).
    /// - billingThresholds: Define thresholds at which an invoice will be sent, and
    ///   the subscription advanced to a new billing period. When updating, pass an
    ///   empty string to remove previously-defined thresholds.
    /// - cancelAt: A timestamp at which the subscription should cancel. If set to a
    ///   date before the current period ends, this will cause a proration if
    ///   prorations have been enabled using `proration_behavior`. If set during a
    ///   future period, this will always cause a proration for that period.
    /// - cancelAtPeriodEnd: Indicate whether this subscription should cancel at the
    ///   end of the current period (`current_period_end`). Defaults to `false`.
    /// - collectionMethod: Either `charge_automatically`, or `send_invoice`. When
    ///   charging automatically, Stripe will attempt to pay this subscription at the
    ///   end of the cycle using the default source attached to the customer. When
    ///   sending an invoice, Stripe will email your customer an invoice with payment
    ///   instructions and mark the subscription as `active`. Defaults to
    ///   `charge_automatically`.
    /// - currency: Three-letter [ISO currency
    ///   code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must
    ///   be a [supported currency](https://stripe.com/docs/currencies).
    /// - customer: The identifier of the customer to subscribe.
    /// - customerAccount: The identifier of the account representing the customer
    ///   to subscribe.
    /// - daysUntilDue: Number of days a customer has to pay invoices generated by
    ///   this subscription. Valid only for subscriptions where `collection_method` is
    ///   set to `send_invoice`.
    /// - defaultPaymentMethod: ID of the default payment method for the
    ///   subscription. It must belong to the customer associated with the
    ///   subscription. This takes precedence over `default_source`. If neither are
    ///   set, invoices will use the customer's
    ///   [invoice_settings.default_payment_method](https://docs.stripe.com/api/custom
    ///   ers/object#customer_object-invoice_settings-default_payment_method) or
    ///   [default_source](https://docs.stripe.com/api/customers/object#customer_objec
    ///   t-default_source).
    /// - defaultSource: ID of the default payment source for the subscription. It
    ///   must belong to the customer associated with the subscription and be in a
    ///   chargeable state. If `default_payment_method` is also set,
    ///   `default_payment_method` will take precedence. If neither are set, invoices
    ///   will use the customer's
    ///   [invoice_settings.default_payment_method](https://docs.stripe.com/api/custom
    ///   ers/object#customer_object-invoice_settings-default_payment_method) or
    ///   [default_source](https://docs.stripe.com/api/customers/object#customer_objec
    ///   t-default_source).
    /// - defaultTaxRates: The tax rates that will apply to any subscription item
    ///   that does not have `tax_rates` set. Invoices created will have their
    ///   `default_tax_rates` populated from the subscription.
    /// - description: The subscription's description, meant to be displayable to
    ///   the customer. Use this field to optionally store an explanation of the
    ///   subscription for rendering in Stripe surfaces and certain local payment
    ///   methods UIs.
    /// - discounts: The coupons to redeem into discounts for the subscription. If
    ///   not specified or empty, inherits the discount from the subscription's
    ///   customer.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - invoiceSettings: All invoices will be billed using the specified settings.
    /// - items: A list of up to 20 subscription items, each with an attached price.
    /// - metadata: Set of [key-value pairs](https://docs.stripe.com/api/metadata)
    ///   that you can attach to an object. This can be useful for storing additional
    ///   information about the object in a structured format. Individual keys can be
    ///   unset by posting an empty value to them. All keys can be unset by posting an
    ///   empty value to `metadata`.
    /// - offSession: Indicates if a customer is on or off-session while an invoice
    ///   payment is attempted. Defaults to `false` (on-session).
    /// - onBehalfOf: The account on behalf of which to charge, for each of the
    ///   subscription's invoices.
    /// - paymentBehavior: Controls how Stripe handles the first invoice when
    ///   payment is required and `collection_method=charge_automatically`.
    ///   Subscriptions with `collection_method=send_invoice` are automatically
    ///   activated regardless of the first Invoice status.
    /// - paymentSettings: Payment settings to pass to invoices created by the
    ///   subscription.
    /// - pendingInvoiceItemInterval: Specifies an interval for how often to bill
    ///   for any pending invoice items. It is analogous to calling Create an invoice
    ///   for the given subscription at the specified interval.
    /// - prorationBehavior: Determines how to handle
    ///   [prorations](https://docs.stripe.com/billing/subscriptions/prorations)
    ///   resulting from the `billing_cycle_anchor`. If no value is passed, the
    ///   default is `create_prorations`.
    /// - transferData: If specified, the funds from the subscription's invoices
    ///   will be transferred to the destination and the ID of the resulting transfers
    ///   will be found on the resulting charges.
    /// - trialEnd: Unix timestamp representing the end of the trial period the
    ///   customer will get before being charged for the first time. If set, trial_end
    ///   will override the default trial period of the plan the customer is being
    ///   subscribed to. The special value `now` can be provided to end the customer's
    ///   trial immediately. Can be at most two years from `billing_cycle_anchor`. See
    ///   [Using trial periods on
    ///   subscriptions](https://docs.stripe.com/billing/subscriptions/trials) to
    ///   learn more.
    /// - trialFromPlan: Indicates if a plan's `trial_period_days` should be applied
    ///   to the subscription. Setting `trial_end` per subscription is preferred, and
    ///   this defaults to `false`. Setting this flag to `true` together with
    ///   `trial_end` is not allowed. See [Using trial periods on
    ///   subscriptions](https://docs.stripe.com/billing/subscriptions/trials) to
    ///   learn more.
    /// - trialPeriodDays: Integer representing the number of trial period days
    ///   before the customer is charged for the first time. This will always
    ///   overwrite any trials that might apply via a subscribed plan. See [Using
    ///   trial periods on
    ///   subscriptions](https://docs.stripe.com/billing/subscriptions/trials) to
    ///   learn more.
    /// - trialSettings: Settings related to subscription trials.
    public static func postSubscriptions(config: ClientConfig, options: PostSubscriptionsOptions) async throws -> Subscription {
        if let customer = options.customer {
            try validateLength("customer", customer, max: 5000)
        }

        if let customerAccount = options.customerAccount {
            try validateLength("customer_account", customerAccount, max: 5000)
        }

        if let defaultPaymentMethod = options.defaultPaymentMethod {
            try validateLength("default_payment_method", defaultPaymentMethod, max: 5000)
        }

        if let defaultSource = options.defaultSource {
            try validateLength("default_source", defaultSource, max: 5000)
        }

        if let description = options.description {
            try validateLength("description", description, max: 500)
        }

        let requestBody = PostSubscriptionsRequestBody(options: options)

        return try (await sdkRequest("POST", "/v1/subscriptions", config: config, body: requestBody, contentType: "application/x-www-form-urlencoded", decoder: .json, operationId: "PostSubscriptions")).data
    }
}
