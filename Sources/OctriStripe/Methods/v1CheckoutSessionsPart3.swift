// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
public extension V1CheckoutSessionsMethods {
    struct PostCheckoutSessionsOptions: Codable {
        public var adaptivePricing: PostCheckoutSessionsRequestBodyAdaptivePricing?
        public var afterExpiration: PostCheckoutSessionsRequestBodyAfterExpiration?
        public var allowPromotionCodes: Bool?
        public var automaticTax: PostCheckoutSessionsRequestBodyAutomaticTax?
        public var billingAddressCollection: PostCheckoutSessionsRequestBodyBillingAddressCollection?
        public var brandingSettings: PostCheckoutSessionsRequestBodyBrandingSettings?
        public var cancelUrl: String?
        public var clientReferenceId: String?
        public var consentCollection: PostCheckoutSessionsRequestBodyConsentCollection?
        public var currency: String?
        public var customFields: [PostCheckoutSessionsRequestBodyCustomFieldsItem]?
        public var customText: PostCheckoutSessionsRequestBodyCustomText?
        public var customer: String?
        public var customerAccount: String?
        public var customerCreation: PostCheckoutSessionsRequestBodyCustomerCreation?
        public var customerEmail: String?
        public var customerUpdate: PostCheckoutSessionsRequestBodyCustomerUpdate?
        public var discounts: [PostCheckoutSessionsRequestBodyDiscountsItem]?
        public var excludedPaymentMethodTypes: [PostCheckoutSessionsRequestBodyExcludedPaymentMethodTypesItem]?
        public var expand: [String]?
        public var expiresAt: Int?
        public var integrationIdentifier: String?
        public var invoiceCreation: PostCheckoutSessionsRequestBodyInvoiceCreation?
        public var lineItems: [PostCheckoutSessionsRequestBodyLineItemsItem]?
        public var locale: PostCheckoutSessionsRequestBodyLocale?
        public var managedPayments: PostCheckoutSessionsRequestBodyManagedPayments?
        public var metadata: [String: String]?
        public var mode: PostCheckoutSessionsRequestBodyMode?
        public var nameCollection: PostCheckoutSessionsRequestBodyNameCollection?
        public var optionalItems: [PostCheckoutSessionsRequestBodyOptionalItemsItem]?
        public var originContext: PostCheckoutSessionsRequestBodyOriginContext?
        public var paymentIntentData: PostCheckoutSessionsRequestBodyPaymentIntentData?
        public var paymentMethodCollection: PostCheckoutSessionsRequestBodyPaymentMethodCollection?
        public var paymentMethodConfiguration: String?
        public var paymentMethodData: PostCheckoutSessionsRequestBodyPaymentMethodData?
        public var paymentMethodOptions: PostCheckoutSessionsRequestBodyPaymentMethodOptions?
        public var paymentMethodTypes: [PostCheckoutSessionsRequestBodyPaymentMethodTypesItem]?
        public var permissions: PostCheckoutSessionsRequestBodyPermissions?
        public var phoneNumberCollection: PostCheckoutSessionsRequestBodyPhoneNumberCollection?
        public var redirectOnCompletion: PostCheckoutSessionsRequestBodyRedirectOnCompletion?
        public var returnUrl: String?
        public var savedPaymentMethodOptions: PostCheckoutSessionsRequestBodySavedPaymentMethodOptions?
        public var setupIntentData: PostCheckoutSessionsRequestBodySetupIntentData?
        public var shippingAddressCollection: PostCheckoutSessionsRequestBodyShippingAddressCollection?
        public var shippingOptions: [PostCheckoutSessionsRequestBodyShippingOptionsItem]?
        public var submitType: PostCheckoutSessionsRequestBodySubmitType?
        public var subscriptionData: PostCheckoutSessionsRequestBodySubscriptionData?
        public var successUrl: String?
        public var taxIdCollection: PostCheckoutSessionsRequestBodyTaxIdCollection?
        public var uiMode: PostCheckoutSessionsRequestBodyUiMode?
        public var walletOptions: PostCheckoutSessionsRequestBodyWalletOptions?

        public init() {}
    }

    /// Creates a Checkout Session for a one-time purchase or subscription. Supply the session configuration, including
    /// its mode, payment settings, line items, customer information, and redirect behaviour as applicable. A 200
    /// response returns the created session for use in the Checkout flow.
    ///
    /// Creates a Checkout Session object.
    ///
    /// - Parameters:
    /// - adaptivePricing: Settings for price localization with [Adaptive
    ///   Pricing](https://docs.stripe.com/payments/checkout/adaptive-pricing).
    /// - afterExpiration: Configure actions after a Checkout Session has expired.
    ///   You can't set this parameter if `ui_mode` is `elements`.
    /// - allowPromotionCodes: Enables user redeemable promotion codes.
    /// - automaticTax: Settings for automatic tax lookup for this session and
    ///   resulting payments, invoices, and subscriptions.
    /// - billingAddressCollection: Specify whether Checkout should collect the
    ///   customer's billing address. Defaults to `auto`.
    /// - brandingSettings: The branding settings for the Checkout Session. This
    ///   parameter is not allowed if ui_mode is `elements`.
    /// - cancelUrl: If set, Checkout displays a back button and customers will be
    ///   directed to this URL if they decide to cancel payment and return to your
    ///   website. This parameter is not allowed if ui_mode is `embedded_page` or
    ///   `elements`.
    /// - clientReferenceId: A unique string to reference the Checkout Session. This
    ///   can be a customer ID, a cart ID, or similar, and can be used to reconcile
    ///   the session with your internal systems.
    /// - consentCollection: Configure fields for the Checkout Session to gather
    ///   active consent from customers.
    /// - currency: Three-letter [ISO currency
    ///   code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must
    ///   be a [supported currency](https://stripe.com/docs/currencies). Required in
    ///   `setup` mode when `payment_method_types` is not set.
    /// - customFields: Collect additional information from your customer using
    ///   custom fields. Up to 3 fields are supported. You can't set this parameter if
    ///   `ui_mode` is `custom`.
    /// - customText: Display additional text for your customers using custom text.
    ///   You can't set this parameter if `ui_mode` is `custom`.
    /// - customer: ID of an existing Customer, if one exists. In `payment` mode,
    ///   the customer’s most recently saved card payment method will be used to
    ///   prefill the email, name, card details, and billing address on the Checkout
    ///   page. In `subscription` mode, the customer’s [default payment
    ///   method](https://docs.stripe.com/api/customers/update#update_customer-invoice
    ///   _settings-default_payment_method) will be used if it’s a card, otherwise the
    ///   most recently saved card will be used. A valid billing address, billing name
    ///   and billing email are required on the payment method for Checkout to prefill
    ///   the customer's card details. If the Customer already has a valid
    ///   [email](https://docs.stripe.com/api/customers/object#customer_object-email)
    ///   set, the email will be prefilled and not editable in Checkout. If the
    ///   Customer does not have a valid `email`, Checkout will set the email entered
    ///   during the session on the Customer. If blank for Checkout Sessions in
    ///   `subscription` mode or with `customer_creation` set as `always` in `payment`
    ///   mode, Checkout will create a new Customer object based on information
    ///   provided during the payment flow. You can set
    ///   [`payment_intent_data.setup_future_usage`](https://docs.stripe.com/api/check
    ///   out/sessions/create#create_checkout_session-payment_intent_data-setup_future
    ///   _usage) to have Checkout automatically attach the payment method to the
    ///   Customer you pass in for future reuse.
    /// - customerAccount: ID of an existing Account, if one exists. Has the same
    ///   behavior as `customer`.
    /// - customerCreation: Configure whether a Checkout Session creates a
    ///   [Customer](https://docs.stripe.com/api/customers) during Session
    ///   confirmation. When a Customer is not created, you can still retrieve email,
    ///   address, and other customer data entered in Checkout with
    ///   [customer_details](https://docs.stripe.com/api/checkout/sessions/object#chec
    ///   kout_session_object-customer_details). Sessions that don't create Customers
    ///   instead are grouped by [guest
    ///   customers](https://docs.stripe.com/payments/checkout/guest-customers) in the
    ///   Dashboard. Promotion codes limited to first time customers will return
    ///   invalid for these Sessions. Can only be set in `payment` and `setup` mode.
    /// - customerEmail: If provided, this value will be used when the Customer
    ///   object is created. If not provided, customers will be asked to enter their
    ///   email address. Use this parameter to prefill customer data if you already
    ///   have an email on file. To access information about the customer once a
    ///   session is complete, use the `customer` field.
    /// - customerUpdate: Controls what fields on Customer can be updated by the
    ///   Checkout Session. Can only be provided when `customer` is provided.
    /// - discounts: The coupon or promotion code to apply to this Session.
    ///   Currently, only up to one may be specified.
    /// - excludedPaymentMethodTypes: A list of the types of payment methods (e.g.,
    ///   `card`) that should be excluded from this Checkout Session. This should only
    ///   be used when payment methods for this Checkout Session are managed through
    ///   the [Stripe
    ///   Dashboard](https://dashboard.stripe.com/settings/payment_methods).
    /// - expand: Specifies which fields in the response should be expanded.
    /// - expiresAt: The Epoch time in seconds at which the Checkout Session will
    ///   expire. It can be anywhere from 30 minutes to 24 hours after Checkout
    ///   Session creation. By default, this value is 24 hours from creation.
    /// - integrationIdentifier: The integration identifier for this Checkout
    ///   Session. Multiple Checkout Sessions can have the same integration
    ///   identifier.
    /// - invoiceCreation: Generate a post-purchase Invoice for one-time payments.
    /// - lineItems: A list of items the customer is purchasing. Use this parameter
    ///   to pass one-time or recurring [Prices](https://docs.stripe.com/api/prices).
    ///   The parameter is required for `payment` and `subscription` mode. For
    ///   `payment` mode, there is a maximum of 100 line items, however it is
    ///   recommended to consolidate line items if there are more than a few dozen.
    ///   For `subscription` mode, there is a maximum of 20 line items with recurring
    ///   Prices and 20 line items with one-time Prices. Line items with one-time
    ///   Prices will be on the initial invoice only.
    /// - locale: The IETF language tag of the locale Checkout is displayed in. If
    ///   blank or `auto`, the browser's locale is used.
    /// - managedPayments: Settings for Managed Payments for this Checkout Session
    ///   and resulting PaymentIntents, Invoices, and Subscriptions.
    /// - metadata: Set of [key-value pairs](https://docs.stripe.com/api/metadata)
    ///   that you can attach to an object. This can be useful for storing additional
    ///   information about the object in a structured format. Individual keys can be
    ///   unset by posting an empty value to them. All keys can be unset by posting an
    ///   empty value to `metadata`.
    /// - mode: The mode of the Checkout Session. Pass `subscription` if the
    ///   Checkout Session includes at least one recurring item.
    /// - nameCollection: Controls name collection settings for the session. You can
    ///   configure Checkout to collect your customers' business names, individual
    ///   names, or both. Each name field can be either required or optional. If a
    ///   [Customer](https://docs.stripe.com/api/customers) is created or provided,
    ///   the names can be saved to the Customer object as well.
    /// - optionalItems: A list of optional items the customer can add to their
    ///   order at checkout. Use this parameter to pass one-time or recurring
    ///   [Prices](https://docs.stripe.com/api/prices). There is a maximum of 10
    ///   optional items allowed on a Checkout Session, and the existing limits on the
    ///   number of line items allowed on a Checkout Session apply to the combined
    ///   number of line items and optional items. For `payment` mode, there is a
    ///   maximum of 100 combined line items and optional items, however it is
    ///   recommended to consolidate items if there are more than a few dozen. For
    ///   `subscription` mode, there is a maximum of 20 line items and optional items
    ///   with recurring Prices and 20 line items and optional items with one-time
    ///   Prices. You can't set this parameter if `ui_mode` is `custom`.
    /// - originContext: Where the user is coming from. This informs the
    ///   optimizations that are applied to the session. You can't set this parameter
    ///   if `ui_mode` is `elements`.
    /// - paymentIntentData: A subset of parameters to be passed to PaymentIntent
    ///   creation for Checkout Sessions in `payment` mode.
    /// - paymentMethodCollection: Specify whether Checkout should collect a payment
    ///   method. When set to `if_required`, Checkout will not collect a payment
    ///   method when the total due for the session is 0. This may occur if the
    ///   Checkout Session includes a free trial or a discount. Can only be set in
    ///   `subscription` mode. Defaults to `always`. If you'd like information on how
    ///   to collect a payment method outside of Checkout, read the guide on
    ///   configuring [subscriptions with a free
    ///   trial](https://docs.stripe.com/payments/checkout/free-trials).
    /// - paymentMethodConfiguration: The ID of the payment method configuration to
    ///   use with this Checkout session.
    /// - paymentMethodData: This parameter allows you to set some attributes on the
    ///   payment method created during a Checkout session.
    /// - paymentMethodOptions: Payment-method-specific configuration.
    /// - paymentMethodTypes: A list of the types of payment methods (e.g., `card`)
    ///   this Checkout Session can accept. You can omit this attribute to manage your
    ///   payment methods from the [Stripe
    ///   Dashboard](https://dashboard.stripe.com/settings/payment_methods). See
    ///   [Dynamic Payment
    ///   Methods](https://docs.stripe.com/payments/payment-methods/integration-option
    ///   s#using-dynamic-payment-methods) for more details. Read more about the
    ///   supported payment methods and their requirements in our payment method
    ///   details guide. If multiple payment methods are passed, Checkout will
    ///   dynamically reorder them to prioritize the most relevant payment methods
    ///   based on the customer's location and other characteristics.
    /// - permissions: This property is used to set up permissions for various
    ///   actions (e.g., update) on the CheckoutSession object. Can only be set when
    ///   creating `embedded` or `custom` sessions. For specific permissions, please
    ///   refer to their dedicated subsections, such as
    ///   `permissions.update_shipping_details`.
    /// - phoneNumberCollection: Controls phone number collection settings for the
    ///   session. We recommend that you review your privacy policy and check with
    ///   your legal contacts before using this feature. Learn more about [collecting
    ///   phone numbers with
    ///   Checkout](https://docs.stripe.com/payments/checkout/phone-numbers).
    /// - redirectOnCompletion: This parameter applies to `ui_mode: embedded_page`.
    ///   Learn more about the [redirect
    ///   behavior](https://docs.stripe.com/payments/checkout/custom-success-page?paym
    ///   ent-ui=embedded-form) of embedded sessions. Defaults to `always`.
    /// - returnUrl: The URL to redirect your customer back to after they
    ///   authenticate or cancel their payment on the payment method's app or site.
    ///   This parameter is required if `ui_mode` is `embedded_page` or `elements` and
    ///   redirect-based payment methods are enabled on the session.
    /// - savedPaymentMethodOptions: Controls saved payment method settings for the
    ///   session. Only available in `payment` and `subscription` mode.
    /// - setupIntentData: A subset of parameters to be passed to SetupIntent
    ///   creation for Checkout Sessions in `setup` mode.
    /// - shippingAddressCollection: When set, provides configuration for Checkout
    ///   to collect a shipping address from a customer.
    /// - shippingOptions: The shipping rate options to apply to this Session. Up to
    ///   a maximum of 5.
    /// - submitType: Describes the type of transaction being performed by Checkout
    ///   in order to customize relevant text on the page, such as the submit button.
    ///   `submit_type` can only be specified on Checkout Sessions in `payment` or
    ///   `subscription` mode. If blank or `auto`, `pay` is used. You can't set this
    ///   parameter if `ui_mode` is `elements`.
    /// - subscriptionData: A subset of parameters to be passed to subscription
    ///   creation for Checkout Sessions in `subscription` mode.
    /// - successUrl: The URL to which Stripe should send customers when payment or
    ///   setup is complete. This parameter is not allowed if ui_mode is
    ///   `embedded_page` or `elements`. If you'd like to use information from the
    ///   successful Checkout Session on your page, read the guide on [customizing
    ///   your success
    ///   page](https://docs.stripe.com/payments/checkout/custom-success-page).
    /// - taxIdCollection: Controls tax ID collection during checkout.
    /// - uiMode: The UI mode of the Session. Defaults to `hosted_page`.
    /// - walletOptions: Wallet-specific configuration.
    static func postCheckoutSessions(
        config: ClientConfig,
        options: PostCheckoutSessionsOptions
    ) async throws -> CheckoutSession {
        if let clientReferenceId = options.clientReferenceId {
            try validateLength("client_reference_id", clientReferenceId, max: 200)
        }

        if let customer = options.customer {
            try validateLength("customer", customer, max: 5000)
        }

        if let customerAccount = options.customerAccount {
            try validateLength("customer_account", customerAccount, max: 5000)
        }

        if let integrationIdentifier = options.integrationIdentifier {
            try validateLength("integration_identifier", integrationIdentifier, max: 200)
        }

        if let paymentMethodConfiguration = options.paymentMethodConfiguration {
            try validateLength("payment_method_configuration", paymentMethodConfiguration, max: 100)
        }

        let requestBody = PostCheckoutSessionsRequestBody(options: options)

        return try await (sdkRequest(
            "POST",
            "/v1/checkout/sessions",
            config: config,
            body: requestBody,
            contentType: "application/x-www-form-urlencoded",
            decoder: .json,
            operationId: "PostCheckoutSessions"
        )).data
    }
}
