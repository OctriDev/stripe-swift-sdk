// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
public extension V1AccountLinksMethods {
    /// Creates an AccountLink object that includes a single-use Stripe URL that the platform can redirect their user to
    /// in order to take them through the Connect Onboarding flow.
    ///
    /// - Parameters:
    /// - account: The identifier of the account to create an account link for.
    /// - type: The type of account link the user is requesting. You can create
    ///   Account Links of type `account_update` only for connected accounts where
    ///   your platform is responsible for collecting requirements, including Custom
    ///   accounts. You can't create them for accounts that have access to a
    ///   Stripe-hosted Dashboard. If you use Connect embedded components, you can
    ///   include components that allow your connected accounts to update their own
    ///   information. For an account without Stripe-hosted Dashboard access where
    ///   Stripe is liable for negative balances, you must use embedded components.
    /// - collect: The collect parameter is deprecated. Use `collection_options`
    ///   instead.
    /// - collectionOptions: Specifies the requirements that Stripe collects from
    ///   connected accounts in the Connect Onboarding flow.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - refreshUrl: The URL the user will be redirected to if the account link is
    ///   expired, has been previously-visited, or is otherwise invalid. The URL you
    ///   specify should attempt to generate a new account link with the same
    ///   parameters used to create the original account link, then redirect the user
    ///   to the new account link's URL so they can continue with Connect Onboarding.
    ///   If a new account link cannot be generated or the redirect fails you should
    ///   display a useful error to the user.
    /// - returnUrl: The URL that the user will be redirected to upon leaving or
    ///   completing the linked flow.
    static func postAccountLinks(
        config: ClientConfig,
        account: String,
        type: PostAccountLinksRequestBodyType,
        collect: PostAccountLinksRequestBodyCollect?,
        collectionOptions: PostAccountLinksRequestBodyCollectionOptions?,
        expand: [String]?,
        refreshUrl: String?,
        returnUrl: String?
    ) async throws -> AccountLink {
        try validateLength("account", account, max: 5000)

        let requestBody = PostAccountLinksRequestBody(
            account: account,
            type: type,
            collect: collect,
            collectionOptions: collectionOptions,
            expand: expand,
            refreshUrl: refreshUrl,
            returnUrl: returnUrl
        )

        return try await (sdkRequest(
            "POST",
            "/v1/account_links",
            config: config,
            body: requestBody,
            contentType: "application/x-www-form-urlencoded",
            decoder: .json,
            operationId: "PostAccountLinks"
        )).data
    }
}
