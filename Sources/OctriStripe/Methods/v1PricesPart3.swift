// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
public extension V1PricesMethods {
    struct PostPricesOptions: Codable {
        public var currency: String
        public var active: Bool?
        public var billingScheme: PostPricesRequestBodyBillingScheme?
        public var currencyOptions: [String: PostPricesRequestBodyCurrencyOptionsValue]?
        public var customUnitAmount: PostPricesRequestBodyCustomUnitAmount?
        public var expand: [String]?
        public var lookupKey: String?
        public var metadata: [String: String]?
        public var nickname: String?
        public var product: String?
        public var productData: PostPricesRequestBodyProductData?
        public var recurring: PostPricesRequestBodyRecurring?
        public var taxBehavior: PostPricesRequestBodyTaxBehavior?
        public var tiers: [PostPricesRequestBodyTiersItem]?
        public var tiersMode: PostPricesRequestBodyTiersMode?
        public var transferLookupKey: Bool?
        public var transformQuantity: PostPricesRequestBodyTransformQuantity?
        public var unitAmount: Int?
        public var unitAmountDecimal: String?

        public init(currency: String) {
            self.currency = currency
        }
    }

    /// Creates a new Price for an existing Product. The Price can be recurring or one-time.
    ///
    /// - Parameters:
    /// - currency: Three-letter [ISO currency
    ///   code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must
    ///   be a [supported currency](https://stripe.com/docs/currencies).
    /// - active: Whether the price can be used for new purchases. Defaults to
    ///   `true`.
    /// - billingScheme: Describes how to compute the price per period. Either
    ///   `per_unit` or `tiered`. `per_unit` indicates that the fixed amount
    ///   (specified in `unit_amount` or `unit_amount_decimal`) will be charged per
    ///   unit in `quantity` (for prices with `usage_type=licensed`), or per unit of
    ///   total usage (for prices with `usage_type=metered`). `tiered` indicates that
    ///   the unit pricing will be computed using a tiering strategy as defined using
    ///   the `tiers` and `tiers_mode` attributes.
    /// - currencyOptions: Prices defined in each available currency option. Each
    ///   key must be a three-letter [ISO currency
    ///   code](https://www.iso.org/iso-4217-currency-codes.html) and a [supported
    ///   currency](https://stripe.com/docs/currencies).
    /// - customUnitAmount: When set, provides configuration for the amount to be
    ///   adjusted by the customer during Checkout Sessions and Payment Links.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - lookupKey: A lookup key used to retrieve prices dynamically from a static
    ///   string. This may be up to 200 characters.
    /// - metadata: Set of [key-value pairs](https://docs.stripe.com/api/metadata)
    ///   that you can attach to an object. This can be useful for storing additional
    ///   information about the object in a structured format. Individual keys can be
    ///   unset by posting an empty value to them. All keys can be unset by posting an
    ///   empty value to `metadata`.
    /// - nickname: A brief description of the price, hidden from customers.
    /// - product: The ID of the [Product](https://docs.stripe.com/api/products)
    ///   that this [Price](https://docs.stripe.com/api/prices) will belong to.
    /// - productData: These fields can be used to create a new product that this
    ///   price will belong to.
    /// - recurring: The recurring components of a price such as `interval` and
    ///   `usage_type`.
    /// - taxBehavior: Only required if a [default tax
    ///   behavior](https://docs.stripe.com/tax/products-prices-tax-categories-tax-beh
    ///   avior#setting-a-default-tax-behavior-(recommended)) was not provided in the
    ///   Stripe Tax settings. Specifies whether the price is considered inclusive of
    ///   taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or
    ///   `unspecified`. Once specified as either `inclusive` or `exclusive`, it
    ///   cannot be changed.
    /// - tiers: Each element represents a pricing tier. This parameter requires
    ///   `billing_scheme` to be set to `tiered`. See also the documentation for
    ///   `billing_scheme`.
    /// - tiersMode: Defines if the tiering price should be `graduated` or `volume`
    ///   based. In `volume`-based tiering, the maximum quantity within a period
    ///   determines the per unit price, in `graduated` tiering pricing can
    ///   successively change as the quantity grows.
    /// - transferLookupKey: If set to true, will atomically remove the lookup key
    ///   from the existing price, and assign it to this price.
    /// - transformQuantity: Apply a transformation to the reported usage or set
    ///   quantity before computing the billed price. Cannot be combined with `tiers`.
    /// - unitAmount: A positive integer in cents (or local equivalent) (or 0 for a
    ///   free price) representing how much to charge. One of `unit_amount`,
    ///   `unit_amount_decimal`, or `custom_unit_amount` is required, unless
    ///   `billing_scheme=tiered`.
    /// - unitAmountDecimal: Same as `unit_amount`, but accepts a decimal value in
    ///   cents (or local equivalent) with at most 12 decimal places. Only one of
    ///   `unit_amount` and `unit_amount_decimal` can be set.
    static func postPrices(config: ClientConfig, options: PostPricesOptions) async throws -> Price {
        if let lookupKey = options.lookupKey {
            try validateLength("lookup_key", lookupKey, max: 200)
        }

        if let nickname = options.nickname {
            try validateLength("nickname", nickname, max: 5000)
        }

        if let product = options.product {
            try validateLength("product", product, max: 5000)
        }

        let requestBody = PostPricesRequestBody(options: options)

        return try await (sdkRequest(
            "POST",
            "/v1/prices",
            config: config,
            body: requestBody,
            contentType: "application/x-www-form-urlencoded",
            decoder: .json,
            operationId: "PostPrices"
        )).data
    }
}
