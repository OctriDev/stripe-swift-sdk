// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
extension V1SubscriptionsMethods {
    public struct GetSubscriptionsOptions: Codable {
        public var automaticTax: GetSubscriptionsParameter?
        public var collectionMethod: GetSubscriptionsParameterXcfd548b6?
        public var created: GetSubscriptionsParameterXd3c9c31a?
        public var currentPeriodEnd: GetSubscriptionsParameterX3b560d97?
        public var currentPeriodStart: GetSubscriptionsParameterX0b5217a5?
        public var customer: String?
        public var customerAccount: String?
        public var endingBefore: String?
        public var expand: [String]?
        public var limit: Int?
        public var price: String?
        public var startingAfter: String?
        public var status: GetSubscriptionsParameterX6bb21d19?
        public var testClock: String?

        public init() {}
    }

    /// Lists subscriptions using filters for customer, account, status, collection method, price, automatic tax, test clock, and lifecycle dates. By default, returns subscriptions that have not been canceled; pass `status=canceled` to include canceled subscriptions. Use cursor parameters and `limit` to paginate the results.
    ///
    /// By default, returns a list of subscriptions that have not been canceled. In order to list canceled subscriptions, specify status=canceled .
    ///
    /// - Parameters:
    /// - automaticTax: Filter subscriptions by their automatic tax settings.
    /// - collectionMethod: The collection method of the subscriptions to retrieve.
    ///   Either `charge_automatically` or `send_invoice`.
    /// - created: Only return subscriptions that were created during the given date
    ///   interval.
    /// - currentPeriodEnd: Only return subscriptions whose minimum item
    ///   current_period_end falls within the given date interval.
    /// - currentPeriodStart: Only return subscriptions whose maximum item
    ///   current_period_start falls within the given date interval.
    /// - customer: The ID of the customer whose subscriptions you're retrieving.
    /// - customerAccount: The ID of the account representing the customer whose
    ///   subscriptions you're retrieving.
    /// - endingBefore: A cursor for use in pagination. `ending_before` is an object
    ///   ID that defines your place in the list. For instance, if you make a list
    ///   request and receive 100 objects, starting with `obj_bar`, your subsequent
    ///   call can include `ending_before=obj_bar` in order to fetch the previous page
    ///   of the list.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - limit: A limit on the number of objects to be returned. Limit can range
    ///   between 1 and 100, and the default is 10.
    /// - price: Filter for subscriptions that contain this recurring price ID.
    /// - startingAfter: A cursor for use in pagination. `starting_after` is an
    ///   object ID that defines your place in the list. For instance, if you make a
    ///   list request and receive 100 objects, ending with `obj_foo`, your subsequent
    ///   call can include `starting_after=obj_foo` in order to fetch the next page of
    ///   the list.
    /// - status: The status of the subscriptions to retrieve. Passing in a value of
    ///   `canceled` will return all canceled subscriptions, including those belonging
    ///   to deleted customers. Pass `ended` to find subscriptions that are canceled
    ///   and subscriptions that are expired due to [incomplete
    ///   payment](https://docs.stripe.com/billing/subscriptions/overview#subscription
    ///   -statuses). Passing in a value of `all` will return subscriptions of all
    ///   statuses. If no value is supplied, all subscriptions that have not been
    ///   canceled are returned.
    /// - testClock: Filter for subscriptions that are associated with the specified
    ///   test clock. The response will not include subscriptions with test clocks if
    ///   this and the customer parameter is not set.
    public static func getSubscriptions(config: ClientConfig, options: GetSubscriptionsOptions) async throws -> GetSubscriptionsResponse {
        if let customer = options.customer {
            try validateLength("customer", customer, max: 5000)
        }

        if let customerAccount = options.customerAccount {
            try validateLength("customer_account", customerAccount, max: 5000)
        }

        if let endingBefore = options.endingBefore {
            try validateLength("ending_before", endingBefore, max: 5000)
        }

        if let price = options.price {
            try validateLength("price", price, max: 5000)
        }

        if let startingAfter = options.startingAfter {
            try validateLength("starting_after", startingAfter, max: 5000)
        }

        if let testClock = options.testClock {
            try validateLength("test_clock", testClock, max: 5000)
        }

        return try (await sdkRequest("GET", "/v1/subscriptions", config: config, query: [
            SdkQueryParameter("automatic_tax", value: options.automaticTax),
            SdkQueryParameter("collection_method", value: options.collectionMethod),
            SdkQueryParameter("created", value: options.created),
            SdkQueryParameter("current_period_end", value: options.currentPeriodEnd),
            SdkQueryParameter("current_period_start", value: options.currentPeriodStart),
            SdkQueryParameter("customer", value: options.customer),
            SdkQueryParameter("customer_account", value: options.customerAccount),
            SdkQueryParameter("ending_before", value: options.endingBefore),
            SdkQueryParameter("expand", values: options.expand, style: "deepObject", explode: true),
            SdkQueryParameter("limit", value: options.limit),
            SdkQueryParameter("price", value: options.price),
            SdkQueryParameter("starting_after", value: options.startingAfter),
            SdkQueryParameter("status", value: options.status),
            SdkQueryParameter("test_clock", value: options.testClock),
        ], decoder: .json, operationId: "GetSubscriptions")).data
    }
}
