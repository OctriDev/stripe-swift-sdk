// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
public enum V1SubscriptionSchedulesCancelMethods {
    /// Cancels a subscription schedule and its associated active subscription immediately when applicable. You can
    /// cancel only a schedule whose status is `not_started` or `active`, and you can control final invoicing and
    /// proration for an active schedule. A successful response contains the canceled schedule and its updated lifecycle
    /// fields.
    ///
    /// Cancels a subscription schedule and its associated subscription immediately (if the subscription schedule has an
    /// active subscription). A subscription schedule can only be canceled if its status is not_started or active .
    ///
    /// - Parameters:
    /// - expand: Specifies which fields in the response should be expanded.
    /// - invoiceNow: If the subscription schedule is `active`, indicates if a final
    ///   invoice will be generated that contains any un-invoiced metered usage and
    ///   new/pending proration invoice items. Defaults to `true`.
    /// - prorate: If the subscription schedule is `active`, indicates if the
    ///   cancellation should be prorated. Defaults to `true`.
    public static func postSubscriptionSchedulesScheduleCancel(
        config: ClientConfig,
        schedule: String,
        expand: [String]?,
        invoiceNow: Bool?,
        prorate: Bool?
    ) async throws -> SubscriptionSchedule {
        try validateLength("schedule", schedule, max: 5000)

        let requestBody = PostSubscriptionSchedulesScheduleCancelRequestBody(
            expand: expand,
            invoiceNow: invoiceNow,
            prorate: prorate
        )

        return try await (sdkRequest(
            "POST",
            ["/v1/subscription_schedules/", sdkEncodePathSegment(sdkWireString(schedule)), "/cancel"].joined(),
            config: config,
            body: requestBody,
            contentType: "application/x-www-form-urlencoded",
            decoder: .json,
            operationId: "PostSubscriptionSchedulesScheduleCancel"
        )).data
    }
}
