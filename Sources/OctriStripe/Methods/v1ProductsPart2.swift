// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
extension V1ProductsMethods {
    public struct GetProductsOptions: Codable {
        public var active: Bool?
        public var created: GetProductsParameter?
        public var endingBefore: String?
        public var expand: [String]?
        public var ids: [String]?
        public var limit: Int?
        public var shippable: Bool?
        public var startingAfter: String?
        public var url: String?

        public init() {}
    }

    /// Lists products sorted by creation date, with the most recently created products first. Use `active`, `shippable`, `url`, `ids`, or a creation-time filter to narrow the results, and use cursor parameters to paginate through the list.
    ///
    /// Returns a list of your products. The products are returned sorted by creation date, with the most recently created products appearing first.
    ///
    /// - Parameters:
    /// - active: Only return products that are active or inactive (e.g., pass
    ///   `false` to list all inactive products).
    /// - created: Only return products that were created during the given date
    ///   interval.
    /// - endingBefore: A cursor for use in pagination. `ending_before` is an object
    ///   ID that defines your place in the list. For instance, if you make a list
    ///   request and receive 100 objects, starting with `obj_bar`, your subsequent
    ///   call can include `ending_before=obj_bar` in order to fetch the previous page
    ///   of the list.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - ids: Only return products with the given IDs. Cannot be used with
    ///   [starting_after](https://api.stripe.com#list_products-starting_after) or
    ///   [ending_before](https://api.stripe.com#list_products-ending_before).
    /// - limit: A limit on the number of objects to be returned. Limit can range
    ///   between 1 and 100, and the default is 10.
    /// - shippable: Only return products that can be shipped (i.e., physical, not
    ///   digital products).
    /// - startingAfter: A cursor for use in pagination. `starting_after` is an
    ///   object ID that defines your place in the list. For instance, if you make a
    ///   list request and receive 100 objects, ending with `obj_foo`, your subsequent
    ///   call can include `starting_after=obj_foo` in order to fetch the next page of
    ///   the list.
    /// - url: Only return products with the given url.
    public static func getProducts(config: ClientConfig, options: GetProductsOptions) async throws -> GetProductsResponse {
        if let endingBefore = options.endingBefore {
            try validateLength("ending_before", endingBefore, max: 5000)
        }

        if let startingAfter = options.startingAfter {
            try validateLength("starting_after", startingAfter, max: 5000)
        }

        if let url = options.url {
            try validateLength("url", url, max: 5000)
        }

        return try (await sdkRequest("GET", "/v1/products", config: config, query: [
            SdkQueryParameter("active", value: options.active),
            SdkQueryParameter("created", value: options.created),
            SdkQueryParameter("ending_before", value: options.endingBefore),
            SdkQueryParameter("expand", values: options.expand, style: "deepObject", explode: true),
            SdkQueryParameter("ids", values: options.ids, style: "deepObject", explode: true),
            SdkQueryParameter("limit", value: options.limit),
            SdkQueryParameter("shippable", value: options.shippable),
            SdkQueryParameter("starting_after", value: options.startingAfter),
            SdkQueryParameter("url", value: options.url),
        ], decoder: .json, operationId: "GetProducts")).data
    }
}
