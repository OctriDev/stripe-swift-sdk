// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
extension V1TreasuryTransactionEntriesMethods {
    public struct GetTreasuryTransactionEntriesOptions: Codable {
        public var financialAccount: String
        public var created: GetTreasuryTransactionEntriesParameter?
        public var effectiveAt: GetTreasuryTransactionEntriesParameterXf0247d5e?
        public var endingBefore: String?
        public var expand: [String]?
        public var limit: Int?
        public var orderBy: GetTreasuryTransactionEntriesParameterX7d557c91?
        public var startingAfter: String?
        public var transaction: String?

        public init(financialAccount: String) {
            self.financialAccount = financialAccount
        }
    }

    /// Lists transaction entries associated with a financial account. Filter by creation or effective timestamps, transaction, and ordering, then use cursor parameters to paginate the results.
    ///
    /// Retrieves a list of TransactionEntry objects.
    ///
    /// - Parameters:
    /// - financialAccount: Returns objects associated with this FinancialAccount.
    /// - created: Only return TransactionEntries that were created during the given
    ///   date interval.
    /// - endingBefore: A cursor for use in pagination. `ending_before` is an object
    ///   ID that defines your place in the list. For instance, if you make a list
    ///   request and receive 100 objects, starting with `obj_bar`, your subsequent
    ///   call can include `ending_before=obj_bar` in order to fetch the previous page
    ///   of the list.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - limit: A limit on the number of objects to be returned. Limit can range
    ///   between 1 and 100, and the default is 10.
    /// - orderBy: The results are in reverse chronological order by `created` or
    ///   `effective_at`. The default is `created`.
    /// - startingAfter: A cursor for use in pagination. `starting_after` is an
    ///   object ID that defines your place in the list. For instance, if you make a
    ///   list request and receive 100 objects, ending with `obj_foo`, your subsequent
    ///   call can include `starting_after=obj_foo` in order to fetch the next page of
    ///   the list.
    /// - transaction: Only return TransactionEntries associated with this
    ///   Transaction.
    public static func getTreasuryTransactionEntries(config: ClientConfig, options: GetTreasuryTransactionEntriesOptions) async throws -> GetTreasuryTransactionEntriesResponse {
        if let endingBefore = options.endingBefore {
            try validateLength("ending_before", endingBefore, max: 5000)
        }

        if let startingAfter = options.startingAfter {
            try validateLength("starting_after", startingAfter, max: 5000)
        }

        if let transaction = options.transaction {
            try validateLength("transaction", transaction, max: 5000)
        }

        return try (await sdkRequest("GET", "/v1/treasury/transaction_entries", config: config, query: [
            SdkQueryParameter("financial_account", value: options.financialAccount),
            SdkQueryParameter("created", value: options.created),
            SdkQueryParameter("effective_at", value: options.effectiveAt),
            SdkQueryParameter("ending_before", value: options.endingBefore),
            SdkQueryParameter("expand", values: options.expand, style: "deepObject", explode: true),
            SdkQueryParameter("limit", value: options.limit),
            SdkQueryParameter("order_by", value: options.orderBy),
            SdkQueryParameter("starting_after", value: options.startingAfter),
            SdkQueryParameter("transaction", value: options.transaction),
        ], decoder: .json, operationId: "GetTreasuryTransactionEntries")).data
    }
}
