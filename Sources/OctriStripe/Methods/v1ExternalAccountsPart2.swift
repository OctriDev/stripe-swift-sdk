// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
extension V1ExternalAccountsMethods {
    public struct PostExternalAccountsIdOptions: Codable {
        public var id: String
        public var accountHolderName: String?
        public var accountHolderType: PostExternalAccountsIdRequestBodyAccountHolderType?
        public var accountType: PostExternalAccountsIdRequestBodyAccountType?
        public var addressCity: String?
        public var addressCountry: String?
        public var addressLine1: String?
        public var addressLine2: String?
        public var addressState: String?
        public var addressZip: String?
        public var defaultForCurrency: Bool?
        public var documents: PostExternalAccountsIdRequestBodyDocuments?
        public var expMonth: String?
        public var expYear: String?
        public var expand: [String]?
        public var metadata: PostExternalAccountsIdRequestBodyMetadata?
        public var name: String?

        public init(id: String) {
            self.id = id
        }
    }

    /// Updates editable details of a bank account belonging to a connected account. Use fields such as `account_holder_name`, `account_holder_type`, `metadata`, or `default_for_currency`; updates are available only when the connected account uses application requirement collection, and an empty update can re-enable a disabled bank account.
    ///
    /// Updates the metadata, account holder name, account holder type of a bank account belonging to a connected account and optionally sets it as the default for its currency. Other bank account details are not editable by design. You can only update bank accounts when account.controller.requirement_collection is application , which includes Custom accounts. You can re-enable a disabled bank account by performing an update call without providing any arguments or changes.
    ///
    /// - Parameters:
    /// - accountHolderName: The name of the person or business that owns the bank
    ///   account.
    /// - accountHolderType: The type of entity that holds the account. This can be
    ///   either `individual` or `company`.
    /// - accountType: The bank account type. This can only be `checking` or
    ///   `savings` in most countries. In Japan, this can only be `futsu` or `toza`.
    /// - addressCity: City/District/Suburb/Town/Village.
    /// - addressCountry: Billing address country, if provided when creating card.
    /// - addressLine1: Address line 1 (Street address/PO Box/Company name).
    /// - addressLine2: Address line 2 (Apartment/Suite/Unit/Building).
    /// - addressState: State/County/Province/Region.
    /// - addressZip: ZIP or postal code.
    /// - defaultForCurrency: When set to true, this becomes the default external
    ///   account for its currency.
    /// - documents: Documents that may be submitted to satisfy various
    ///   informational requests.
    /// - expMonth: Two digit number representing the card’s expiration month.
    /// - expYear: Four digit number representing the card’s expiration year.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - metadata: Set of [key-value pairs](https://docs.stripe.com/api/metadata)
    ///   that you can attach to an object. This can be useful for storing additional
    ///   information about the object in a structured format. Individual keys can be
    ///   unset by posting an empty value to them. All keys can be unset by posting an
    ///   empty value to `metadata`.
    /// - name: Cardholder name.
    public static func postExternalAccountsId(config: ClientConfig, options: PostExternalAccountsIdOptions) async throws -> ExternalAccount {
        if let accountHolderName = options.accountHolderName {
            try validateLength("account_holder_name", accountHolderName, max: 5000)
        }

        if let accountHolderType = options.accountHolderType {
            try validateLength("account_holder_type", accountHolderType.rawValue, max: 5000)
        }

        if let accountType = options.accountType {
            try validateLength("account_type", accountType.rawValue, max: 5000)
        }

        if let addressCity = options.addressCity {
            try validateLength("address_city", addressCity, max: 5000)
        }

        if let addressCountry = options.addressCountry {
            try validateLength("address_country", addressCountry, max: 5000)
        }

        if let addressLine1 = options.addressLine1 {
            try validateLength("address_line1", addressLine1, max: 5000)
        }

        if let addressLine2 = options.addressLine2 {
            try validateLength("address_line2", addressLine2, max: 5000)
        }

        if let addressState = options.addressState {
            try validateLength("address_state", addressState, max: 5000)
        }

        if let addressZip = options.addressZip {
            try validateLength("address_zip", addressZip, max: 5000)
        }

        if let expMonth = options.expMonth {
            try validateLength("exp_month", expMonth, max: 5000)
        }

        if let expYear = options.expYear {
            try validateLength("exp_year", expYear, max: 5000)
        }

        if let name = options.name {
            try validateLength("name", name, max: 5000)
        }

        let requestBody = PostExternalAccountsIdRequestBody(options: options)

        return try (await sdkRequest("POST", ["/v1/external_accounts/", sdkEncodePathSegment(sdkWireString(options.id))].joined(), config: config, body: requestBody, contentType: "application/x-www-form-urlencoded", decoder: .json, operationId: "PostExternalAccountsId")).data
    }
}
