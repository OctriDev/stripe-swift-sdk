// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
public extension V1QuotesMethods {
    struct PostQuotesQuoteOptions: Codable {
        public var quote: String
        public var applicationFeeAmount: PostQuotesQuoteRequestBodyApplicationFeeAmount?
        public var applicationFeePercent: PostQuotesQuoteRequestBodyApplicationFeePercent?
        public var automaticTax: PostQuotesQuoteRequestBodyAutomaticTax?
        public var collectionMethod: PostQuotesQuoteRequestBodyCollectionMethod?
        public var customer: String?
        public var customerAccount: String?
        public var defaultTaxRates: PostQuotesQuoteRequestBodyDefaultTaxRates?
        public var description: PostQuotesQuoteRequestBodyDescriptionVariant1?
        public var discounts: PostQuotesQuoteRequestBodyDiscounts?
        public var expand: [String]?
        public var expiresAt: Int?
        public var footer: PostQuotesQuoteRequestBodyFooterVariant1?
        public var header: PostQuotesQuoteRequestBodyHeaderVariant1?
        public var invoiceSettings: PostQuotesQuoteRequestBodyInvoiceSettings?
        public var lineItems: [PostQuotesQuoteRequestBodyLineItemsItem]?
        public var metadata: [String: String]?
        public var onBehalfOf: String?
        public var subscriptionData: PostQuotesQuoteRequestBodySubscriptionData?
        public var transferData: PostQuotesQuoteRequestBodyTransferData?

        public init(quote: String) {
            self.quote = quote
        }
    }

    /// Updates an existing quote with customer, pricing, tax, invoice, and presentation settings. Supply only the
    /// fields you want to change, and use `collection_method` to select automatic charging or invoice-based collection.
    /// Use `expires_at` to set a future Unix timestamp for cancellation while the quote is open or in draft status.
    ///
    /// A quote models prices and services for a customer.
    ///
    /// - Parameters:
    /// - applicationFeeAmount: The amount of the application fee (if any) that will
    ///   be requested to be applied to the payment and transferred to the application
    ///   owner's Stripe account. There cannot be any line items with recurring prices
    ///   when using this field.
    /// - applicationFeePercent: A non-negative decimal between 0 and 100, with at
    ///   most two decimal places. This represents the percentage of the subscription
    ///   invoice total that will be transferred to the application owner's Stripe
    ///   account. There must be at least 1 line item with a recurring price to use
    ///   this field.
    /// - automaticTax: Settings for automatic tax lookup for this quote and
    ///   resulting invoices and subscriptions.
    /// - collectionMethod: Either `charge_automatically`, or `send_invoice`. When
    ///   charging automatically, Stripe will attempt to pay invoices at the end of
    ///   the subscription cycle or at invoice finalization using the default payment
    ///   method attached to the subscription or customer. When sending an invoice,
    ///   Stripe will email your customer an invoice with payment instructions and
    ///   mark the subscription as `active`. Defaults to `charge_automatically`.
    /// - customer: The customer for which this quote belongs to. A customer is
    ///   required before finalizing the quote. Once specified, it cannot be changed.
    /// - customerAccount: The account for which this quote belongs to. A customer
    ///   or account is required before finalizing the quote. Once specified, it
    ///   cannot be changed.
    /// - defaultTaxRates: The tax rates that will apply to any line item that does
    ///   not have `tax_rates` set.
    /// - description: A description that will be displayed on the quote PDF.
    /// - discounts: The discounts applied to the quote.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - expiresAt: A future timestamp on which the quote will be canceled if in
    ///   `open` or `draft` status. Measured in seconds since the Unix epoch.
    /// - footer: A footer that will be displayed on the quote PDF.
    /// - header: A header that will be displayed on the quote PDF.
    /// - invoiceSettings: All invoices will be billed using the specified settings.
    /// - lineItems: A list of line items the customer is being quoted for. Each
    ///   line item includes information about the product, the quantity, and the
    ///   resulting cost.
    /// - metadata: Set of [key-value pairs](https://docs.stripe.com/api/metadata)
    ///   that you can attach to an object. This can be useful for storing additional
    ///   information about the object in a structured format. Individual keys can be
    ///   unset by posting an empty value to them. All keys can be unset by posting an
    ///   empty value to `metadata`.
    /// - onBehalfOf: The account on behalf of which to charge.
    /// - subscriptionData: When creating a subscription or subscription schedule,
    ///   the specified configuration data will be used. There must be at least one
    ///   line item with a recurring price for a subscription or subscription schedule
    ///   to be created. A subscription schedule is created if
    ///   `subscription_data[effective_date]` is present and in the future, otherwise
    ///   a subscription is created.
    /// - transferData: The data with which to automatically create a Transfer for
    ///   each of the invoices.
    static func postQuotesQuote(config: ClientConfig, options: PostQuotesQuoteOptions) async throws -> Quote {
        try validateLength("quote", options.quote, max: 5000)

        if let customer = options.customer {
            try validateLength("customer", customer, max: 5000)
        }

        if let customerAccount = options.customerAccount {
            try validateLength("customer_account", customerAccount, max: 5000)
        }

        let requestBody = PostQuotesQuoteRequestBody(options: options)

        return try await (sdkRequest(
            "POST",
            ["/v1/quotes/", sdkEncodePathSegment(sdkWireString(options.quote))].joined(),
            config: config,
            body: requestBody,
            contentType: "application/x-www-form-urlencoded",
            decoder: .json,
            operationId: "PostQuotesQuote"
        )).data
    }
}
