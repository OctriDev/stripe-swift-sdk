// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
extension V1PaymentLinksMethods {
    public struct PostPaymentLinksOptions: Codable {
        public var lineItems: [PostPaymentLinksRequestBodyLineItemsItem]
        public var afterCompletion: PostPaymentLinksRequestBodyAfterCompletion?
        public var allowPromotionCodes: Bool?
        public var applicationFeeAmount: Int?
        public var applicationFeePercent: Double?
        public var automaticTax: PostPaymentLinksRequestBodyAutomaticTax?
        public var billingAddressCollection: PostPaymentLinksRequestBodyBillingAddressCollection?
        public var consentCollection: PostPaymentLinksRequestBodyConsentCollection?
        public var currency: String?
        public var customFields: [PostPaymentLinksRequestBodyCustomFieldsItem]?
        public var customText: PostPaymentLinksRequestBodyCustomText?
        public var customerCreation: PostPaymentLinksRequestBodyCustomerCreation?
        public var expand: [String]?
        public var inactiveMessage: String?
        public var invoiceCreation: PostPaymentLinksRequestBodyInvoiceCreation?
        public var managedPayments: PostPaymentLinksRequestBodyManagedPayments?
        public var metadata: [String: String]?
        public var nameCollection: PostPaymentLinksRequestBodyNameCollection?
        public var onBehalfOf: String?
        public var optionalItems: [PostPaymentLinksRequestBodyOptionalItemsItem]?
        public var paymentIntentData: PostPaymentLinksRequestBodyPaymentIntentData?
        public var paymentMethodCollection: PostPaymentLinksRequestBodyPaymentMethodCollection?
        public var paymentMethodOptions: PostPaymentLinksRequestBodyPaymentMethodOptions?
        public var paymentMethodTypes: [PostPaymentLinksRequestBodyPaymentMethodTypesItem]?
        public var phoneNumberCollection: PostPaymentLinksRequestBodyPhoneNumberCollection?
        public var restrictions: PostPaymentLinksRequestBodyRestrictions?
        public var shippingAddressCollection: PostPaymentLinksRequestBodyShippingAddressCollection?
        public var shippingOptions: [PostPaymentLinksRequestBodyShippingOptionsItem]?
        public var submitType: PostPaymentLinksRequestBodySubmitType?
        public var subscriptionData: PostPaymentLinksRequestBodySubscriptionData?
        public var taxIdCollection: PostPaymentLinksRequestBodyTaxIdCollection?
        public var transferData: PostPaymentLinksRequestBodyTransferData?

        public init(lineItems: [PostPaymentLinksRequestBodyLineItemsItem]) {
            self.lineItems = lineItems
        }
    }

    /// Creates a payment link that provides customers with a hosted payment page for the configured items. Supply `line_items` and configure checkout behaviour such as `after_completion`, `automatic_tax`, payment methods, customer creation, and shipping collection as needed. The response contains the created payment link and its hosted URL.
    ///
    /// Creates a payment link.
    ///
    /// - Parameters:
    /// - lineItems: The line items representing what is being sold. Each line item
    ///   represents an item being sold. Up to 20 line items are supported.
    /// - afterCompletion: Behavior after the purchase is complete.
    /// - allowPromotionCodes: Enables user redeemable promotion codes.
    /// - applicationFeeAmount: The amount of the application fee (if any) that will
    ///   be requested to be applied to the payment and transferred to the application
    ///   owner's Stripe account. Can only be applied when there are no line items
    ///   with recurring prices.
    /// - applicationFeePercent: A non-negative decimal between 0 and 100, with at
    ///   most two decimal places. This represents the percentage of the subscription
    ///   invoice total that will be transferred to the application owner's Stripe
    ///   account. There must be at least 1 line item with a recurring price to use
    ///   this field.
    /// - automaticTax: Configuration for automatic tax collection.
    /// - billingAddressCollection: Configuration for collecting the customer's
    ///   billing address. Defaults to `auto`.
    /// - consentCollection: Configure fields to gather active consent from
    ///   customers.
    /// - currency: Three-letter [ISO currency
    ///   code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must
    ///   be a [supported currency](https://stripe.com/docs/currencies) and supported
    ///   by each line item's price.
    /// - customFields: Collect additional information from your customer using
    ///   custom fields. Up to 3 fields are supported. You can't set this parameter if
    ///   `ui_mode` is `custom`.
    /// - customText: Display additional text for your customers using custom text.
    ///   You can't set this parameter if `ui_mode` is `custom`.
    /// - customerCreation: Configures whether [checkout
    ///   sessions](https://docs.stripe.com/api/checkout/sessions) created by this
    ///   payment link create a [Customer](https://docs.stripe.com/api/customers).
    /// - expand: Specifies which fields in the response should be expanded.
    /// - inactiveMessage: The custom message to be displayed to a customer when a
    ///   payment link is no longer active.
    /// - invoiceCreation: Generate a post-purchase Invoice for one-time payments.
    /// - managedPayments: Settings for Managed Payments for this Payment Link and
    ///   resulting CheckoutSessions, PaymentIntents, Invoices, and Subscriptions.
    /// - metadata: Set of [key-value pairs](https://docs.stripe.com/api/metadata)
    ///   that you can attach to an object. This can be useful for storing additional
    ///   information about the object in a structured format. Individual keys can be
    ///   unset by posting an empty value to them. All keys can be unset by posting an
    ///   empty value to `metadata`. Metadata associated with this Payment Link will
    ///   automatically be copied to [checkout
    ///   sessions](https://docs.stripe.com/api/checkout/sessions) created by this
    ///   payment link.
    /// - nameCollection: Controls settings applied for collecting the customer's
    ///   name.
    /// - onBehalfOf: The account on behalf of which to charge.
    /// - optionalItems: A list of optional items the customer can add to their
    ///   order at checkout. Use this parameter to pass one-time or recurring
    ///   [Prices](https://docs.stripe.com/api/prices). There is a maximum of 10
    ///   optional items allowed on a payment link, and the existing limits on the
    ///   number of line items allowed on a payment link apply to the combined number
    ///   of line items and optional items. There is a maximum of 20 combined line
    ///   items and optional items.
    /// - paymentIntentData: A subset of parameters to be passed to PaymentIntent
    ///   creation for Checkout Sessions in `payment` mode.
    /// - paymentMethodCollection: Specify whether Checkout should collect a payment
    ///   method. When set to `if_required`, Checkout will not collect a payment
    ///   method when the total due for the session is 0.This may occur if the
    ///   Checkout Session includes a free trial or a discount. Can only be set in
    ///   `subscription` mode. Defaults to `always`. If you'd like information on how
    ///   to collect a payment method outside of Checkout, read the guide on
    ///   [configuring subscriptions with a free
    ///   trial](https://docs.stripe.com/payments/checkout/free-trials).
    /// - paymentMethodOptions: Payment-method-specific configuration.
    /// - paymentMethodTypes: The list of payment method types that customers can
    ///   use. If no value is passed, Stripe will dynamically show relevant payment
    ///   methods from your [payment method
    ///   settings](https://dashboard.stripe.com/settings/payment_methods) (20+
    ///   payment methods
    ///   [supported](https://docs.stripe.com/payments/payment-methods/integration-opt
    ///   ions#payment-method-product-support)).
    /// - phoneNumberCollection: Controls phone number collection settings during
    ///   checkout. We recommend that you review your privacy policy and check with
    ///   your legal contacts.
    /// - restrictions: Settings that restrict the usage of a payment link.
    /// - shippingAddressCollection: Configuration for collecting the customer's
    ///   shipping address.
    /// - shippingOptions: The shipping rate options to apply to [checkout
    ///   sessions](https://docs.stripe.com/api/checkout/sessions) created by this
    ///   payment link.
    /// - submitType: Describes the type of transaction being performed in order to
    ///   customize relevant text on the page, such as the submit button. Changing
    ///   this value will also affect the hostname in the
    ///   [url](https://docs.stripe.com/api/payment_links/payment_links/object#url)
    ///   property (example: `donate.stripe.com`).
    /// - subscriptionData: When creating a subscription, the specified
    ///   configuration data will be used. There must be at least one line item with a
    ///   recurring price to use `subscription_data`.
    /// - taxIdCollection: Controls tax ID collection during checkout.
    /// - transferData: The account (if any) the payments will be attributed to for
    ///   tax reporting, and where funds from each payment will be transferred to.
    public static func postPaymentLinks(config: ClientConfig, options: PostPaymentLinksOptions) async throws -> PaymentLink {
        if let inactiveMessage = options.inactiveMessage {
            try validateLength("inactive_message", inactiveMessage, max: 500)
        }

        let requestBody = PostPaymentLinksRequestBody(options: options)

        return try (await sdkRequest("POST", "/v1/payment_links", config: config, body: requestBody, contentType: "application/x-www-form-urlencoded", decoder: .json, operationId: "PostPaymentLinks")).data
    }
}
