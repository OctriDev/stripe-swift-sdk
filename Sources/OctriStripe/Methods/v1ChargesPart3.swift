// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
public extension V1ChargesMethods {
    struct PostChargesOptions: Codable {
        public var amount: Int?
        public var applicationFee: Int?
        public var applicationFeeAmount: Int?
        public var capture: Bool?
        public var card: PostChargesRequestBodyCard?
        public var currency: String?
        public var customer: String?
        public var description: String?
        public var destination: PostChargesRequestBodyDestination?
        public var expand: [String]?
        public var metadata: PostChargesRequestBodyMetadata?
        public var onBehalfOf: String?
        public var radarOptions: PostChargesRequestBodyRadarOptions?
        public var receiptEmail: String?
        public var shipping: PostChargesRequestBodyShipping?
        public var source: String?
        public var statementDescriptor: String?
        public var statementDescriptorSuffix: String?
        public var transferData: PostChargesRequestBodyTransferData?
        public var transferGroup: String?

        public init() {}
    }

    /// Creates a charge to move funds into your account using the legacy Charges API. Supply the payment amount and
    /// currency, and use `capture` to choose immediate capture or authorization for later capture. Use the Payment
    /// Intents API for new payment flows.
    ///
    /// This method is no longer recommended—use the Payment Intents API to initiate a new payment instead. Confirmation
    /// of the PaymentIntent creates the Charge object used to request payment.
    ///
    /// - Parameters:
    /// - amount: Amount intended to be collected by this payment. A positive
    ///   integer representing how much to charge in the [smallest currency
    ///   unit](https://docs.stripe.com/currencies#zero-decimal) (e.g., 100 cents to
    ///   charge $1.00 or 100 to charge ¥100, a zero-decimal currency). The minimum
    ///   amount is $0.50 US or [equivalent in charge
    ///   currency](https://docs.stripe.com/currencies#minimum-and-maximum-charge-amou
    ///   nts). The amount value supports up to eight digits (e.g., a value of
    ///   99999999 for a USD charge of $999,999.99).
    /// - applicationFeeAmount: A fee in cents (or local equivalent) that will be
    ///   applied to the charge and transferred to the application owner's Stripe
    ///   account. The request must be made with an OAuth key or the `Stripe-Account`
    ///   header in order to take an application fee. For more information, see the
    ///   application fees
    ///   [documentation](https://docs.stripe.com/connect/direct-charges#collect-fees)
    ///   .
    /// - capture: Whether to immediately capture the charge. Defaults to `true`.
    ///   When `false`, the charge issues an authorization (or pre-authorization), and
    ///   will need to be [captured](https://api.stripe.com#capture_charge) later.
    ///   Uncaptured charges expire after a set number of days (7 by default). For
    ///   more information, see the [authorizing charges and settling
    ///   later](https://docs.stripe.com/charges/placing-a-hold) documentation.
    /// - card: A token, like the ones returned by
    ///   [Stripe.js](https://stripe.com/docs/js).
    /// - currency: Three-letter [ISO currency
    ///   code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must
    ///   be a [supported currency](https://stripe.com/docs/currencies).
    /// - customer: The ID of an existing customer that will be charged in this
    ///   request.
    /// - description: An arbitrary string which you can attach to a `Charge`
    ///   object. It is displayed when in the web interface alongside the charge. Note
    ///   that if you use Stripe to send automatic email receipts to your customers,
    ///   your receipt emails will include the `description` of the charge(s) that
    ///   they are describing.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - metadata: Set of [key-value pairs](https://docs.stripe.com/api/metadata)
    ///   that you can attach to an object. This can be useful for storing additional
    ///   information about the object in a structured format. Individual keys can be
    ///   unset by posting an empty value to them. All keys can be unset by posting an
    ///   empty value to `metadata`.
    /// - onBehalfOf: The Stripe account ID for which these funds are intended. You
    ///   can specify the business of record as the connected account using the
    ///   `on_behalf_of` attribute on the charge. For details, see [Creating Separate
    ///   Charges and
    ///   Transfers](https://docs.stripe.com/connect/separate-charges-and-transfers#se
    ///   ttlement-merchant).
    /// - radarOptions: Options to configure Radar. See [Radar
    ///   Session](https://docs.stripe.com/radar/radar-session) for more information.
    /// - receiptEmail: The email address to which this charge's
    ///   [receipt](https://docs.stripe.com/dashboard/receipts) will be sent. The
    ///   receipt will not be sent until the charge is paid, and no receipts will be
    ///   sent for test mode charges. If this charge is for a
    ///   [Customer](https://docs.stripe.com/api/customers/object), the email address
    ///   specified here will override the customer's email address. If
    ///   `receipt_email` is specified for a charge in live mode, a receipt will be
    ///   sent regardless of your [email
    ///   settings](https://dashboard.stripe.com/account/emails).
    /// - shipping: Shipping information for the charge. Helps prevent fraud on
    ///   charges for physical goods.
    /// - source: A payment source to be charged. This can be the ID of a
    ///   [card](https://docs.stripe.com/api#cards) (i.e., credit or debit card), a
    ///   [bank account](https://docs.stripe.com/api#bank_accounts), a
    ///   [source](https://docs.stripe.com/api#sources), a
    ///   [token](https://docs.stripe.com/api#tokens), or a [connected
    ///   account](https://docs.stripe.com/connect/account-debits#charging-a-connected
    ///   -account). For certain sources---namely,
    ///   [cards](https://docs.stripe.com/api#cards), [bank
    ///   accounts](https://docs.stripe.com/api#bank_accounts), and attached
    ///   [sources](https://docs.stripe.com/api#sources)---you must also pass the ID
    ///   of the associated customer.
    /// - statementDescriptor: For a non-card charge, text that appears on the
    ///   customer's statement as the statement descriptor. This value overrides the
    ///   account's default statement descriptor. For information about requirements,
    ///   including the 22-character limit, see [the Statement Descriptor
    ///   docs](https://docs.stripe.com/get-started/account/statement-descriptors).
    ///   For a card charge, this value is ignored unless you don't specify a
    ///   `statement_descriptor_suffix`, in which case this value is used as the
    ///   suffix.
    /// - statementDescriptorSuffix: Provides information about a card charge.
    ///   Concatenated to the account's [statement descriptor
    ///   prefix](https://docs.stripe.com/get-started/account/statement-descriptors#st
    ///   atic) to form the complete statement descriptor that appears on the
    ///   customer's statement. If the account has no prefix value, the suffix is
    ///   concatenated to the account's statement descriptor.
    /// - transferData: An optional dictionary including the account to
    ///   automatically transfer to as part of a destination charge. [See the Connect
    ///   documentation](https://docs.stripe.com/connect/destination-charges) for
    ///   details.
    /// - transferGroup: A string that identifies this transaction as part of a
    ///   group. For details, see [Grouping
    ///   transactions](https://docs.stripe.com/connect/separate-charges-and-transfers
    ///   #transfer-options).
    static func postCharges(config: ClientConfig, options: PostChargesOptions) async throws -> Charge {
        if let customer = options.customer {
            try validateLength("customer", customer, max: 500)
        }

        if let description = options.description {
            try validateLength("description", description, max: 40000)
        }

        if let onBehalfOf = options.onBehalfOf {
            try validateLength("on_behalf_of", onBehalfOf, max: 5000)
        }

        if let source = options.source {
            try validateLength("source", source, max: 5000)
        }

        if let statementDescriptor = options.statementDescriptor {
            try validateLength("statement_descriptor", statementDescriptor, max: 22)
        }

        if let statementDescriptorSuffix = options.statementDescriptorSuffix {
            try validateLength("statement_descriptor_suffix", statementDescriptorSuffix, max: 22)
        }

        let requestBody = PostChargesRequestBody(options: options)

        return try await (sdkRequest(
            "POST",
            "/v1/charges",
            config: config,
            body: requestBody,
            contentType: "application/x-www-form-urlencoded",
            decoder: .json,
            operationId: "PostCharges"
        )).data
    }
}
