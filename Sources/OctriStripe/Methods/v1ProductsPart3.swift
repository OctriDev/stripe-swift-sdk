// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
extension V1ProductsMethods {
    public struct PostProductsOptions: Codable {
        public var name: String
        public var active: Bool?
        public var defaultPriceData: PostProductsRequestBodyDefaultPriceData?
        public var description: String?
        public var expand: [String]?
        public var id: String?
        public var images: [String]?
        public var marketingFeatures: [PostProductsRequestBodyMarketingFeaturesItem]?
        public var metadata: [String: String]?
        public var packageDimensions: PostProductsRequestBodyPackageDimensions?
        public var shippable: Bool?
        public var statementDescriptor: String?
        public var taxCode: String?
        public var unitLabel: String?
        public var url: String?

        public init(name: String) {
            self.name = name
        }
    }

    /// Creates a new product object for goods or services that you offer. Supply `name` and optionally configure pricing, descriptions, images, marketing features, shipping information, tax settings, and display metadata. A nested `default_price_data` object can create and assign the product's default price.
    ///
    /// Creates a new product object.
    ///
    /// - Parameters:
    /// - name: The product's name, meant to be displayable to the customer.
    /// - active: Whether the product is currently available for purchase. Defaults
    ///   to `true`.
    /// - defaultPriceData: Data used to generate a new
    ///   [Price](https://docs.stripe.com/api/prices) object. This Price will be set
    ///   as the default price for this product.
    /// - description: The product's description, meant to be displayable to the
    ///   customer. Use this field to optionally store a long form explanation of the
    ///   product being sold for your own rendering purposes.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - id: An identifier will be randomly generated by Stripe. You can optionally
    ///   override this ID, but the ID must be unique across all products in your
    ///   Stripe account.
    /// - images: A list of up to 8 URLs of images for this product, meant to be
    ///   displayable to the customer.
    /// - marketingFeatures: A list of up to 15 marketing features for this product.
    ///   These are displayed in [pricing
    ///   tables](https://docs.stripe.com/payments/checkout/pricing-table).
    /// - metadata: Set of [key-value pairs](https://docs.stripe.com/api/metadata)
    ///   that you can attach to an object. This can be useful for storing additional
    ///   information about the object in a structured format. Individual keys can be
    ///   unset by posting an empty value to them. All keys can be unset by posting an
    ///   empty value to `metadata`.
    /// - packageDimensions: The dimensions of this product for shipping purposes.
    /// - shippable: Whether this product is shipped (i.e., physical goods).
    /// - statementDescriptor: An arbitrary string to be displayed on your
    ///   customer's credit card or bank statement. While most banks display this
    ///   information consistently, some may display it incorrectly or not at all.
    ///   This may be up to 22 characters. The statement description may not include
    ///   `<`, `>`, `\`, `"`, `'` characters, and will appear on your customer's
    ///   statement in capital letters. Non-ASCII characters are automatically
    ///   stripped. It must contain at least one letter. Only used for subscription
    ///   payments.
    /// - taxCode: A [tax code](https://docs.stripe.com/tax/tax-categories) ID.
    /// - unitLabel: A label that represents units of this product. When set, this
    ///   will be included in customers' receipts, invoices, Checkout, and the
    ///   customer portal.
    /// - url: A URL of a publicly-accessible webpage for this product.
    public static func postProducts(config: ClientConfig, options: PostProductsOptions) async throws -> Product {
        try validateLength("name", options.name, max: 5000)

        if let description = options.description {
            try validateLength("description", description, max: 40000)
        }

        if let id = options.id {
            try validateLength("id", id, max: 5000)
        }

        if let statementDescriptor = options.statementDescriptor {
            try validateLength("statement_descriptor", statementDescriptor, max: 22)
        }

        if let unitLabel = options.unitLabel {
            try validateLength("unit_label", unitLabel, max: 12)
        }

        if let url = options.url {
            try validateLength("url", url, max: 5000)
        }

        let requestBody = PostProductsRequestBody(options: options)

        return try (await sdkRequest("POST", "/v1/products", config: config, body: requestBody, contentType: "application/x-www-form-urlencoded", decoder: .json, operationId: "PostProducts")).data
    }
}
