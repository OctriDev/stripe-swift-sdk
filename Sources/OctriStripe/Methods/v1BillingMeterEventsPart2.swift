// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
public extension V1BillingMeterEventsMethods {
    /// Creates a billing meter event for recording customer usage. Supply `event_name` and a `payload` containing the
    /// customer mapping and measured value fields expected by the meter. You can provide an `identifier` for retry-safe
    /// uniqueness and a Unix timestamp within the permitted event window.
    ///
    /// Creates a billing meter event.
    ///
    /// - Parameters:
    /// - eventName: The name of the meter event. Corresponds with the `event_name`
    ///   field on a meter.
    /// - payload: The payload of the event. This must contain the fields
    ///   corresponding to a meter's `customer_mapping.event_payload_key` (default is
    ///   `stripe_customer_id`) and `value_settings.event_payload_key` (default is
    ///   `value`). Read more about the
    ///   [payload](https://docs.stripe.com/billing/subscriptions/usage-based/meters/c
    ///   onfigure#meter-configuration-attributes).
    /// - expand: Specifies which fields in the response should be expanded.
    /// - identifier: A unique identifier for the event. If not provided, one is
    ///   generated. We recommend using UUID-like identifiers. Stripe enforces
    ///   uniqueness within a rolling period of at least 24 hours. The enforcement of
    ///   uniqueness primarily addresses issues arising from accidental retries or
    ///   other problems occurring within extremely brief time intervals. This
    ///   approach helps prevent duplicate entries and ensures data integrity in
    ///   high-frequency operations.
    /// - timestamp: The time of the event. Measured in seconds since the Unix
    ///   epoch. Must be within the past 35 calendar days or up to 5 minutes in the
    ///   future. Defaults to current timestamp if not specified.
    static func postBillingMeterEvents(
        config: ClientConfig,
        eventName: String,
        payload: [String: String],
        expand: [String]?,
        identifier: String?,
        timestamp: Int?
    ) async throws -> BillingMeterEvent {
        try validateLength("event_name", eventName, max: 100)

        if let identifier {
            try validateLength("identifier", identifier, max: 100)
        }

        let requestBody = PostBillingMeterEventsRequestBody(
            eventName: eventName,
            payload: payload,
            expand: expand,
            identifier: identifier,
            timestamp: timestamp
        )

        return try await (sdkRequest(
            "POST",
            "/v1/billing/meter_events",
            config: config,
            body: requestBody,
            contentType: "application/x-www-form-urlencoded",
            decoder: .json,
            operationId: "PostBillingMeterEvents"
        )).data
    }
}
