// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
public extension V1InvoicesPayMethods {
    struct PostInvoicesInvoicePayOptions: Codable {
        public var invoice: String
        public var expand: [String]?
        public var forgive: Bool?
        public var mandate: PostInvoicesInvoicePayRequestBodyMandateVariant1?
        public var offSession: Bool?
        public var paidOutOfBand: Bool?
        public var paymentMethod: String?
        public var source: String?

        public init(invoice: String) {
            self.invoice = invoice
        }
    }

    /// Attempts to collect payment for a specific invoice outside its normal automatic collection schedule. Supply the
    /// `invoice` identifier and optionally choose the `payment_method`, `source`, or other payment behaviour. The
    /// operation can also record an invoice as paid outside Stripe with `paid_out_of_band`.
    ///
    /// Stripe automatically creates and then attempts to collect payment on invoices for customers on subscriptions
    /// according to your subscriptions settings. However, if you’d like to attempt payment on an invoice out of the
    /// normal collection schedule or for some other reason, you can do so.
    ///
    /// - Parameters:
    /// - expand: Specifies which fields in the response should be expanded.
    /// - forgive: In cases where the source used to pay the invoice has
    ///   insufficient funds, passing `forgive=true` controls whether a charge should
    ///   be attempted for the full amount available on the source, up to the amount
    ///   to fully pay the invoice. This effectively forgives the difference between
    ///   the amount available on the source and the amount due. Passing
    ///   `forgive=false` will fail the charge if the source hasn't been pre-funded
    ///   with the right amount. An example for this case is with ACH Credit Transfers
    ///   and wires: if the amount wired is less than the amount due by a small
    ///   amount, you might want to forgive the difference. Defaults to `false`.
    /// - mandate: ID of the mandate to be used for this invoice. It must correspond
    ///   to the payment method used to pay the invoice, including the payment_method
    ///   param or the invoice's default_payment_method or default_source, if set.
    /// - offSession: Indicates if a customer is on or off-session while an invoice
    ///   payment is attempted. Defaults to `true` (off-session).
    /// - paidOutOfBand: Boolean representing whether an invoice is paid outside of
    ///   Stripe. This will result in no charge being made. Defaults to `false`.
    /// - paymentMethod: A PaymentMethod to be charged. The PaymentMethod must be
    ///   the ID of a PaymentMethod belonging to the customer associated with the
    ///   invoice being paid.
    /// - source: A payment source to be charged. The source must be the ID of a
    ///   source belonging to the customer associated with the invoice being paid.
    static func postInvoicesInvoicePay(
        config: ClientConfig,
        options: PostInvoicesInvoicePayOptions
    ) async throws -> Invoice {
        try validateLength("invoice", options.invoice, max: 5000)

        if let paymentMethod = options.paymentMethod {
            try validateLength("payment_method", paymentMethod, max: 5000)
        }

        if let source = options.source {
            try validateLength("source", source, max: 5000)
        }

        let requestBody = PostInvoicesInvoicePayRequestBody(options: options)

        return try await (sdkRequest(
            "POST",
            ["/v1/invoices/", sdkEncodePathSegment(sdkWireString(options.invoice)), "/pay"].joined(),
            config: config,
            body: requestBody,
            contentType: "application/x-www-form-urlencoded",
            decoder: .json,
            operationId: "PostInvoicesInvoicePay"
        )).data
    }
}
