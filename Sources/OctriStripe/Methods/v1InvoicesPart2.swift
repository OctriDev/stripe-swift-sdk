// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
public extension V1InvoicesMethods {
    struct GetInvoicesOptions: Codable {
        public var collectionMethod: GetInvoicesParameter?
        public var created: GetInvoicesParameterX01e2042c?
        public var customer: String?
        public var customerAccount: String?
        public var dueDate: GetInvoicesParameterXab65bf54?
        public var endingBefore: String?
        public var expand: [String]?
        public var limit: Int?
        public var startingAfter: String?
        public var status: GetInvoicesParameterX224e8ed5?
        public var subscription: String?

        public init() {}
    }

    /// Lists invoices ordered by creation date, with the newest invoices first. Filter by customer, subscription,
    /// collection method, due date, creation date, or invoice `status`, and use `starting_after` or `ending_before` to
    /// paginate the results.
    ///
    /// You can list all invoices, or list the invoices for a specific customer. The invoices are returned sorted by
    /// creation date, with the most recently created invoices appearing first.
    ///
    /// - Parameters:
    /// - collectionMethod: The collection method of the invoice to retrieve. Either
    ///   `charge_automatically` or `send_invoice`.
    /// - created: Only return invoices that were created during the given date
    ///   interval.
    /// - customer: Only return invoices for the customer specified by this customer
    ///   ID.
    /// - customerAccount: Only return invoices for the account representing the
    ///   customer specified by this account ID.
    /// - endingBefore: A cursor for use in pagination. `ending_before` is an object
    ///   ID that defines your place in the list. For instance, if you make a list
    ///   request and receive 100 objects, starting with `obj_bar`, your subsequent
    ///   call can include `ending_before=obj_bar` in order to fetch the previous page
    ///   of the list.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - limit: A limit on the number of objects to be returned. Limit can range
    ///   between 1 and 100, and the default is 10.
    /// - startingAfter: A cursor for use in pagination. `starting_after` is an
    ///   object ID that defines your place in the list. For instance, if you make a
    ///   list request and receive 100 objects, ending with `obj_foo`, your subsequent
    ///   call can include `starting_after=obj_foo` in order to fetch the next page of
    ///   the list.
    /// - status: The status of the invoice, one of `draft`, `open`, `paid`,
    ///   `uncollectible`, or `void`. [Learn
    ///   more](https://docs.stripe.com/billing/invoices/workflow#workflow-overview)
    /// - subscription: Only return invoices for the subscription specified by this
    ///   subscription ID.
    static func getInvoices(config: ClientConfig, options: GetInvoicesOptions) async throws -> GetInvoicesResponse {
        if let customer = options.customer {
            try validateLength("customer", customer, max: 5000)
        }

        if let customerAccount = options.customerAccount {
            try validateLength("customer_account", customerAccount, max: 5000)
        }

        if let endingBefore = options.endingBefore {
            try validateLength("ending_before", endingBefore, max: 5000)
        }

        if let startingAfter = options.startingAfter {
            try validateLength("starting_after", startingAfter, max: 5000)
        }

        if let subscription = options.subscription {
            try validateLength("subscription", subscription, max: 5000)
        }

        return try await (sdkRequest("GET", "/v1/invoices", config: config, query: [
            SdkQueryParameter("collection_method", value: options.collectionMethod),
            SdkQueryParameter("created", value: options.created),
            SdkQueryParameter("customer", value: options.customer),
            SdkQueryParameter("customer_account", value: options.customerAccount),
            SdkQueryParameter("due_date", value: options.dueDate),
            SdkQueryParameter("ending_before", value: options.endingBefore),
            SdkQueryParameter("expand", values: options.expand, style: "deepObject", explode: true),
            SdkQueryParameter("limit", value: options.limit),
            SdkQueryParameter("starting_after", value: options.startingAfter),
            SdkQueryParameter("status", value: options.status),
            SdkQueryParameter("subscription", value: options.subscription),
        ], decoder: .json, operationId: "GetInvoices")).data
    }
}
