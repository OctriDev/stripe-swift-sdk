// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
extension V1ChargesCaptureMethods {
    public struct PostChargesChargeCaptureOptions: Codable {
        public var charge: String
        public var amount: Int?
        public var applicationFee: Int?
        public var applicationFeeAmount: Int?
        public var expand: [String]?
        public var receiptEmail: String?
        public var statementDescriptor: String?
        public var statementDescriptorSuffix: String?
        public var transferData: PostChargesChargeCaptureRequestBodyTransferData?
        public var transferGroup: String?

        public init(charge: String) {
            self.charge = charge
        }
    }

    /// Captures an existing charge that was created with capture disabled. Supply `charge` and optionally provide a partial capture amount and related transfer or receipt details. Do not use this operation for a PaymentIntent-initiated charge; capture the PaymentIntent instead.
    ///
    /// Capture the payment of an existing, uncaptured charge that was created with the capture option set to false. Uncaptured payments expire a set number of days after they are created (7 by default), after which they are marked as refunded and capture attempts will fail. Don’t use this method to capture a PaymentIntent-initiated charge. Use Capture a PaymentIntent.
    ///
    /// - Parameters:
    /// - amount: The amount to capture, which must be less than or equal to the
    ///   original amount.
    /// - applicationFee: An application fee to add on to this charge.
    /// - applicationFeeAmount: An application fee amount to add on to this charge,
    ///   which must be less than or equal to the original amount.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - receiptEmail: The email address to send this charge's receipt to. This
    ///   will override the previously-specified email address for this charge, if one
    ///   was set. Receipts will not be sent in test mode.
    /// - statementDescriptor: For a non-card charge, text that appears on the
    ///   customer's statement as the statement descriptor. This value overrides the
    ///   account's default statement descriptor. For information about requirements,
    ///   including the 22-character limit, see [the Statement Descriptor
    ///   docs](https://docs.stripe.com/get-started/account/statement-descriptors).
    ///   For a card charge, this value is ignored unless you don't specify a
    ///   `statement_descriptor_suffix`, in which case this value is used as the
    ///   suffix.
    /// - statementDescriptorSuffix: Provides information about a card charge.
    ///   Concatenated to the account's [statement descriptor
    ///   prefix](https://docs.stripe.com/get-started/account/statement-descriptors#st
    ///   atic) to form the complete statement descriptor that appears on the
    ///   customer's statement. If the account has no prefix value, the suffix is
    ///   concatenated to the account's statement descriptor.
    /// - transferData: An optional dictionary including the account to
    ///   automatically transfer to as part of a destination charge. [See the Connect
    ///   documentation](https://docs.stripe.com/connect/destination-charges) for
    ///   details.
    /// - transferGroup: A string that identifies this transaction as part of a
    ///   group. `transfer_group` may only be provided if it has not been set. See the
    ///   [Connect
    ///   documentation](https://docs.stripe.com/connect/separate-charges-and-transfer
    ///   s#transfer-options) for details.
    public static func postChargesChargeCapture(config: ClientConfig, options: PostChargesChargeCaptureOptions) async throws -> Charge {
        try validateLength("charge", options.charge, max: 5000)

        if let statementDescriptor = options.statementDescriptor {
            try validateLength("statement_descriptor", statementDescriptor, max: 22)
        }

        if let statementDescriptorSuffix = options.statementDescriptorSuffix {
            try validateLength("statement_descriptor_suffix", statementDescriptorSuffix, max: 22)
        }

        let requestBody = PostChargesChargeCaptureRequestBody(options: options)

        return try (await sdkRequest("POST", ["/v1/charges/", sdkEncodePathSegment(sdkWireString(options.charge)), "/capture"].joined(), config: config, body: requestBody, contentType: "application/x-www-form-urlencoded", decoder: .json, operationId: "PostChargesChargeCapture")).data
    }
}
