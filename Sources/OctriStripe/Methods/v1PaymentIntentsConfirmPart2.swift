// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
extension V1PaymentIntentsConfirmMethods {
    public struct PostPaymentIntentsIntentConfirmOptions: Codable {
        public var intent: String
        public var allowedPaymentMethodTypes: [PostPaymentIntentsIntentConfirmRequestBodyAllowedPaymentMethodTypesItem]?
        public var amountDetails: PostPaymentIntentsIntentConfirmRequestBodyAmountDetails?
        public var amountToConfirm: Int?
        public var captureMethod: PostPaymentIntentsIntentConfirmRequestBodyCaptureMethod?
        public var clientSecret: String?
        public var confirmationToken: String?
        public var errorOnRequiresAction: Bool?
        public var excludedPaymentMethodTypes: PostPaymentIntentsIntentConfirmRequestBodyExcludedPaymentMethodTypes?
        public var expand: [String]?
        public var hooks: PostPaymentIntentsIntentConfirmRequestBodyHooks?
        public var mandate: String?
        public var mandateData: PostPaymentIntentsIntentConfirmRequestBodyMandateData?
        public var offSession: PostPaymentIntentsIntentConfirmRequestBodyOffSession?
        public var paymentDetails: PostPaymentIntentsIntentConfirmRequestBodyPaymentDetails?
        public var paymentMethod: String?
        public var paymentMethodData: PostPaymentIntentsIntentConfirmRequestBodyPaymentMethodData?
        public var paymentMethodOptions: PostPaymentIntentsIntentConfirmRequestBodyPaymentMethodOptions?
        public var paymentMethodTypes: [String]?
        public var radarOptions: PostPaymentIntentsIntentConfirmRequestBodyRadarOptions?
        public var receiptEmail: String?
        public var returnUrl: String?
        public var setupFutureUsage: PostPaymentIntentsIntentConfirmRequestBodySetupFutureUsage?
        public var shipping: PostPaymentIntentsIntentConfirmRequestBodyShipping?
        public var useStripeSdk: Bool?

        public init(intent: String) {
            self.intent = intent
        }
    }

    /// Confirms that the customer intends to pay with the current or a supplied payment method and initiates a payment attempt. The PaymentIntent may require customer action, return to requires-confirmation for another server-side attempt, succeed, require capture, or become canceled after the confirmation limit is reached.
    ///
    /// Confirm that your customer intends to pay with current or provided payment method. Upon confirmation, the PaymentIntent will attempt to initiate a payment. If the selected payment method requires additional authentication steps, the PaymentIntent will transition to the requires_action status and suggest additional actions via next_action . If payment fails, the PaymentIntent transitions to the requires_payment_method status or the canceled status if the confirmation limit is reached. If payment succeeds, the PaymentIntent will transition to the succeeded status (or requires_capture , if capture_method is set to manual ). If the confirmation_method is automatic , payment may be attempted using our client SDKs and the PaymentIntent’s client_secret. After next_action s are handled by the client, no additional confirmation is required to complete the payment. If the confirmation_method is manual , all payment attempts must be initiated using a secret key. If any actions are required for the payment, the PaymentIntent will return to the requires_confirmation state after those actions are completed. Your server needs to then explicitly re-confirm the PaymentIntent to initiate the next payment attempt. There is a variable upper limit on how many times a PaymentIntent can be confirmed. After this limit is reached, any further calls to this endpoint will transition the PaymentIntent to the canceled state.
    ///
    /// - Parameters:
    /// - allowedPaymentMethodTypes: The list of payment method types allowed for
    ///   use with this payment. Stripe automatically returns compatible payment
    ///   methods from this list in the `payment_method_types` field of the response,
    ///   based on the other PaymentIntent parameters, such as `currency`, `amount`,
    ///   and `customer`.
    /// - amountDetails: Provides industry-specific information about the amount.
    /// - amountToConfirm: Amount to confirm on the PaymentIntent. Defaults to
    ///   `amount` if not provided.
    /// - captureMethod: Controls when the funds will be captured from the
    ///   customer's account.
    /// - clientSecret: The client secret of the PaymentIntent.
    /// - confirmationToken: ID of the ConfirmationToken used to confirm this
    ///   PaymentIntent. If the provided ConfirmationToken contains properties that
    ///   are also being provided in this request, such as `payment_method`, then the
    ///   values in this request will take precedence.
    /// - errorOnRequiresAction: Set to `true` to fail the payment attempt if the
    ///   PaymentIntent transitions into `requires_action`. This parameter is intended
    ///   for simpler integrations that do not handle customer actions, like [saving
    ///   cards without
    ///   authentication](https://docs.stripe.com/payments/save-card-without-authentic
    ///   ation).
    /// - excludedPaymentMethodTypes: The list of payment method types to exclude
    ///   from use with this payment.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - hooks: Automations to be run during the PaymentIntent lifecycle
    /// - mandate: ID of the mandate that's used for this payment.
    /// - offSession: Set to `true` to indicate that the customer isn't in your
    ///   checkout flow during this payment attempt and can't authenticate. Use this
    ///   parameter in scenarios where you collect payment method details and [charge
    ///   them later](https://docs.stripe.com/payments/save-during-payment).
    /// - paymentDetails: Provides industry-specific information about the charge.
    /// - paymentMethod: ID of the payment method (a PaymentMethod, Card, or
    ///   [compatible
    ///   Source](https://docs.stripe.com/payments/payment-methods/transitioning#compa
    ///   tibility) object) to attach to this PaymentIntent. If the payment method is
    ///   attached to a Customer, it must match the
    ///   [customer](https://api.stripe.com#create_payment_intent-customer) that is
    ///   set on this PaymentIntent.
    /// - paymentMethodData: If provided, this hash will be used to create a
    ///   PaymentMethod. The new PaymentMethod will appear in the
    ///   [payment_method](https://docs.stripe.com/api/payment_intents/object#payment_
    ///   intent_object-payment_method) property on the PaymentIntent.
    /// - paymentMethodOptions: Payment method-specific configuration for this
    ///   PaymentIntent.
    /// - paymentMethodTypes: The list of payment method types (for example, a card)
    ///   that this PaymentIntent can use. If you don't provide this, Stripe will
    ///   dynamically show relevant payment methods from your [payment method
    ///   settings](https://dashboard.stripe.com/settings/payment_methods). A list of
    ///   valid payment method types can be found
    ///   [here](https://docs.stripe.com/api/payment_methods/object#payment_method_obj
    ///   ect-type).
    /// - radarOptions: Options to configure Radar. Learn more about [Radar
    ///   Sessions](https://docs.stripe.com/radar/radar-session).
    /// - receiptEmail: Email address that the receipt for the resulting payment
    ///   will be sent to. If `receipt_email` is specified for a payment in live mode,
    ///   a receipt will be sent regardless of your [email
    ///   settings](https://dashboard.stripe.com/account/emails).
    /// - returnUrl: The URL to redirect your customer back to after they
    ///   authenticate or cancel their payment on the payment method's app or site. If
    ///   you'd prefer to redirect to a mobile application, you can alternatively
    ///   supply an application URI scheme. This parameter is only used for cards and
    ///   other redirect-based payment methods.
    /// - setupFutureUsage: Indicates that you intend to make future payments with
    ///   this PaymentIntent's payment method. If you provide a Customer with the
    ///   PaymentIntent, you can use this parameter to attach the payment method to
    ///   the Customer after the PaymentIntent is confirmed and the customer completes
    ///   any required actions. If you don't provide a Customer, you can still attach
    ///   the payment method to a Customer after the transaction completes. If the
    ///   payment method is `card_present` and isn't a digital wallet, Stripe creates
    ///   and attaches a generated_card payment method representing the card to the
    ///   Customer instead. When processing card payments, Stripe uses
    ///   `setup_future_usage` to help you comply with regional legislation and
    ///   network rules, such as SCA. If you've already set `setup_future_usage` and
    ///   you're performing a request using a publishable key, you can only update the
    ///   value from `on_session` to `off_session`.
    /// - shipping: Shipping information for this PaymentIntent.
    /// - useStripeSdk: Set to `true` when confirming server-side and using
    ///   Stripe.js, iOS, or Android client-side SDKs to handle the next actions.
    public static func postPaymentIntentsIntentConfirm(config: ClientConfig, options: PostPaymentIntentsIntentConfirmOptions) async throws -> PaymentIntent {
        try validateLength("intent", options.intent, max: 5000)

        if let clientSecret = options.clientSecret {
            try validateLength("client_secret", clientSecret, max: 5000)
        }

        if let confirmationToken = options.confirmationToken {
            try validateLength("confirmation_token", confirmationToken, max: 5000)
        }

        if let mandate = options.mandate {
            try validateLength("mandate", mandate, max: 5000)
        }

        if let paymentMethod = options.paymentMethod {
            try validateLength("payment_method", paymentMethod, max: 5000)
        }

        let requestBody = PostPaymentIntentsIntentConfirmRequestBody(options: options)

        return try (await sdkRequest("POST", ["/v1/payment_intents/", sdkEncodePathSegment(sdkWireString(options.intent)), "/confirm"].joined(), config: config, body: requestBody, contentType: "application/x-www-form-urlencoded", decoder: .json, operationId: "PostPaymentIntentsIntentConfirm")).data
    }
}
