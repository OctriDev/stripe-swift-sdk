// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
extension V1PayoutsMethods {
    public struct PostPayoutsOptions: Codable {
        public var amount: Int
        public var currency: String
        public var description: String?
        public var destination: String?
        public var expand: [String]?
        public var metadata: [String: String]?
        public var method: PostPayoutsRequestBodyMethod?
        public var payoutMethod: String?
        public var sourceType: PostPayoutsRequestBodySourceType?
        public var statementDescriptor: String?

        public init(amount: Int, currency: String) {
            self.amount = amount
            self.currency = currency
        }
    }

    /// To send funds to your own bank account, create a new payout object. Your Stripe balance must cover the payout amount. If it doesn’t, you receive an “Insufficient Funds” error. If your API key is in test mode, money won’t actually be sent, though every other action occurs as if you’re in live mode. If you create a manual payout on a Stripe account that uses multiple payment source types, you need to specify the source type balance that the payout draws from. The balance object details available and pending amounts by source type.
    ///
    /// - Parameters:
    /// - amount: A positive integer in cents representing how much to payout.
    /// - currency: Three-letter [ISO currency
    ///   code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must
    ///   be a [supported currency](https://stripe.com/docs/currencies).
    /// - description: An arbitrary string attached to the object. Often useful for
    ///   displaying to users.
    /// - destination: The ID of a bank account or a card to send the payout to. If
    ///   you don't provide a destination, we use the default external account for the
    ///   specified currency.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - metadata: Set of [key-value pairs](https://docs.stripe.com/api/metadata)
    ///   that you can attach to an object. This can be useful for storing additional
    ///   information about the object in a structured format. Individual keys can be
    ///   unset by posting an empty value to them. All keys can be unset by posting an
    ///   empty value to `metadata`.
    /// - method: The method used to send this payout, which is `standard` or
    ///   `instant`. We support `instant` for payouts to debit cards and bank accounts
    ///   in certain countries. Learn more about [bank support for Instant
    ///   Payouts](https://stripe.com/docs/payouts/instant-payouts-banks).
    /// - payoutMethod: The ID of a v2 FinancialAccount to send funds to.
    /// - sourceType: The balance type of your Stripe balance to draw this payout
    ///   from. Balances for different payment sources are kept separately. You can
    ///   find the amounts with the Balances API. One of `bank_account`, `card`, or
    ///   `fpx`.
    /// - statementDescriptor: A string that displays on the recipient's bank or
    ///   card statement (up to 22 characters). A `statement_descriptor` that's longer
    ///   than 22 characters return an error. Most banks truncate this information and
    ///   display it inconsistently. Some banks might not display it at all. For US
    ///   ACH payouts, this maps to the ACH Company Entry Description field, which the
    ///   NACHA standard limits to 10 characters. Stripe truncates descriptors longer
    ///   than 10 characters for US ACH payouts.
    public static func postPayouts(config: ClientConfig, options: PostPayoutsOptions) async throws -> Payout {
        if let description = options.description {
            try validateLength("description", description, max: 5000)
        }

        if let method = options.method {
            try validateLength("method", method.rawValue, max: 5000)
        }

        if let sourceType = options.sourceType {
            try validateLength("source_type", sourceType.rawValue, max: 5000)
        }

        if let statementDescriptor = options.statementDescriptor {
            try validateLength("statement_descriptor", statementDescriptor, max: 22)
        }

        let requestBody = PostPayoutsRequestBody(options: options)

        return try (await sdkRequest("POST", "/v1/payouts", config: config, body: requestBody, contentType: "application/x-www-form-urlencoded", decoder: .json, operationId: "PostPayouts")).data
    }
}
