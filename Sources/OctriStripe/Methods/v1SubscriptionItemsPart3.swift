// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
public extension V1SubscriptionItemsMethods {
    struct PostSubscriptionItemsOptions: Codable {
        public var subscription: String
        public var billingThresholds: PostSubscriptionItemsRequestBodyBillingThresholds?
        public var discounts: PostSubscriptionItemsRequestBodyDiscounts?
        public var expand: [String]?
        public var metadata: [String: String]?
        public var paymentBehavior: PostSubscriptionItemsRequestBodyPaymentBehavior?
        public var price: String?
        public var priceData: PostSubscriptionItemsRequestBodyPriceData?
        public var prorationBehavior: PostSubscriptionItemsRequestBodyProrationBehavior?
        public var prorationDate: Int?
        public var quantity: Int?
        public var taxRates: PostSubscriptionItemsRequestBodyTaxRates?

        public init(subscription: String) {
            self.subscription = subscription
        }
    }

    /// Adds a new item to an existing subscription. No existing items will be changed or replaced.
    ///
    /// - Parameters:
    /// - subscription: The identifier of the subscription to modify.
    /// - billingThresholds: Define thresholds at which an invoice will be sent, and
    ///   the subscription advanced to a new billing period. Pass an empty string to
    ///   remove previously-defined thresholds.
    /// - discounts: The coupons to redeem into discounts for the subscription item.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - metadata: Set of [key-value pairs](https://docs.stripe.com/api/metadata)
    ///   that you can attach to an object. This can be useful for storing additional
    ///   information about the object in a structured format. Individual keys can be
    ///   unset by posting an empty value to them. All keys can be unset by posting an
    ///   empty value to `metadata`.
    /// - paymentBehavior: Controls how Stripe handles payment when a subscription
    ///   update requires payment and `collection_method=charge_automatically`.
    /// - price: The ID of the price object.
    /// - priceData: Data used to generate a new
    ///   [Price](https://docs.stripe.com/api/prices) object inline.
    /// - prorationBehavior: Determines how to handle
    ///   [prorations](https://docs.stripe.com/billing/subscriptions/prorations) when
    ///   the billing cycle changes (e.g., when switching plans, resetting
    ///   `billing_cycle_anchor=now`, or starting a trial), or if an item's `quantity`
    ///   changes. The default value is `create_prorations`.
    /// - prorationDate: If set, the proration will be calculated as though the
    ///   subscription was updated at the given time. This can be used to apply the
    ///   same proration that was previewed with the upcoming invoice endpoint.
    /// - quantity: The quantity you'd like to apply to the subscription item you're
    ///   creating.
    /// - taxRates: A list of [Tax Rate](https://docs.stripe.com/api/tax_rates) ids.
    ///   These Tax Rates will override the
    ///   [`default_tax_rates`](https://docs.stripe.com/api/subscriptions/create#creat
    ///   e_subscription-default_tax_rates) on the Subscription. When updating, pass
    ///   an empty string to remove previously-defined tax rates.
    static func postSubscriptionItems(
        config: ClientConfig,
        options: PostSubscriptionItemsOptions
    ) async throws -> SubscriptionItem {
        try validateLength("subscription", options.subscription, max: 5000)

        if let price = options.price {
            try validateLength("price", price, max: 5000)
        }

        let requestBody = PostSubscriptionItemsRequestBody(options: options)

        return try await (sdkRequest(
            "POST",
            "/v1/subscription_items",
            config: config,
            body: requestBody,
            contentType: "application/x-www-form-urlencoded",
            decoder: .json,
            operationId: "PostSubscriptionItems"
        )).data
    }
}
