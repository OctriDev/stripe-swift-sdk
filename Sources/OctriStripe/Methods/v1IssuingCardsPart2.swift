// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
extension V1IssuingCardsMethods {
    public struct GetIssuingCardsOptions: Codable {
        public var cardholder: String?
        public var created: GetIssuingCardsParameter?
        public var endingBefore: String?
        public var expMonth: Int?
        public var expYear: Int?
        public var expand: [String]?
        public var last4: String?
        public var limit: Int?
        public var personalizationDesign: String?
        public var startingAfter: String?
        public var status: GetIssuingCardsParameterXae590db5?
        public var type: GetIssuingCardsParameterX23c8c667?

        public init() {}
    }

    /// Lists Issuing cards in descending creation order, with the newest cards first. Filter results by cardholder, creation time, expiration, last four digits, personalization design, status, or type, and use cursor parameters to paginate the collection.
    ///
    /// Returns a list of Issuing Card objects. The objects are sorted in descending order by creation date, with the most recently created object appearing first.
    ///
    /// - Parameters:
    /// - cardholder: Only return cards belonging to the Cardholder with the
    ///   provided ID.
    /// - created: Only return cards that were issued during the given date
    ///   interval.
    /// - endingBefore: A cursor for use in pagination. `ending_before` is an object
    ///   ID that defines your place in the list. For instance, if you make a list
    ///   request and receive 100 objects, starting with `obj_bar`, your subsequent
    ///   call can include `ending_before=obj_bar` in order to fetch the previous page
    ///   of the list.
    /// - expMonth: Only return cards that have the given expiration month.
    /// - expYear: Only return cards that have the given expiration year.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - last4: Only return cards that have the given last four digits.
    /// - limit: A limit on the number of objects to be returned. Limit can range
    ///   between 1 and 100, and the default is 10.
    /// - startingAfter: A cursor for use in pagination. `starting_after` is an
    ///   object ID that defines your place in the list. For instance, if you make a
    ///   list request and receive 100 objects, ending with `obj_foo`, your subsequent
    ///   call can include `starting_after=obj_foo` in order to fetch the next page of
    ///   the list.
    /// - status: Only return cards that have the given status. One of `active`,
    ///   `inactive`, or `canceled`.
    /// - type: Only return cards that have the given type. One of `virtual` or
    ///   `physical`.
    public static func getIssuingCards(config: ClientConfig, options: GetIssuingCardsOptions) async throws -> GetIssuingCardsResponse {
        if let cardholder = options.cardholder {
            try validateLength("cardholder", cardholder, max: 5000)
        }

        if let endingBefore = options.endingBefore {
            try validateLength("ending_before", endingBefore, max: 5000)
        }

        if let last4 = options.last4 {
            try validateLength("last4", last4, max: 5000)
        }

        if let personalizationDesign = options.personalizationDesign {
            try validateLength("personalization_design", personalizationDesign, max: 5000)
        }

        if let startingAfter = options.startingAfter {
            try validateLength("starting_after", startingAfter, max: 5000)
        }

        return try (await sdkRequest("GET", "/v1/issuing/cards", config: config, query: [
            SdkQueryParameter("cardholder", value: options.cardholder),
            SdkQueryParameter("created", value: options.created),
            SdkQueryParameter("ending_before", value: options.endingBefore),
            SdkQueryParameter("exp_month", value: options.expMonth),
            SdkQueryParameter("exp_year", value: options.expYear),
            SdkQueryParameter("expand", values: options.expand, style: "deepObject", explode: true),
            SdkQueryParameter("last4", value: options.last4),
            SdkQueryParameter("limit", value: options.limit),
            SdkQueryParameter("personalization_design", value: options.personalizationDesign),
            SdkQueryParameter("starting_after", value: options.startingAfter),
            SdkQueryParameter("status", value: options.status),
            SdkQueryParameter("type", value: options.type),
        ], decoder: .json, operationId: "GetIssuingCards")).data
    }
}
