// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
public extension V1PlansMethods {
    struct PostPlansOptions: Codable {
        public var currency: String
        public var interval: PostPlansRequestBodyInterval
        public var active: Bool?
        public var amount: Int?
        public var amountDecimal: String?
        public var billingScheme: PostPlansRequestBodyBillingScheme?
        public var expand: [String]?
        public var id: String?
        public var intervalCount: Int?
        public var metadata: PostPlansRequestBodyMetadata?
        public var meter: String?
        public var nickname: String?
        public var product: PostPlansRequestBodyProduct?
        public var tiers: [PostPlansRequestBodyTiersItem]?
        public var tiersMode: PostPlansRequestBodyTiersMode?
        public var transformUsage: PostPlansRequestBodyTransformUsage?
        public var trialPeriodDays: Int?
        public var usageType: PostPlansRequestBodyUsageType?

        public init(currency: String, interval: PostPlansRequestBodyInterval) {
            self.currency = currency
            self.interval = interval
        }
    }

    /// Creates a plan that defines recurring pricing for a product. Supply `currency` and `interval` at minimum, and
    /// use `billing_scheme`, tiers, or product details to define how recurring charges are calculated.
    ///
    /// You can now model subscriptions more flexibly using the Prices API. It replaces the Plans API and is backwards
    /// compatible to simplify your migration.
    ///
    /// - Parameters:
    /// - currency: Three-letter [ISO currency
    ///   code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must
    ///   be a [supported currency](https://stripe.com/docs/currencies).
    /// - interval: Specifies billing frequency. Either `day`, `week`, `month` or
    ///   `year`.
    /// - active: Whether the plan is currently available for new subscriptions.
    ///   Defaults to `true`.
    /// - amount: A positive integer in cents (or local equivalent) (or 0 for a free
    ///   plan) representing how much to charge on a recurring basis.
    /// - amountDecimal: Same as `amount`, but accepts a decimal value with at most
    ///   12 decimal places. Only one of `amount` and `amount_decimal` can be set.
    /// - billingScheme: Describes how to compute the price per period. Either
    ///   `per_unit` or `tiered`. `per_unit` indicates that the fixed amount
    ///   (specified in `amount`) will be charged per unit in `quantity` (for plans
    ///   with `usage_type=licensed`), or per unit of total usage (for plans with
    ///   `usage_type=metered`). `tiered` indicates that the unit pricing will be
    ///   computed using a tiering strategy as defined using the `tiers` and
    ///   `tiers_mode` attributes.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - id: An identifier randomly generated by Stripe. Used to identify this plan
    ///   when subscribing a customer. You can optionally override this ID, but the ID
    ///   must be unique across all plans in your Stripe account. You can, however,
    ///   use the same plan ID in both live and test modes.
    /// - intervalCount: The number of intervals between subscription billings. For
    ///   example, `interval=month` and `interval_count=3` bills every 3 months.
    ///   Maximum of three years interval allowed (3 years, 36 months, or 156 weeks).
    /// - metadata: Set of [key-value pairs](https://docs.stripe.com/api/metadata)
    ///   that you can attach to an object. This can be useful for storing additional
    ///   information about the object in a structured format. Individual keys can be
    ///   unset by posting an empty value to them. All keys can be unset by posting an
    ///   empty value to `metadata`.
    /// - meter: The meter tracking the usage of a metered price
    /// - nickname: A brief description of the plan, hidden from customers.
    /// - tiers: Each element represents a pricing tier. This parameter requires
    ///   `billing_scheme` to be set to `tiered`. See also the documentation for
    ///   `billing_scheme`.
    /// - tiersMode: Defines if the tiering price should be `graduated` or `volume`
    ///   based. In `volume`-based tiering, the maximum quantity within a period
    ///   determines the per unit price, in `graduated` tiering pricing can
    ///   successively change as the quantity grows.
    /// - transformUsage: Apply a transformation to the reported usage or set
    ///   quantity before computing the billed price. Cannot be combined with `tiers`.
    /// - trialPeriodDays: Default number of trial days when subscribing a customer
    ///   to this plan using
    ///   [`trial_from_plan=true`](https://docs.stripe.com/api#create_subscription-tri
    ///   al_from_plan).
    /// - usageType: Configures how the quantity per period should be determined.
    ///   Can be either `metered` or `licensed`. `licensed` automatically bills the
    ///   `quantity` set when adding it to a subscription. `metered` aggregates the
    ///   total usage based on usage records. Defaults to `licensed`.
    static func postPlans(config: ClientConfig, options: PostPlansOptions) async throws -> Plan {
        if let id = options.id {
            try validateLength("id", id, max: 5000)
        }

        if let meter = options.meter {
            try validateLength("meter", meter, max: 5000)
        }

        if let nickname = options.nickname {
            try validateLength("nickname", nickname, max: 5000)
        }

        let requestBody = PostPlansRequestBody(options: options)

        return try await (sdkRequest(
            "POST",
            "/v1/plans",
            config: config,
            body: requestBody,
            contentType: "application/x-www-form-urlencoded",
            decoder: .json,
            operationId: "PostPlans"
        )).data
    }
}
