// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
extension V1PaymentIntentsMethods {
    public struct PostPaymentIntentsIntentOptions: Codable {
        public var intent: String
        public var allowedPaymentMethodTypes: [PostPaymentIntentsIntentRequestBodyAllowedPaymentMethodTypesItem]?
        public var amount: Int?
        public var amountDetails: PostPaymentIntentsIntentRequestBodyAmountDetails?
        public var applicationFeeAmount: PostPaymentIntentsIntentRequestBodyApplicationFeeAmount?
        public var captureMethod: PostPaymentIntentsIntentRequestBodyCaptureMethod?
        public var currency: String?
        public var customer: String?
        public var customerAccount: String?
        public var description: String?
        public var excludedPaymentMethodTypes: PostPaymentIntentsIntentRequestBodyExcludedPaymentMethodTypes?
        public var expand: [String]?
        public var hooks: PostPaymentIntentsIntentRequestBodyHooks?
        public var metadata: PostPaymentIntentsIntentRequestBodyMetadata?
        public var paymentDetails: PostPaymentIntentsIntentRequestBodyPaymentDetails?
        public var paymentMethod: String?
        public var paymentMethodConfiguration: String?
        public var paymentMethodData: PostPaymentIntentsIntentRequestBodyPaymentMethodData?
        public var paymentMethodOptions: PostPaymentIntentsIntentRequestBodyPaymentMethodOptions?
        public var paymentMethodTypes: [String]?
        public var receiptEmail: String?
        public var setupFutureUsage: PostPaymentIntentsIntentRequestBodySetupFutureUsage?
        public var shipping: PostPaymentIntentsIntentRequestBodyShipping?
        public var statementDescriptor: String?
        public var statementDescriptorSuffix: String?
        public var transferData: PostPaymentIntentsIntentRequestBodyTransferData?
        public var transferGroup: String?

        public init(intent: String) {
            self.intent = intent
        }
    }

    /// Updates properties on an existing PaymentIntent without confirming it. Supply only the properties you want to change; changing `payment_method` requires you to confirm the PaymentIntent again, while updating and confirming together requires the confirm operation.
    ///
    /// Updates properties on a PaymentIntent object without confirming. Depending on which properties you update, you might need to confirm the PaymentIntent again. For example, updating the payment_method always requires you to confirm the PaymentIntent again. If you prefer to update and confirm at the same time, we recommend updating properties through the confirm API instead.
    ///
    /// - Parameters:
    /// - allowedPaymentMethodTypes: The list of payment method types allowed for
    ///   use with this payment. Stripe automatically returns compatible payment
    ///   methods from this list in the `payment_method_types` field of the response,
    ///   based on the other PaymentIntent parameters, such as `currency`, `amount`,
    ///   and `customer`.
    /// - amount: Amount intended to be collected by this PaymentIntent. A positive
    ///   integer representing how much to charge in the [smallest currency
    ///   unit](https://docs.stripe.com/currencies#zero-decimal) (e.g., 100 cents to
    ///   charge $1.00 or 100 to charge ¥100, a zero-decimal currency). The minimum
    ///   amount is $0.50 US or [equivalent in charge
    ///   currency](https://docs.stripe.com/currencies#minimum-and-maximum-charge-amou
    ///   nts). The amount value supports up to eight digits (e.g., a value of
    ///   99999999 for a USD charge of $999,999.99).
    /// - amountDetails: Provides industry-specific information about the amount.
    /// - applicationFeeAmount: The amount of the application fee (if any) that will
    ///   be requested to be applied to the payment and transferred to the application
    ///   owner's Stripe account. The amount of the application fee collected will be
    ///   capped at the total amount captured. For more information, see the
    ///   PaymentIntents [use case for connected
    ///   accounts](https://docs.stripe.com/payments/connected-accounts).
    /// - captureMethod: Controls when the funds will be captured from the
    ///   customer's account.
    /// - currency: Three-letter [ISO currency
    ///   code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must
    ///   be a [supported currency](https://stripe.com/docs/currencies).
    /// - customer: ID of the Customer this PaymentIntent belongs to, if one exists.
    ///   Payment methods attached to other Customers cannot be used with this
    ///   PaymentIntent. If
    ///   [setup_future_usage](https://api.stripe.com#payment_intent_object-setup_futu
    ///   re_usage) is set and this PaymentIntent's payment method is not
    ///   `card_present`, then the payment method attaches to the Customer after the
    ///   PaymentIntent has been confirmed and any required actions from the user are
    ///   complete. If the payment method is `card_present` and isn't a digital
    ///   wallet, then a
    ///   [generated_card](https://docs.stripe.com/api/charges/object#charge_object-pa
    ///   yment_method_details-card_present-generated_card) payment method
    ///   representing the card is created and attached to the Customer instead.
    /// - customerAccount: ID of the Account representing the customer that this
    ///   PaymentIntent belongs to, if one exists. Payment methods attached to other
    ///   Accounts cannot be used with this PaymentIntent. If
    ///   [setup_future_usage](https://api.stripe.com#payment_intent_object-setup_futu
    ///   re_usage) is set and this PaymentIntent's payment method is not
    ///   `card_present`, then the payment method attaches to the Account after the
    ///   PaymentIntent has been confirmed and any required actions from the user are
    ///   complete. If the payment method is `card_present` and isn't a digital
    ///   wallet, then a
    ///   [generated_card](https://docs.stripe.com/api/charges/object#charge_object-pa
    ///   yment_method_details-card_present-generated_card) payment method
    ///   representing the card is created and attached to the Account instead.
    /// - description: An arbitrary string attached to the object. Often useful for
    ///   displaying to users.
    /// - excludedPaymentMethodTypes: The list of payment method types to exclude
    ///   from use with this payment.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - hooks: Automations to be run during the PaymentIntent lifecycle
    /// - metadata: Set of [key-value pairs](https://docs.stripe.com/api/metadata)
    ///   that you can attach to an object. This can be useful for storing additional
    ///   information about the object in a structured format. Individual keys can be
    ///   unset by posting an empty value to them. All keys can be unset by posting an
    ///   empty value to `metadata`.
    /// - paymentDetails: Provides industry-specific information about the charge.
    /// - paymentMethod: ID of the payment method (a PaymentMethod, Card, or
    ///   [compatible
    ///   Source](https://docs.stripe.com/payments/payment-methods/transitioning#compa
    ///   tibility) object) to attach to this PaymentIntent. To unset this field to
    ///   null, pass in an empty string.
    /// - paymentMethodConfiguration: The ID of the [payment method
    ///   configuration](https://docs.stripe.com/api/payment_method_configurations) to
    ///   use with this PaymentIntent.
    /// - paymentMethodData: If provided, this hash will be used to create a
    ///   PaymentMethod. The new PaymentMethod will appear in the
    ///   [payment_method](https://docs.stripe.com/api/payment_intents/object#payment_
    ///   intent_object-payment_method) property on the PaymentIntent.
    /// - paymentMethodOptions: Payment-method-specific configuration for this
    ///   PaymentIntent.
    /// - paymentMethodTypes: The list of payment method types (for example, card)
    ///   that this PaymentIntent can use. Use `automatic_payment_methods` to manage
    ///   payment methods from the [Stripe
    ///   Dashboard](https://dashboard.stripe.com/settings/payment_methods). A list of
    ///   valid payment method types can be found
    ///   [here](https://docs.stripe.com/api/payment_methods/object#payment_method_obj
    ///   ect-type).
    /// - receiptEmail: Email address that the receipt for the resulting payment
    ///   will be sent to. If `receipt_email` is specified for a payment in live mode,
    ///   a receipt will be sent regardless of your [email
    ///   settings](https://dashboard.stripe.com/account/emails).
    /// - setupFutureUsage: Indicates that you intend to make future payments with
    ///   this PaymentIntent's payment method. If you provide a Customer with the
    ///   PaymentIntent, you can use this parameter to attach the payment method to
    ///   the Customer after the PaymentIntent is confirmed and the customer completes
    ///   any required actions. If you don't provide a Customer, you can still attach
    ///   the payment method to a Customer after the transaction completes. If the
    ///   payment method is `card_present` and isn't a digital wallet, Stripe creates
    ///   and attaches a generated_card payment method representing the card to the
    ///   Customer instead. When processing card payments, Stripe uses
    ///   `setup_future_usage` to help you comply with regional legislation and
    ///   network rules, such as SCA. If you've already set `setup_future_usage` and
    ///   you're performing a request using a publishable key, you can only update the
    ///   value from `on_session` to `off_session`.
    /// - shipping: Shipping information for this PaymentIntent.
    /// - statementDescriptor: Text that appears on the customer's statement as the
    ///   statement descriptor for a non-card charge. This value overrides the
    ///   account's default statement descriptor. For information about requirements,
    ///   including the 22-character limit, see [the Statement Descriptor
    ///   docs](https://docs.stripe.com/get-started/account/statement-descriptors).
    ///   Setting this value for a card charge returns an error. For card charges, set
    ///   the
    ///   [statement_descriptor_suffix](https://docs.stripe.com/get-started/account/st
    ///   atement-descriptors#dynamic) instead.
    /// - statementDescriptorSuffix: Provides information about a card charge.
    ///   Concatenated to the account's [statement descriptor
    ///   prefix](https://docs.stripe.com/get-started/account/statement-descriptors#st
    ///   atic) to form the complete statement descriptor that appears on the
    ///   customer's statement.
    /// - transferData: Use this parameter to automatically create a Transfer when
    ///   the payment succeeds. Learn more about the [use case for connected
    ///   accounts](https://docs.stripe.com/payments/connected-accounts).
    /// - transferGroup: A string that identifies the resulting payment as part of a
    ///   group. You can only provide `transfer_group` if it hasn't been set. Learn
    ///   more about the [use case for connected
    ///   accounts](https://docs.stripe.com/payments/connected-accounts).
    public static func postPaymentIntentsIntent(config: ClientConfig, options: PostPaymentIntentsIntentOptions) async throws -> PaymentIntent {
        try validateLength("intent", options.intent, max: 5000)

        if let customer = options.customer {
            try validateLength("customer", customer, max: 5000)
        }

        if let customerAccount = options.customerAccount {
            try validateLength("customer_account", customerAccount, max: 5000)
        }

        if let description = options.description {
            try validateLength("description", description, max: 1000)
        }

        if let paymentMethod = options.paymentMethod {
            try validateLength("payment_method", paymentMethod, max: 5000)
        }

        if let paymentMethodConfiguration = options.paymentMethodConfiguration {
            try validateLength("payment_method_configuration", paymentMethodConfiguration, max: 100)
        }

        if let statementDescriptor = options.statementDescriptor {
            try validateLength("statement_descriptor", statementDescriptor, max: 22)
        }

        if let statementDescriptorSuffix = options.statementDescriptorSuffix {
            try validateLength("statement_descriptor_suffix", statementDescriptorSuffix, max: 22)
        }

        let requestBody = PostPaymentIntentsIntentRequestBody(options: options)

        return try (await sdkRequest("POST", ["/v1/payment_intents/", sdkEncodePathSegment(sdkWireString(options.intent))].joined(), config: config, body: requestBody, contentType: "application/x-www-form-urlencoded", decoder: .json, operationId: "PostPaymentIntentsIntent")).data
    }
}
