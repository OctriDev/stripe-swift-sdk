// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
extension V1PromotionCodesMethods {
    public struct PostPromotionCodesOptions: Codable {
        public var promotion: PostPromotionCodesRequestBodyPromotion
        public var active: Bool?
        public var code: String?
        public var customer: String?
        public var customerAccount: String?
        public var expand: [String]?
        public var expiresAt: Int?
        public var maxRedemptions: Int?
        public var metadata: [String: String]?
        public var restrictions: PostPromotionCodesRequestBodyRestrictions?

        public init(promotion: PostPromotionCodesRequestBodyPromotion) {
            self.promotion = promotion
        }
    }

    /// A promotion code points to an underlying promotion. You can optionally restrict the code to a specific customer, redemption limit, and expiration date.
    ///
    /// - Parameters:
    /// - promotion: The promotion referenced by this promotion code.
    /// - active: Whether the promotion code is currently active.
    /// - code: The customer-facing code. Regardless of case, this code must be
    ///   unique across all active promotion codes for a specific customer. Valid
    ///   characters are lower case letters (a-z), upper case letters (A-Z), digits
    ///   (0-9), and dashes (-). If left blank, we will generate one automatically.
    /// - customer: The customer who can use this promotion code. If not set, all
    ///   customers can use the promotion code.
    /// - customerAccount: The account representing the customer who can use this
    ///   promotion code. If not set, all customers can use the promotion code.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - expiresAt: The timestamp at which this promotion code will expire. If the
    ///   coupon has specified a `redeems_by`, then this value cannot be after the
    ///   coupon's `redeems_by`.
    /// - maxRedemptions: A positive integer specifying the number of times the
    ///   promotion code can be redeemed. If the coupon has specified a
    ///   `max_redemptions`, then this value cannot be greater than the coupon's
    ///   `max_redemptions`.
    /// - metadata: Set of [key-value pairs](https://docs.stripe.com/api/metadata)
    ///   that you can attach to an object. This can be useful for storing additional
    ///   information about the object in a structured format. Individual keys can be
    ///   unset by posting an empty value to them. All keys can be unset by posting an
    ///   empty value to `metadata`.
    /// - restrictions: Settings that restrict the redemption of the promotion code.
    public static func postPromotionCodes(config: ClientConfig, options: PostPromotionCodesOptions) async throws -> PromotionCode {
        if let code = options.code {
            try validateLength("code", code, max: 500)
        }

        if let customer = options.customer {
            try validateLength("customer", customer, max: 5000)
        }

        if let customerAccount = options.customerAccount {
            try validateLength("customer_account", customerAccount, max: 5000)
        }

        let requestBody = PostPromotionCodesRequestBody(options: options)

        return try (await sdkRequest("POST", "/v1/promotion_codes", config: config, body: requestBody, contentType: "application/x-www-form-urlencoded", decoder: .json, operationId: "PostPromotionCodes")).data
    }
}
