// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
extension V1AccountsMethods {
    public struct PostAccountsAccountOptions: Codable {
        public var account: String
        public var accountToken: String?
        public var businessProfile: PostAccountsAccountRequestBodyBusinessProfile?
        public var businessType: PostAccountsAccountRequestBodyBusinessType?
        public var capabilities: PostAccountsAccountRequestBodyCapabilities?
        public var company: PostAccountsAccountRequestBodyCompany?
        public var defaultCurrency: String?
        public var documents: PostAccountsAccountRequestBodyDocuments?
        public var email: String?
        public var expand: [String]?
        public var externalAccount: String?
        public var groups: PostAccountsAccountRequestBodyGroups?
        public var individual: PostAccountsAccountRequestBodyIndividual?
        public var metadata: PostAccountsAccountRequestBodyMetadata?
        public var settings: PostAccountsAccountRequestBodySettings?
        public var tosAcceptance: PostAccountsAccountRequestBodyTosAcceptance?

        public init(account: String) {
            self.account = account
        }
    }

    /// Updates a connected account by changing only the fields included in the request. Use the account-specific fields to update business information, capabilities, payout details, metadata, or terms-of-service acceptance. Some account properties can no longer be updated after Connect onboarding begins.
    ///
    /// Updates a connected account by setting the values of the parameters passed. Any parameters not provided are left unchanged. For accounts where controller.requirement_collection is application , which includes Custom accounts, you can update any information on the account. For accounts where controller.requirement_collection is stripe , which includes Standard and Express accounts, you can update all information until you create an Account Link or Account Session to start Connect onboarding, after which some properties can no longer be updated. To update your own account, use the Dashboard. Refer to our Connect documentation to learn more about updating accounts.
    ///
    /// - Parameters:
    /// - accountToken: An [account
    ///   token](https://api.stripe.com#create_account_token), used to securely
    ///   provide details to the account.
    /// - businessProfile: Business information about the account.
    /// - businessType: The business type. Once you create an Account Link or
    ///   Account Session, this property can only be updated for accounts where
    ///   controller.requirement_collection is `application`, which includes Custom
    ///   accounts.
    /// - capabilities: Each key of the dictionary represents a capability, and each
    ///   capability maps to its settings (for example, whether it has been requested
    ///   or not). Each capability is inactive until you have provided its specific
    ///   requirements and Stripe has verified them. An account might have some of its
    ///   requested capabilities be active and some be inactive. Required when
    ///   account.controller.stripe_dashboard.type is `none`, which includes Custom
    ///   accounts.
    /// - company: Information about the company or business. This field is
    ///   available for any `business_type`. Once you create an Account Link or
    ///   Account Session, this property can only be updated for accounts where
    ///   controller.requirement_collection is `application`, which includes Custom
    ///   accounts.
    /// - defaultCurrency: Three-letter ISO currency code representing the default
    ///   currency for the account. This must be a currency that [Stripe supports in
    ///   the account's country](https://docs.stripe.com/payouts).
    /// - documents: Documents that may be submitted to satisfy various
    ///   informational requests.
    /// - email: The email address of the account holder. This is only to make the
    ///   account easier to identify to you. If controller.requirement_collection is
    ///   `application`, which includes Custom accounts, Stripe doesn't email the
    ///   account without your consent.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - externalAccount: A card or bank account to attach to the account for
    ///   receiving payouts (you won’t be able to use it for top-ups). You can provide
    ///   either a token, like the ones returned by Stripe.js, or a dictionary, as
    ///   documented in the `external_account` parameter for bank account creation. By
    ///   default, providing an external account sets it as the new default external
    ///   account for its currency, and deletes the old default if one exists. To add
    ///   additional external accounts without replacing the existing default for the
    ///   currency, use the bank account or card creation APIs. After you create an
    ///   Account Link or Account Session, this property can only be updated for
    ///   accounts where controller.requirement_collection is `application`, which
    ///   includes Custom accounts.
    /// - groups: A hash of account group type to tokens. These are account groups
    ///   this account should be added to.
    /// - individual: Information about the person represented by the account. This
    ///   field is null unless `business_type` is set to `individual`. Once you create
    ///   an Account Link or Account Session, this property can only be updated for
    ///   accounts where controller.requirement_collection is `application`, which
    ///   includes Custom accounts.
    /// - metadata: Set of [key-value pairs](https://docs.stripe.com/api/metadata)
    ///   that you can attach to an object. This can be useful for storing additional
    ///   information about the object in a structured format. Individual keys can be
    ///   unset by posting an empty value to them. All keys can be unset by posting an
    ///   empty value to `metadata`.
    /// - settings: Options for customizing how the account functions within Stripe.
    /// - tosAcceptance: Details on the account's acceptance of the Stripe Services
    ///   Agreement. This property can only be updated for accounts where
    ///   controller.requirement_collection is `application`, which includes Custom
    ///   accounts. This property defaults to a `full` service agreement when empty.
    public static func postAccountsAccount(config: ClientConfig, options: PostAccountsAccountOptions) async throws -> Account {
        try validateLength("account", options.account, max: 5000)

        if let accountToken = options.accountToken {
            try validateLength("account_token", accountToken, max: 5000)
        }

        if let externalAccount = options.externalAccount {
            try validateLength("external_account", externalAccount, max: 5000)
        }

        let requestBody = PostAccountsAccountRequestBody(options: options)

        return try (await sdkRequest("POST", ["/v1/accounts/", sdkEncodePathSegment(sdkWireString(options.account))].joined(), config: config, body: requestBody, contentType: "application/x-www-form-urlencoded", decoder: .json, operationId: "PostAccountsAccount")).data
    }
}
