// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
public extension V1PaymentIntentsCaptureMethods {
    struct PostPaymentIntentsIntentCaptureOptions: Codable {
        public var intent: String
        public var amountDetails: PostPaymentIntentsIntentCaptureRequestBodyAmountDetails?
        public var amountToCapture: Int?
        public var applicationFeeAmount: Int?
        public var expand: [String]?
        public var finalCapture: Bool?
        public var hooks: PostPaymentIntentsIntentCaptureRequestBodyHooks?
        public var metadata: PostPaymentIntentsIntentCaptureRequestBodyMetadata?
        public var paymentDetails: PostPaymentIntentsIntentCaptureRequestBodyPaymentDetails?
        public var statementDescriptor: String?
        public var statementDescriptorSuffix: String?
        public var transferData: PostPaymentIntentsIntentCaptureRequestBodyTransferData?

        public init(intent: String) {
            self.intent = intent
        }
    }

    /// Captures funds from an uncaptured PaymentIntent whose status is `requires_capture`. Use `amount_to_capture` to
    /// capture less than the available amount; uncaptured PaymentIntents are automatically canceled after a set period,
    /// seven days by default.
    ///
    /// Capture the funds of an existing uncaptured PaymentIntent when its status is requires_capture . Uncaptured
    /// PaymentIntents are cancelled a set number of days (7 by default) after their creation. Learn more about separate
    /// authorization and capture.
    ///
    /// - Parameters:
    /// - amountDetails: Provides industry-specific information about the amount.
    /// - amountToCapture: The amount to capture from the PaymentIntent, which must
    ///   be less than or equal to the original amount. Defaults to the full
    ///   `amount_capturable` if it's not provided.
    /// - applicationFeeAmount: The amount of the application fee (if any) that will
    ///   be requested to be applied to the payment and transferred to the application
    ///   owner's Stripe account. The amount of the application fee collected will be
    ///   capped at the total amount captured. For more information, see the
    ///   PaymentIntents [use case for connected
    ///   accounts](https://docs.stripe.com/payments/connected-accounts).
    /// - expand: Specifies which fields in the response should be expanded.
    /// - finalCapture: Defaults to `true`. When capturing a PaymentIntent, setting
    ///   `final_capture` to `false` notifies Stripe to not release the remaining
    ///   uncaptured funds to make sure that they're captured in future requests. You
    ///   can only use this setting when
    ///   [multicapture](https://docs.stripe.com/payments/multicapture) is available
    ///   for PaymentIntents.
    /// - hooks: Automations to be run during the PaymentIntent lifecycle
    /// - metadata: Set of [key-value pairs](https://docs.stripe.com/api/metadata)
    ///   that you can attach to an object. This can be useful for storing additional
    ///   information about the object in a structured format. Individual keys can be
    ///   unset by posting an empty value to them. All keys can be unset by posting an
    ///   empty value to `metadata`.
    /// - paymentDetails: Provides industry-specific information about the charge.
    /// - statementDescriptor: Text that appears on the customer's statement as the
    ///   statement descriptor for a non-card charge. This value overrides the
    ///   account's default statement descriptor. For information about requirements,
    ///   including the 22-character limit, see [the Statement Descriptor
    ///   docs](https://docs.stripe.com/get-started/account/statement-descriptors).
    ///   Setting this value for a card charge returns an error. For card charges, set
    ///   the
    ///   [statement_descriptor_suffix](https://docs.stripe.com/get-started/account/st
    ///   atement-descriptors#dynamic) instead.
    /// - statementDescriptorSuffix: Provides information about a card charge.
    ///   Concatenated to the account's [statement descriptor
    ///   prefix](https://docs.stripe.com/get-started/account/statement-descriptors#st
    ///   atic) to form the complete statement descriptor that appears on the
    ///   customer's statement.
    /// - transferData: The parameters that you can use to automatically create a
    ///   transfer after the payment is captured. Learn more about the [use case for
    ///   connected accounts](https://docs.stripe.com/payments/connected-accounts).
    static func postPaymentIntentsIntentCapture(
        config: ClientConfig,
        options: PostPaymentIntentsIntentCaptureOptions
    ) async throws -> PaymentIntent {
        try validateLength("intent", options.intent, max: 5000)

        if let statementDescriptor = options.statementDescriptor {
            try validateLength("statement_descriptor", statementDescriptor, max: 22)
        }

        if let statementDescriptorSuffix = options.statementDescriptorSuffix {
            try validateLength("statement_descriptor_suffix", statementDescriptorSuffix, max: 22)
        }

        let requestBody = PostPaymentIntentsIntentCaptureRequestBody(options: options)

        return try await (sdkRequest(
            "POST",
            ["/v1/payment_intents/", sdkEncodePathSegment(sdkWireString(options.intent)), "/capture"].joined(),
            config: config,
            body: requestBody,
            contentType: "application/x-www-form-urlencoded",
            decoder: .json,
            operationId: "PostPaymentIntentsIntentCapture"
        )).data
    }
}
