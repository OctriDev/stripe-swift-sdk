// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
extension V1IdentityVerificationReportsMethods {
    public struct GetIdentityVerificationReportsOptions: Codable {
        public var clientReferenceId: String?
        public var created: GetIdentityVerificationReportsParameter?
        public var endingBefore: String?
        public var expand: [String]?
        public var limit: Int?
        public var startingAfter: String?
        public var type: GetIdentityVerificationReportsParameterX2bed7a6a?
        public var verificationSession: String?

        public init() {}
    }

    /// Lists verification reports generated from identity verification attempts. Use `client_reference_id`, `verification_session`, and `type` to filter results, and use cursor parameters with `limit` to control pagination.
    ///
    /// List all verification reports.
    ///
    /// - Parameters:
    /// - clientReferenceId: A string to reference this user. This can be a customer
    ///   ID, a session ID, or similar, and can be used to reconcile this verification
    ///   with your internal systems.
    /// - created: Only return VerificationReports that were created during the
    ///   given date interval.
    /// - endingBefore: A cursor for use in pagination. `ending_before` is an object
    ///   ID that defines your place in the list. For instance, if you make a list
    ///   request and receive 100 objects, starting with `obj_bar`, your subsequent
    ///   call can include `ending_before=obj_bar` in order to fetch the previous page
    ///   of the list.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - limit: A limit on the number of objects to be returned. Limit can range
    ///   between 1 and 100, and the default is 10.
    /// - startingAfter: A cursor for use in pagination. `starting_after` is an
    ///   object ID that defines your place in the list. For instance, if you make a
    ///   list request and receive 100 objects, ending with `obj_foo`, your subsequent
    ///   call can include `starting_after=obj_foo` in order to fetch the next page of
    ///   the list.
    /// - type: Only return VerificationReports of this type
    /// - verificationSession: Only return VerificationReports created by this
    ///   VerificationSession ID. It is allowed to provide a VerificationIntent ID.
    public static func getIdentityVerificationReports(config: ClientConfig, options: GetIdentityVerificationReportsOptions) async throws -> GetIdentityVerificationReportsResponse {
        if let clientReferenceId = options.clientReferenceId {
            try validateLength("client_reference_id", clientReferenceId, max: 5000)
        }

        if let endingBefore = options.endingBefore {
            try validateLength("ending_before", endingBefore, max: 5000)
        }

        if let startingAfter = options.startingAfter {
            try validateLength("starting_after", startingAfter, max: 5000)
        }

        if let verificationSession = options.verificationSession {
            try validateLength("verification_session", verificationSession, max: 5000)
        }

        return try (await sdkRequest("GET", "/v1/identity/verification_reports", config: config, query: [
            SdkQueryParameter("client_reference_id", value: options.clientReferenceId),
            SdkQueryParameter("created", value: options.created),
            SdkQueryParameter("ending_before", value: options.endingBefore),
            SdkQueryParameter("expand", values: options.expand, style: "deepObject", explode: true),
            SdkQueryParameter("limit", value: options.limit),
            SdkQueryParameter("starting_after", value: options.startingAfter),
            SdkQueryParameter("type", value: options.type),
            SdkQueryParameter("verification_session", value: options.verificationSession),
        ], decoder: .json, operationId: "GetIdentityVerificationReports")).data
    }
}
