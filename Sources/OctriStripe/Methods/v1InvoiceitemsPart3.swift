// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
public extension V1InvoiceitemsMethods {
    struct PostInvoiceitemsOptions: Codable {
        public var amount: Int?
        public var currency: String?
        public var customer: String?
        public var customerAccount: String?
        public var description: String?
        public var discountable: Bool?
        public var discounts: PostInvoiceitemsRequestBodyDiscounts?
        public var expand: [String]?
        public var invoice: String?
        public var metadata: PostInvoiceitemsRequestBodyMetadata?
        public var period: PostInvoiceitemsRequestBodyPeriod?
        public var priceData: PostInvoiceitemsRequestBodyPriceData?
        public var pricing: PostInvoiceitemsRequestBodyPricing?
        public var quantity: Int?
        public var quantityDecimal: String?
        public var subscription: String?
        public var taxBehavior: PostInvoiceitemsRequestBodyTaxBehavior?
        public var taxCode: String?
        public var taxRates: [String]?
        public var unitAmountDecimal: String?

        public init() {}
    }

    /// Creates an item to be added to a draft invoice (up to 250 items per invoice). If no invoice is specified, the
    /// item will be on the next invoice created for the customer specified.
    ///
    /// - Parameters:
    /// - amount: The integer amount in cents (or local equivalent) of the charge to
    ///   be applied to the upcoming invoice. Passing in a negative `amount` will
    ///   reduce the `amount_due` on the invoice.
    /// - currency: Three-letter [ISO currency
    ///   code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must
    ///   be a [supported currency](https://stripe.com/docs/currencies).
    /// - customer: The ID of the customer to bill for this invoice item.
    /// - customerAccount: The ID of the account representing the customer to bill
    ///   for this invoice item.
    /// - description: An arbitrary string which you can attach to the invoice item.
    ///   The description is displayed in the invoice for easy tracking.
    /// - discountable: Controls whether discounts apply to this invoice item.
    ///   Defaults to false for prorations or negative invoice items, and true for all
    ///   other invoice items.
    /// - discounts: The coupons and promotion codes to redeem into discounts for
    ///   the invoice item or invoice line item.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - invoice: The ID of an existing invoice to add this invoice item to. For
    ///   subscription invoices, when left blank, the invoice item will be added to
    ///   the next upcoming scheduled invoice. For standalone invoices, the invoice
    ///   item won't be automatically added unless you pass
    ///   `pending_invoice_item_behavior: 'include'` when creating the invoice. This
    ///   is useful when adding invoice items in response to an invoice.created
    ///   webhook. You can only add invoice items to draft invoices and there is a
    ///   maximum of 250 items per invoice.
    /// - metadata: Set of [key-value pairs](https://docs.stripe.com/api/metadata)
    ///   that you can attach to an object. This can be useful for storing additional
    ///   information about the object in a structured format. Individual keys can be
    ///   unset by posting an empty value to them. All keys can be unset by posting an
    ///   empty value to `metadata`.
    /// - period: The period associated with this invoice item. When set to
    ///   different values, the period will be rendered on the invoice. If you have
    ///   [Stripe Revenue Recognition](https://docs.stripe.com/revenue-recognition)
    ///   enabled, the period will be used to recognize and defer revenue. See the
    ///   [Revenue Recognition
    ///   documentation](https://docs.stripe.com/revenue-recognition/methodology/subsc
    ///   riptions-and-invoicing) for details.
    /// - priceData: Data used to generate a new
    ///   [Price](https://docs.stripe.com/api/prices) object inline.
    /// - pricing: The pricing information for the invoice item.
    /// - quantity: Non-negative integer. The quantity of units for the invoice
    ///   item. Use `quantity_decimal` instead to provide decimal precision. This
    ///   field will be deprecated in favor of `quantity_decimal` in a future version.
    /// - quantityDecimal: Non-negative decimal with at most 12 decimal places. The
    ///   quantity of units for the invoice item.
    /// - subscription: The ID of a subscription to add this invoice item to. When
    ///   left blank, the invoice item is added to the next upcoming scheduled
    ///   invoice. When set, scheduled invoices for subscriptions other than the
    ///   specified subscription will ignore the invoice item. Use this when you want
    ///   to express that an invoice item has been accrued within the context of a
    ///   particular subscription.
    /// - taxBehavior: Only required if a [default tax
    ///   behavior](https://docs.stripe.com/tax/products-prices-tax-categories-tax-beh
    ///   avior#setting-a-default-tax-behavior-(recommended)) was not provided in the
    ///   Stripe Tax settings. Specifies whether the price is considered inclusive of
    ///   taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or
    ///   `unspecified`. Once specified as either `inclusive` or `exclusive`, it
    ///   cannot be changed.
    /// - taxCode: A [tax code](https://docs.stripe.com/tax/tax-categories) ID.
    /// - taxRates: The tax rates which apply to the invoice item. When set, the
    ///   `default_tax_rates` on the invoice do not apply to this invoice item.
    /// - unitAmountDecimal: The decimal unit amount in cents (or local equivalent)
    ///   of the charge to be applied to the upcoming invoice. This
    ///   `unit_amount_decimal` will be multiplied by the quantity to get the full
    ///   amount. Passing in a negative `unit_amount_decimal` will reduce the
    ///   `amount_due` on the invoice. Accepts at most 12 decimal places.
    static func postInvoiceitems(config: ClientConfig, options: PostInvoiceitemsOptions) async throws -> Invoiceitem {
        if let customer = options.customer {
            try validateLength("customer", customer, max: 5000)
        }

        if let customerAccount = options.customerAccount {
            try validateLength("customer_account", customerAccount, max: 5000)
        }

        if let description = options.description {
            try validateLength("description", description, max: 5000)
        }

        if let invoice = options.invoice {
            try validateLength("invoice", invoice, max: 5000)
        }

        if let subscription = options.subscription {
            try validateLength("subscription", subscription, max: 5000)
        }

        let requestBody = PostInvoiceitemsRequestBody(options: options)

        return try await (sdkRequest(
            "POST",
            "/v1/invoiceitems",
            config: config,
            body: requestBody,
            contentType: "application/x-www-form-urlencoded",
            decoder: .json,
            operationId: "PostInvoiceitems"
        )).data
    }
}
