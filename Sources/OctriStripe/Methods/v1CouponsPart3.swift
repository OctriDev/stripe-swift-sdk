// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
extension V1CouponsMethods {
    public struct PostCouponsOptions: Codable {
        public var amountOff: Int?
        public var appliesTo: PostCouponsRequestBodyAppliesTo?
        public var currency: String?
        public var currencyOptions: [String: PostCouponsRequestBodyCurrencyOptionsValue]?
        public var duration: PostCouponsRequestBodyDuration?
        public var durationInMonths: Int?
        public var expand: [String]?
        public var id: String?
        public var maxRedemptions: Int?
        public var metadata: PostCouponsRequestBodyMetadata?
        public var name: String?
        public var percentOff: Double?
        public var redeemBy: Int?

        public init() {}
    }

    /// You can create coupons easily via the coupon management page of the Stripe dashboard. Coupon creation is also accessible via the API if you need to create coupons on the fly. A coupon has either a percent_off or an amount_off and currency . If you set an amount_off , that amount will be subtracted from any invoice’s subtotal. For example, an invoice with a subtotal of <currency>100</currency> will have a final total of <currency>0</currency> if a coupon with an amount_off of <amount>200</amount> is applied to it and an invoice with a subtotal of <currency>300</currency> will have a final total of <currency>100</currency> if a coupon with an amount_off of <amount>200</amount> is applied to it.
    ///
    /// - Parameters:
    /// - amountOff: A positive integer representing the amount to subtract from an
    ///   invoice total (required if `percent_off` is not passed).
    /// - appliesTo: A hash containing directions for what this Coupon will apply
    ///   discounts to.
    /// - currency: Three-letter [ISO code for the
    ///   currency](https://stripe.com/docs/currencies) of the `amount_off` parameter
    ///   (required if `amount_off` is passed).
    /// - currencyOptions: Coupons defined in each available currency option (only
    ///   supported if `amount_off` is passed). Each key must be a three-letter [ISO
    ///   currency code](https://www.iso.org/iso-4217-currency-codes.html) and a
    ///   [supported currency](https://stripe.com/docs/currencies).
    /// - duration: Specifies how long the discount will be in effect if used on a
    ///   subscription. Defaults to `once`.
    /// - durationInMonths: Required only if `duration` is `repeating`, in which
    ///   case it must be a positive integer that specifies the number of months the
    ///   discount will be in effect.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - id: Unique string of your choice that will be used to identify this coupon
    ///   when applying it to a customer. If you don't want to specify a particular
    ///   code, you can leave the ID blank and we'll generate a random code for you.
    /// - maxRedemptions: A positive integer specifying the number of times the
    ///   coupon can be redeemed before it's no longer valid. For example, you might
    ///   have a 50% off coupon that the first 20 readers of your blog can use.
    /// - metadata: Set of [key-value pairs](https://docs.stripe.com/api/metadata)
    ///   that you can attach to an object. This can be useful for storing additional
    ///   information about the object in a structured format. Individual keys can be
    ///   unset by posting an empty value to them. All keys can be unset by posting an
    ///   empty value to `metadata`.
    /// - name: Name of the coupon displayed to customers on, for instance invoices,
    ///   or receipts. By default the `id` is shown if `name` is not set.
    /// - percentOff: A positive float larger than 0, and smaller or equal to 100,
    ///   that represents the discount the coupon will apply (required if `amount_off`
    ///   is not passed).
    /// - redeemBy: Unix timestamp specifying the last time at which the coupon can
    ///   be redeemed (cannot be set to more than 5 years in the future). After the
    ///   redeem_by date, the coupon can no longer be applied to new customers.
    public static func postCoupons(config: ClientConfig, options: PostCouponsOptions) async throws -> Coupon {
        if let id = options.id {
            try validateLength("id", id, max: 5000)
        }

        if let name = options.name {
            try validateLength("name", name, max: 40)
        }

        let requestBody = PostCouponsRequestBody(options: options)

        return try (await sdkRequest("POST", "/v1/coupons", config: config, body: requestBody, contentType: "application/x-www-form-urlencoded", decoder: .json, operationId: "PostCoupons")).data
    }
}
