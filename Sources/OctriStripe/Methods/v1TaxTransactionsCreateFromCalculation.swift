// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
public enum V1TaxTransactionsCreateFromCalculationMethods {
    /// Creates a Tax Transaction from a calculation, if that calculation hasn’t expired. Calculations expire after 90
    /// days.
    ///
    /// - Parameters:
    /// - calculation: Tax Calculation ID to be used as input when creating the
    ///   transaction.
    /// - reference: A custom order or sale identifier, such as 'myOrder_123'. Must
    ///   be unique across all transactions, including reversals.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - metadata: Set of [key-value pairs](https://docs.stripe.com/api/metadata)
    ///   that you can attach to an object. This can be useful for storing additional
    ///   information about the object in a structured format. Individual keys can be
    ///   unset by posting an empty value to them. All keys can be unset by posting an
    ///   empty value to `metadata`.
    /// - postedAt: The Unix timestamp representing when the tax liability is
    ///   assumed or reduced, which determines the liability posting period and
    ///   handling in tax liability reports. The timestamp must fall within the
    ///   `tax_date` and the current time, unless the `tax_date` is scheduled in
    ///   advance. Defaults to the current time.
    public static func postTaxTransactionsCreateFromCalculation(
        config: ClientConfig,
        calculation: String,
        reference: String,
        expand: [String]?,
        metadata: [String: String]?,
        postedAt: Int?
    ) async throws -> TaxTransaction {
        try validateLength("calculation", calculation, max: 5000)

        try validateLength("reference", reference, max: 500)

        let requestBody = PostTaxTransactionsCreateFromCalculationRequestBody(
            calculation: calculation,
            reference: reference,
            expand: expand,
            metadata: metadata,
            postedAt: postedAt
        )

        return try await (sdkRequest(
            "POST",
            "/v1/tax/transactions/create_from_calculation",
            config: config,
            body: requestBody,
            contentType: "application/x-www-form-urlencoded",
            decoder: .json,
            operationId: "PostTaxTransactionsCreateFromCalculation"
        )).data
    }
}
