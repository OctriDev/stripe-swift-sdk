// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
extension V1ChargesMethods {
    public struct PostChargesChargeOptions: Codable {
        public var charge: String
        public var customer: String?
        public var description: String?
        public var expand: [String]?
        public var fraudDetails: PostChargesChargeRequestBodyFraudDetails?
        public var metadata: PostChargesChargeRequestBodyMetadata?
        public var receiptEmail: String?
        public var shipping: PostChargesChargeRequestBodyShipping?
        public var transferGroup: String?

        public init(charge: String) {
            self.charge = charge
        }
    }

    /// Updates selected properties of an existing charge without changing fields that you omit. Supply `charge` and include only the charge attributes you want to modify. Some attributes, including an associated customer or transfer group, can only be set when their existing value permits the change.
    ///
    /// Updates the specified charge by setting the values of the parameters passed. Any parameters not provided will be left unchanged.
    ///
    /// - Parameters:
    /// - customer: The ID of an existing customer that will be associated with this
    ///   request. This field may only be updated if there is no existing associated
    ///   customer with this charge.
    /// - description: An arbitrary string which you can attach to a charge object.
    ///   It is displayed when in the web interface alongside the charge. Note that if
    ///   you use Stripe to send automatic email receipts to your customers, your
    ///   receipt emails will include the `description` of the charge(s) that they are
    ///   describing.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - fraudDetails: A set of key-value pairs you can attach to a charge giving
    ///   information about its riskiness. If you believe a charge is fraudulent,
    ///   include a `user_report` key with a value of `fraudulent`. If you believe a
    ///   charge is safe, include a `user_report` key with a value of `safe`. Stripe
    ///   will use the information you send to improve our fraud detection algorithms.
    /// - metadata: Set of [key-value pairs](https://docs.stripe.com/api/metadata)
    ///   that you can attach to an object. This can be useful for storing additional
    ///   information about the object in a structured format. Individual keys can be
    ///   unset by posting an empty value to them. All keys can be unset by posting an
    ///   empty value to `metadata`.
    /// - receiptEmail: This is the email address that the receipt for this charge
    ///   will be sent to. If this field is updated, then a new email receipt will be
    ///   sent to the updated address.
    /// - shipping: Shipping information for the charge. Helps prevent fraud on
    ///   charges for physical goods.
    /// - transferGroup: A string that identifies this transaction as part of a
    ///   group. `transfer_group` may only be provided if it has not been set. See the
    ///   [Connect
    ///   documentation](https://docs.stripe.com/connect/separate-charges-and-transfer
    ///   s#transfer-options) for details.
    public static func postChargesCharge(config: ClientConfig, options: PostChargesChargeOptions) async throws -> Charge {
        try validateLength("charge", options.charge, max: 5000)

        if let customer = options.customer {
            try validateLength("customer", customer, max: 5000)
        }

        if let description = options.description {
            try validateLength("description", description, max: 40000)
        }

        if let receiptEmail = options.receiptEmail {
            try validateLength("receipt_email", receiptEmail, max: 5000)
        }

        let requestBody = PostChargesChargeRequestBody(options: options)

        return try (await sdkRequest("POST", ["/v1/charges/", sdkEncodePathSegment(sdkWireString(options.charge))].joined(), config: config, body: requestBody, contentType: "application/x-www-form-urlencoded", decoder: .json, operationId: "PostChargesCharge")).data
    }
}
