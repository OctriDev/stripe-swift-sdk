// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
extension V1PricesMethods {
    public struct GetPricesOptions: Codable {
        public var active: Bool?
        public var created: GetPricesParameter?
        public var currency: String?
        public var endingBefore: String?
        public var expand: [String]?
        public var limit: Int?
        public var lookupKeys: [String]?
        public var product: String?
        public var recurring: GetPricesParameterX51c8ff06?
        public var startingAfter: String?
        public var type: GetPricesParameterX2e5acfe0?

        public init() {}
    }

    /// Lists active or inactive prices, excluding inline prices by default. Use `active`, `currency`, `product`, `recurring`, or `type` to filter the results, and use cursor parameters to navigate between pages.
    ///
    /// Returns a list of your active prices, excluding inline prices. For the list of inactive prices, set active to false.
    ///
    /// - Parameters:
    /// - active: Only return prices that are active or inactive (e.g., pass `false`
    ///   to list all inactive prices).
    /// - created: A filter on the list, based on the object `created` field. The
    ///   value can be a string with an integer Unix timestamp, or it can be a
    ///   dictionary with a number of different query options.
    /// - currency: Only return prices for the given currency.
    /// - endingBefore: A cursor for use in pagination. `ending_before` is an object
    ///   ID that defines your place in the list. For instance, if you make a list
    ///   request and receive 100 objects, starting with `obj_bar`, your subsequent
    ///   call can include `ending_before=obj_bar` in order to fetch the previous page
    ///   of the list.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - limit: A limit on the number of objects to be returned. Limit can range
    ///   between 1 and 100, and the default is 10.
    /// - lookupKeys: Only return the price with these lookup_keys, if any exist.
    ///   You can specify up to 10 lookup_keys.
    /// - product: Only return prices for the given product.
    /// - recurring: Only return prices with these recurring fields.
    /// - startingAfter: A cursor for use in pagination. `starting_after` is an
    ///   object ID that defines your place in the list. For instance, if you make a
    ///   list request and receive 100 objects, ending with `obj_foo`, your subsequent
    ///   call can include `starting_after=obj_foo` in order to fetch the next page of
    ///   the list.
    /// - type: Only return prices of type `recurring` or `one_time`.
    public static func getPrices(config: ClientConfig, options: GetPricesOptions) async throws -> GetPricesResponse {
        if let endingBefore = options.endingBefore {
            try validateLength("ending_before", endingBefore, max: 5000)
        }

        if let product = options.product {
            try validateLength("product", product, max: 5000)
        }

        if let startingAfter = options.startingAfter {
            try validateLength("starting_after", startingAfter, max: 5000)
        }

        return try (await sdkRequest("GET", "/v1/prices", config: config, query: [
            SdkQueryParameter("active", value: options.active),
            SdkQueryParameter("created", value: options.created),
            SdkQueryParameter("currency", value: options.currency),
            SdkQueryParameter("ending_before", value: options.endingBefore),
            SdkQueryParameter("expand", values: options.expand, style: "deepObject", explode: true),
            SdkQueryParameter("limit", value: options.limit),
            SdkQueryParameter("lookup_keys", values: options.lookupKeys, style: "deepObject", explode: true),
            SdkQueryParameter("product", value: options.product),
            SdkQueryParameter("recurring", value: options.recurring),
            SdkQueryParameter("starting_after", value: options.startingAfter),
            SdkQueryParameter("type", value: options.type),
        ], decoder: .json, operationId: "GetPrices")).data
    }
}
