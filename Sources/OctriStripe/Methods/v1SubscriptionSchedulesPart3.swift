// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
public extension V1SubscriptionSchedulesMethods {
    struct PostSubscriptionSchedulesOptions: Codable {
        public var billingMode: PostSubscriptionSchedulesRequestBodyBillingMode?
        public var customer: String?
        public var customerAccount: String?
        public var defaultSettings: PostSubscriptionSchedulesRequestBodyDefaultSettings?
        public var endBehavior: PostSubscriptionSchedulesRequestBodyEndBehavior?
        public var expand: [String]?
        public var fromSubscription: String?
        public var metadata: PostSubscriptionSchedulesRequestBodyMetadata?
        public var phases: [PostSubscriptionSchedulesRequestBodyPhasesItem]?
        public var startDate: PostSubscriptionSchedulesRequestBodyStartDate?

        public init() {}
    }

    /// Creates a new subscription schedule object. Each customer can have up to 500 active or scheduled subscriptions.
    ///
    /// - Parameters:
    /// - billingMode: Controls how prorations and invoices for subscriptions are
    ///   calculated and orchestrated.
    /// - customer: The identifier of the customer to create the subscription
    ///   schedule for.
    /// - customerAccount: The identifier of the account to create the subscription
    ///   schedule for.
    /// - defaultSettings: Object representing the subscription schedule's default
    ///   settings.
    /// - endBehavior: Behavior of the subscription schedule and underlying
    ///   subscription when it ends. Possible values are `release` or `cancel` with
    ///   the default being `release`. `release` will end the subscription schedule
    ///   and keep the underlying subscription running. `cancel` will end the
    ///   subscription schedule and cancel the underlying subscription.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - fromSubscription: Migrate an existing subscription to be managed by a
    ///   subscription schedule. If this parameter is set, a subscription schedule
    ///   will be created using the subscription's item(s), set to auto-renew using
    ///   the subscription's interval. When using this parameter, other parameters
    ///   (such as phase values) cannot be set. To create a subscription schedule with
    ///   other modifications, we recommend making two separate API calls.
    /// - metadata: Set of [key-value pairs](https://docs.stripe.com/api/metadata)
    ///   that you can attach to an object. This can be useful for storing additional
    ///   information about the object in a structured format. Individual keys can be
    ///   unset by posting an empty value to them. All keys can be unset by posting an
    ///   empty value to `metadata`.
    /// - phases: List representing phases of the subscription schedule. Each phase
    ///   can be customized to have different durations, plans, and coupons. If there
    ///   are multiple phases, the `end_date` of one phase will always equal the
    ///   `start_date` of the next phase.
    /// - startDate: When the subscription schedule starts. We recommend using `now`
    ///   so that it starts the subscription immediately, and to avoid unexpected
    ///   behavior due to request delays or clock skew resulting in a slightly
    ///   backdated or postdated start. You can also use a Unix timestamp to backdate
    ///   the subscription so that it starts on a past date, or set a future date for
    ///   the subscription to start on.
    static func postSubscriptionSchedules(
        config: ClientConfig,
        options: PostSubscriptionSchedulesOptions
    ) async throws -> SubscriptionSchedule {
        if let customer = options.customer {
            try validateLength("customer", customer, max: 5000)
        }

        if let customerAccount = options.customerAccount {
            try validateLength("customer_account", customerAccount, max: 5000)
        }

        if let fromSubscription = options.fromSubscription {
            try validateLength("from_subscription", fromSubscription, max: 5000)
        }

        let requestBody = PostSubscriptionSchedulesRequestBody(options: options)

        return try await (sdkRequest(
            "POST",
            "/v1/subscription_schedules",
            config: config,
            body: requestBody,
            contentType: "application/x-www-form-urlencoded",
            decoder: .json,
            operationId: "PostSubscriptionSchedules"
        )).data
    }
}
