// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
extension V1InvoicesCreatePreviewMethods {
    public struct PostInvoicesCreatePreviewOptions: Codable {
        public var automaticTax: PostInvoicesCreatePreviewRequestBodyAutomaticTax?
        public var currency: String?
        public var customer: String?
        public var customerAccount: String?
        public var customerDetails: PostInvoicesCreatePreviewRequestBodyCustomerDetails?
        public var discounts: PostInvoicesCreatePreviewRequestBodyDiscounts?
        public var expand: [String]?
        public var invoiceItems: [PostInvoicesCreatePreviewRequestBodyInvoiceItemsItem]?
        public var issuer: PostInvoicesCreatePreviewRequestBodyIssuer?
        public var onBehalfOf: String?
        public var previewMode: PostInvoicesCreatePreviewRequestBodyPreviewMode?
        public var schedule: String?
        public var scheduleDetails: PostInvoicesCreatePreviewRequestBodyScheduleDetails?
        public var subscription: String?
        public var subscriptionDetails: PostInvoicesCreatePreviewRequestBodySubscriptionDetails?

        public init() {}
    }

    /// At any time, you can preview the upcoming invoice for a subscription or subscription schedule. This will show you all the charges that are pending, including subscription renewal charges, invoice item charges, etc. It will also show you any discounts that are applicable to the invoice. You can also preview the effects of creating or updating a subscription or subscription schedule, including a preview of any prorations that will take place. To ensure that the actual proration is calculated exactly the same as the previewed proration, you should pass the subscription_details.proration_date parameter when doing the actual subscription update. The recommended way to get only the prorations being previewed on the invoice is to consider line items where parent.subscription_item_details.proration is true . Note that when you are viewing an upcoming invoice, you are simply viewing a preview – the invoice has not yet been created. As such, the upcoming invoice will not show up in invoice listing calls, and you cannot use the API to pay or edit the invoice. If you want to change the amount that your customer will be billed, you can add, remove, or update pending invoice items, or update the customer’s discount. Note: Currency conversion calculations use the latest exchange rates. Exchange rates may vary between the time of the preview and the time of the actual invoice creation. Learn more
    ///
    /// - Parameters:
    /// - automaticTax: Settings for automatic tax lookup for this invoice preview.
    /// - currency: The currency to preview this invoice in. Defaults to that of
    ///   `customer` if not specified.
    /// - customer: The identifier of the customer whose upcoming invoice you're
    ///   retrieving. If `automatic_tax` is enabled then one of `customer`,
    ///   `customer_details`, `subscription`, or `schedule` must be set.
    /// - customerAccount: The identifier of the account representing the customer
    ///   whose upcoming invoice you're retrieving. If `automatic_tax` is enabled then
    ///   one of `customer`, `customer_account`, `customer_details`, `subscription`,
    ///   or `schedule` must be set.
    /// - customerDetails: Details about the customer you want to invoice or
    ///   overrides for an existing customer. If `automatic_tax` is enabled then one
    ///   of `customer`, `customer_details`, `subscription`, or `schedule` must be
    ///   set.
    /// - discounts: The coupons to redeem into discounts for the invoice preview.
    ///   If not specified, inherits the discount from the subscription or customer.
    ///   This works for both coupons directly applied to an invoice and coupons
    ///   applied to a subscription. Pass an empty string to avoid inheriting any
    ///   discounts.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - invoiceItems: List of invoice items to add or update in the upcoming
    ///   invoice preview (up to 250).
    /// - issuer: The connected account that issues the invoice. The invoice is
    ///   presented with the branding and support information of the specified
    ///   account.
    /// - onBehalfOf: The account (if any) for which the funds of the invoice
    ///   payment are intended. If set, the invoice will be presented with the
    ///   branding and support information of the specified account. See the [Invoices
    ///   with Connect](https://docs.stripe.com/billing/invoices/connect)
    ///   documentation for details.
    /// - previewMode: Customizes the types of values to include when calculating
    ///   the invoice. Defaults to `next` if unspecified.
    /// - schedule: The identifier of the schedule whose upcoming invoice you'd like
    ///   to retrieve. Cannot be used with subscription or subscription fields.
    /// - scheduleDetails: The schedule creation or modification params to apply as
    ///   a preview. Cannot be used with `subscription` or `subscription_` prefixed
    ///   fields.
    /// - subscription: The identifier of the subscription for which you'd like to
    ///   retrieve the upcoming invoice. If not provided, but a
    ///   `subscription_details.items` is provided, you will preview creating a
    ///   subscription with those items. If neither `subscription` nor
    ///   `subscription_details.items` is provided, you will retrieve the next
    ///   upcoming invoice from among the customer's subscriptions.
    /// - subscriptionDetails: The subscription creation or modification params to
    ///   apply as a preview. Cannot be used with `schedule` or `schedule_details`
    ///   fields.
    public static func postInvoicesCreatePreview(config: ClientConfig, options: PostInvoicesCreatePreviewOptions) async throws -> Invoice {
        if let customer = options.customer {
            try validateLength("customer", customer, max: 5000)
        }

        if let customerAccount = options.customerAccount {
            try validateLength("customer_account", customerAccount, max: 5000)
        }

        if let schedule = options.schedule {
            try validateLength("schedule", schedule, max: 5000)
        }

        if let subscription = options.subscription {
            try validateLength("subscription", subscription, max: 5000)
        }

        let requestBody = PostInvoicesCreatePreviewRequestBody(options: options)

        return try (await sdkRequest("POST", "/v1/invoices/create_preview", config: config, body: requestBody, contentType: "application/x-www-form-urlencoded", decoder: .json, operationId: "PostInvoicesCreatePreview")).data
    }
}
