// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
public extension V1BillingCreditGrantsMethods {
    struct PostBillingCreditGrantsOptions: Codable {
        public var amount: PostBillingCreditGrantsRequestBodyAmount
        public var applicabilityConfig: PostBillingCreditGrantsRequestBodyApplicabilityConfig
        public var category: PostBillingCreditGrantsRequestBodyCategory?
        public var customer: String?
        public var customerAccount: String?
        public var effectiveAt: Int?
        public var expand: [String]?
        public var expiresAt: Int?
        public var metadata: [String: String]?
        public var name: String?
        public var priority: Int?

        public init(
            amount: PostBillingCreditGrantsRequestBodyAmount,
            applicabilityConfig: PostBillingCreditGrantsRequestBodyApplicabilityConfig
        ) {
            self.amount = amount
            self.applicabilityConfig = applicabilityConfig
        }
    }

    /// Creates a credit grant that allocates billing credits to a customer or customer account. Supply `amount` and
    /// `applicability_config`, and optionally set the category, effective and expiration times, metadata, name, or
    /// application priority. The created grant records how and when its credits can be applied.
    ///
    /// Creates a credit grant.
    ///
    /// - Parameters:
    /// - amount: Amount of this credit grant.
    /// - applicabilityConfig: Configuration specifying what this credit grant
    ///   applies to. We currently only support `metered` prices that have a [Billing
    ///   Meter](https://docs.stripe.com/api/billing/meter) attached to them.
    /// - category: The category of this credit grant. It defaults to `paid` if not
    ///   specified.
    /// - customer: ID of the customer receiving the billing credits.
    /// - customerAccount: ID of the account representing the customer receiving the
    ///   billing credits.
    /// - effectiveAt: The time when the billing credits become effective-when
    ///   they're eligible for use. It defaults to the current timestamp if not
    ///   specified.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - expiresAt: The time when the billing credits expire. If not specified, the
    ///   billing credits don't expire.
    /// - metadata: Set of key-value pairs that you can attach to an object. You can
    ///   use this to store additional information about the object (for example, cost
    ///   basis) in a structured format.
    /// - name: A descriptive name shown in the Dashboard.
    /// - priority: The desired priority for applying this credit grant. If not
    ///   specified, it will be set to the default value of 50. The highest priority
    ///   is 0 and the lowest is 100.
    static func postBillingCreditGrants(
        config: ClientConfig,
        options: PostBillingCreditGrantsOptions
    ) async throws -> BillingCreditGrant {
        if let customer = options.customer {
            try validateLength("customer", customer, max: 5000)
        }

        if let customerAccount = options.customerAccount {
            try validateLength("customer_account", customerAccount, max: 5000)
        }

        if let name = options.name {
            try validateLength("name", name, max: 100)
        }

        let requestBody = PostBillingCreditGrantsRequestBody(options: options)

        return try await (sdkRequest(
            "POST",
            "/v1/billing/credit_grants",
            config: config,
            body: requestBody,
            contentType: "application/x-www-form-urlencoded",
            decoder: .json,
            operationId: "PostBillingCreditGrants"
        )).data
    }
}
