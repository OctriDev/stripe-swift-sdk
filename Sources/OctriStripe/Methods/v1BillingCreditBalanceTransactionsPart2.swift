// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
public extension V1BillingCreditBalanceTransactionsMethods {
    /// Lists credit balance transactions recorded against credit grants. Use `credit_grant`, `customer`, or
    /// `customer_account` to scope the transactions, and use cursor parameters to navigate through the results. The
    /// response is a paginated list with transaction data and a continuation indicator.
    ///
    /// Retrieve a list of credit balance transactions.
    ///
    /// - Parameters:
    /// - creditGrant: The credit grant for which to fetch credit balance
    ///   transactions.
    /// - customer: The customer whose credit balance transactions you're
    ///   retrieving.
    /// - customerAccount: The account representing the customer whose credit
    ///   balance transactions you're retrieving.
    /// - endingBefore: A cursor for use in pagination. `ending_before` is an object
    ///   ID that defines your place in the list. For instance, if you make a list
    ///   request and receive 100 objects, starting with `obj_bar`, your subsequent
    ///   call can include `ending_before=obj_bar` in order to fetch the previous page
    ///   of the list.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - limit: A limit on the number of objects to be returned. Limit can range
    ///   between 1 and 100, and the default is 10.
    /// - startingAfter: A cursor for use in pagination. `starting_after` is an
    ///   object ID that defines your place in the list. For instance, if you make a
    ///   list request and receive 100 objects, ending with `obj_foo`, your subsequent
    ///   call can include `starting_after=obj_foo` in order to fetch the next page of
    ///   the list.
    static func getBillingCreditBalanceTransactions(
        config: ClientConfig,
        creditGrant: String?,
        customer: String?,
        customerAccount: String?,
        endingBefore: String?,
        expand: [String]?,
        limit: Int?,
        startingAfter: String?
    ) async throws -> GetBillingCreditBalanceTransactionsResponse {
        if let creditGrant {
            try validateLength("credit_grant", creditGrant, max: 5000)
        }

        if let customer {
            try validateLength("customer", customer, max: 5000)
        }

        if let customerAccount {
            try validateLength("customer_account", customerAccount, max: 5000)
        }

        if let endingBefore {
            try validateLength("ending_before", endingBefore, max: 5000)
        }

        if let startingAfter {
            try validateLength("starting_after", startingAfter, max: 5000)
        }

        return try await (sdkRequest("GET", "/v1/billing/credit_balance_transactions", config: config, query: [
            SdkQueryParameter("credit_grant", value: creditGrant),
            SdkQueryParameter("customer", value: customer),
            SdkQueryParameter("customer_account", value: customerAccount),
            SdkQueryParameter("ending_before", value: endingBefore),
            SdkQueryParameter("expand", values: expand, style: "deepObject", explode: true),
            SdkQueryParameter("limit", value: limit),
            SdkQueryParameter("starting_after", value: startingAfter),
        ], decoder: .json, operationId: "GetBillingCreditBalanceTransactions")).data
    }
}
