// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
public extension V1BillingPortalSessionsMethods {
    struct PostBillingPortalSessionsOptions: Codable {
        public var configuration: String?
        public var customer: String?
        public var customerAccount: String?
        public var expand: [String]?
        public var flowData: PostBillingPortalSessionsRequestBodyFlowData?
        public var locale: PostBillingPortalSessionsRequestBodyLocale?
        public var onBehalfOf: String?
        public var returnUrl: String?

        public init() {}
    }

    /// Creates a customer portal session for an existing customer or account. Supply `customer` or `customer_account`,
    /// optionally select a `configuration`, and use `flow_data` to direct the customer through a specific portal flow;
    /// the response includes a short-lived portal URL.
    ///
    /// Creates a session of the customer portal.
    ///
    /// - Parameters:
    /// - configuration: The ID of an existing
    ///   [configuration](https://docs.stripe.com/api/customer_portal/configurations)
    ///   to use for this session, describing its functionality and features. If not
    ///   specified, the session uses the default configuration.
    /// - customer: The ID of an existing customer.
    /// - customerAccount: The ID of an existing account.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - flowData: Information about a specific flow for the customer to go
    ///   through. See the
    ///   [docs](https://docs.stripe.com/customer-management/portal-deep-links) to
    ///   learn more about using customer portal deep links and flows.
    /// - locale: The IETF language tag of the locale customer portal is displayed
    ///   in. If blank or auto, the customer’s `preferred_locales` or browser’s locale
    ///   is used.
    /// - onBehalfOf: The `on_behalf_of` account to use for this session. When
    ///   specified, only subscriptions and invoices with this `on_behalf_of` account
    ///   appear in the portal. For more information, see the
    ///   [docs](https://docs.stripe.com/connect/separate-charges-and-transfers#settle
    ///   ment-merchant). Use the [Accounts
    ///   API](https://docs.stripe.com/api/accounts/object#account_object-settings-bra
    ///   nding) to modify the `on_behalf_of` account's branding settings, which the
    ///   portal displays.
    /// - returnUrl: The default URL to redirect customers to when they click on the
    ///   portal's link to return to your website.
    static func postBillingPortalSessions(
        config: ClientConfig,
        options: PostBillingPortalSessionsOptions
    ) async throws -> BillingPortalSession {
        if let configuration = options.configuration {
            try validateLength("configuration", configuration, max: 5000)
        }

        if let customer = options.customer {
            try validateLength("customer", customer, max: 5000)
        }

        if let customerAccount = options.customerAccount {
            try validateLength("customer_account", customerAccount, max: 5000)
        }

        let requestBody = PostBillingPortalSessionsRequestBody(options: options)

        return try await (sdkRequest(
            "POST",
            "/v1/billing_portal/sessions",
            config: config,
            body: requestBody,
            contentType: "application/x-www-form-urlencoded",
            decoder: .json,
            operationId: "PostBillingPortalSessions"
        )).data
    }
}
