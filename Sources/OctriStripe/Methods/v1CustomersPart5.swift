// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
extension V1CustomersMethods {
    public struct PostCustomersCustomerOptions: Codable {
        public var customer: String
        public var address: PostCustomersCustomerRequestBodyAddress?
        public var balance: Int?
        public var bankAccount: PostCustomersCustomerRequestBodyBankAccount?
        public var businessName: PostCustomersCustomerRequestBodyBusinessNameVariant1?
        public var card: PostCustomersCustomerRequestBodyCard?
        public var cashBalance: PostCustomersCustomerRequestBodyCashBalance?
        public var defaultAlipayAccount: String?
        public var defaultBankAccount: String?
        public var defaultCard: String?
        public var defaultSource: String?
        public var description: String?
        public var email: String?
        public var expand: [String]?
        public var individualName: PostCustomersCustomerRequestBodyIndividualNameVariant1?
        public var invoicePrefix: String?
        public var invoiceSettings: PostCustomersCustomerRequestBodyInvoiceSettings?
        public var metadata: PostCustomersCustomerRequestBodyMetadata?
        public var name: String?
        public var nextInvoiceSequence: Int?
        public var phone: String?
        public var preferredLocales: [String]?
        public var shipping: PostCustomersCustomerRequestBodyShipping?
        public var source: String?
        public var tax: PostCustomersCustomerRequestBodyTax?
        public var taxExempt: PostCustomersCustomerRequestBodyTaxExempt?

        public init(customer: String) {
            self.customer = customer
        }
    }

    /// Updates an existing customer using only the fields supplied in the request. Change contact, billing, payment source, invoice, balance, or metadata settings as needed, while omitted fields remain unchanged. Supplying a new `source` can retry eligible past-due subscription invoices, whereas changing `default_source` does not trigger that retry behavior.
    ///
    /// Updates the specified customer by setting the values of the parameters passed. Any parameters not provided are left unchanged. For example, if you pass the source parameter, that becomes the customer’s active source (such as a card) to be used for all charges in the future. When you update a customer to a new valid card source by passing the source parameter: for each of the customer’s current subscriptions, if the subscription bills automatically and is in the past_due state, then the latest open invoice for the subscription with automatic collection enabled is retried. This retry doesn’t count as an automatic retry, and doesn’t affect the next regularly scheduled payment for the invoice. Changing the default_source for a customer doesn’t trigger this behavior. This request accepts mostly the same arguments as the customer creation call.
    ///
    /// - Parameters:
    /// - address: The customer's address. Learn about [country-specific
    ///   requirements for calculating
    ///   tax](https://docs.stripe.com/invoicing/taxes?dashboard-or-api=dashboard#set-
    ///   up-customer).
    /// - balance: An integer amount in cents (or local equivalent) that represents
    ///   the customer's current balance, which affect the customer's future invoices.
    ///   A negative amount represents a credit that decreases the amount due on an
    ///   invoice; a positive amount increases the amount due on an invoice.
    /// - bankAccount: Either a token, like the ones returned by
    ///   [Stripe.js](https://stripe.com/docs/js), or a dictionary containing a user's
    ///   bank account details.
    /// - businessName: The customer's business name. This may be up to *150
    ///   characters*.
    /// - card: A token, like the ones returned by
    ///   [Stripe.js](https://stripe.com/docs/js).
    /// - cashBalance: Balance information and default balance settings for this
    ///   customer.
    /// - defaultAlipayAccount: ID of Alipay account to make the customer's new
    ///   default for invoice payments.
    /// - defaultBankAccount: ID of bank account to make the customer's new default
    ///   for invoice payments.
    /// - defaultCard: ID of card to make the customer's new default for invoice
    ///   payments.
    /// - defaultSource: If you are using payment methods created via the
    ///   PaymentMethods API, see the
    ///   [invoice_settings.default_payment_method](https://docs.stripe.com/api/custom
    ///   ers/update#update_customer-invoice_settings-default_payment_method)
    ///   parameter. Provide the ID of a payment source already attached to this
    ///   customer to make it this customer's default payment source. If you want to
    ///   add a new payment source and make it the default, see the
    ///   [source](https://docs.stripe.com/api/customers/update#update_customer-source
    ///   ) property.
    /// - description: An arbitrary string that you can attach to a customer object.
    ///   It is displayed alongside the customer in the dashboard.
    /// - email: Customer's email address. It's displayed alongside the customer in
    ///   your dashboard and can be useful for searching and tracking. This may be up
    ///   to *512 characters*.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - individualName: The customer's full name. This may be up to *150
    ///   characters*.
    /// - invoicePrefix: The prefix for the customer used to generate unique invoice
    ///   numbers. Must be 3–12 uppercase letters or numbers.
    /// - invoiceSettings: Default invoice settings for this customer.
    /// - metadata: Set of [key-value pairs](https://docs.stripe.com/api/metadata)
    ///   that you can attach to an object. This can be useful for storing additional
    ///   information about the object in a structured format. Individual keys can be
    ///   unset by posting an empty value to them. All keys can be unset by posting an
    ///   empty value to `metadata`.
    /// - name: The customer's full name or business name.
    /// - nextInvoiceSequence: The sequence to be used on the customer's next
    ///   invoice. Defaults to 1.
    /// - phone: The customer's phone number.
    /// - preferredLocales: Customer's preferred languages, ordered by preference.
    /// - shipping: The customer's shipping information. Appears on invoices emailed
    ///   to this customer.
    /// - tax: Tax details about the customer.
    /// - taxExempt: The customer's tax exemption. One of `none`, `exempt`, or
    ///   `reverse`.
    public static func postCustomersCustomer(config: ClientConfig, options: PostCustomersCustomerOptions) async throws -> Customer {
        try validateLength("customer", options.customer, max: 5000)

        if let defaultAlipayAccount = options.defaultAlipayAccount {
            try validateLength("default_alipay_account", defaultAlipayAccount, max: 500)
        }

        if let defaultBankAccount = options.defaultBankAccount {
            try validateLength("default_bank_account", defaultBankAccount, max: 500)
        }

        if let defaultCard = options.defaultCard {
            try validateLength("default_card", defaultCard, max: 500)
        }

        if let defaultSource = options.defaultSource {
            try validateLength("default_source", defaultSource, max: 500)
        }

        if let description = options.description {
            try validateLength("description", description, max: 5000)
        }

        if let email = options.email {
            try validateLength("email", email, max: 512)
        }

        if let invoicePrefix = options.invoicePrefix {
            try validateLength("invoice_prefix", invoicePrefix, max: 5000)
        }

        if let name = options.name {
            try validateLength("name", name, max: 256)
        }

        if let phone = options.phone {
            try validateLength("phone", phone, max: 20)
        }

        if let source = options.source {
            try validateLength("source", source, max: 5000)
        }

        let requestBody = PostCustomersCustomerRequestBody(options: options)

        return try (await sdkRequest("POST", ["/v1/customers/", sdkEncodePathSegment(sdkWireString(options.customer))].joined(), config: config, body: requestBody, contentType: "application/x-www-form-urlencoded", decoder: .json, operationId: "PostCustomersCustomer")).data
    }
}
