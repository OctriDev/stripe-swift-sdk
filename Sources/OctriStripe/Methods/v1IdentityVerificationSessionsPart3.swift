// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
public extension V1IdentityVerificationSessionsMethods {
    struct PostIdentityVerificationSessionsOptions: Codable {
        public var clientReferenceId: String?
        public var expand: [String]?
        public var metadata: [String: String]?
        public var options: PostIdentityVerificationSessionsRequestBodyOptions?
        public var providedDetails: PostIdentityVerificationSessionsRequestBodyProvidedDetails?
        public var relatedCustomer: String?
        public var relatedCustomerAccount: String?
        public var relatedPerson: PostIdentityVerificationSessionsRequestBodyRelatedPerson?
        public var returnUrl: String?
        public var type: PostIdentityVerificationSessionsRequestBodyType?
        public var verificationFlow: String?

        public init() {}
    }

    /// Creates a VerificationSession object. After the VerificationSession is created, display a verification modal
    /// using the session client_secret or send your users to the session’s url . If your API key is in test mode,
    /// verification checks won’t actually process, though everything else will occur as if in live mode. Related guide:
    /// Verify your users’ identity documents
    ///
    /// - Parameters:
    /// - clientReferenceId: A string to reference this user. This can be a customer
    ///   ID, a session ID, or similar, and can be used to reconcile this verification
    ///   with your internal systems.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - metadata: Set of [key-value pairs](https://docs.stripe.com/api/metadata)
    ///   that you can attach to an object. This can be useful for storing additional
    ///   information about the object in a structured format. Individual keys can be
    ///   unset by posting an empty value to them. All keys can be unset by posting an
    ///   empty value to `metadata`.
    /// - options: A set of options for the session’s verification checks.
    /// - providedDetails: Details provided about the user being verified. These
    ///   details might be shown to the user.
    /// - relatedCustomer: Customer ID
    /// - relatedCustomerAccount: The ID of the Account representing a customer.
    /// - relatedPerson: Tokens referencing a Person resource and its associated
    ///   account.
    /// - returnUrl: The URL that the user will be redirected to upon completing the
    ///   verification flow.
    /// - type: The type of [verification
    ///   check](https://docs.stripe.com/identity/verification-checks) to be
    ///   performed. You must provide a `type` if not passing `verification_flow`.
    /// - verificationFlow: The ID of a verification flow from the Dashboard. See
    ///   https://docs.stripe.com/identity/verification-flows.
    static func postIdentityVerificationSessions(
        config: ClientConfig,
        options: PostIdentityVerificationSessionsOptions
    ) async throws -> IdentityVerificationSession {
        if let clientReferenceId = options.clientReferenceId {
            try validateLength("client_reference_id", clientReferenceId, max: 5000)
        }

        if let relatedCustomer = options.relatedCustomer {
            try validateLength("related_customer", relatedCustomer, max: 5000)
        }

        if let relatedCustomerAccount = options.relatedCustomerAccount {
            try validateLength("related_customer_account", relatedCustomerAccount, max: 5000)
        }

        if let verificationFlow = options.verificationFlow {
            try validateLength("verification_flow", verificationFlow, max: 5000)
        }

        let requestBody = PostIdentityVerificationSessionsRequestBody(options: options)

        return try await (sdkRequest(
            "POST",
            "/v1/identity/verification_sessions",
            config: config,
            body: requestBody,
            contentType: "application/x-www-form-urlencoded",
            decoder: .json,
            operationId: "PostIdentityVerificationSessions"
        )).data
    }
}
