// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
extension V1TransfersReversalsMethods {
    /// Creates a reversal for a specific transfer. Supply `amount` to reverse only part of the unreversed transfer amount, or omit it to reverse the entire remaining amount; partial reversals are allowed only for transfers to Stripe Accounts.
    ///
    /// When you create a new reversal, you must specify a transfer to create it on. When reversing transfers, you can optionally reverse part of the transfer. You can do so as many times as you wish until the entire transfer has been reversed. Once entirely reversed, a transfer can’t be reversed again. This method will return an error when called on an already-reversed transfer, or when trying to reverse more money than is left on a transfer.
    ///
    /// - Parameters:
    /// - amount: A positive integer in cents (or local equivalent) representing how
    ///   much of this transfer to reverse. Can only reverse up to the unreversed
    ///   amount remaining of the transfer. Partial transfer reversals are only
    ///   allowed for transfers to Stripe Accounts. Defaults to the entire transfer
    ///   amount.
    /// - description: An arbitrary string which you can attach to a reversal
    ///   object. This will be unset if you POST an empty value.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - metadata: Set of [key-value pairs](https://docs.stripe.com/api/metadata)
    ///   that you can attach to an object. This can be useful for storing additional
    ///   information about the object in a structured format. Individual keys can be
    ///   unset by posting an empty value to them. All keys can be unset by posting an
    ///   empty value to `metadata`.
    /// - refundApplicationFee: Boolean indicating whether the application fee
    ///   should be refunded when reversing this transfer. If a full transfer reversal
    ///   is given, the full application fee will be refunded. Otherwise, the
    ///   application fee will be refunded with an amount proportional to the amount
    ///   of the transfer reversed.
    public static func postTransfersIdReversals(config: ClientConfig, id: String, amount: Int?, description: String?, expand: [String]?, metadata: PostTransfersIdReversalsRequestBodyMetadata?, refundApplicationFee: Bool?) async throws -> TransferReversal {
        try validateLength("id", id, max: 5000)

        if let description = description {
            try validateLength("description", description, max: 5000)
        }

        let requestBody = PostTransfersIdReversalsRequestBody(amount: amount, description: description, expand: expand, metadata: metadata, refundApplicationFee: refundApplicationFee)

        return try (await sdkRequest("POST", ["/v1/transfers/", sdkEncodePathSegment(sdkWireString(id)), "/reversals"].joined(), config: config, body: requestBody, contentType: "application/x-www-form-urlencoded", decoder: .json, operationId: "PostTransfersIdReversals")).data
    }
}
