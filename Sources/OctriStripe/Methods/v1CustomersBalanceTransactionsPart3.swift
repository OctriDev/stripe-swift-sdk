// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
public extension V1CustomersBalanceTransactionsMethods {
    /// Creates an immutable transaction that changes a customer's credit balance. Supply `amount` and `currency`, and
    /// optionally add a `description` or `metadata` to explain or categorize the adjustment.
    ///
    /// Creates an immutable transaction that updates the customer’s credit balance.
    ///
    /// - Parameters:
    /// - amount: The integer amount in **cents (or local equivalent)** to apply to
    ///   the customer's credit balance.
    /// - currency: Three-letter [ISO currency
    ///   code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must
    ///   be a [supported currency](https://stripe.com/docs/currencies). Specifies the
    ///   [`invoice_credit_balance`](https://docs.stripe.com/api/customers/object#cust
    ///   omer_object-invoice_credit_balance) that this transaction will apply to. If
    ///   the customer's `currency` is not set, it will be updated to this value.
    /// - description: An arbitrary string attached to the object. Often useful for
    ///   displaying to users.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - metadata: Set of [key-value pairs](https://docs.stripe.com/api/metadata)
    ///   that you can attach to an object. This can be useful for storing additional
    ///   information about the object in a structured format. Individual keys can be
    ///   unset by posting an empty value to them. All keys can be unset by posting an
    ///   empty value to `metadata`.
    static func postCustomersCustomerBalanceTransactions(
        config: ClientConfig,
        customer: String,
        amount: Int,
        currency: String,
        description: String?,
        expand: [String]?,
        metadata: PostCustomersCustomerBalanceTransactionsRequestBodyMetadata?
    ) async throws -> CustomerBalanceTransaction {
        try validateLength("customer", customer, max: 5000)

        if let description {
            try validateLength("description", description, max: 350)
        }

        let requestBody = PostCustomersCustomerBalanceTransactionsRequestBody(
            amount: amount,
            currency: currency,
            description: description,
            expand: expand,
            metadata: metadata
        )

        return try await (sdkRequest(
            "POST",
            ["/v1/customers/", sdkEncodePathSegment(sdkWireString(customer)), "/balance_transactions"].joined(),
            config: config,
            body: requestBody,
            contentType: "application/x-www-form-urlencoded",
            decoder: .json,
            operationId: "PostCustomersCustomerBalanceTransactions"
        )).data
    }
}
