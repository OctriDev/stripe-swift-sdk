// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
public extension V1TreasuryOutboundPaymentsMethods {
    struct PostTreasuryOutboundPaymentsOptions: Codable {
        public var amount: Int
        public var currency: String
        public var financialAccount: String
        public var customer: String?
        public var description: String?
        public var destinationPaymentMethod: String?
        public var destinationPaymentMethodData: PostTreasuryOutboundPaymentsRequestBodyDestinationPaymentMethodData?
        public var destinationPaymentMethodOptions: PostTreasuryOutboundPaymentsRequestBodyDestinationPaymentMethodOptions?
        public var endUserDetails: PostTreasuryOutboundPaymentsRequestBodyEndUserDetails?
        public var expand: [String]?
        public var metadata: [String: String]?
        public var statementDescriptor: String?

        public init(amount: Int, currency: String, financialAccount: String) {
            self.amount = amount
            self.currency = currency
            self.financialAccount = financialAccount
        }
    }

    /// Creates a new OutboundPayment from a FinancialAccount to another party's external bank account or
    /// FinancialAccount. Supply `amount`, `currency`, and `financial_account`, then provide either an existing
    /// `destination_payment_method` or `destination_payment_method_data` to specify the payment instrument.
    ///
    /// Creates an OutboundPayment.
    ///
    /// - Parameters:
    /// - amount: Amount (in cents) to be transferred.
    /// - currency: Three-letter [ISO currency
    ///   code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must
    ///   be a [supported currency](https://stripe.com/docs/currencies).
    /// - financialAccount: The FinancialAccount to pull funds from.
    /// - customer: ID of the customer to whom the OutboundPayment is sent. Must
    ///   match the Customer attached to the `destination_payment_method` passed in.
    /// - description: An arbitrary string attached to the object. Often useful for
    ///   displaying to users.
    /// - destinationPaymentMethod: The PaymentMethod to use as the payment
    ///   instrument for the OutboundPayment. Exclusive with
    ///   `destination_payment_method_data`.
    /// - destinationPaymentMethodData: Hash used to generate the PaymentMethod to
    ///   be used for this OutboundPayment. Exclusive with
    ///   `destination_payment_method`.
    /// - destinationPaymentMethodOptions: Payment method-specific configuration for
    ///   this OutboundPayment.
    /// - endUserDetails: End user details.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - metadata: Set of [key-value pairs](https://docs.stripe.com/api/metadata)
    ///   that you can attach to an object. This can be useful for storing additional
    ///   information about the object in a structured format. Individual keys can be
    ///   unset by posting an empty value to them. All keys can be unset by posting an
    ///   empty value to `metadata`.
    /// - statementDescriptor: The description that appears on the receiving end for
    ///   this OutboundPayment (for example, bank statement for external bank
    ///   transfer). Maximum 10 characters for `ach` payments, 140 characters for
    ///   `us_domestic_wire` payments, or 500 characters for `stripe` network
    ///   transfers. Can only include -#.$&*, spaces, and alphanumeric characters. The
    ///   default value is "payment".
    static func postTreasuryOutboundPayments(
        config: ClientConfig,
        options: PostTreasuryOutboundPaymentsOptions
    ) async throws -> TreasuryOutboundPayment {
        if let customer = options.customer {
            try validateLength("customer", customer, max: 5000)
        }

        if let description = options.description {
            try validateLength("description", description, max: 5000)
        }

        if let destinationPaymentMethod = options.destinationPaymentMethod {
            try validateLength("destination_payment_method", destinationPaymentMethod, max: 5000)
        }

        if let statementDescriptor = options.statementDescriptor {
            try validateLength("statement_descriptor", statementDescriptor, max: 5000)
        }

        let requestBody = PostTreasuryOutboundPaymentsRequestBody(options: options)

        return try await (sdkRequest(
            "POST",
            "/v1/treasury/outbound_payments",
            config: config,
            body: requestBody,
            contentType: "application/x-www-form-urlencoded",
            decoder: .json,
            operationId: "PostTreasuryOutboundPayments"
        )).data
    }
}
