// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
public extension V1TaxRatesMethods {
    struct PostTaxRatesOptions: Codable {
        public var displayName: String
        public var inclusive: Bool
        public var percentage: Double
        public var active: Bool?
        public var country: String?
        public var description: String?
        public var expand: [String]?
        public var jurisdiction: String?
        public var metadata: [String: String]?
        public var state: String?
        public var taxType: PostTaxRatesRequestBodyTaxType?

        public init(displayName: String, inclusive: Bool, percentage: Double) {
            self.displayName = displayName
            self.inclusive = inclusive
            self.percentage = percentage
        }
    }

    /// Creates a new tax rate for use with invoices, subscriptions, or Checkout Sessions. Supply `display_name`,
    /// `inclusive`, and `percentage`, and optionally provide jurisdiction, country, state, metadata, or tax
    /// classification details. Inactive rates cannot be used with new applications or Checkout Sessions, although
    /// existing subscriptions and invoices can continue using them.
    ///
    /// Creates a new tax rate.
    ///
    /// - Parameters:
    /// - displayName: The display name of the tax rate, which will be shown to
    ///   users.
    /// - inclusive: This specifies if the tax rate is inclusive or exclusive.
    /// - percentage: This represents the tax rate percent out of 100.
    /// - active: Flag determining whether the tax rate is active or inactive
    ///   (archived). Inactive tax rates cannot be used with new applications or
    ///   Checkout Sessions, but will still work for subscriptions and invoices that
    ///   already have it set.
    /// - country: Two-letter country code ([ISO 3166-1
    ///   alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)).
    /// - description: An arbitrary string attached to the tax rate for your
    ///   internal use only. It will not be visible to your customers.
    /// - expand: Specifies which fields in the response should be expanded.
    /// - jurisdiction: The jurisdiction for the tax rate. You can use this label
    ///   field for tax reporting purposes. It also appears on your customer’s
    ///   invoice.
    /// - metadata: Set of [key-value pairs](https://docs.stripe.com/api/metadata)
    ///   that you can attach to an object. This can be useful for storing additional
    ///   information about the object in a structured format. Individual keys can be
    ///   unset by posting an empty value to them. All keys can be unset by posting an
    ///   empty value to `metadata`.
    /// - state: [ISO 3166-2 subdivision
    ///   code](https://en.wikipedia.org/wiki/ISO_3166-2), without country prefix. For
    ///   example, "NY" for New York, United States.
    /// - taxType: The high-level tax type, such as `vat` or `sales_tax`.
    static func postTaxRates(config: ClientConfig, options: PostTaxRatesOptions) async throws -> TaxRate {
        try validateLength("display_name", options.displayName, max: 50)

        if let country = options.country {
            try validateLength("country", country, max: 5000)
        }

        if let description = options.description {
            try validateLength("description", description, max: 5000)
        }

        if let jurisdiction = options.jurisdiction {
            try validateLength("jurisdiction", jurisdiction, max: 50)
        }

        if let state = options.state {
            try validateLength("state", state, max: 5000)
        }

        let requestBody = PostTaxRatesRequestBody(options: options)

        return try await (sdkRequest(
            "POST",
            "/v1/tax_rates",
            config: config,
            body: requestBody,
            contentType: "application/x-www-form-urlencoded",
            decoder: .json,
            operationId: "PostTaxRates"
        )).data
    }
}
