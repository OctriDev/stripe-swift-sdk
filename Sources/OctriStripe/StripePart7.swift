// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

public class V1CheckoutNamespace {
    public let sessions: V1CheckoutSessionsNamespace
    public init(config: ClientConfig) {
        sessions = V1CheckoutSessionsNamespace(config: config)
    }
}

public class V1ClimateOrdersCancelNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

/// Cancels a Climate order. You can cancel an order within 24 hours of creation. Stripe refunds the reservation amount_subtotal , but not the amount_fees for user-triggered cancellations. Frontier might cancel reservations if suppliers fail to deliver. If Frontier cancels the reservation, Stripe provides 90 days advance notice and refunds the amount_total .
    public func postClimateOrdersOrder(order: String, expand: [String]?) async throws -> ClimateOrder {
        return try await V1ClimateOrdersCancelMethods.postClimateOrdersOrderCancel(config: config, order: order, expand: expand)
    }
}

public class V1ClimateOrdersNamespace {
    let config: ClientConfig
    public let cancel: V1ClimateOrdersCancelNamespace
    public init(config: ClientConfig) {
        self.config = config
        cancel = V1ClimateOrdersCancelNamespace(config: config)
    }

/// Lists Climate orders sorted by creation date, with the most recently created orders first. Use cursor parameters and `limit` to paginate the results, and use `expand` to request expanded response fields. Each order represents an intent to purchase a Climate product funded from the merchant balance.
    ///
    /// Lists all Climate order objects. The orders are returned sorted by creation date, with the most recently created orders appearing first.
    public func getClimate(endingBefore: String?, expand: [String]?, limit: Int?, startingAfter: String?) async throws -> GetClimateOrdersResponse {
        return try await V1ClimateOrdersMethods.getClimateOrders(config: config, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter)
    }

/// Creates a Climate order object for a given Climate product. The order will be processed immediately after creation and payment will be deducted your Stripe balance.
    public func postClimate(product: String, amount: Int?, beneficiary: PostClimateOrdersRequestBodyBeneficiary?, currency: String?, expand: [String]?, metadata: [String: String]?, metricTons: String?) async throws -> ClimateOrder {
        return try await V1ClimateOrdersMethods.postClimateOrders(config: config, product: product, amount: amount, beneficiary: beneficiary, currency: currency, expand: expand, metadata: metadata, metricTons: metricTons)
    }

/// Retrieves the details of a Climate order by its unique identifier. Use `expand` to request expanded response fields when additional object details are needed.
    ///
    /// Retrieves the details of a Climate order object with the given ID.
    public func getClimateOrder(order: String, expand: [String]?) async throws -> ClimateOrder {
        return try await V1ClimateOrdersMethods.getClimateOrdersOrder(config: config, order: order, expand: expand)
    }

/// Updates the specified order by setting the values of the parameters passed.
    public func postClimateOrder(order: String, beneficiary: PostClimateOrdersOrderRequestBodyBeneficiary?, expand: [String]?, metadata: [String: String]?) async throws -> ClimateOrder {
        return try await V1ClimateOrdersMethods.postClimateOrdersOrder(config: config, order: order, beneficiary: beneficiary, expand: expand, metadata: metadata)
    }
}

public class V1ClimateProductsNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

/// Lists all available Climate products. Use `starting_after` and `ending_before` to navigate between result pages, and `limit` to control the number of products returned.
    ///
    /// Lists all available Climate product objects.
    public func getClimate(endingBefore: String?, expand: [String]?, limit: Int?, startingAfter: String?) async throws -> GetClimateProductsResponse {
        return try await V1ClimateProductsMethods.getClimateProducts(config: config, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter)
    }

/// Retrieves the details of a Climate product by its unique identifier. Use `expand` to request expanded response fields when you need additional object details beyond the standard product representation.
    ///
    /// Retrieves the details of a Climate product with the given ID.
    public func getClimateProduct(product: String, expand: [String]?) async throws -> ClimateProduct {
        return try await V1ClimateProductsMethods.getClimateProductsProduct(config: config, product: product, expand: expand)
    }
}

public class V1ClimateSuppliersNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

/// Lists all available Climate suppliers. Use `starting_after` and `ending_before` to navigate between result pages, and `limit` to control the number of suppliers returned.
    ///
    /// Lists all available Climate supplier objects.
    public func getClimate(endingBefore: String?, expand: [String]?, limit: Int?, startingAfter: String?) async throws -> GetClimateSuppliersResponse {
        return try await V1ClimateSuppliersMethods.getClimateSuppliers(config: config, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter)
    }

/// Retrieves a Climate supplier by its unique identifier. Use `expand` to request expanded response fields when you need additional supplier details.
    ///
    /// Retrieves a Climate supplier object.
    public func getClimateSupplier(supplier: String, expand: [String]?) async throws -> ClimateSupplier {
        return try await V1ClimateSuppliersMethods.getClimateSuppliersSupplier(config: config, supplier: supplier, expand: expand)
    }
}

public class V1ClimateNamespace {
    public let orders: V1ClimateOrdersNamespace
    public let products: V1ClimateProductsNamespace
    public let suppliers: V1ClimateSuppliersNamespace
    public init(config: ClientConfig) {
        orders = V1ClimateOrdersNamespace(config: config)
        products = V1ClimateProductsNamespace(config: config)
        suppliers = V1ClimateSuppliersNamespace(config: config)
    }
}

public class V1ConfirmationTokensNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

/// Retrieves an existing ConfirmationToken by its unique identifier. Use `expand` to request expanded response fields when retrieving data for a PaymentIntent or SetupIntent confirmation flow.
    ///
    /// Retrieves an existing ConfirmationToken object
    public func getConfirmationToken(confirmationToken: String, expand: [String]?) async throws -> ConfirmationToken {
        return try await V1ConfirmationTokensMethods.getConfirmationTokensConfirmationToken(config: config, confirmationToken: confirmationToken, expand: expand)
    }
}

public class V1CountrySpecsNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

/// Lists all Country Spec objects available in the API. Use `starting_after` and `ending_before` to navigate between result pages, and `limit` to control the number of country specifications returned.
    ///
    /// Lists all Country Spec objects available in the API.
    public func get(endingBefore: String?, expand: [String]?, limit: Int?, startingAfter: String?) async throws -> GetCountrySpecsResponse {
        return try await V1CountrySpecsMethods.getCountrySpecs(config: config, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter)
    }

/// Retrieves the country specification for a specified country code. Use `expand` to request additional response fields when needed, and ensure `country` identifies the country specification you want to retrieve.
    ///
    /// Returns a Country Spec for a given Country code.
    public func getCountry(country: String, expand: [String]?) async throws -> CountrySpec {
        return try await V1CountrySpecsMethods.getCountrySpecsCountry(config: config, country: country, expand: expand)
    }
}

public class V1CouponsNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

/// Lists coupons available to your account. Filter results by creation time and use `starting_after` or `ending_before` to navigate the paginated collection; set `limit` to control the page size.
    ///
    /// Returns a list of your coupons.
    public func get(created: GetCouponsParameter?, endingBefore: String?, expand: [String]?, limit: Int?, startingAfter: String?) async throws -> GetCouponsResponse {
        return try await V1CouponsMethods.getCoupons(config: config, created: created, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter)
    }

/// You can create coupons easily via the coupon management page of the Stripe dashboard. Coupon creation is also accessible via the API if you need to create coupons on the fly. A coupon has either a percent_off or an amount_off and currency . If you set an amount_off , that amount will be subtracted from any invoice’s subtotal. For example, an invoice with a subtotal of <currency>100</currency> will have a final total of <currency>0</currency> if a coupon with an amount_off of <amount>200</amount> is applied to it and an invoice with a subtotal of <currency>300</currency> will have a final total of <currency>100</currency> if a coupon with an amount_off of <amount>200</amount> is applied to it.
    public func post(options: V1CouponsMethods.PostCouponsOptions) async throws -> Coupon {
        return try await V1CouponsMethods.postCoupons(config: config, options: options)
    }

/// Deletes a coupon so that new customers can no longer redeem it. Deleting the coupon does not affect customers who have already applied it, so use this operation only when you want to prevent future redemptions.
    ///
    /// You can delete coupons via the coupon management page of the Stripe dashboard. However, deleting a coupon does not affect any customers who have already applied the coupon; it means that new customers can’t redeem the coupon. You can also delete coupons via the API.
    public func deleteCoupon(coupon: String) async throws -> DeletedCoupon {
        return try await V1CouponsMethods.deleteCouponsCoupon(config: config, coupon: coupon)
    }

/// Retrieves a coupon by its identifier. Use `expand` when you need selected response fields expanded, and provide the identifier of the coupon you want to inspect.
    ///
    /// Retrieves the coupon with the given ID.
    public func getCoupon(coupon: String, expand: [String]?) async throws -> Coupon {
        return try await V1CouponsMethods.getCouponsCoupon(config: config, coupon: coupon, expand: expand)
    }

/// Updates the metadata and display name of an existing coupon. Supply only editable fields such as `name`, `metadata`, or amount-based `currency_options`; coupon currency, duration, and discount amount details cannot be changed.
    ///
    /// Updates the metadata of a coupon. Other coupon details (currency, duration, amount_off) are, by design, not editable.
    public func postCoupon(coupon: String, currencyOptions: [String: PostCouponsCouponRequestBodyCurrencyOptionsValue]?, expand: [String]?, metadata: PostCouponsCouponRequestBodyMetadata?, name: String?) async throws -> Coupon {
        return try await V1CouponsMethods.postCouponsCoupon(config: config, coupon: coupon, currencyOptions: currencyOptions, expand: expand, metadata: metadata, name: name)
    }
}

public class V1CreditNotesPreviewLinesNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

/// Lists the line items for a credit note preview. Provide the same preview inputs used to define the credit note, including `invoice` and one of `amount`, `lines`, or `shipping_cost`, then use the pagination cursors to retrieve additional items.
    ///
    /// When retrieving a credit note preview, you’ll get a lines property containing the first handful of those items. This URL you can retrieve the full (paginated) list of line items.
    public func getCreditNotesPreview(options: V1CreditNotesPreviewLinesMethods.GetCreditNotesPreviewLinesOptions) async throws -> GetCreditNotesPreviewLinesResponse {
        return try await V1CreditNotesPreviewLinesMethods.getCreditNotesPreviewLines(config: config, options: options)
    }
}

public class V1CreditNotesPreviewNamespace {
    let config: ClientConfig
    public let lines: V1CreditNotesPreviewLinesNamespace
    public init(config: ClientConfig) {
        self.config = config
        lines = V1CreditNotesPreviewLinesNamespace(config: config)
    }

/// Previews a credit note for an invoice without creating it. Provide `invoice` and at least one of `amount`, `lines`, or `shipping_cost`, then use the remaining parameters to define credits, refunds, shipping, customer-facing text, and metadata.
    ///
    /// Get a preview of a credit note without creating it.
    public func getCreditNotes(options: V1CreditNotesPreviewMethods.GetCreditNotesPreviewOptions) async throws -> CreditNote {
        return try await V1CreditNotesPreviewMethods.getCreditNotesPreview(config: config, options: options)
    }
}
