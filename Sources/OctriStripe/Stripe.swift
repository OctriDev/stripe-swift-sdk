// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

public class V1AccountNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

/// Retrieves the details of the authenticated user's account. Use `expand` to include additional nested fields in the response when the default account representation is insufficient.
    ///
    /// Retrieves the details of an account.
    public func get(expand: [String]?) async throws -> Account {
        return try await V1AccountMethods.getAccount(config: config, expand: expand)
    }
}

public class V1AccountLinksNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

/// Creates an AccountLink object that includes a single-use Stripe URL that the platform can redirect their user to in order to take them through the Connect Onboarding flow.
    public func post(account: String, type: PostAccountLinksRequestBodyType, collect: PostAccountLinksRequestBodyCollect?, collectionOptions: PostAccountLinksRequestBodyCollectionOptions?, expand: [String]?, refreshUrl: String?, returnUrl: String?) async throws -> AccountLink {
        return try await V1AccountLinksMethods.postAccountLinks(config: config, account: account, type: type, collect: collect, collectionOptions: collectionOptions, expand: expand, refreshUrl: refreshUrl, returnUrl: returnUrl)
    }
}

public class V1AccountSessionsNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

/// Creates a AccountSession object that includes a single-use token that the platform can use on their front-end to grant client-side API access.
    public func post(account: String, components: PostAccountSessionsRequestBodyComponents, expand: [String]?) async throws -> AccountSession {
        return try await V1AccountSessionsMethods.postAccountSessions(config: config, account: account, components: components, expand: expand)
    }
}

public class V1AccountsBankAccountsNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

/// Creates an external account for a connected account. Supply a bank-account token or bank-account details, and optionally mark the account as the default for its currency or attach metadata.
    ///
    /// Create an external account for a given account.
    public func postAccountsAccount(account: String, bankAccount: PostAccountsAccountBankAccountsRequestBodyBankAccount?, defaultForCurrency: Bool?, expand: [String]?, externalAccount: String?, metadata: [String: String]?) async throws -> ExternalAccount {
        return try await V1AccountsBankAccountsMethods.postAccountsAccountBankAccounts(config: config, account: account, bankAccount: bankAccount, defaultForCurrency: defaultForCurrency, expand: expand, externalAccount: externalAccount, metadata: metadata)
    }

/// Deletes a specified bank account or card used as an external account for a connected account. Provide the parent account and external account identifiers to remove the selected external account. The response identifies the deleted object and its deletion state.
    ///
    /// Delete a specified external account for a given account.
    public func deleteAccountsAccountId(account: String, id: String) async throws -> DeletedExternalAccount {
        return try await V1AccountsBankAccountsMethods.deleteAccountsAccountBankAccountsId(config: config, account: account, id: id)
    }

/// Retrieves a specific external account attached to a connected account. Use `account` to identify the connected account and `id` to identify the bank account or card, with `expand` available for additional nested fields.
    ///
    /// Retrieve a specified external account for a given account.
    public func getAccountsAccountId(account: String, id: String, expand: [String]?) async throws -> ExternalAccount {
        return try await V1AccountsBankAccountsMethods.getAccountsAccountBankAccountsId(config: config, account: account, id: id, expand: expand)
    }

/// Updates a bank account belonging to a connected account. Use the body to change editable account metadata, holder details, address information, or the default currency account; submit no changes to re-enable a disabled bank account. Updates are available when the account's requirement collection is set to application.
    ///
    /// Updates the metadata, account holder name, account holder type of a bank account belonging to a connected account and optionally sets it as the default for its currency. Other bank account details are not editable by design. You can only update bank accounts when account.controller.requirement_collection is application , which includes Custom accounts. You can re-enable a disabled bank account by performing an update call without providing any arguments or changes.
    public func postAccountsAccountId(options: V1AccountsBankAccountsMethods.PostAccountsAccountBankAccountsIdOptions) async throws -> ExternalAccount {
        return try await V1AccountsBankAccountsMethods.postAccountsAccountBankAccountsId(config: config, options: options)
    }
}

public class V1AccountsCapabilitiesNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

/// Lists the capabilities associated with an account. Results are sorted by creation date, with the most recently created capability first. Use `expand` when you need selected response fields expanded.
    ///
    /// Returns a list of capabilities associated with the account. The capabilities are returned sorted by creation date, with the most recent capability appearing first.
    public func getAccountsAccount(account: String, expand: [String]?) async throws -> GetAccountsAccountCapabilitiesResponse {
        return try await V1AccountsCapabilitiesMethods.getAccountsAccountCapabilities(config: config, account: account, expand: expand)
    }

/// Retrieves information about a specific capability associated with an account. Provide the account and capability identifiers to inspect its request state, status, requirements, and future requirements. Use `expand` to include selected response fields inline.
    ///
    /// Retrieves information about the specified Account Capability.
    public func getAccountsAccountCapability(account: String, capability: String, expand: [String]?) async throws -> Capability {
        return try await V1AccountsCapabilitiesMethods.getAccountsAccountCapabilitiesCapability(config: config, account: account, capability: capability, expand: expand)
    }

/// Updates an account capability by requesting it or removing the request for it. Set `requested` to true to request the capability or false to remove the request when supported. Activation can be delayed, and the response includes requirements when additional information is needed.
    ///
    /// Updates an existing Account Capability. Request or remove a capability by updating its requested parameter.
    public func postAccountsAccountCapability(account: String, capability: String, expand: [String]?, requested: Bool?) async throws -> Capability {
        return try await V1AccountsCapabilitiesMethods.postAccountsAccountCapabilitiesCapability(config: config, account: account, capability: capability, expand: expand, requested: requested)
    }
}

public class V1AccountsExternalAccountsNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

/// Lists the external accounts attached to a connected account. Results can include bank accounts and cards, and you can filter them by object type. Use cursor parameters to retrieve adjacent pages and `limit` to control the page size.
    ///
    /// List external accounts for an account.
    public func getAccountsAccount(account: String, endingBefore: String?, expand: [String]?, limit: Int?, object: GetAccountsAccountExternalAccountsParameter?, startingAfter: String?) async throws -> GetAccountsAccountExternalAccountsResponse {
        return try await V1AccountsExternalAccountsMethods.getAccountsAccountExternalAccounts(config: config, account: account, endingBefore: endingBefore, expand: expand, limit: limit, object: object, startingAfter: startingAfter)
    }

/// Creates an external account for a connected account. Provide an external account token or bank account details, and optionally make the new account the default for its currency. You can also attach metadata and request expanded response fields.
    ///
    /// Create an external account for a given account.
    public func postAccountsAccount(account: String, bankAccount: PostAccountsAccountExternalAccountsRequestBodyBankAccount?, defaultForCurrency: Bool?, expand: [String]?, externalAccount: String?, metadata: [String: String]?) async throws -> ExternalAccount {
        return try await V1AccountsExternalAccountsMethods.postAccountsAccountExternalAccounts(config: config, account: account, bankAccount: bankAccount, defaultForCurrency: defaultForCurrency, expand: expand, externalAccount: externalAccount, metadata: metadata)
    }

/// Deletes a specified external account for a connected account. Provide the parent account and external account identifiers to remove the selected bank account or card. The response identifies the deleted object and its deletion state.
    ///
    /// Delete a specified external account for a given account.
    public func deleteAccountsAccountId(account: String, id: String) async throws -> DeletedExternalAccount {
        return try await V1AccountsExternalAccountsMethods.deleteAccountsAccountExternalAccountsId(config: config, account: account, id: id)
    }

/// Retrieves a specified external account for a connected account. The external account can be a bank account or card identified by `id`. Use `expand` to include selected response fields in the returned object.
    ///
    /// Retrieve a specified external account for a given account.
    public func getAccountsAccountId(account: String, id: String, expand: [String]?) async throws -> ExternalAccount {
        return try await V1AccountsExternalAccountsMethods.getAccountsAccountExternalAccountsId(config: config, account: account, id: id, expand: expand)
    }

/// Updates a bank account belonging to a connected account. Use the body to change editable account metadata, holder details, address information, or the default currency account; submit no changes to re-enable a disabled bank account. Updates are available when the account's requirement collection is set to application.
    ///
    /// Updates the metadata, account holder name, account holder type of a bank account belonging to a connected account and optionally sets it as the default for its currency. Other bank account details are not editable by design. You can only update bank accounts when account.controller.requirement_collection is application , which includes Custom accounts. You can re-enable a disabled bank account by performing an update call without providing any arguments or changes.
    public func postAccountsAccountId(options: V1AccountsExternalAccountsMethods.PostAccountsAccountExternalAccountsIdOptions) async throws -> ExternalAccount {
        return try await V1AccountsExternalAccountsMethods.postAccountsAccountExternalAccountsId(config: config, options: options)
    }
}

public class V1AccountsLoginLinksNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

/// Creates a single-use login link for a connected account to access the Express Dashboard. Use `account` for an account connected to your platform and using the Express Dashboard; optionally use `expand` to expand response fields. The response contains the login URL and its creation time.
    ///
    /// Creates a login link for a connected account to access the Express Dashboard. You can only create login links for accounts that use the Express Dashboard and are connected to your platform .
    public func postAccountsAccount(account: String, expand: [String]?) async throws -> LoginLink {
        return try await V1AccountsLoginLinksMethods.postAccountsAccountLoginLinks(config: config, account: account, expand: expand)
    }
}

public class V1AccountsPeopleNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

/// Lists people associated with an account's legal entity. Use `relationship` to filter people and `starting_after` or `ending_before` to navigate the results; results are sorted with the most recently created people first. Use `limit` to control the page size.
    ///
    /// Returns a list of people associated with the account’s legal entity. The people are returned sorted by creation date, with the most recent people appearing first.
    public func getAccountsAccount(account: String, endingBefore: String?, expand: [String]?, limit: Int?, relationship: GetAccountsAccountPeopleParameter?, startingAfter: String?) async throws -> GetAccountsAccountPeopleResponse {
        return try await V1AccountsPeopleMethods.getAccountsAccountPeople(config: config, account: account, endingBefore: endingBefore, expand: expand, limit: limit, relationship: relationship, startingAfter: startingAfter)
    }

/// Creates a new person associated with an account's legal entity. Supply the person's identifying, address, relationship, date-of-birth, and verification information as needed, and use `expand` to expand response fields. Nested address, date-of-birth, document, and acceptance data must use the structures defined for those fields.
    ///
    /// Creates a new person.
    public func postAccountsAccount(options: V1AccountsPeopleMethods.PostAccountsAccountPeopleOptions) async throws -> Person {
        return try await V1AccountsPeopleMethods.postAccountsAccountPeople(config: config, options: options)
    }

/// Deletes a person's relationship to an account's legal entity. Use `account` and `person` to identify the relationship to remove. You cannot delete the account's representative, and integrations using the `executive` parameter cannot delete the only verified executive on file.
    ///
    /// Deletes an existing person’s relationship to the account’s legal entity. Any person with a relationship for an account can be deleted through the API, except if the person is the representative . If your integration is using the executive parameter, you cannot delete the only verified executive on file.
    public func deleteAccountsAccountPerson(account: String, person: String) async throws -> DeletedPerson {
        return try await V1AccountsPeopleMethods.deleteAccountsAccountPeoplePerson(config: config, account: account, person: person)
    }

/// Retrieves a person associated with an account's legal entity. Provide both `account` and `person` to identify the account-person relationship, and optionally use `expand` for additional response fields. The response contains the person's account, identifier, creation time, and available legal-entity information.
    ///
    /// Retrieves an existing person.
    public func getAccountsAccountPerson(account: String, person: String, expand: [String]?) async throws -> Person {
        return try await V1AccountsPeopleMethods.getAccountsAccountPeoplePerson(config: config, account: account, person: person, expand: expand)
    }

/// Updates an existing person associated with an account's legal entity. Identify the relationship with `account` and `person`, then submit only the person details you want to change. Use nested address, date-of-birth, document, relationship, and verification structures according to their defined schemas.
    ///
    /// Updates an existing person.
    public func postAccountsAccountPerson(options: V1AccountsPeopleMethods.PostAccountsAccountPeoplePersonOptions) async throws -> Person {
        return try await V1AccountsPeopleMethods.postAccountsAccountPeoplePerson(config: config, options: options)
    }
}
