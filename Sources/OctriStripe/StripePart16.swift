// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

public class V1IssuingDisputesNamespace {
    let config: ClientConfig
    public let submit: V1IssuingDisputesSubmitNamespace
    public init(config: ClientConfig) {
        self.config = config
        submit = V1IssuingDisputesSubmitNamespace(config: config)
    }

    /// Lists Issuing disputes in descending creation order, with the most recently created dispute first. Use
    /// `created`, `status`, and `transaction` to filter results, and use `starting_after` or `ending_before` to
    /// navigate between pages. Use `limit` to control page size and `expand` to request expanded response fields.
    ///
    /// Returns a list of Issuing Dispute objects. The objects are sorted in descending order by creation date, with the
    /// most recently created object appearing first.
    public func getIssuing(
        created: GetIssuingDisputesParameter?,
        endingBefore: String?,
        expand: [String]?,
        limit: Int?,
        startingAfter: String?,
        status: GetIssuingDisputesParameterXfda2f4f2?,
        transaction: String?
    ) async throws -> GetIssuingDisputesResponse {
        try await V1IssuingDisputesMethods.getIssuingDisputes(
            config: config,
            created: created,
            endingBefore: endingBefore,
            expand: expand,
            limit: limit,
            startingAfter: startingAfter,
            status: status,
            transaction: transaction
        )
    }

    /// Creates an Issuing Dispute object. Individual pieces of evidence within the evidence object are optional at this
    /// point. Stripe only validates that required evidence is present during submission. Refer to Dispute reasons and
    /// evidence for more details about evidence requirements.
    public func postIssuing(
        amount: Int?,
        evidence: PostIssuingDisputesRequestBodyEvidence?,
        expand: [String]?,
        metadata: [String: String]?,
        transaction: String?,
        treasury: PostIssuingDisputesRequestBodyTreasury?
    ) async throws -> IssuingDispute {
        try await V1IssuingDisputesMethods.postIssuingDisputes(
            config: config,
            amount: amount,
            evidence: evidence,
            expand: expand,
            metadata: metadata,
            transaction: transaction,
            treasury: treasury
        )
    }

    /// Retrieves a single Issuing dispute by its identifier. Use `expand` when you need selected fields expanded in the
    /// response rather than returned as references. The `dispute` value must be no longer than 5000 characters.
    ///
    /// Retrieves an Issuing Dispute object.
    public func getIssuingDispute(dispute: String, expand: [String]?) async throws -> IssuingDispute {
        try await V1IssuingDisputesMethods.getIssuingDisputesDispute(config: config, dispute: dispute, expand: expand)
    }

    /// Updates an existing Issuing dispute with the values you provide, leaving omitted properties unchanged. Use
    /// `evidence` to add or clear evidence fields, and use `amount` to set the disputed amount in the card's smallest
    /// currency unit. Pass an empty string for an evidence property or `metadata` value when the schema permits
    /// clearing it.
    ///
    /// Updates the specified Issuing Dispute object by setting the values of the parameters passed. Any parameters not
    /// provided will be left unchanged. Properties on the evidence object can be unset by passing in an empty string.
    public func postIssuingDispute(
        dispute: String,
        amount: Int?,
        evidence: PostIssuingDisputesDisputeRequestBodyEvidence?,
        expand: [String]?,
        metadata: PostIssuingDisputesDisputeRequestBodyMetadata?
    ) async throws -> IssuingDispute {
        try await V1IssuingDisputesMethods.postIssuingDisputesDispute(
            config: config,
            dispute: dispute,
            amount: amount,
            evidence: evidence,
            expand: expand,
            metadata: metadata
        )
    }
}

public class V1IssuingPersonalizationDesignsNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// Lists personalization designs in descending creation order, with the most recently created design first. Filter
    /// results by `lookup_keys`, `preferences`, or `status`, and use cursor parameters to navigate between pages. Use
    /// `limit` to control the number of designs returned and `expand` to request expanded fields.
    ///
    /// Returns a list of personalization design objects. The objects are sorted in descending order by creation date,
    /// with the most recently created object appearing first.
    public func getIssuing(
        endingBefore: String?,
        expand: [String]?,
        limit: Int?,
        lookupKeys: [String]?,
        preferences: GetIssuingPersonalizationDesignsParameter?,
        startingAfter: String?,
        status: GetIssuingPersonalizationDesignsParameterX1136c1a1?
    ) async throws -> GetIssuingPersonalizationDesignsResponse {
        try await V1IssuingPersonalizationDesignsMethods.getIssuingPersonalizationDesigns(
            config: config,
            endingBefore: endingBefore,
            expand: expand,
            limit: limit,
            lookupKeys: lookupKeys,
            preferences: preferences,
            startingAfter: startingAfter,
            status: status
        )
    }

    /// Creates a personalization design that groups a physical bundle with optional card logo and carrier text
    /// settings. Supply `physical_bundle` and optionally set `card_logo`, `carrier_text`, `lookup_key`, `name`,
    /// `metadata`, or `preferences`. A 200 response returns the created personalization design with its generated
    /// identifier and current status.
    ///
    /// Creates a personalization design object.
    public func postIssuing(options: V1IssuingPersonalizationDesignsMethods
        .PostIssuingPersonalizationDesignsOptions) async throws -> IssuingPersonalizationDesign {
        try await V1IssuingPersonalizationDesignsMethods.postIssuingPersonalizationDesigns(
            config: config,
            options: options
        )
    }

    /// Retrieves a single personalization design by its identifier. Use `expand` to request selected fields in expanded
    /// form. The returned design includes its physical bundle, preferences, metadata, rejection reasons, and current
    /// status.
    ///
    /// Retrieves a personalization design object.
    public func getIssuingPersonalizationDesign(
        personalizationDesign: String,
        expand: [String]?
    ) async throws -> IssuingPersonalizationDesign {
        try await V1IssuingPersonalizationDesignsMethods.getIssuingPersonalizationDesignsPersonalizationDesign(
            config: config,
            personalizationDesign: personalizationDesign,
            expand: expand
        )
    }

    /// Updates an existing personalization design with the fields you provide, leaving omitted fields unchanged. Use
    /// empty strings to clear supported values such as `card_logo`, `lookup_key`, or `name`, and use
    /// `transfer_lookup_key` to move a lookup key atomically. Supply `preferences.is_default` when updating the
    /// preferences object.
    ///
    /// Updates a card personalization object.
    public func postIssuingPersonalizationDesign(options: V1IssuingPersonalizationDesignsMethods
        .PostIssuingPersonalizationDesignsPersonalizationDesignOptions) async throws -> IssuingPersonalizationDesign {
        try await V1IssuingPersonalizationDesignsMethods.postIssuingPersonalizationDesignsPersonalizationDesign(
            config: config,
            options: options
        )
    }
}

public class V1IssuingPhysicalBundlesNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// Lists physical bundles in descending creation order, with the most recently created bundle first. Filter results
    /// by `status` or `type`, and use `starting_after` or `ending_before` to navigate between pages. Use `limit` to
    /// control page size and `expand` to request expanded fields.
    ///
    /// Returns a list of physical bundle objects. The objects are sorted in descending order by creation date, with the
    /// most recently created object appearing first.
    public func getIssuing(
        endingBefore: String?,
        expand: [String]?,
        limit: Int?,
        startingAfter: String?,
        status: GetIssuingPhysicalBundlesParameter?,
        type: GetIssuingPhysicalBundlesParameterX94a3bca5?
    ) async throws -> GetIssuingPhysicalBundlesResponse {
        try await V1IssuingPhysicalBundlesMethods.getIssuingPhysicalBundles(
            config: config,
            endingBefore: endingBefore,
            expand: expand,
            limit: limit,
            startingAfter: startingAfter,
            status: status,
            type: type
        )
    }

    /// Retrieves an Issuing physical bundle object by its identifier. Use `physical_bundle` to select the bundle and
    /// `expand` to request expanded response fields when needed.
    ///
    /// Retrieves a physical bundle object.
    public func getIssuingPhysicalBundle(
        physicalBundle: String,
        expand: [String]?
    ) async throws -> IssuingPhysicalBundle {
        try await V1IssuingPhysicalBundlesMethods.getIssuingPhysicalBundlesPhysicalBundle(
            config: config,
            physicalBundle: physicalBundle,
            expand: expand
        )
    }
}

public class V1IssuingSettlementsNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// Retrieves an Issuing settlement object by its identifier. Use `settlement` to select the settlement and `expand`
    /// to request expanded response fields when needed.
    ///
    /// Retrieves an Issuing Settlement object.
    public func getIssuingSettlement(settlement: String, expand: [String]?) async throws -> IssuingSettlement {
        try await V1IssuingSettlementsMethods.getIssuingSettlementsSettlement(
            config: config,
            settlement: settlement,
            expand: expand
        )
    }

    /// Updates an Issuing settlement with the values supplied in the request. Omitted fields remain unchanged, while
    /// `metadata` lets you attach or remove string key-value pairs and `expand` controls expanded response fields.
    ///
    /// Updates the specified Issuing Settlement object by setting the values of the parameters passed. Any parameters
    /// not provided will be left unchanged.
    public func postIssuingSettlement(
        settlement: String,
        expand: [String]?,
        metadata: [String: String]?
    ) async throws -> IssuingSettlement {
        try await V1IssuingSettlementsMethods.postIssuingSettlementsSettlement(
            config: config,
            settlement: settlement,
            expand: expand,
            metadata: metadata
        )
    }
}

public class V1IssuingTokensNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// Lists Issuing tokens associated with a specified card. Use `card` to scope the results, optional filters to
    /// narrow them by creation time or status, and cursor parameters to navigate the list in pages.
    ///
    /// Lists all Issuing Token objects for a given card.
    public func getIssuing(
        card: String,
        created: GetIssuingTokensParameter?,
        endingBefore: String?,
        expand: [String]?,
        limit: Int?,
        startingAfter: String?,
        status: GetIssuingTokensParameterX95ebd4ae?
    ) async throws -> GetIssuingTokensResponse {
        try await V1IssuingTokensMethods.getIssuingTokens(
            config: config,
            card: card,
            created: created,
            endingBefore: endingBefore,
            expand: expand,
            limit: limit,
            startingAfter: startingAfter,
            status: status
        )
    }

    /// Retrieves an Issuing token by its identifier. Use `token` to select the token and `expand` to request expanded
    /// response fields when needed.
    ///
    /// Retrieves an Issuing Token object.
    public func getIssuingToken(token: String, expand: [String]?) async throws -> IssuingToken {
        try await V1IssuingTokensMethods.getIssuingTokensToken(config: config, token: token, expand: expand)
    }

    /// Updates the status of an Issuing token. Supply `status` to select the token state, and use `expand` when the
    /// response should include expanded fields.
    ///
    /// Attempts to update the specified Issuing Token object to the status specified.
    public func postIssuingToken(
        token: String,
        status: PostIssuingTokensTokenRequestBodyStatus,
        expand: [String]?
    ) async throws -> IssuingToken {
        try await V1IssuingTokensMethods.postIssuingTokensToken(
            config: config,
            token: token,
            status: status,
            expand: expand
        )
    }
}

public class V1IssuingTransactionsNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// Lists Issuing transactions in descending creation order, with the most recently created transaction first.
    /// Filter results by card, cardholder, creation time, or transaction `type`, and use cursor parameters to navigate
    /// additional pages.
    ///
    /// Returns a list of Issuing Transaction objects. The objects are sorted in descending order by creation date, with
    /// the most recently created object appearing first.
    public func getIssuing(options: V1IssuingTransactionsMethods
        .GetIssuingTransactionsOptions) async throws -> GetIssuingTransactionsResponse {
        try await V1IssuingTransactionsMethods.getIssuingTransactions(config: config, options: options)
    }

    /// Retrieves an Issuing transaction by its identifier. Use `transaction` to select the transaction and `expand` to
    /// request expanded response fields when needed.
    ///
    /// Retrieves an Issuing Transaction object.
    public func getIssuingTransaction(transaction: String, expand: [String]?) async throws -> IssuingTransaction {
        try await V1IssuingTransactionsMethods.getIssuingTransactionsTransaction(
            config: config,
            transaction: transaction,
            expand: expand
        )
    }

    /// Updates an Issuing transaction with the values supplied in the request. Omitted fields remain unchanged, while
    /// `metadata` lets you attach string key-value pairs or clear metadata and `expand` controls expanded response
    /// fields.
    ///
    /// Updates the specified Issuing Transaction object by setting the values of the parameters passed. Any parameters
    /// not provided will be left unchanged.
    public func postIssuingTransaction(
        transaction: String,
        expand: [String]?,
        metadata: PostIssuingTransactionsTransactionRequestBodyMetadata?
    ) async throws -> IssuingTransaction {
        try await V1IssuingTransactionsMethods.postIssuingTransactionsTransaction(
            config: config,
            transaction: transaction,
            expand: expand,
            metadata: metadata
        )
    }
}

public class V1IssuingNamespace {
    public let authorizations: V1IssuingAuthorizationsNamespace
    public let cardholders: V1IssuingCardholdersNamespace
    public let cards: V1IssuingCardsNamespace
    public let disputes: V1IssuingDisputesNamespace
    public let personalizationDesigns: V1IssuingPersonalizationDesignsNamespace
    public let physicalBundles: V1IssuingPhysicalBundlesNamespace
    public let settlements: V1IssuingSettlementsNamespace
    public let tokens: V1IssuingTokensNamespace
    public let transactions: V1IssuingTransactionsNamespace
    public init(config: ClientConfig) {
        authorizations = V1IssuingAuthorizationsNamespace(config: config)
        cardholders = V1IssuingCardholdersNamespace(config: config)
        cards = V1IssuingCardsNamespace(config: config)
        disputes = V1IssuingDisputesNamespace(config: config)
        personalizationDesigns = V1IssuingPersonalizationDesignsNamespace(config: config)
        physicalBundles = V1IssuingPhysicalBundlesNamespace(config: config)
        settlements = V1IssuingSettlementsNamespace(config: config)
        tokens = V1IssuingTokensNamespace(config: config)
        transactions = V1IssuingTransactionsNamespace(config: config)
    }
}

public class V1LinkAccountSessionsNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// To launch the Financial Connections authorization flow, create a Session . The session’s client_secret can be
    /// used to launch the flow using Stripe.js.
    public func post(options: V1LinkAccountSessionsMethods
        .PostLinkAccountSessionsOptions) async throws -> FinancialConnectionsSession {
        try await V1LinkAccountSessionsMethods.postLinkAccountSessions(config: config, options: options)
    }

    /// Retrieves the details of a Financial Connections Session. Use `session` to identify the session whose collected
    /// account data and client-side authentication details you need, and use `expand` to request expanded response
    /// fields.
    ///
    /// Retrieves the details of a Financial Connections Session
    public func getSession(session: String, expand: [String]?) async throws -> FinancialConnectionsSession {
        try await V1LinkAccountSessionsMethods.getLinkAccountSessionsSession(
            config: config,
            session: session,
            expand: expand
        )
    }
}
