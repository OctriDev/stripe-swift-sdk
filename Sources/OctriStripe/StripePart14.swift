// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

public class V1InvoicesSearchNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// Searches invoices using the supplied search query. Use `query` with the invoice search query language, and use
    /// `page` and `limit` to paginate results; search data can lag behind recent changes and is unavailable to
    /// merchants in India.
    ///
    /// Search for invoices you’ve previously created using Stripe’s Search Query Language. Don’t use search in
    /// read-after-write flows where strict consistency is necessary. Under normal operating conditions, data is
    /// searchable in less than a minute. Occasionally, propagation of new or updated data can be up to an hour behind
    /// during outages. Search functionality is not available to merchants in India.
    public func getInvoices(
        query: String,
        expand: [String]?,
        limit: Int?,
        page: String?
    ) async throws -> GetInvoicesSearchResponse {
        try await V1InvoicesSearchMethods.getInvoicesSearch(
            config: config,
            query: query,
            expand: expand,
            limit: limit,
            page: page
        )
    }

    /// Searches invoices using the supplied search query. Use `query` with the invoice search query language, and use
    /// `page` and `limit` to paginate results; search data can lag behind recent changes and is unavailable to
    /// merchants in India.
    ///
    /// Search for invoices you’ve previously created using Stripe’s Search Query Language. Don’t use search in
    /// read-after-write flows where strict consistency is necessary. Under normal operating conditions, data is
    /// searchable in less than a minute. Occasionally, propagation of new or updated data can be up to an hour behind
    /// during outages. Search functionality is not available to merchants in India.
    public func getInvoicesPaginated(
        query: String,
        expand: [String]?,
        limit: Int?,
        page: String?
    ) -> AsyncThrowingStream<Invoice, Swift.Error> {
        V1InvoicesSearchMethods.getInvoicesSearchPaginated(
            config: config,
            query: query,
            expand: expand,
            limit: limit,
            page: page
        )
    }
}

public class V1InvoicesAddLinesNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// Adds multiple line items to an invoice in one request. Use `lines` to provide the items and `invoice_metadata`
    /// to update invoice metadata; the invoice must still be in draft state when you add them.
    ///
    /// Adds multiple line items to an invoice. This is only possible when an invoice is still a draft.
    public func postInvoicesInvoice(
        invoice: String,
        lines: [PostInvoicesInvoiceAddLinesRequestBodyLinesItem],
        expand: [String]?,
        invoiceMetadata: PostInvoicesInvoiceAddLinesRequestBodyInvoiceMetadata?
    ) async throws -> Invoice {
        try await V1InvoicesAddLinesMethods.postInvoicesInvoiceAddLines(
            config: config,
            invoice: invoice,
            lines: lines,
            expand: expand,
            invoiceMetadata: invoiceMetadata
        )
    }
}

public class V1InvoicesAttachPaymentNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// Attaches a PaymentIntent or an out-of-band payment to an invoice. Use `payment_intent` or `payment_record` to
    /// add the payment to the invoice's payments, and a succeeded PaymentIntent credits the invoice immediately or when
    /// it succeeds later.
    ///
    /// Attaches a PaymentIntent or an Out of Band Payment to the invoice, adding it to the list of payments . For the
    /// PaymentIntent, when the PaymentIntent’s status changes to succeeded , the payment is credited to the invoice,
    /// increasing its amount_paid . When the invoice is fully paid, the invoice’s status becomes paid . If the
    /// PaymentIntent’s status is already succeeded when it’s attached, it’s credited to the invoice immediately. See:
    /// Partial payments to learn more.
    public func postInvoicesInvoice(
        invoice: String,
        expand: [String]?,
        paymentIntent: String?,
        paymentRecord: String?
    ) async throws -> Invoice {
        try await V1InvoicesAttachPaymentMethods.postInvoicesInvoiceAttachPayment(
            config: config,
            invoice: invoice,
            expand: expand,
            paymentIntent: paymentIntent,
            paymentRecord: paymentRecord
        )
    }
}

public class V1InvoicesFinalizeNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// Finalizes a draft invoice manually. Use this operation when you need to finalize the invoice before its
    /// automatic finalization process; set `auto_advance` to false if you do not want automatic collection after
    /// finalization.
    ///
    /// Stripe automatically finalizes drafts before sending and attempting payment on invoices. However, if you’d like
    /// to finalize a draft invoice manually, you can do so using this method.
    public func postInvoicesInvoice(invoice: String, autoAdvance: Bool?, expand: [String]?) async throws -> Invoice {
        try await V1InvoicesFinalizeMethods.postInvoicesInvoiceFinalize(
            config: config,
            invoice: invoice,
            autoAdvance: autoAdvance,
            expand: expand
        )
    }
}

public class V1InvoicesLinesNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// Lists the line items belonging to a specific invoice. Use `starting_after` and `ending_before` with `limit` to
    /// paginate the results, and use `expand` to include additional fields inline.
    ///
    /// When retrieving an invoice, you’ll get a lines property containing the total count of line items and the first
    /// handful of those items. There is also a URL where you can retrieve the full (paginated) list of line items.
    public func getInvoicesInvoice(
        invoice: String,
        endingBefore: String?,
        expand: [String]?,
        limit: Int?,
        startingAfter: String?
    ) async throws -> GetInvoicesInvoiceLinesResponse {
        try await V1InvoicesLinesMethods.getInvoicesInvoiceLines(
            config: config,
            invoice: invoice,
            endingBefore: endingBefore,
            expand: expand,
            limit: limit,
            startingAfter: startingAfter
        )
    }

    /// Updates an invoice’s line item. Some fields, such as tax_amounts , only live on the invoice line item, so they
    /// can only be updated through this endpoint. Other fields, such as amount , live on both the invoice item and the
    /// invoice line item, so updates on this endpoint will propagate to the invoice item as well. Updating an invoice’s
    /// line item is only possible before the invoice is finalized.
    public func postInvoicesInvoiceLineItemId(options: V1InvoicesLinesMethods
        .PostInvoicesInvoiceLinesLineItemIdOptions) async throws -> LineItem {
        try await V1InvoicesLinesMethods.postInvoicesInvoiceLinesLineItemId(config: config, options: options)
    }
}

public class V1InvoicesMarkUncollectibleNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// Marks a specific invoice as uncollectible for accounting purposes. Supply the `invoice` identifier to record the
    /// bad debt while preserving the invoice's history. The invoice must be identified by a string no longer than 5,000
    /// characters.
    ///
    /// Marking an invoice as uncollectible is useful for keeping track of bad debts that can be written off for
    /// accounting purposes.
    public func postInvoicesInvoice(invoice: String, expand: [String]?) async throws -> Invoice {
        try await V1InvoicesMarkUncollectibleMethods.postInvoicesInvoiceMarkUncollectible(
            config: config,
            invoice: invoice,
            expand: expand
        )
    }
}

public class V1InvoicesPayNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// Attempts to collect payment for a specific invoice outside its normal automatic collection schedule. Supply the
    /// `invoice` identifier and optionally choose the `payment_method`, `source`, or other payment behaviour. The
    /// operation can also record an invoice as paid outside Stripe with `paid_out_of_band`.
    ///
    /// Stripe automatically creates and then attempts to collect payment on invoices for customers on subscriptions
    /// according to your subscriptions settings. However, if you’d like to attempt payment on an invoice out of the
    /// normal collection schedule or for some other reason, you can do so.
    public func postInvoicesInvoice(options: V1InvoicesPayMethods
        .PostInvoicesInvoicePayOptions) async throws -> Invoice {
        try await V1InvoicesPayMethods.postInvoicesInvoicePay(config: config, options: options)
    }
}

public class V1InvoicesRemoveLinesNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// Removes multiple line items from a draft invoice. Supply `lines` with each line item's identifier and the
    /// removal `behavior`, choosing whether to delete or unassign the item. The operation only applies while the
    /// invoice remains in draft status.
    ///
    /// Removes multiple line items from an invoice. This is only possible when an invoice is still a draft.
    public func postInvoicesInvoice(
        invoice: String,
        lines: [PostInvoicesInvoiceRemoveLinesRequestBodyLinesItem],
        expand: [String]?,
        invoiceMetadata: PostInvoicesInvoiceRemoveLinesRequestBodyInvoiceMetadata?
    ) async throws -> Invoice {
        try await V1InvoicesRemoveLinesMethods.postInvoicesInvoiceRemoveLines(
            config: config,
            invoice: invoice,
            lines: lines,
            expand: expand,
            invoiceMetadata: invoiceMetadata
        )
    }
}

public class V1InvoicesSendNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// Sends a specific invoice to its customer for manual payment outside the normal automatic schedule. Supply the
    /// `invoice` identifier and optionally use `expand` to include expanded response fields. In test mode, the
    /// operation emits an `invoice.sent` event but does not send an email.
    ///
    /// Stripe will automatically send invoices to customers according to your subscriptions settings. However, if you’d
    /// like to manually send an invoice to your customer out of the normal schedule, you can do so. When sending
    /// invoices that have already been paid, there will be no reference to the payment in the email. Requests made in
    /// test-mode result in no emails being sent, despite sending an invoice.sent event.
    public func postInvoicesInvoice(invoice: String, expand: [String]?) async throws -> Invoice {
        try await V1InvoicesSendMethods.postInvoicesInvoiceSend(config: config, invoice: invoice, expand: expand)
    }
}

public class V1InvoicesUpdateLinesNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// Updates multiple line items on a draft invoice. Supply `lines` with each line item's identifier and the values
    /// to change, including pricing, quantity, description, or tax settings as needed. The operation only applies while
    /// the invoice remains in draft status.
    ///
    /// Updates multiple line items on an invoice. This is only possible when an invoice is still a draft.
    public func postInvoicesInvoice(
        invoice: String,
        lines: [PostInvoicesInvoiceUpdateLinesRequestBodyLinesItem],
        expand: [String]?,
        invoiceMetadata: PostInvoicesInvoiceUpdateLinesRequestBodyInvoiceMetadata?
    ) async throws -> Invoice {
        try await V1InvoicesUpdateLinesMethods.postInvoicesInvoiceUpdateLines(
            config: config,
            invoice: invoice,
            lines: lines,
            expand: expand,
            invoiceMetadata: invoiceMetadata
        )
    }
}

public class V1InvoicesVoidNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// Voids a finalized invoice while preserving it for historical and accounting records. Supply the `invoice`
    /// identifier to mark the invoice as void instead of deleting it. Voiding is irreversible, so verify that the
    /// invoice should be voided before submitting the request.
    ///
    /// Mark a finalized invoice as void. This cannot be undone. Voiding an invoice is similar to deletion, however it
    /// only applies to finalized invoices and maintains a papertrail where the invoice can still be found. Consult with
    /// local regulations to determine whether and how an invoice might be amended, canceled, or voided in the
    /// jurisdiction you’re doing business in. You might need to issue another invoice or credit note instead. Stripe
    /// recommends that you consult with your legal counsel for advice specific to your business.
    public func postInvoicesInvoice(invoice: String, expand: [String]?) async throws -> Invoice {
        try await V1InvoicesVoidMethods.postInvoicesInvoiceVoid(config: config, invoice: invoice, expand: expand)
    }
}
