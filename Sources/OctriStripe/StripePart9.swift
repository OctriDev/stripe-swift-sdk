// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

public class V1CustomersCardsNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// Lists the cards belonging to a customer. Use cursor parameters to move forward or backward through the cards,
    /// and use `limit` to control the number returned per page. The response includes pagination metadata and the cards
    /// in the requested range.
    ///
    /// You can see a list of the cards belonging to a customer. Note that the 10 most recent sources are always
    /// available on the Customer object. If you need more than those 10, you can use this API method and the limit and
    /// starting_after parameters to page through additional cards.
    ///
    /// - Warning: This operation is deprecated and may be removed in a future release.
    public func getCustomersCustomer(
        customer: String,
        endingBefore: String?,
        expand: [String]?,
        limit: Int?,
        startingAfter: String?
    ) async throws -> GetCustomersCustomerCardsResponse {
        try await V1CustomersCardsMethods.getCustomersCustomerCards(
            config: config,
            customer: customer,
            endingBefore: endingBefore,
            expand: expand,
            limit: limit,
            startingAfter: startingAfter
        )
    }

    /// Creates a new card or other payment source for a customer. Provide a token or the payment details in `card` or
    /// `bank_account`, and use `metadata` or `expand` when needed. If the customer has no default card, the new card
    /// becomes the default payment source.
    ///
    /// When you create a new credit card, you must specify a customer or recipient on which to create it. If the card’s
    /// owner has no default card, then the new card will become the default. However, if the owner already has a
    /// default, then it will not change. To change the default, you should update the customer to have a new
    /// default_source .
    public func postCustomersCustomer(
        customer: String,
        alipayAccount: String?,
        bankAccount: PostCustomersCustomerCardsRequestBodyBankAccount?,
        card: PostCustomersCustomerCardsRequestBodyCard?,
        expand: [String]?,
        metadata: [String: String]?,
        source: String?
    ) async throws -> PaymentSource {
        try await V1CustomersCardsMethods.postCustomersCustomerCards(
            config: config,
            customer: customer,
            alipayAccount: alipayAccount,
            bankAccount: bankAccount,
            card: card,
            expand: expand,
            metadata: metadata,
            source: source
        )
    }

    /// Deletes a card or other payment source stored on a customer. Provide the customer and source identifiers to
    /// remove the specified source, and use `expand` if you need expanded fields in the response. The response
    /// identifies the deleted payment source.
    ///
    /// Delete a specified source for a given customer.
    public func deleteCustomersCustomerId(
        customer: String,
        id: String,
        expand: [String]?
    ) async throws -> DeleteCustomersCustomerCardsIdResponse {
        try await V1CustomersCardsMethods.deleteCustomersCustomerCardsId(
            config: config,
            customer: customer,
            id: id,
            expand: expand
        )
    }

    /// Retrieves a specific card stored on a customer. Provide the customer and card identifiers to view the card's
    /// details, and use `expand` for additional response fields. The response contains card identity, billing,
    /// currency, and funding information.
    ///
    /// You can always see the 10 most recent cards directly on a customer; this method lets you retrieve details about
    /// a specific card stored on the customer.
    ///
    /// - Warning: This operation is deprecated and may be removed in a future release.
    public func getCustomersCustomerId(customer: String, id: String, expand: [String]?) async throws -> Card {
        try await V1CustomersCardsMethods.getCustomersCustomerCardsId(
            config: config,
            customer: customer,
            id: id,
            expand: expand
        )
    }

    /// Updates a card or other payment source stored on a customer. Send only the payment-source fields you want to
    /// change, such as billing address, expiration details, cardholder information, or metadata. The response contains
    /// the updated payment source, which may be a card, bank account, or other source type.
    ///
    /// Update a specified source for a given customer.
    public func postCustomersCustomerId(options: V1CustomersCardsMethods
        .PostCustomersCustomerCardsIdOptions) async throws -> PostCustomersCustomerCardsIdResponse {
        try await V1CustomersCardsMethods.postCustomersCustomerCardsId(config: config, options: options)
    }
}

public class V1CustomersCashBalanceNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// Retrieves the cash balance associated with a customer. Use `expand` to request additional fields in the returned
    /// cash balance object. The response includes available balances, customer association, mode, object type, and
    /// balance settings.
    ///
    /// Retrieves a customer’s cash balance.
    public func getCustomersCustomer(customer: String, expand: [String]?) async throws -> CashBalance {
        try await V1CustomersCashBalanceMethods.getCustomersCustomerCashBalance(
            config: config,
            customer: customer,
            expand: expand
        )
    }

    /// Updates the settings for a customer's cash balance. Use `settings.reconciliation_mode` to choose how incoming
    /// cash balance funds are reconciled, and use `expand` to request expanded response fields. The response contains
    /// the updated cash balance and its settings.
    ///
    /// Changes the settings on a customer’s cash balance.
    public func postCustomersCustomer(
        customer: String,
        expand: [String]?,
        settings: PostCustomersCustomerCashBalanceRequestBodySettings?
    ) async throws -> CashBalance {
        try await V1CustomersCashBalanceMethods.postCustomersCustomerCashBalance(
            config: config,
            customer: customer,
            expand: expand,
            settings: settings
        )
    }
}

public class V1CustomersCashBalanceTransactionsNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// Lists the transactions that changed a customer's cash balance. Use cursor parameters to page through
    /// transactions and `limit` to control the number returned, with optional response expansion through `expand`. Each
    /// transaction records funds moving into or out of the cash balance.
    ///
    /// Returns a list of transactions that modified the customer’s cash balance.
    public func getCustomersCustomer(
        customer: String,
        endingBefore: String?,
        expand: [String]?,
        limit: Int?,
        startingAfter: String?
    ) async throws -> GetCustomersCustomerCashBalanceTransactionsResponse {
        try await V1CustomersCashBalanceTransactionsMethods.getCustomersCustomerCashBalanceTransactions(
            config: config,
            customer: customer,
            endingBefore: endingBefore,
            expand: expand,
            limit: limit,
            startingAfter: startingAfter
        )
    }

    /// Retrieves a specific transaction that changed a customer's cash balance. Provide the customer and transaction
    /// identifiers to retrieve the transaction, and use `expand` to request additional response fields. The response
    /// includes the transaction amount, resulting balance, currency, type, and funding or payment details.
    ///
    /// Retrieves a specific cash balance transaction, which updated the customer’s cash balance.
    public func getCustomersCustomerTransaction(
        customer: String,
        transaction: String,
        expand: [String]?
    ) async throws -> CustomerCashBalanceTransaction {
        try await V1CustomersCashBalanceTransactionsMethods.getCustomersCustomerCashBalanceTransactionsTransaction(
            config: config,
            customer: customer,
            transaction: transaction,
            expand: expand
        )
    }
}

public class V1CustomersDiscountNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// Deletes the discount currently applied to a customer. Use `customer` to identify the customer whose discount you
    /// want to remove, and the response confirms deletion with a `deleted` value of true.
    ///
    /// Removes the currently applied discount on a customer.
    public func deleteCustomersCustomer(customer: String) async throws -> DeletedDiscount {
        try await V1CustomersDiscountMethods.deleteCustomersCustomerDiscount(config: config, customer: customer)
    }

    /// Retrieves the discount currently applied to a customer. Use `customer` to identify the customer and `expand` to
    /// include additional response fields when needed.
    public func getCustomersCustomer(customer: String, expand: [String]?) async throws -> Discount {
        try await V1CustomersDiscountMethods.getCustomersCustomerDiscount(
            config: config,
            customer: customer,
            expand: expand
        )
    }
}

public class V1CustomersFundingInstructionsNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// Creates or retrieves stable bank-transfer funding instructions for a customer's cash balance. Supply
    /// `bank_transfer`, `currency`, and `funding_type` to request instructions, and repeated requests for the same
    /// customer return the same instructions. Use `bank_transfer` to select the supported transfer type and any
    /// requested address types.
    ///
    /// Retrieve funding instructions for a customer cash balance. If funding instructions do not yet exist for the
    /// customer, new funding instructions will be created. If funding instructions have already been created for a
    /// given customer, the same funding instructions will be retrieved. In other words, we will return the same funding
    /// instructions each time.
    public func postCustomersCustomer(
        customer: String,
        bankTransfer: PostCustomersCustomerFundingInstructionsRequestBodyBankTransfer,
        currency: String,
        fundingType: PostCustomersCustomerFundingInstructionsRequestBodyFundingType,
        expand: [String]?
    ) async throws -> FundingInstructions {
        try await V1CustomersFundingInstructionsMethods.postCustomersCustomerFundingInstructions(
            config: config,
            customer: customer,
            bankTransfer: bankTransfer,
            currency: currency,
            fundingType: fundingType,
            expand: expand
        )
    }
}

public class V1CustomersPaymentMethodsNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// Lists the payment methods attached to a customer. Use `type` to restrict results to a payment method type,
    /// `allow_redisplay` to filter by checkout redisplay policy, and cursor parameters to paginate the list.
    ///
    /// Returns a list of PaymentMethods for a given Customer
    public func getCustomersCustomer(
        customer: String,
        allowRedisplay: GetCustomersCustomerPaymentMethodsParameter?,
        endingBefore: String?,
        expand: [String]?,
        limit: Int?,
        startingAfter: String?,
        type: GetCustomersCustomerPaymentMethodsParameterXbd61a528?
    ) async throws -> GetCustomersCustomerPaymentMethodsResponse {
        try await V1CustomersPaymentMethodsMethods.getCustomersCustomerPaymentMethods(
            config: config,
            customer: customer,
            allowRedisplay: allowRedisplay,
            endingBefore: endingBefore,
            expand: expand,
            limit: limit,
            startingAfter: startingAfter,
            type: type
        )
    }

    /// Retrieves a specific payment method attached to a customer. Provide both `customer` and `payment_method` to
    /// identify the relationship, and use `expand` when you need additional fields in the response.
    ///
    /// Retrieves a PaymentMethod object for a given Customer.
    public func getCustomersCustomerPaymentMethod(
        customer: String,
        paymentMethod: String,
        expand: [String]?
    ) async throws -> PaymentMethod {
        try await V1CustomersPaymentMethodsMethods.getCustomersCustomerPaymentMethodsPaymentMethod(
            config: config,
            customer: customer,
            paymentMethod: paymentMethod,
            expand: expand
        )
    }
}

public class V1CustomersSourcesVerifyNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// Verifies a bank account attached to a customer using the microdeposit amounts sent to that account. Supply the
    /// customer and bank account identifiers, then provide the two positive microdeposit amounts in cents. You can use
    /// `expand` to request expanded response fields.
    ///
    /// Verify a specified bank account for a given customer.
    public func postCustomersCustomerSourcesId(
        customer: String,
        id: String,
        amounts: [Int]?,
        expand: [String]?
    ) async throws -> BankAccount {
        try await V1CustomersSourcesVerifyMethods.postCustomersCustomerSourcesIdVerify(
            config: config,
            customer: customer,
            id: id,
            amounts: amounts,
            expand: expand
        )
    }
}

public class V1CustomersSourcesNamespace {
    let config: ClientConfig
    public let verify: V1CustomersSourcesVerifyNamespace
    public init(config: ClientConfig) {
        self.config = config
        verify = V1CustomersSourcesVerifyNamespace(config: config)
    }

    /// Lists the payment sources attached to a customer. Use `object` to filter source types and `starting_after` or
    /// `ending_before` to navigate through the paginated results.
    ///
    /// List sources for a specified customer.
    public func getCustomersCustomer(
        customer: String,
        endingBefore: String?,
        expand: [String]?,
        limit: Int?,
        object: String?,
        startingAfter: String?
    ) async throws -> GetCustomersCustomerSourcesResponse {
        try await V1CustomersSourcesMethods.getCustomersCustomerSources(
            config: config,
            customer: customer,
            endingBefore: endingBefore,
            expand: expand,
            limit: limit,
            object: object,
            startingAfter: startingAfter
        )
    }

    /// Creates and attaches a payment source to a customer. Supply a card, bank account, or token in the request body,
    /// and the new source becomes the default only when the customer does not already have a default source.
    ///
    /// When you create a new credit card, you must specify a customer or recipient on which to create it. If the card’s
    /// owner has no default card, then the new card will become the default. However, if the owner already has a
    /// default, then it will not change. To change the default, you should update the customer to have a new
    /// default_source .
    public func postCustomersCustomer(
        customer: String,
        alipayAccount: String?,
        bankAccount: PostCustomersCustomerSourcesRequestBodyBankAccount?,
        card: PostCustomersCustomerSourcesRequestBodyCard?,
        expand: [String]?,
        metadata: [String: String]?,
        source: String?
    ) async throws -> PaymentSource {
        try await V1CustomersSourcesMethods.postCustomersCustomerSources(
            config: config,
            customer: customer,
            alipayAccount: alipayAccount,
            bankAccount: bankAccount,
            card: card,
            expand: expand,
            metadata: metadata,
            source: source
        )
    }

    /// Deletes a payment source attached to a customer. Provide `customer` and `id` to identify the source, and use
    /// `expand` if the deleted source response should include expanded fields.
    ///
    /// Delete a specified source for a given customer.
    public func deleteCustomersCustomerId(
        customer: String,
        id: String,
        expand: [String]?
    ) async throws -> DeleteCustomersCustomerSourcesIdResponse {
        try await V1CustomersSourcesMethods.deleteCustomersCustomerSourcesId(
            config: config,
            customer: customer,
            id: id,
            expand: expand
        )
    }

    /// Retrieves a specific payment source attached to a customer. Supply `customer` and `id` to identify the source,
    /// and use `expand` to include additional response fields when needed.
    ///
    /// Retrieve a specified source for a given customer.
    public func getCustomersCustomerId(customer: String, id: String, expand: [String]?) async throws -> PaymentSource {
        try await V1CustomersSourcesMethods.getCustomersCustomerSourcesId(
            config: config,
            customer: customer,
            id: id,
            expand: expand
        )
    }

    /// Updates a payment source attached to a customer. Use `customer` and `id` to identify the source, then provide
    /// only the card, bank-account, owner, address, expiry, metadata, or expansion fields you want to change.
    ///
    /// Update a specified source for a given customer.
    public func postCustomersCustomerId(options: V1CustomersSourcesMethods
        .PostCustomersCustomerSourcesIdOptions) async throws -> PostCustomersCustomerSourcesIdResponse {
        try await V1CustomersSourcesMethods.postCustomersCustomerSourcesId(config: config, options: options)
    }
}
